# 第8回 地理空間データの活用1（座標変換、バッファ、空間結合）

皆さん、こんにちは！プログラミング応用A（コンピューティング応用G）、第8回の講義です。担当の教員です。

前回の第7回では、GISの基本概念や、丸い地球を平らにする「座標系」の不思議、そして「GeoPandas」を使った最初の地図描画を学びました。
今回はその知識をさらに一歩進め、ビジネスや社会課題解決の現場で最もよく使う**「空間データの実践的な加工・操作技術」**を学びます！

「店舗から徒歩10分（800m圏内）に住む人口を集計したい」「このGPS位置情報のピンは、東京都の何区にあるのかを自動判定したい」といった課題は、今日の講義をマスターすれば、Pythonでわずか数行で自動化できるようになります。

「バッファ」「空間結合（Spatial Join）」など、一見難しそうな処理が登場しますが、**大丈夫です！**
「店舗の縄張り（テリトリー）」や「ピンを区というバケツに投げ入れるゲーム」に例えながら、アスキーアートも豊富に使って解説します。
データ分析の仕事で「即戦力」となるGIS応用スキルを身につけていきましょう！

---
## ⏱️ 本日の100分スケジュール（タイムテーブル）

| 時間 | セクション | 学習内容と活動 |
| :--- | :--- | :--- |
| **00:00 - 00:10**<br>(10分) | **オープニング＆前回の復習** | 経緯度（WGS84）とメートル単位の計算の壁について確認 |
| **00:10 - 00:30**<br>(20分) | **8.1 座標参照系（CRS）の変換** | なぜ `to_crs()` が必要なのか。スイカの皮に定規をあてる比喩で解説 |
| **00:30 - 00:50**<br>(20分) | **8.2 バッファ（buffer）と重心** | 店舗の「1km商圏」を自動で作るバッファ処理と、ポリゴンの「真ん中（重心）」の算出 |
| **00:50 - 01:10**<br>(20分) | **8.3 空間結合（Spatial Join）** | 緯度経度のピンを「行政区画（ポリゴン）」に自動的に紐付ける最強の結合テクニック |
| **01:10 - 01:35**<br>(25分) | **💻 難易度別演習（1〜5）** | 座標変換、商圏（バッファ）作成、距離計算、空間結合の実践 |
| **01:35 - 01:40**<br>(5分) | **解答確認 ＆ Q&A ＆ まとめ** | 本日の重要事項の整理と、次回（地理空間データの活用2：leafmapでの動的可視化）へのブリッジ |

---
## 🎯 到達目標
この100分の授業が終わるころには、以下のことができるようになります。
1. **`.to_crs()`** を使って、データフレームの座標参照系（経緯度 ➡ 平面直角座標系など）を正しく変換できる。
2. **バッファ（`.buffer()`）** を用いて、特定の地点から「半径〇メートル」の商圏・影響エリアを自動生成できる。
3. 位置情報を持った2つの表を重ね合わせる**「空間結合（Spatial Join: `.sjoin()`）」**の仕組みを理解し、実装できる。
4. **代表点・重心（`.centroid`）**や、2点間の**距離計算（`.distance()`）**を使って、地理空間的な距離関係を算出できる。

---
## 8.1 座標参照系（CRS）の変換（なぜ `.to_crs()` が必要なのか？） (約20分)

### 経緯度（度）のままで距離を計るとどうなる？
前回、「スマホのGPSデータ（WGS84）は角度（緯度経度）で場所を記録している」とお話ししました。
もし、この「角度」のデータのままで、「店舗の半径500m圏内を計算して！」とプログラムに指示を出すとどうなるでしょうか？

プログラムは「半径500度」と勘違いして、地球を何周もする巨大な円を描いてしまいます。あるいは、「緯度1度って何メートルだっけ？」と迷い、日本と赤道直下で全く長さが異なる不正確な計算をしてしまいます。

**【丸い地球にメジャーはあてられない】**
```text
  角度データ (WGS84) [度]              平面直角座標系 (JGD2011) [メートル]
        ╭━━━━━╮                              ┌───────────────┐
       ┃  (度) ┃  ── to_crs() で変換 ──▶     │  (メートル)   │
        ╰━━━━━╯                              └───────────────┘
  「半径0.01度って何m？」                「半径1000mを定規でピシッと計測！」
```

### `.to_crs()` という強力な魔法
距離（メートル）や面積（平方メートル）を正確に計算したい時は、必ず **`.to_crs()`** メソッドを使って、データを**「メートル単位の平面座標系（投影座標系）」**に変換します。

*   **世界共通（メートル単位）**：`EPSG:3857`（Webメルカトル。Google Maps等で使用）
*   **日本国内（メートル単位・超高精度）**：平面直角座標系（JGD2011）。東京や武蔵野市周辺なら **`EPSG:6677`（第9系）** を使います。

```python
# （例）座標系の変換
# WGS84（経緯度：EPSG:4326）から、東京都周辺の平面直角座標系（メートル：EPSG:6677）へ一発変換！
# df_planar = df.to_crs(epsg=6677)
```

---
## 8.2 バッファ（buffer）と重心（centroid） (約20分)

### 1. バッファ（buffer）処理：商圏やテリトリーの作成
バッファとは、**「ある点・線・面から、指定された距離（メートルなど）の範囲をぐるりと囲む面（ポリゴン）を作る処理」**です。

ビジネスでの「新規出店したカフェから、半径500m（徒歩圏内）のエリア」や、防災での「氾濫の危険がある河川の周囲100mの避難警戒エリア」は、すべてこのバッファ処理で作られます。

**【バッファのイメージ図】**
```text
           [ 店舗（点：Point）]
                    │
                    ▼  buffer(500) ＝ 半径500メートルの円を作る！
             ╭  ━━━━  ╮
          /             \
         |       ●       | (この円の中にいる顧客や競合店を集計できる！)
          \             /
             ╰  ━━━━  ╯
```

### 2. 重心・代表点（centroid）：エリアの中心
ある市区町村や浸水エリアの「真ん中の点」を代表として計算したい時は、**`.centroid`** というプロパティを使います。
「店舗エリア（ポリゴン）の中心に、看板ピン（Point）を1本立てる」といった用途でよく使います。

---
## 8.3 空間結合（Spatial Join：緯度経度を行政区画に投げ入れる） (約20分)

通常のPandasでは、2つの表をくっつける時に `pd.merge()` を使い、「顧客ID」などの「共通の文字・数値（キー）」を仲立ちにして結合しましたね。
しかし、地図データには共通のキー（文字）がありません。あるのは「位置（座標）」だけです。

「この緯度経度のピンは、何区のポリゴンの内側にあるか？」のように、**共通の文字キーがなくても、位置の重なり（包含関係）をキーにして自動で表同士をくっつける処理**を**空間結合（Spatial Join：`.sjoin()`）**と呼びます。

**【空間結合のイメージ（バケツ投げゲーム）】**
```text
  [ 表 A : 顧客の位置ピン (Point) ]        [ 表 B : 東京都の行政区画 (Polygon) ]
    ・顧客ID: 01                           ・区名: 杉並区 (ポリゴン)
    ・座標: (139.62, 35.70)                 ・区名: 武蔵野市 (ポリゴン)
              │                                      │
              ╰───────────── 空間結合 (sjoin) ────────╯
                                    │
                                    ▼ (ピンがどのエリアの内側にあるかを判定！)
  [ 結合された新しい表 ]
    ・顧客ID: 01, 座標: (139.62, 35.70), 判定された区名: 「杉並区」！
```

GeoPandasでは、**`gpd.sjoin(表A, 表B, how="inner", predicate="within")`** という1行のコードでこれが実現します。
`predicate="within"` は「内側に入っているものを結合してね」という指示です。

---
## 💻 演習課題（難易度別5問）(約25分)

それでは、今日学んだ「メートル単位への座標変換」「バッファ商圏の作成」「空間結合」のプログラムを書いてみましょう！
これらが書ければ、GISデータ分析の実務プロセスを自力で回せるようになります。

以下の `# TODO: ` の部分を書き換えて、プログラムを完成させてください。

```python
# 演習1: 座標参照系（CRS）の平面直角座標系への変換（難易度：易）
# 杉並区周辺の避難所データ（経緯度：WGS84、EPSG:4326）があります。
# TODO: .to_crs() メソッドを使って、東京都周辺をメートル単位で正確に計算できる平面直角座標系「EPSG:6677」に変換し、
#       df_planar に代入して座標系情報（df_planar.crs）を表示してください。

import geopandas as gpd
from shapely.geometry import Point

# WGS84（経緯度）の模擬避難所データを作成
data_wgs84 = gpd.GeoDataFrame({
    "避難所名": ["桃井小学校", "高井戸中学校"],
    "geometry": [Point(139.6105, 35.7082), Point(139.6152, 35.6821)]
}, crs="EPSG:4326")

print("【変換前】現在の座標系:", data_wgs84.crs)

# TODO: EPSG:6677 (メートル単位) に座標変換する
df_planar = 

print("\n【変換後】新しい座標系:")
print(df_planar.crs) # 「EPSG:6677」と表示されれば大正解！
```

```python
# 演習2: メートル単位での避難所周辺の「100mバッファ」作成（難易度：易）
# 演習1で作成したメートル単位のデータフレーム df_planar を使用します。
# TODO: .buffer(100) を使って、各避難所から「半径100mの円（ポリゴン）」を作成し、
#       新しく「buffer_100m」という列を追加してください。
# ※GeoPandasでは、新しく作った位置データ（バッファの円）を geometry 列に指定すると地図描画がしやすくなります。

# TODO: geometry列に対して半径100mのバッファを作成し、新しい列「buffer_100m」に代入する
df_planar["buffer_100m"] = 

print("--- 100mバッファ作成後のデータフレーム ---")
display(df_planar[["避難所名", "buffer_100m"]])
print("バッファの型:", type(df_planar["buffer_100m"])) # GeoSeries (Polygon) と出れば正解！
```

```python
# 演習3: 平面座標系での避難所間の「距離（メートル）」の計算（難易度：中）
# メートル座標系（df_planar）を使うと、2つの避難所が何メートル離れているかを正確に計算できます。
# df_planar には 0番目（桃井小学校）と 1番目（高井戸中学校）のデータが入っています。
# TODO: .distance() メソッドを使って、0番目の避難所の位置（df_planar.geometry.iloc）と
#       1番目の避難所の位置（df_planar.geometry.iloc）の間の距離を計算し、表示してください。

# 0番目の位置を取得
pos_0 = df_planar.geometry.iloc

# 1番目の位置を取得
pos_1 = df_planar.geometry.iloc

# TODO: pos_0 から pos_1 までの距離を .distance() で計算して変数 dist_meters に代入する
dist_meters = 

print(f"桃井小学校と高井戸中学校の直線距離は: {dist_meters:.2f} メートルです！")
# 約 2900〜3000 メートル（約3km）と計算されれば大正解です！
```

```python
# 演習4: ポリゴンの中心（重心：centroid）の自動計算（難易度：中）
# 演習2で作った「100mバッファ（ポリゴン）」列の中心点を計算します（円の中心なので、元の避難所の位置に戻るはずです）。
# GeoPandasでは、ポリゴンデータの入った列に対して「.centroid」を呼び出すと、重心を自動計算してくれます。
# TODO: df_planar["buffer_100m"] 列の重心（centroid）を計算し、変数 centroids に代入して表示してください。

# TODO: buffer_100m 列の centroid プロパティを呼び出す
centroids = 

print("--- 算出されたバッファポリゴンの重心（中心点） ---")
print(centroids)
```

```python
# 演習5: 実践！空間結合（Spatial Join）による地区名自動判定（難易度：難）
# 顧客のGPS位置データ（緯度経度）が、どの「地区ポリゴン」の内側にあるかを自動判定します。
# 2つのデータ（表A：顧客、表B：地区）を用意しました。
# TODO: gpd.sjoin() を使って、顧客データ（customers）と地区データ（districts）を空間結合し、
#       結合後のデータフレーム df_joined を作成・表示してください。
# ※結合ルール：how="inner", predicate="within" を指定します。

import geopandas as gpd
from shapely.geometry import Point, Polygon

# 表A: 顧客の位置ピン（WGS84 経緯度）
customers = gpd.GeoDataFrame({
    "顧客ID": ["C01", "C02"],
    "geometry": [Point(139.612, 35.705), Point(139.618, 35.685)]
}, crs="EPSG:4326")

# 表B: 杉並区内の簡易な地区ポリゴン（桃井地区、高井戸地区）
# ※ 経緯度を囲む多角形（Polygon）を作っています
momoi_poly = Polygon([(139.60, 35.70), (139.62, 35.70), (139.62, 35.72), (139.60, 35.72)])
takaido_poly = Polygon([(139.60, 35.67), (139.62, 35.67), (139.62, 35.69), (139.60, 35.69)])

districts = gpd.GeoDataFrame({
    "地区名": ["桃井地区", "高井戸地区"],
    "geometry": [momoi_poly, takaido_poly]
}, crs="EPSG:4326")

# TODO: gpd.sjoin() を使って、customers と districts を空間結合する
df_joined = 

print("--- 空間結合（Spatial Join）の実行結果 ---")
display(df_joined[["顧客ID", "地区名", "geometry"]])
# C01が桃井地区、C02が高井戸地区と自動的に紐付いていれば完璧です！
```

---
## 💡 演習問題の解答例

（お疲れ様でした！「空間的な位置」を基準にしてデータが自動で紐付いた瞬間、プログラミングでのGIS活用の威力を実感できたのではないでしょうか。答え合わせをしてみましょう！）

<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

**演習1の解答**
```python
df_planar = data_wgs84.to_crs(epsg=6677)
```

**演習2の解答**
```python
df_planar["buffer_100m"] = df_planar.geometry.buffer(100)
```
※ `df_planar.geometry` は平面直角座標系（メートル単位）になっているため、`.buffer(100)` と書くだけで「正確な100メートル」のバッファが作られます。

**演習3の解答**
```python
dist_meters = pos_0.distance(pos_1)
```

**演習4の解答**
```python
centroids = df_planar["buffer_100m"].centroid
```

**演習5の解答**
```python
df_joined = gpd.sjoin(customers, districts, how="inner", predicate="within")
```
※ 空間結合により、文字の一致がなくても「緯度経度」の包含関係（within）だけで、顧客と地区名が完璧にガッチャンコされました！

</details>

---
## 📚 第8回のまとめ

お疲れ様でした！GISの実務で最も強力な「データの座標変換・バッファ・空間結合」をマスターしましたね。

### ✨ 今日の重要ポイントまとめ
*   **`.to_crs()`**: 正確な距離や面積を計るために、角度（度）の座標系からメートル単位の平面座標系（EPSG:6677など）に変換する必須処理。
*   **`.buffer()`**: 特定の点から「半径〇メートル」のテリトリー（面データ）を自動生成する。
*   **`.distance()`**: 2つの位置データ間のメートル単位の直線距離を一瞬で計算する。
*   **`.sjoin()` (空間結合)**: 共通の文字キーがなくても、「ピンがポリゴンの内側にある（within）」という空間的な重なりだけで表をくっつける最強テクニック。

### ❓ よくある質問と回答 (FAQ)
*   **Q. `gpd.sjoin()` を実行したら `UserWarning: CRS mismatch between the CRS of left gdf and right gdf.` という警告（またはエラー）が出ました。**
    *   **A.** これもGIS実務で非常に多いトラブルです！
        「結合したい左側の表（WGS84：経緯度）」と「右側の表（平面直角座標系：メートル）」のように、**2つのデータの座標系（CRS）が一致していないと空間結合は計算できません**（リンゴとオレンジを比べるようなものです）。
        空間結合を行う前には、必ず両方のデータの `.crs` を確認し、もし異なっていれば、どちらか一方を `.to_crs()` で変換して、**完全に座標系を揃えてから** `sjoin` を実行してくださいね。

---
次回（第9回）は、本コースのGISシリーズの総仕上げとして、**「地理空間データの活用2：leafmapによる動的可視化とダッシュボード」**を学びます。
静的な画像（matplotlib）としての地図ではなく、Google Mapsのように「ドラッグで動かせる」「ズームインできる」「クリックしたら詳細がポップアップする」といった、誰もが感動するインタラクティブな地図をPythonだけで一瞬で作る魔法を学びます。お楽しみに！今日もお疲れ様でした！
