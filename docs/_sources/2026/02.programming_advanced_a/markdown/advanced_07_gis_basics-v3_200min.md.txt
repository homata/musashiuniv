# 第7回 地理空間情報の基礎（GISの仕組みとGeoPandasの第一歩）[200分講義対応・完全版]

皆さん、こんにちは！プログラミング応用A（コンピューティング応用G）、第7回の講義です。担当の教員です。
応用クラスの授業もいよいよ後半戦、そして本コースの最大のハイライトの一つである**「地理空間情報（GIS: Geographic Information System）」**の領域に突入します！

「地図データなんて理系の専門分野じゃないの？」「難しそう…」と思うかもしれません。
しかし、実は皆さんが毎日無意識に使っている「Google Maps」や「降雨レーダー（アメダス）」、お気に入りのカフェの「出店計画（エリアマーケティング）」、災害時の「ハザードマップ」などは、すべて本日学ぶ地理空間情報の技術（GIS）で動いています。

この第7回・第8回・第9回の3回（GISシリーズ）を通して、皆さんのPython環境に「地図と位置データを自在に操る魔法」をインストールします。
「座標系」や「投影」といった難しい幾何学用語が出てきますが、**大丈夫です！**
地球を「みかんの皮」に例えたり、地図を「トッピングを重ねたパンケーキ」に例えたりしながら、文系の皆さんにも直感的にわかるストーリーとビジュアル（アスキーアート）で丁寧に解説します。

それでは、200分の集中講義（2コマ連結）、新しい地図の世界へ元気に出発しましょう！

---

## ⏱️ 本日の200分スケジュール（タイムテーブル・指導案）

本講義は、2つのコマ（前半100分、後半100分）を連結した200分の集中授業用として、理論の納得と手を動かすハンズオンを交互に行えるように設計されています。
※**「総合演習課題5問（実技テスト・宿題）」は、この講義の200分枠内には含めず、授業後の課題として切り離しています。**

### 【前半：GISの全体像と座標系の罠（100分）】
| 時間（目安） | セクションと活動内容 | 指導のポイントと教員の役割 |
| :--- | :--- | :--- |
| **00:00 - 00:15**<br>（15分） | **オープニング＆本日の目標確認**<br>・位置データの価値の解説<br>・環境構築（ライブラリインポート） | 地図データが現代のビジネス（Uber、コンビニ出店、デリバリー）やシビックテック（防災）を支える血流であることを紹介し、学生の学習意欲を高めます。 |
| **00:15 - 00:55**<br>（40分） | **7.1 GIS（地理空間情報システム）の全体像**<br>・パンケーキの重ね合わせ（レイヤー構造）<br>・ベクタデータとラスタデータの違い | **【講義①】**<br>・地図データを「トッピングを重ねたパンケーキ（ベース地図、道路、店舗、ハザードエリア）」に例えて図解します。<br>・拡大してもぼやけない「ベクタ（点・線・面）」と、マス目で表す「ラスタ（人工衛星や雨雲レーダー画像）」の違いを教えます。 |
| **00:55 - 01:35**<br>（40分） | **7.2 座標系と投影法の罠**<br>・地理座標系（WGS84）と投影座標系<br>・みかんの皮の比喩（歪み） | **【講義②：理論の山場】**<br>・「丸い地球を角度で表す地理座標系」と、「平らに印刷してメートル単位で距離を測る投影座標系」の違いを語ります。<br>・「丸いみかんの皮を破らずに、机の上にきれいな四角形で広げることはできない」という比喩を使い、平らな地図には必ず「面積・距離・角度」のどこかに歪みが生じる必然性を解説します。 |
| **01:35 - 01:45**<br>（10分） | **中間ブレイク（質疑応答・小休止）** | 前半の理論の疑問をクリアにし、頭をリフレッシュさせます。 |

### 【後半：地図データ形式とGeoPandasハンズオン（100分）】
| 時間（目安） | セクションと活動内容 | 指導のポイントと教員の役割 |
| :--- | :--- | :--- |
| **01:45 - 02:25**<br>（40分） | **7.3 地理空間データの代表的なファイル形式**<br>・Shapefile「最低3ファイルセット」の罠<br>・GeoJSON の構造（JSONの入れ子） | **【講義③】**<br>・最低3つのファイル（.shp, .shx, .dbf）が揃わないと絶対に動かない「Shapefile」の扱いと、Webで大人気の「GeoJSON」を比較します。<br>・通常のPandasの表に、位置情報が詰まった **`geometry` 列** を1つ足したものが「GeoPandas（GeoDataFrame）」の正体であり、これまでのPandasの文法が100%そのまま使えることを伝えて安心させます。 |
| **02:25 - 02:50**<br>（25分） | **7.4 GeoPandasによる最初の地図描画**<br>・世界地図描画ハンズオン<br>・条件抽出（アジア大陸切り出し） | **【実習ハンズオン】**<br>・Google Colabで実際にGeoPandasを動かし、世界地図やアジア大陸を plot() するハンズオンを行います。<br>・万が一の内蔵データ消滅に備え、GitHubのバックアップURLからGeoJSONを一瞬でロードする2重のセーフティネットコードを体験させます。 |
| **02:50 - 03:10**<br>（20分） | **7.5 授業内確認練習問題**<br>・理論＆記述クイズ（5問） | **【授業内理解度チェック】**<br>・授業内の理解度チェックとして、作成した「理論確認クイズ5問」をクラス全員で解き、**「Xが経度、Yが緯度だから、Point(経度, 緯度)の順で指定しないと南極や宇宙へ店舗が吹き飛ぶ罠」**を楽しく解説します。 |
| **03:10 - 03:20**<br>（10分） | **まとめと次回への架け橋**<br>・本日の総括<br>・次回（第8回：バッファ・空間結合）へのブリッジ | ・今日学んだ基礎（経緯度）が、次回「特定の店舗から1km以内の商圏を切り出す（バッファ処理）」や「店舗GPSが何区に入っているかを自動判定する（空間結合）」というメートル単位の高度な計算に繋がることを予告し、授業を締めくくります。 |

---

## 🎯 本日の到達目標
この200分の授業が終わるころには、以下のことができるようになります。
1. **GISの基本的な仕組み（レイヤー構造）** を、具体的なビジネスや社会課題解決の例と結びつけて説明できる。
2. **「地理座標系（経緯度）」と「投影座標系（平面）」の違い** を理解し、地図が歪む理由を説明できる。
3. 代表的な地図データ形式（**Shapefile, GeoJSON**）の特徴と用途、およびShapefileのファイル破損の罠を回避できる。
4. **GeoPandas（ジオパンダス）** ライブラリを読み込み、位置情報が含まれるデータ（Geometry列）を操作・条件抽出してプロットできる。
5. **Point（経度, 緯度）の順番ルール** を正確に守り、プログラム上での位置情報の取り扱いに関するバグを自力でデバッグできる。

---

## 7.1 GIS（地理空間情報システム）とは何か？ (約40分)

### 7.1.1 地図は「パンケーキの重ね合わせ」である
GIS（地理空間情報システム）とは、一言で言えば**「地球上にあるあらゆるモノや出来事の位置情報を、コンピュータ上で重ね合わせて分析・視覚化するシステム」**です。
最も重要で、最も美しい概念が、地図を複数の**「レイヤー（層）」**に切り分けて管理するということです。

甘いパンケーキに例えてみましょう！
*   一番下のパンケーキ：**「地形・道路・河川（土台のレイヤー）」**
*   その上のホイップクリーム：**「カフェや避難所の位置（点データのレイヤー）」**
*   その上のイチゴ：**「ハザードマップの浸水想定エリア（面データのレイヤー）」**
*   その上のシロップ：**「時間帯別の人流・人口密度のデータ（動的なデータレイヤー）」**

これらをコンピュータの中で1枚に「綺麗に重ね合わせる（オーバーレイ）」ことで、「川の近くで、人通りが多く、浸水のリスクが少ない場所に、新しく店舗を出店する、あるいは避難所を新設するにはどこが最適か？」という高度な都市計画やビジネス分析が、一瞬でできるようになります。

**【GISのレイヤー構造（イメージ図）】**
```text
  [ レイヤー 4: 店舗 ]    🍒 カフェの位置（点データ：Point）
         │
  [ レイヤー 3: 道路 ]    🛣️ 道路や鉄道路線（線データ：LineString）
         │
  [ レイヤー 2: 行政区 ]  🟩 地区区画や浸水エリア（面データ：Polygon）
         │
  [ レイヤー 1: 地形 ]    🗺️ 背景の白地図（ベースマップ）
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  【ガッチャンコ！】 ＝ 1枚の便利な「防災マップ」や「ビジネスエリア分析マップ」の完成！
```

### 7.1.2 空間データの表し方：ベクタデータ と ラスタデータ
コンピュータが地図の形を覚える方法には、大きく分けて**「ベクタ（Vector）」**と**「ラスタ（Raster）」**の2つの型があります。

#### ① ベクタデータ（幾何学データ）
位置を「座標（数値）」の組み合わせで表す方法です。
*   **特徴**：どれだけ地図を拡大（ズームイン）しても、輪郭がカクカクしたり、ぼやけたりせず、常にシャープで綺麗な線が描かれます。データサイズも小さくて済みます。
*   **種類**：
    *   **点（Point）**：店舗、駅、避難所の場所（1組の [緯度, 経度] ）。
    *   **線（LineString）**：道路、線路、河川（点の並び `[[x1,y1], [x2,y2]...]`）。
    *   **面（Polygon）**：豊島区、東京都、日本国、浸水エリア（始点と終点が繋がった閉じたエリア）。

#### ② ラスタデータ（グリッド画像データ）
地図を細かな「マス目（ピクセル）」の集合体として、写真のように表す方法です。
*   **特徴**：人工衛星から撮影した地表の写真（航空写真）や、気象庁の降雨レーダー画像（アメダス）など、グラデーションや複雑な色彩を表現するのに適しています。ただし、ズームインすると「ピクセル（モザイク）」が目立ち、ぼやけます。

**【ベクタ（滑らか） vs ラスタ（マス目）の比較】**
```text
  【ベクタデータ（Vector）】             【ラスタデータ（Raster）】
       (点・線・面で記録)                   (ピクセルの集まりで記録)
            ╱                                ░ ░ █ ░ ░
          ╱                                  ░ █ ░ █ ░
        ╱                                    █ ░ ░ ░ █
      (どれだけ拡大しても滑らか！)           (拡大するとギザギザになる！)
```

データ分析やビジネス分析では、主に集計や距離計算が圧倒的に得意な**「ベクタデータ（点・線・面）」**をメインに使用します。

---

## 7.2 丸い地球をどうやって平らにする？（座標系と投影法の罠） (約40分)

ここが、GISを扱う上で初心者が最も「うっ…」となる幾何学の理論パートです。
しかし、**数式は一切使いません！** イメージで完全に理解しましょう。

### 7.2.1 地理座標系（丸い地球を「角度」で表す）
地球は、実は完全な球体ではなく、自転の遠心力で少し横に潰れた「楕円体」をしています。
この丸い地球上の場所を示すために、地球の中心からの**「角度」**を使って場所を表す仕組みを**地理座標系（Geographic Coordinate System: GCS）**と呼びます。

*   **緯度（Latitude）**：赤道を0度とし、北（北緯）に90度、南（南緯）に90度まで測る角度（Y軸）。
*   **経度（Longitude）**：イギリスの旧グリニッジ天文台を通る線を0度（本初子午線）とし、東（東経）に180度、西（西経）に180度まで測る角度（X軸）。

世界共通で使われている代表的な地理座標系が **「WGS84」** という世界測地系です。皆さんのスマートフォンに入っているGPSや、Google Mapsも標準ではこのWGS84（経度・緯度の角度データ）で動いています。

### 7.2.2 投影座標系（丸い地球を無理やり「平面」にする）
しかし、地理座標系のデータ（度単位）のままでは、データ分析において非常に困ったことが起きます。
「店舗から駅まで何メートル？」
「この公園の面積は何平方メートル？」
という計算をするのが、地球が丸いために非常に難しくなるのです。また、パソコンの画面や紙のノートは「平ら」です。

そこで、丸い地球を**「無理やり引っ張って平らな紙に投影（印刷）」**し、単位を「度（角度）」から**「メートル（長さ）」**に変換した仕組みを**投影座標系（Projected Coordinate System: PCS）**と呼びます。

ここで、おいしい**「みかんの皮」**を想像してください。
丸いみかんの皮を、破いたり引き伸ばしたりせずに、机の上に「完全な平らな四角形」として広げることはできますか？
絶対にできませんよね。どうしても途中で破けてしまうか、皮のどこかをググッと引き伸ばさないと、平らな四角形にはなりません。

**【投影のイメージ（みかんの皮）】**
```text
      丸いみかん（地球：角度）         平面に広げた皮（世界地図：メートル）
         ╭━━━━━╮                     ┌───────────────┐
        ┃   ●   ┃   ──投影(flatten)─▶ │   ●   (歪み)   │
         ╰━━━━━╯                     └───────────────┘
      「角度は正確！」                「距離や面積が不自然に引き伸ばされる！」
```

このように、丸い球体を平らな平面に投影するときには、必ず**「面積」「距離」「角度」のどれかが犠牲（歪む）**になります。
*   **メルカトル図法**：
    *   世界地図でおなじみ。角度が正しいため大航海時代に船の羅針盤で多用されました。
    *   ただし、北極や南極に近づくほど面積が異常に引き伸ばされるため、ロシアやグリーンランドが実物より何倍も巨大に描かれる（面積の歪みが極大）というデメリットがあります。

### 7.2.3 日本国内の正確な投影：平面直角座標系
「日本国内で、面積や距離を1cmの狂いもなく正確に測定・分析したい」という場合、地球全体をカバーするメルカトル図法を使うと、歪みが大きすぎて使えません。

そこで、国土地理院は日本を南北に細長く19のエリアに分割し、それぞれのエリアの「中心の基準点（原点）」付近だけを平らにして、歪みを極限までゼロに抑える特別な投影系を作りました。これを**「平面直角座標系（JGD2011）」**と呼びます。

*   **第9系（EPSG:6677）**：東京、神奈川、千葉、埼玉などの「関東エリア」を正確に測るための平面メートル座標系。関東を分析する時は、常にこの座標系に変換（プロジェクション）して、メートル単位で計算します。

---

## 7.3 地理空間データの代表的なファイル形式 (約40分)

実務のデータ分析で遭遇する、地図データの代表的なファイル形式を2つ覚えましょう。特に1つ目の「Shapefile」には、初心者が100%引っかかる恐ろしい罠があります。

### 7.3.1 Shapefile（シェープファイル：.shp）の「3つで1つ」の罠
Shapefileは、アメリカのESRI社が開発した、GISの世界における「超定番（長老）」のファイル形式です。官公庁のオープンデータや市区町村の境界線は、現在もほぼこの形式で配布されています。

**【最大の罠：.shp だけでは動かない！】**
「都道府県のデータが欲しいので、`prefecture.shp` というファイルだけをメールで送ってください」
これは**絶対にやってはいけない致命的なミス**です！

Shapefileは、実は1つのファイルではなく、**「同じ名前の複数のファイルが同じフォルダ内にセットで存在すること」**で初めて動く仕組みになっています。

*   **`.shp`**：位置の「形（ジオメトリ：点・線・面）」が保存されているファイル。
*   **`.dbf`**：各図形に関連づけられた「属性データ（名前や人口などの表データ）」が保存されているファイル。
*   **`.shx`**：位置と属性データを素早く検索するための「インデックス」ファイル。
*   *`.prj`*（推奨）：その地図データが「どの座標系（WGS84か、平面直角座標系か）」で作られているかを記録したファイル。これが無いと地図がどこにプロットされるべきか迷子になります。

**【Shapefileの内部構造（イメージ図）】**
```text
  同じフォルダ内：
  ├── 📂 tokyo_map.shp  (形データ：PointやPolygon)
  ├── 📂 tokyo_map.dbf  (属性データ：人口や店舗名などのエクセル表)   👈 すべてセットで1つ！
  ├── 📂 tokyo_map.shx  (検索用の接着剤)                             どれが欠けてもエラーで死ぬ！
  └── 📂 tokyo_map.prj  (座標系の案内紙: EPSG番号など)
```

**【教訓】**：Shapefileを他人に送る時や、Colabにアップロードする時は、`.shp` だけでなく、`.dbf` や `.shx` など**すべてのセットをZIPファイル等に固めて一緒に扱う**必要があります。

### 7.3.2 GeoJSON（ジオジェイソン：.geojson）
Webアプリやプログラミングの世界における「現代の主役」となる形式です。
中身は、第11回で学んだ「JSON（辞書形式のテキスト）」を拡張しただけのシンプルな1枚のテキストファイルです。

*   **特徴**：
    *   1つのファイル（`.geojson`）の中に、形データも属性データも座標系情報もすべて美しく収まっています。
    *   テキストデータなので、Pythonで開きやすく、Webブラウザの地図アプリとも非常に相性が良いです。

---

## 7.4 GeoPandasの導入と最初の地図描画 (約25分)

それでは、実際にPythonコードを動かして、地図空間データを操作してみましょう！
Pandas（パンダス）はエクセルのような表データを扱いましたが、**GeoPandas（ジオパンダス）**は、その表の中に**「形や位置情報（Geometry：ジオメトリ）」の列を1つ追加した、最強の地図分析・可視化ライブラリ**です。

通常のPandasの文法（`.head()`, `groupby`, 条件抽出 `df[df[...] == ...]`）が、**そのまま100%使える**ため、これまでの皆さんの知識がそのまま活かされます！

### 7.4.1 Google Colabの環境構築
Colabには標準でGeoPandasが入っていますが、時々バージョンの不整合で可視化の際に赤いエラーが出ることがあります。まずは環境を安全に整えるために、以下の「おまじない」セルを実行しましょう。

```python
# （準備）地図データの日本語化ライブラリとGeoPandasのインポート
!pip install -q japanize-matplotlib
import geopandas as gpd
import matplotlib.pyplot as plt
import japanize_matplotlib

print("✨ GeoPandasのインポートに成功しました！")
print("現在のGeoPandasバージョン:", gpd.__version__)
```

### 7.4.2 世界地図データの読み込みと描画
GeoPandasには、最初から練習用の「世界地図データ（世界各国の境界線ポリゴン）」が内蔵されています。これを読み込んでみましょう！

※ライブラリのバージョンアップにより、内蔵データセット（`naturalearth_lowres`）の取得方法が非推奨になるケースがあるため、**「もし内蔵読み込みが失敗した場合は、自動でGitHub上のバックアップURLからGeoJSON世界地図を一瞬で読み込み直す」という2重のセーフティネットコード**で実装しています。

```python
# 1. 世界地図データを読み込む（セーフティネット対応）
try:
    # 内蔵パスの取得を試みる
    world_path = gpd.datasets.get_path('naturalearth_lowres')
    world = gpd.read_file(world_path)
    print("🌍 GeoPandasの内蔵世界地図をロードしました。")
except Exception:
    # 失敗した場合は、GitHubに配置されたバックアップデータ（GeoJSON）から直接読み込む
    backup_url = "https://raw.githubusercontent.com/homata/musashiuniv/main/data/naturalearth_lowres.geojson"
    world = gpd.read_file(backup_url)
    print("🌍 バックアップURLから世界地図をダイレクトにロードしました。")

# 2. データフレームの構造を表示してみる（通常のPandasの表と見比べてみましょう）
print("\n--- 世界地図データのデータフレーム（DataFrame） ---")
display(world.head(3))
```

通常のPandasと全く同じ表形式ですが、一番右端に **`geometry`（ジオメトリ）** という列がありますね！
ここに `MULTIPOLYGON`（複数の多角形：国境線で囲まれた面）などの位置情報がギッシャーリ詰まっています。

この `world` に対して、`.plot()` メソッドを呼び出すだけで、GeoPandasが裏側で自動的に幾何学座標を計算し、一瞬で世界地図を描画してくれます！

```python
# 世界地図をプロット
fig, ax = plt.subplots(figsize=(12, 6))

# world.plot() に ax(描画キャンバス) を指定してプロットします
world.plot(ax=ax, color='lightgreen', edgecolor='white')

# グラフ（地図）の装飾
plt.title("GeoPandasで描いた世界地図 🌍")
plt.xlabel("経度 (Longitude)")
plt.ylabel("緯度 (Latitude)")
plt.grid(True, linestyle='--', alpha=0.5)
plt.show()
```

### 7.4.3 特定のエリアだけを切り出す（Pandasの知識の応用）
基礎クラスで学んだ「条件抽出」をそのまま使うことができます！
例えば、`continent`（大陸名）が `"Asia"`（アジア）の国だけを抽出して、アジアだけの部分地図にしてみましょう。

```python
# アジア大陸の国だけを条件抽出
asia_countries = world[world["continent"] == "Asia"]

# 画面に表の一部を表示して確認
print(f"アジア大陸の国々の数: {len(asia_countries)} ヶ国")
display(asia_countries[["name", "gdp_md_est", "geometry"]].head(3))

# アジア地図のプロット
fig, ax = plt.subplots(figsize=(10, 6))
# color=塗りつぶしの色, edgecolor=国境線の色
asia_countries.plot(ax=ax, color='skyblue', edgecolor='darkblue')

plt.title("アジア大陸の国々の分布 🗺️")
plt.xlabel("経度")
plt.ylabel("緯度")
plt.grid(True, linestyle=':', alpha=0.6)
plt.show()
```

これまでに学んだPandasの抽出やグラフ描画のコードが、そのまま「地図の作成」に直結している感動を体験できたでしょうか？
これが、Pythonを使った地理空間分析の第一歩です！

---

## 7.5 授業内確認練習問題（理論・挙動チェック：5問） (約20分)

授業のまとめとして、ここまでの理論とGeoPandasの基本に関するクイズに挑戦してみましょう。
頭の中で答えを決定してから、クリックして解答と解説をスッキリ確認してくださいね！

### 練習問題 1: GISのレイヤー概念
GISで、地図を「道路」「店舗」「河川」といった異なる情報の層に切り分けて重ね合わせて分析する仕組みのことを、英語で何と呼ぶでしょうか？
また、拡大しても形が一切劣化しない「点・線・面」の幾何学データの種類を何と呼ぶでしょうか？

<details>
<summary>👉 練習問題 1 の解答と解説を見る</summary>

*   **正解**：
    *   地図を重ね合わせる層の仕組み：**「レイヤー（Layer）構造」**
    *   劣化しない幾何学データ：**「ベクタ（Vector）データ」**
*   **解説**：
    *   地図は、1枚の巨大な絵として管理されているのではなく、「ベース地図」「道路」「建物」といった透明なシート（レイヤー）をコンピュータ上で何層も重ね合わせる（オーバーレイ）ことで、自由な分析を可能にしています。
    *   拡大するとギザギザにぼやける写真は「ラスタデータ」であり、数値（座標）で形を管理し、何倍に拡大しても滑らかなままの図形データを「ベクタデータ」と呼びます。

</details>

---

### 練習問題 2: 座標系とみかんの皮の罠
1.  地球の形をそのまま「角度（経度・緯度）」で表現した座標系を **( A ) 座標系**と呼びます。
2.  丸い地球を無理やり平らに引き伸ばし、単位を「メートル（長さ）」に直して距離や面積を計りやすくした座標系を **( B ) 座標系**と呼びます。
3.  日本国内の特定のエリア（例えば関東地域など）を歪みほぼゼロで正確に測定するために、日本を19地域に分割して定義された平面座標系を **( C )** と呼びます。
空欄(A), (B), (C)に入る適切な用語の組み合わせを答えてください。

<details>
<summary>👉 練習問題 2 の解答と解説を見る</summary>

*   **正解**：
    *   **( A ) = 地理（ちり）座標系**
    *   **( B ) = 投影（とうえい）座標系**
    *   **( C ) = 平面直角座標系（JGD2011）**
*   **解説**：
    *   GPSなどで使われる「経度・緯度（WGS84など）」は**地理座標系**であり、単位は「度」です。
    *   みかんの皮を広げるように、無理やり平らにしてメートル単位にしたものが**投影座標系**です。
    *   日本独自の、狭い地域ごとに歪みを完全に封じ込めた投影系が、国土地理院の定める**平面直角座標系**（関東地域は「第9系：EPSG:6677」など）です。

</details>

---

### 練習問題 3: Shapefileの「長老」問題
あなたのチームの学生が「自治体からShapefile形式の避難所データをダウンロードしてColabで開こうとしたが、どうしてもエラーが起きて読み込めない」と相談に来ました。
学生のパソコン画面を見たところ、Colabのフォルダの中に `shelter.shp` というファイルだけが単体でアップロードされていました。
データ分析官として、この学生に「何が原因で、どうすれば解決するか」を1分で教えてあげてください。

<details>
<summary>👉 練習問題 3 の解答と解説を見る</summary>

*   **正解のアドバイス**：
    *   **「原因」**：Shapefile（シェープファイル）は `.shp` だけでは動きません。図形の中身（属性データ）を記録した **`.dbf`** や、検索用インデックスの **`.shx`** などの異なる拡張子のファイルが同じ名前でセットになって初めて動く「3つで1つ」のルールがあるからです。
    *   **「解決策」**：自治体の配布サイトからダウンロードしたZIPファイルを一度パソコンで解凍し、中に入っている `.shp`, `.dbf`, `.shx` などのファイルを**すべてセットで同じColabのフォルダにアップロード（またはZIPごとアップロードして解凍）**してください。
*   **解説**：
    *   これは実務でGISに触れる初心者が100%犯す最大の罠です。Shapefileをやり取りする時は、必ず「複数ファイルがセットで同じ名前でフォルダに入っているか」を確認しましょう！

</details>

---

### 練習問題 4: 地図上の Point 定義における「全人類共通の罠」
Pythonの地図幾何学ライブラリ（shapely）を使って、経度 139.75 度、緯度 35.68 度（皇居付近）の位置にピンを立てるため、以下のコードを書きました。

`point = Point(35.68, 139.75)`

このコードには、実行したときに全く見当違いの場所（例えば南極の近くや、海の上）に位置がズレてしまう致命的なバグが潜んでいます。
このバグの原因と、正しいコードの書き方を答えてください。

<details>
<summary>👉 練習問題 4 の解答と解説を見る</summary>

*   **正解**：
    *   **バグの原因**：Pointを定義する際は、日常会話の「緯度・経度」の順番ではなく、数学のグラフと同じ **`Point(経度, 緯度)` （ X軸 = 経度, Y軸 = 緯度 ）** の順番で渡す必要があるから。
    *   **正しいコード**： `point = Point(139.75, 35.68)`
*   **解説**：
    *   「緯度経度」という口癖のせいで、`Point(緯度, 経度)` と書いてしまい、皇居のはずが「経度35.68度（サウジアラビア付近）、緯度139.75度（南極大陸付近）」へピンが吹き飛んでしまうバグは、**全人類が1度は絶対に踏む最大の罠**です。
    *   「Pointは X, Y の順。X軸は横に動く『経度』、Y軸は縦に動く『緯度』！」と体に刻み込みましょう。

</details>

---

### 練習問題 5: GeoPandasの「crs」属性
GeoPandasで読み込んだ地理空間データフレーム（GeoDataFrame）が、現在「世界測地系WGS84（経緯度：度単位）」なのか「平面直角座標系（メートル単位）」なのかを、プログラム上で確認したいです。
データフレームが `gdf` という名前のとき、座標参照系情報を取得・確認するためのプロパティ（属性）は何でしょうか？

<details>
<summary>👉 練習問題 5 の解答と解説を見る</summary>

*   **正解**： **`gdf.crs`**
*   **解説**：
    *   データフレーム名の後に **`.crs`** (Coordinate Reference System: 座標参照系) を添えて実行するだけで、現在の座標参照系が「EPSG:4326（WGS84）」なのか「EPSG:6677（平面直角座標9系）」なのかといった詳細なシステム情報を一瞬で確認できます。

</details>

---

## 💻 総合演習課題（難易度別5問：時間外レポート・宿題用）

ここからは、本日の200分大講義で身につけたGISの基礎とGeoPandasの基本スキルを実証するための総合演習課題です。
自力で、あるいは生成AIを心強いアシスタントとして相談しながら、1行ずつコードを穴埋めして完成させましょう！

すべての課題は、Colabのセルにコピー＆ペーストするだけで、外部ファイルを用意しなくてもその場でダミーデータを生成・読み込みして動作するように親切に設計されています。

---

### 課題 1: ヨーロッパ（Europe）大陸の切り出しと地図描画
これまでの世界地図データフレーム `world` から、**「continent」が「Europe」の国々だけを条件抽出**し、国境線を「黒色（black）」、塗りつぶしを「オレンジ色（orange）」にした、美しいヨーロッパの地域地図を描画してください。

```python
# (準備コード) 世界地図データの自動再ロード
try:
    world_path = gpd.datasets.get_path('naturalearth_lowres')
    world = gpd.read_file(world_path)
except Exception:
    backup_url = "https://raw.githubusercontent.com/homata/musashiuniv/main/data/naturalearth_lowres.geojson"
    world = gpd.read_file(backup_url)

# --- 課題 1 の実装 ---
# TODO: 1. continent が Europe の国だけを抽出し、df_europe に代入する
df_europe = 

# 地図の描画（ここを埋めて完成させてください）
fig, ax = plt.subplots(figsize=(10, 8))

# TODO: 2. df_europe を、国境線を「black」、塗りつぶしを「orange」にして ax 上にプロットする
df_europe.plot(               )

# グラフ（地図）の装飾
plt.title("ヨーロッパ諸国の地域地図 🇪🇺")
# ヨーロッパ周辺が見えやすいように経緯度の表示範囲（ズーム）を指定します
ax.set_xlim(-25, 45) # 経度の範囲
ax.set_ylim(34, 72)  # 緯度の範囲
plt.grid(True, linestyle=":", alpha=0.5)
plt.show()
```

<details>
<summary>👉 課題 1 の教員用模範解答コードと採点ガイドを見る</summary>

#### 【模範解答コード】
```python
# 1. ヨーロッパの条件抽出
df_europe = world[world["continent"] == "Europe"]

# 2. 地図の描画設定
df_europe.plot(ax=ax, color='orange', edgecolor='black')
```

#### 【教員用採点・減点基準（10点満点）】
*   **【10点満点】**：ヨーロッパ諸国がオレンジ色、黒い境界線で綺麗に関東周辺ズームと同義の範囲でプロットされていれば満点。
*   **【3点減点】**：条件抽出（`world["continent"] == "Europe"`）の綴り間違いや、データフレームの指定ミス。
*   **【2点減点】**：カラー指定（`color='orange'` や `edgecolor='black'`）の間違い、もしくはキャンバス指定（`ax=ax`）の漏れ。

</details>

---

### 課題 2: 世界の「人口が少ない国（ワースト5）」のデータ抽出とソート
データフレーム `world` の中から、**人口（pop_est）が「1人以上（0人を省くため）」の国を対象**とし、**人口が「少ない順（昇順）」**に並び替えた際の上位5ヶ国を抽出してください。
抽出したデータフレームの「name（国名）」、「pop_est（推計人口）」、「geometry（形）」の3つの列だけを画面に表示してください。

```python
# --- 課題 2 の実装 ---
# TODO: 1. 人口（pop_est）が 1 以上の国だけを条件抽出して df_valid_pop に代入する
df_valid_pop = 

# TODO: 2. 人口（pop_est）の少ない順（昇順：ascending=True）でソートする
df_sorted = 

print("--- 人口が極めて少ない国ワースト5 ---")
# TODO: 3. 国名、推計人口、geometry 列だけを選択し、先頭の5行を表示する
display(df_sorted[                               ].head(5))
```

<details>
<summary>👉 課題 2 の教員用模範解答コードと採点ガイドを見る</summary>

#### 【模範解答コード】
```python
# 1. 人口1人以上の抽出
df_valid_pop = world[world["pop_est"] >= 1]

# 2. 人口の昇順でソート
df_sorted = df_valid_pop.sort_values(by="pop_est", ascending=True)

# 3. 指定列の表示
display(df_sorted[["name", "pop_est", "geometry"]].head(5))
```

#### 【教員用採点・減点基準（10点満点）】
*   **【10点満点】**：バチカンやフォークランド諸島などの人口極小国が、昇順で正しく5ヶ国、指定された3つの列名で表示されていれば満点。
*   **【3点減点】**：ソートの昇順（`ascending=True`）の指定を忘れ、降順（人口最大国）を並べてしまった場合。
*   **【3点減点】**：指定された「name」「pop_est」「geometry」の3つの列の絞り込み表示を忘れてしまった場合（キーエラーなど）。

</details>

---

### 課題 3: 新設避難所3箇所の緯度・経度からのPoint作成
あなたの地域に、新しく3つの「新設避難所」ができることになりました。
それぞれの避難所の名前と緯度・経度情報は以下の通りです。

*   **避難所A**：東経 139.670、北緯 35.735
*   **避難所B**：東経 139.682、北緯 35.741
*   **避難所C**：東経 139.658、北緯 35.729

Pythonの `shapely.geometry.Point` を使い、この3つの緯度経度から**「全人類共通の罠（指定の順番）」を正しく回避して、正しいPointオブジェクトのリストを作成してください。**

```python
# TODO: 1. shapely.geometry から Point をインポートする
from 

# TODO: 2. 各避難所の緯度経度からPointを作成する（指定順に魂を込めて注意！）
point_A = 
point_B = 
point_C = 

# リストにまとめます
point_list = [point_A, point_B, point_C]

print("作成された避難所Pointデータ:")
for p in point_list:
    print(p) # Point (139.xxx, 35.xxx) と経度が左、緯度が右に出ていれば成功！
```

<details>
<summary>👉 課題 3 の教員用模範解答コードと採点ガイドを見る</summary>

#### 【模範解答コード】
```python
# 1. Pointのインポート
from shapely.geometry import Point

# 2. Pointの作成（経度がX軸、緯度がY軸なので 139.xxが最初になります）
point_A = Point(139.670, 35.735)
point_B = Point(139.682, 35.741)
point_C = Point(139.658, 35.729)
```

#### 【教員用採点・減点基準（10点満点）】
*   **【10点満点】**：Pointの引数がすべて `Point(139.xxx, 35.xxx)` の順番（経度・緯度の順）で記述され、インポートも完璧なら満点。
*   **【5点減点（超重要）】**：`Point(35.735, 139.670)` のように、日常の「緯度・経度」という言葉順につられて引数を逆にしてしまった場合。位置がサウジアラビアや南極大陸付近へ飛ぶ、GISにおける最大のエラー要因のため重く減点します。

</details>

---

### 課題 4: 避難所データベースの新規地理データフレーム（GeoDataFrame）化
課題3で作成した「3つのPoint（位置データ）」と、それぞれの「備蓄食料数（Food_Stock）」が格納された辞書データがあります。
これをもとに、GeoPandasの **`gpd.GeoDataFrame()`** を用いて、初期の座標参照系（CRS）を世界測地系である **「WGS84 (EPSG:4326)」** に設定した、新しい地理データフレーム `df_new_shelter` を新規作成してください。

```python
import geopandas as gpd
from shapely.geometry import Point

# 避難所Pointリスト
point_list = [Point(139.670, 35.735), Point(139.682, 35.741), Point(139.658, 35.729)]

# 辞書データ
raw_dict_data = {
    \"避難所名\": [\"新設A館\", \"新設B館\", \"新設C館\"],
    \"備蓄食料\": [1200, 1500, 800],
    \"geometry\": point_list # 位置情報のリストを geometry という列名に配置します
}

# TODO: gpd.GeoDataFrame() を使い、辞書データ「raw_dict_data」を地理空間データフレームに変換する。
#       その際、位置情報の列名を「geometry=\"geometry\"」で指定し、
#       初期座標系（crs）を世界測地系 WGS84 である \"EPSG:4326\" に定義してください。
df_new_shelter = 

print("--- 新設避難所地理データフレーム ---")
display(df_new_shelter)
print("現在のデータ型:", type(df_new_shelter)) # GeoDataFrame と表示されれば正解
print("定義された座標系(CRS):", df_new_shelter.crs) # EPSG:4326
```

<details>
<summary>👉 課題 4 の教員用模範解答コードと採点ガイドを見る</summary>

#### 【模範解答コード】
```python
df_new_shelter = gpd.GeoDataFrame(raw_dict_data, geometry="geometry", crs="EPSG:4326")
```

#### 【教員用採点・減点基準（10点満点）】
*   **【10点満点】**：`gpd.GeoDataFrame` を使って、辞書、位置の列名指定（geometry）、座標系指定（crs="EPSG:4326"）のすべてが1行で正しく定義され、表示されていれば満点。
*   **【3点減点】**：引数 `geometry="geometry"` を忘れてしまった場合（GeoPandasはどの列が形データか判定できなくなります）。
*   **【3点減点】**：引数 `crs="EPSG:4326"`（あるいは `crs=4326`）の指定を忘れてしまった場合。

</details>

---

### 課題 5: 【集大成】地理座標系から平面直角座標系（メートル座標）への投影変換
課題4で作成した `df_new_shelter` は現在、世界測地系である `EPSG:4326` であり、位置情報の単位は「度（角度）」です。
これでは「避難所の周りに半径500mの円を描いて商圏を調べる」といったメートル単位の計算ができません。

GeoPandasの **`.to_crs()`** メソッドを用いて、関東地方のメートル単位の正確な平面直角座標系である **「平面直角座標系第9系（JGD2011：EPSG:6677）」に投影変換したデータフレーム `df_shelter_meter` を作成してください。**
また、geometryの中身が `Point (139.xxx, 35.xxx)` という角度から、`-xx, xxx`, `-xx, xxx` のような**大きなメートル値**に変化したことを確認してください。

```python
# (準備) 課題4のデータフレームを再現
df_new_shelter = gpd.GeoDataFrame(
    {
        "避難所名": ["新設A館", "新設B館", "新設C館"],
        "備蓄食料": [1200, 1500, 800],
        "geometry": [Point(139.670, 35.735), Point(139.682, 35.741), Point(139.658, 35.729)]
    },
    crs="EPSG:4326"
)

# --- 課題 5 の実装 ---
# TODO: .to_crs() を使い、座標参照系を平面直角座標第9系 \"EPSG:6677\" に変換してください。
df_shelter_meter = 

print("【変換前：世界測地系 (EPSG:4326)】 単位は『度』")
print(df_new_shelter["geometry"])

print("\n【変換後：平面直角座標第9系 (EPSG:6677)】 単位は『メートル』")
# geometryのPointの中身が、巨大なメートル数値（例: Point (-3142.23, -12423.44)等）に変化したか確認！
print(df_shelter_meter["geometry"])
print("\n変換後の新しい座標参照系(CRS):", df_shelter_meter.crs)
```

<details>
<summary>👉 課題 5 の教員用模範解答コードと採点ガイドを見る</summary>

#### 【模範解答コード】
```python
df_shelter_meter = df_new_shelter.to_crs(epsg=6677) # もしくは .to_crs("EPSG:6677")
```

#### 【教員用採点・減点基準（10点満点）】
*   **【10点満点】**：`.to_crs()` メソッドに正しく `epsg=6677`（または `"EPSG:6677"`）が渡され、実行画面で geometry の数値が度からメートル（大きな桁数）に変化していれば満点。
*   **【4点減点】**：メソッド名を `.to_crs()` ではなく、間違えて定義用の `crs=` で上書きしようとしてしまった場合（データ構造が破壊され、投影変換に失敗します）。
*   **【3点減点】**：EPSGコードの数値（`6677`）のタイポ、あるいは指定ミス。

</details>

---

## 📚 第7回のまとめと、次回の「空間分析」へ向けて

全200分の集中講義、大変お疲れ様でした！！！

これまでは「表データ」や「グラフの画像」を扱っていた皆さんが、本日から**「地理空間データ（GIS）」**という、現実の社会・位置情報をコンピュータ内にコピーして分析する強力な技術の扉を開けました。

### 📌 今日の最重要おさらい
1.  **レイヤー構造**：地図は「道路」「建物」といった透明なシート（トッピングケーキ）を重ねることでできており、コンピュータ上で自由にマージや条件分析ができる。
2.  **点・線・面（ベクタデータ）**：拡大しても崩れないシャープな図形。それぞれ `Point`, `LineString`, `Polygon` というデータ型でGeoPandasのgeometry列に保存される。
3.  **座標系の歪み（みかんの皮）**：地球は丸いため、平らにすると必ずどこかに歪みが出る。GPS等の経緯度を使う「地理座標系（度単位）」から、正確な「平面直角座標系（メートル単位：関東は `EPSG:6677`）」に変換（`.to_crs()`）して計算を行うのがGIS分析の鉄則。
4.  **Shapefileの罠**：`.shp` だけでは動かない。`.dbf` や `.shx` など同じ名前の複数ファイルをセットで扱う「長老」ならではのルールを守ること。
5.  **Point(経度, 緯度)の順**：数学座標の `(X, Y)` に基づくため、必ず `Point(経度, 緯度)` の順で指定すること。

次回（第8回）は、本日学んだ座標変換（メートル単位）を最大限に活かし、**「地理空間データの活用1：商圏バッファの作成と空間結合」**を学びます。
「カフェの店舗から半径1km以内の商圏を切り出して売上を予測する」
「地域のGPSピンが、何区ポリゴンのなかに含まれているかを全自動で交差判定する」
といった、ビジネスや防災、シビックテックの現場で最もエキサイティングで即戦力となる計算処理が次々と登場します。

次回もPythonと地図の力をさらに高めていきましょう。どうぞお楽しみに！お疲れ様でした！！！
