# 第5回 Webサービスの活用1：StreamlitによるWebアプリ開発の基礎（200分集中講義版）

皆さん、こんにちは！プログラミング応用A（コンピューティング応用G）、第5回の講義です。担当の教員です。
前回（第4回）は、Pandasを使って「平均値の罠」を見破り、データのばらつき（標準偏差）や関係性の強さ（相関係数）を可視化する「データ解析」の基本を学びました。

今回からは、データ分析をさらに一歩進めて、**「Webサービスの活用」**という新しい領域に突入します！
具体的には、Pythonだけで本格的なWebアプリケーションを構築できる魔法のライブラリ **Streamlit（ストリームリット）** を使って、あなた専用の「データダッシュボード」を作っていきます。

この講義は**「200分（2コマ連続、または集中講義）」**のスケールで、基礎から実務レベルまでを一本の線で繋ぎます。
「HTMLやCSS、JavaScriptは難しそう…」「Webサーバーって何？」と思うかもしれませんが、**大丈夫です！**
Streamlitを使えば、そうした難しいデザインのコードは一切書く必要がありません。Pythonのコードを数行書くだけで、オシャレなボタンやスライダー、グラフが画面に自動的に配置されます。

前半の100分では、Google Colab上でWebアプリを安全に起動するための特別な「トンネリング技術」の仕組みと構築を。
後半の100分では、オシャレなボタンやスライダーを配置し、ユーザーの操作に応じて画面が自動で書き換わる「対話型（インタラクティブ）」なプログラムの作り方と、Streamlit特有の動作ルールを深く学んでいきます。

自分で作ったプログラムがWebブラウザ上で「動くアプリ」として生命を吹き込まれる感動を、ぜひこの200分間で体験してください！

---
## 🎯 本日の到達目標

この200分の集中講義が終わるころには、以下のことができるようになります。

1. **データのWebアプリ化（ダッシュボード化）の目的と意義**（なぜデータを静止画やExcelではなく、アプリとして公開するのか）をビジネスや社会の視点から説明できる。
2. **Google Colab上でStreamlitを安全に起動**し、ローカルトンネル（localtunnel）の仕組みを用いて、スマートフォンや外部ブラウザからアクセス可能な公開URLを発行できる。
3. **Streamlitの基本出力コマンド**（タイトル、見出し、テキスト、マークダウン）を使い分けて、Web画面のレイアウトを美しく構成できる。
4. **様々な入力部品（ウィジェット）**（ボタン、スライダー、セレクトボックス、チェックボックス、テキスト入力）を自由自在に配置できる。
5. **Streamlit特有の動作システム「Rerun（再実行）」**と**「st.buttonの消える罠」**の仕組みをロジカルに説明し、バグのない対話型（インタラクティブ）アプリを設計できる。

---
## ⏱️ 200分講義のタイムテーブル

2コマ連続（計200分）の時間を、講義（解説）、ライブデモ（教員の実演）、個別ハンズオン（学生の体験）を組み合わせて進行します。

### 【前半：環境構築とWeb公開の仕組み（100分）】
*   **00:00 - 00:15 (15分) イントロダクション ＆ データの「アプリ化」の価値**
    *   なぜPythonのグラフ画像をメールで送るだけではダメなのか？ダッシュボードのビジネス上の役割を解説。
*   **00:15 - 00:55 (40分) 5.1 Google ColabにおけるWebアプリ開発の壁とトンネリング技術**
    *   クラウド上の仮想マシン（Colab）の中で動いているアプリを、どうやって手元のパソコンやスマホのブラウザに映し出すのか？ 「ローカルトンネル（localtunnel）」と「ポート番号（8501）」の概念をビジュアル図解。
    *   セキュリティキーとしての「IPアドレス（パスワード）」の意味を解説。
*   **00:55 - 01:35 (40分) 5.2 実践ハンズオン：Hello Worldアプリの起動（手順A, B, C）**
    *   `%%writefile app.py` を使った、Colab内でのPythonファイル自動書き出しの魔法。
    *   パッケージのインストール、IPアドレスの確認、トンネル起動コマンドの実行。
    *   クラスの全員が「自分のブラウザに、自分で作った最初のWebアプリが表示される」まで手厚く巡回サポート。
*   **01:35 - 01:45 (10分) 前半のまとめ ＆ 10分小休止**

---

### 【後半：基本ウィジェットとリアクティブRerunの理解（100分）】
*   **01:45 - 02:15 (30分) 5.3 Streamlitの表示系コマンドと入力部品ウィジェット**
    *   テキスト、タイトル、マークダウンの書き分け。
    *   スライダー（`st.slider`）、セレクトボックス（`st.selectbox`）、チェックボックス（`st.checkbox`）の配置と、戻り値（変数）の受け取り。
*   **02:15 - 02:45 (30分) 5.4 Streamlitの動作メカニズム「Rerunシステム」と「st.buttonの罠」**
    *   **【本日の最重要ポイント】** なぜスライダーを動かすたびに、Pythonプログラムが「上から下まで、最初から毎回実行され直す」のか？
    *   初心者が100%はまる、**「ボタン（st.button）を押して計算結果を表示したのに、次にスライダーを動かした瞬間、その計算結果が一瞬で消え去ってしまうバグ」**のメカニズムを、アスキーアートを用いて丁寧に図解解説。
*   **02:45 - 03:05 (20分) 5.5 授業内確認練習問題（クイズ：5問）**
    *   理論・挙動の確認クイズをクラス全員で解き、ボタンの仕様やRerunの概念を頭に定着させます。
*   **03:05 - 03:20 (15分) 総括 ＆ FAQの解説 ＆ 次回（ダッシュボード・グラフ連動）への接続**
    *   起動エラー時の対処、ファイル構成の確認。
    *   次回（第6回）、本日作成したUIに「Pandasデータ処理」と「Matplotlibグラフ」を結合し、本物の売上分析ダッシュボードを完成させるロードマップを宣言。

※注意：授業時間外の自習・評価レポート用として、末尾に「総合演習課題（5問）」および「教員用採点基準・詳細解説」を完備しています（200分の講義時間内には含めません）。

---
## 5.1 なぜデータを「アプリ」にするのか？（ダッシュボード化の意義） (約15分)

### データ分析を「社会や現場に届ける」最後の壁
これまでの授業で、私たちはデータを収集し、Pandasで集計し、Matplotlibでグラフを描くことができるようになりました。
しかし、その分析結果を「プログラミングを全く知らない会社の上司、店舗マネージャー、あるいは一般のお客様」に見せたいとき、どう共有すればいいでしょうか？

*   **「Pythonのコードと、グラフの画像ファイルをメールで送る」**
    *   😱 **問題点**：相手はPythonを実行できません。また、画像は「静止画」なので、相手が「特定の店舗だけ」「特定の年代だけ」に絞り込んだデータを見たいと思っても、拡大したり、条件を変えて計算し直したりすることができません。
*   **「Excelファイルを共有する」**
    *   😱 **問題点**：共有された相手が誤って関数や数式を壊して（消して）しまったり、スマートフォンからだと画面が崩れて操作しづらかったりします。

一番良い解決策は、**「誰でもスマホやパソコンのブラウザからアクセスして、ボタン一つでデータを絞り込める『Webアプリ』にして渡すこと」**です。このように、データと操作盤が合体した画面を**「データダッシュボード」**と呼びます。

**【ダッシュボード開発の今と昔（イメージ図）】**
```text
▼ 昔の開発方法（学習コスト：極大 😱）
  Python(分析) ＋ HTML/CSS(画面デザイン) ＋ JavaScript(動き) ＋ サーバー構築
  ➡ 覚えることが多すぎて、文系学生やデータ分析者は画面を作る前に挫折してしまいます。

▼ Streamlitを使った開発（学習コスト：極小 😍）
  [ 🐍 Pythonコード数行 ] ──▶  [ 🎨 Streamlitが自動変換 ] ──▶ [ 💻 プロ級のWebアプリ ]
  ➡ HTMLもサーバーの知識も不要！いつものPythonコードがそのまま美しいアプリに生まれ変わります。
```

---
## 5.2 Google ColabにおけるWebアプリ開発の壁とトンネリング技術 (約40分)

通常、Streamlitはお手元のパソコン（ローカル環境）のターミナル（黒い画面）で起動します。
しかし、本コースでは誰もがすぐに同じ環境で学習を始められるよう、インストールの手間がない **Google Colab** を利用しています。

ここで1つ、ネットワーク上の重大な問題が発生します。
Google Colabは、**「Googleが管理する、インターネットの向こう側（クラウド）にある仮想パソコン」**です。
Colabの中で「Webアプリ（Streamlit）」を起動しても、そのアプリはクラウドの奥深くに隠れているため、手元にあるあなたのパソコンのブラウザ（Google Chromeなど）から、直接住所（URL）を入力して覗きに行くことができません。

**【Colabと手元のブラウザの距離（イメージ図）】**
```text
  [ 💻 あなたのパソコン ]                      [ 🏛️ Google Colab（クラウド） ]
  ┌───────────────────┐                      ┌────────────────────────────┐
  │  Chromeブラウザ    │  ──(見に行けない)──▶ │   Streamlit起動！          │
  │  (手元)           │  ⚠️ 壁（ファイアウォール）│   (ポート: 8501)           │
  └───────────────────┘                      └────────────────────────────┘
```

この「壁」を突破し、一時的に外からアプリを覗き見するための安全な通路（トンネル）を作ってくれるツールが、**localtunnel（ローカルトンネル）** です。

1.  Colabの中でStreamlitアプリを起動します（内部ポート **`8501`** 番という窓口で待機します）。
2.  localtunnelが、8501番の窓口と、世界中からアクセスできる一時的なWebアドレス（`https://xxxx.loca.lt`）を直結します。
3.  あなたがその一時的アドレスにアクセスすると、トンネルを通って、Colabの中のStreamlitアプリの画面が手元のブラウザに映し出されます。

```text
  [ 手元のブラウザ ] ──▶ [ https://xxxx.loca.lt (トンネルURL) ] ──▶ [ Colabの8501番窓口 ] ──▶ [ アプリ画面 ]
```

### セキュリティの鍵「IPアドレス」
トンネルを開通してアクセスする際、不審なハッカーの侵入を防ぐため、localtunnelから「接続先Colabのセキュリティキー（パスワード）」を入力するよう求められます。
このキーとして使用するのが、あなたのColab環境に割り当てられた **「IPアドレス（4つの数字の羅列）」** です。これは後ほど、手順の中で実際に取得します。

---
## 5.3 実践ハンズオン：Hello Worldアプリの起動（手順A, B, C） (約40分)

それでは、実際に手を動かして、あなた専用の最初のWebアプリをインターネット上に誕生させてみましょう！
以下の「黄金の3ステップ」を順番に実行していきます。

### ステップ 1: 必要なライブラリのインストール
まずは、Colabのセルに以下のコマンドを入力して実行し、Streamlitと、トンネリングツール（localtunnel）をColabの仮想パソコンにインストールします。
※コマンドの先頭についている「`!`」は、PythonではなくColabのシステムに命令を下すおまじないです。

```python
# 必要なパッケージをインストールします（1回実行すればOKです）
!pip install -q streamlit
!npm install -g localtunnel
```

---

### 🚀 手順A: アプリのソースコードを書き出す（`%%writefile`）

Colabは通常、セル単位でプログラムを実行しますが、Webアプリを起動するためには、プログラムを「1つの独立したファイル（`app.py`）」として保存する必要があります。

セルの1行目に **`%%writefile app.py`** と書くと、そのセルに記述されたコードが、Colabのフォルダ内に `app.py` という名前のファイルとして自動で書き出され、保存されます。

以下のコードを新しいセルにコピペして、実行してみましょう！

```python
%%writefile app.py
# -------------------------------------------------------------
# ここに書いたコードが、そのまま「app.py」というファイルに書き出されます
# -------------------------------------------------------------
import streamlit as st

# アプリのタイトルを表示
st.title("Musashi Cafe アプリ ☕")

# メッセージを表示
st.write("プログラミング応用Aの授業で作成した、私の最初のWebアプリです！")
st.write("スライダーやボタンを追加して、これからPOSレジやダッシュボードに進化させていきます。")
```
*実行後、Colabの画面左側にある「フォルダ（ファイル）」アイコンをクリックすると、`app.py` というファイルが生成されていることを確認してください。*

---

### 🔑 手順B: トンネルの鍵（あなたのIPアドレス）を確認する

次に、トンネルを開くためのセキュリティパスワード（Colabの外部IPアドレス）を取得します。
以下のコマンドを実行すると、数字の羅列（例：`34.125.12.34`）が表示されます。**この数字をコピーしてメモ帳などに貼り付けておいてください。**

```python
# あなたのColab環境のIPアドレス（パスワード）を表示します
!wget -qO- ipv4.icanhazip.com
```

---

### 🌐 手順C: アプリをバックグラウンドで起動し、トンネルを開通する！

いよいよアプリを裏側で起動させ、世界に公開します。
以下のコマンドを実行してください。

```python
# 1. Streamlitをバックグラウンド（裏側）で実行し、ログファイルを保存します
!streamlit run app.py &>/dev/null &

# 2. ポート8501番（Streamlitの標準ポート）を外部に公開するトンネルを開通します
!npx localtunnel --port 8501
```

**【接続のやり方（超重要）】**
1. 上記のセルを実行すると、数秒後に画面下に **`your url is: https://xxxx.loca.lt`** という青いリンクが表示されます。このリンクをクリックしてください。
2. 画面が切り替わり、「**Friendly Reminder**」という英文のセキュリティ画面（または「Tunnel Password」入力画面）が開きます。
3. 画面中央の **「Tunnel Password」** という入力欄に、先ほどの **【手順Bで確認したIPアドレス（数字の羅列）】** をそのまま貼り付け（ペースト）してください。
4. その下にある **「Click to Submit」**（またはSubmit）ボタンをクリックします。
5. 画面が読み込まれ、あなたが作った **「Musashi Cafe アプリ ☕」** の真っ白で美しいWeb画面が目の前に起動します！

---
## 5.4 Streamlitの表示系コマンドと入力部品ウィジェット (約30分)

最初のアプリが起動したことに感動したら、今度はアプリの中身を豪華に、そしてインタラクティブにしていきましょう！
アプリのファイルを書き換えるときは、常に **「手順A（%%writefile app.py）のセルを書き換えて実行」➡「開いているWebアプリ画面の右上にある『Always rerun（またはRerun）』ボタンを押す」** という開発サイクルを回します。

### 1. 情報を伝える表示系コマンド
Streamlitには、文字を綺麗にデザインして表示するためのコマンドがたくさん用意されています。

```python
st.title("大きなタイトル") # ページの一番上に置く大見出し
st.header("中見出し")      # セクションの区切りに使う見出し
st.subheader("小見出し")   # さらに細かい段落の見出し
st.write("通常のテキスト。Pandasの表やグラフもこれ1つで綺麗に表示できます。")
st.markdown("**太字** や *斜体*、[リンク](https://...) などを自由に書くためのマークダウン。")
```

### 2. ユーザーの入力を受け取るウィジェット（入力部品）
ここからがStreamlitの真骨頂です。エクセルや通常のWebサイトにあるような操作部品を、たった1行で設置できます。
ユーザーが操作した結果（選択した文字や数値）は、**そのまま左辺の変数にリアルタイムに代入されます。**

#### ① ドロップダウンメニュー（`st.selectbox`）
```python
# st.selectbox("ラベル名", [選択肢のリスト])
selected_item = st.selectbox(
    "ご注文の商品を選んでください:",
    ["ブレンドコーヒー (450円)", "ダージリン紅茶 (400円)", "チョコケーキ (550円)"]
)
# ユーザーが「チョコケーキ (550円)」を選ぶと、変数 selected_item にその文字列が代入されます。
```

#### ② 数値調整スライダー（`st.slider`）
```python
# st.slider("ラベル名", 最小値, 最大値, 初期値)
quantity = st.slider("数量を選択してください:", 1, 10, 1)
# ユーザーがスライダーを「3」に合わせると、変数 quantity に 3 という数値が代入されます。
```

#### ③ チェックボックス（`st.checkbox`）
```python
# st.checkbox("ラベル名")
# チェックが入ると True、外れていると False という真偽値（boolean）が代入されます。
add_whipped_cream = st.checkbox("ホイップクリームを追加しますか？ (+50円)")
```

---
## 5.5 ハンズオン：自動計算POSレジアプリの作成 (約30分)

それでは、これまでに学んだ「表示系コマンド」と「入力部品」を組み合わせ、ユーザーの選択に応じて合計代金を自動で計算し、お見積書を画面に表示する「自動POSレジアプリ」を作ってみましょう！

手順Aのセル（`app.py` を書き出すセル）の中身を、以下のコードで丸ごと上書きして実行し、ブラウザ画面の右上にある「Rerun（再読み込み）」をクリックしてください。

```python
%%writefile app.py
import streamlit as st

# 1. ページのヘッダーと見出し
st.title("カフェ自動POSレジアプリ ☕")
st.write("商品の選択と数量、トッピングを選択すると、自動でお会計を瞬時に計算します。")

st.header("🛒 お会計シミュレーター")

# 2. セレクトボックス（商品の選択）
selected_item = st.selectbox(
    "ご注文の商品を選んでください:",
    ["ブレンドコーヒー (450円)", "アイスティー (400円)", "抹茶パフェ (600円)"]
)

# 選択肢に応じて「基本単価」をPythonの条件分岐で設定します
if "コーヒー" in selected_item:
    base_price = 450
elif "アイスティー" in selected_item:
    base_price = 400
else:
    base_price = 600

# 3. スライダー（数量の選択）
quantity = st.slider("個数を選択してください:", 1, 10, 1)

# 4. チェックボックス（トッピングの選択）
add_cream = st.checkbox("ホイップクリームを追加する (+50円/個)")

# トッピング料金の加算判定
topping_price = 0
if add_cream:
    topping_price = 50

# 5. 計算ロジック（単価 ✕ 数量 ＝ 合計金額）
total_price = (base_price + topping_price) * quantity

# 6. 結果の表示（st.metric というオシャレなKPIメーターを使います！）
st.subheader("💳 お会計結果")

col1, col2 = st.columns(2) # 画面を2分割して横並びにする
with col1:
    st.write(f"**ご注文商品**: {selected_item}")
    st.write(f"**数量**: {quantity} 個")
    st.write(f"**トッピング**: {'あり' if add_cream else 'なし'}")
with col2:
    # st.metric(タイトル, 数値) で、ダッシュボード風のカッコいい表示になります
    st.metric(label="合計お支払金額 (税込)", value=f"{total_price:,} 円")

# お礼のメッセージ
st.success("お買い上げありがとうございます！")
```

スライダーを「5」に動かしたり、トッピングにチェックを入れたり外したりしてみてください。
**カチカチと動かすたびに、画面右側の「合計お支払金額」の数値が一瞬で、1ミリの遅れもなく連動して再計算されたでしょうか？**

これが、データをWebアプリにして価値を提供する「対話型ダッシュボード」の第一歩です！

---
## 5.6 最重要：Streamlitの動作メカニズム「Rerunシステム」と「st.buttonの罠」 (約30分)

Streamlitは非常に簡単にアプリが作れますが、他のプログラミング言語とは全く異なる**「特有の動作ルール（動作システム）」**を持っています。
これを知らないと、今後「なぜかアプリが思い通りに動かない！」という深刻な泥沼（バグ）にはまることになります。200分集中講義の後半の主役として、この仕組みを完璧に脳に叩き込みましょう！

### 1. 「Rerun（再実行）」システムの真実

通常のPythonスクリプトは、上から下まで実行したら、そこでプログラムは「終了」して消えてしまいます。
しかし、Streamlitは違います。

> **🌟 Streamlitの鉄則：**
> ユーザーが画面上の何か（スライダー、セレクトボックス、チェックボックスなど）を**「1ミリでも動かした瞬間」**、Streamlitは裏側で、記述されたPythonプログラム（`app.py`）を**「1行目から最後（一番下）まで、毎回すべて丸ごと再実行（Rerun）」**します。

**【Rerun（再実行）の流れ】**
1. スライダーを「2」から「3」に動かす。
2. Pythonが「よっしゃ、もう一度1行目からやり直すぞ！」と立ち上がる。
3. `quantity = st.slider(...)` の行に来たとき、画面のスライダーが「3」であることを読み取って、変数 `quantity` に `3` を代入する。
4. 下の行の `total_price = ...` を計算し直し、画面全体の数値を書き換える。
5. プログラムの最後まで到達したら、一時停止して次の操作をじっと待つ。

この「毎回、最初からすべてをやり直す」というシンプルな豪快さがあるからこそ、私たちは面倒な「画面の書き換え処理」を一切書かなくても、常に最新の計算結果を画面に表示できるのです。

---

### 2. 初心者が100%はまる「st.button の消える罠」

この「毎回最初からやり直す（Rerun）」という仕組みが原因で、ボタン（`st.button`）を扱う際に、初心者が100%パニックを起こす「罠」が発生します。

例えば、以下のようなアプリを思い浮かべてください。
*   「送信」ボタンを押すと、画面に「お見積もりを送信しました！」とメッセージが出る。
*   その下には、数量を調整するスライダーがある。

実際にこのコード（バグのある例）を実行したと仮定して、何が起きるか見てみましょう。

```python
# ⚠️ バグのあるプログラムのイメージ
import streamlit as st

st.title("お見積もり送信アプリ")
quantity = st.slider("数量を選んでください", 1, 10, 1)

# ボタンの設置
# ボタンがクリックされた「その瞬間だけ」、st.button は「True（真）」になります
if st.button("お見積もりを確定して送信"):
    # ユーザーがクリックした瞬間だけ、この中身が実行されます
    st.success("🎉 メールを送信しました！")
```

**【バグの発生プロセス（なぜ消えるのか？）】**

1.  **初期画面**：
    *   ユーザーが画面を開く。プログラムが上から下まで走り、ボタンは押されていないので `st.button` は `False`。画面にはスライダーとボタンだけがある。
2.  **確定ボタンをクリックする**：
    *   クリックした瞬間、Rerun（再実行）が発生！
    *   1行目から再実行され、`st.button` の判定部分に来る。
    *   「今、クリックされた瞬間」なので、`st.button` は **`True`** を返す。
    *   `if` の中に入り、画面に **「🎉 メールを送信しました！」** という緑のボックスが表示される。**（ここまでは大成功）**
3.  **スライダーを動かして個数を変更する**：
    *   個数を変更した瞬間、またRerun（再実行）が発生！
    *   1行目から再びプログラムが走り始める。
    *   `st.button` の判定部分に来る。
    *   しかし、今回のRerunのきっかけは「スライダーの移動」であり、「ボタンのクリック」ではありません。
    *   したがって、`st.button` は **`False`**（押されていない）に戻ってしまいます！
    *   `if` の中の処理はスキップされ、**画面から「🎉 メールを送信しました！」というメッセージが、煙のように綺麗に消え去ってしまいます。**

**【st.buttonの消える罠（イメージ図）】**
```text
  【ボタンを押した瞬間】                    【スライダーを動かした瞬間】
  1. Rerun発生（ボタン押された）             1. Rerun発生（スライダー動いた）
  2. st.button が True になる                2. st.button が False に戻る
  3. [🎉 送信完了！] ──▶ 表示される！       3. [🎉 送信完了！] ──▶ 画面から消滅する！😱
```

### どうやって対策するのか？
ボタンを押した「状態」を維持するためには、画面が再起動（Rerun）してもデータが消えない特別な保管庫である **「セッション状態（`st.session_state`）」** という高度な仕組みを使う必要があります。
今回の基礎クラス（第5回）では、「ボタンは、押したその瞬間の一時的なアクション（おまじないを実行するなど）にのみ使い、計算結果の表示などの状態キープにはスライダーやチェックボックスを主役に使う！」というルールを覚えておきましょう。

---
## 📝 授業内確認練習問題（理論・挙動チェック：5問） (約20分)

後半の講義で学んだ「Streamlitの仕組み（Rerun）」や「ウィジェットの挙動」を復習しましょう。
頭の中で答えを決定してから、クリックして解答と解説を確認してください。

### 練習問題 1: 従来のWeb開発との最大の違い
従来の一般的なWebサイト制作では、画面にオシャレなボタンを配置したり、入力された数値を取得して画面を書き換えるために「HTML」や「CSS」に加えて「JavaScript」という言語を書く必要がありました。
Streamlitを使う場合、それらの知識が一切不要であるのはなぜでしょうか？ 仕組みを説明してください。

<details>
<summary>👉 練習問題 1 の解答と解説を見る</summary>

*   **解答例**:
    *   Streamlitが、記述された**Pythonコードを読み取り、対応するWeb用の画面デザイン（HTML/CSS）や動作プログラム（JavaScript）へと、裏側で全自動で「翻訳（コンパイル）」してブラウザに送信してくれているから。**
*   **解説**:
    *   この自動翻訳のおかげで、私たちは「データの分析（Python）」に100%集中したまま、プロ級のWebアプリケーション画面を瞬時に手に入れることができます。

</details>

---

### 練習問題 2: Colab特有の「窓口」と「トンネル」
Google ColabでStreamlitを動かす際、`!npx localtunnel --port 8501` というコマンドを実行しました。
1. この **「`8501`」** という数字は何を表しているでしょうか？
2. なぜ、ただ起動するだけでなく「localtunnel」という追加のツールを使う必要があるのか、理由を説明してください。

<details>
<summary>👉 練習問題 2 の解答と解説を見る</summary>

*   **正解**:
    1.  **8501**: Streamlitアプリが起動したときに、データを受信するためにColab内で開く「標準の窓口番号（ポート番号）」のこと。
    2.  **localtunnelが必要な理由**: Google Colabのパソコンは「Googleのクラウド内部（プライベートネットワーク）」にあり、外からのアクセスを遮断する壁があるため、手元のブラウザから直接アクセスできない。localtunnelを使うことで、8501番窓口から手元のブラウザを直結する「一時的な公開トンネルURL」を発行できるから。
*   **解説**:
    *   「起動する窓口（ポート：8501）」と「そこへ繋ぐ外の道路（localtunnel）」がセットになることで、世界中のどこからでも（自分のスマホからでも！）Colab上のアプリを動かせるようになります。

</details>

---

### 練習問題 3: スライダーを動かした時の「Rerun」の動き
Streamlitアプリ上で、ユーザーが数量調整スライダーを `2` から `5` に変更しました。
この時、裏側でPythonプログラム（`app.py`）はどのようなステップで実行され、画面が書き換わるでしょうか？ Streamlitのルール（鉄則）に沿って説明してください。

<details>
<summary>👉 練習問題 3 の解答と解説を見る</summary>

*   **解答例**:
    1.  スライダーが動いた瞬間、裏側で**Rerun（自動再実行）**がトリガーされる。
    2.  `app.py` というPythonファイルが、**「1行目から一番最後の行まで、最初から丸ごと実行し直される」**。
    3.  スライダーのコードの行に来たとき、画面上のスライダーが「5」に合わされていることをシステムが検知し、変数に `5` が代入される。
    4.  それ以降の合計金額の計算や画面表示コードが新しい「5」を使って再計算され、ブラウザの画面が一瞬で最新の状態に更新される。
*   **解説**:
    *   「上から下まで、毎回全部やり直す」というシンプルな豪快さこそが、Streamlitの俊敏なリアルタイム連動（リアクティブ）の秘密です。

</details>

---

### 練習問題 4: チェックボックスの「戻り値（データ型）」
チェックボックスを設置する `is_takeout = st.checkbox("お持ち帰りですか？")` というコードを実行した際、変数 `is_takeout` の中に代入されるデータは、次のうちどれでしょうか？

1.  チェックが入っている場合は `"お持ち帰りですか？"` という文字列、入っていない場合は `None`。
2.  チェックが入っている場合は `True`、入っていない場合は `False` という真偽値（boolean型）。
3.  チェックが入っている場合は `1`、入っていない場合は `0` という整数値。

<details>
<summary>👉 練習問題 4 の解答と解説を見る</summary>

*   **正解**: **2**
    *   チェックが入っている時は **`True`**、外れている時は **`False`** という「真偽値（boolean型）」が変数に代入されます。
*   **解説**:
    *   戻り値が `True` / `False` のため、その後の条件分岐で `if is_takeout:`（もしお持ち帰りなら）というように、非常にシンプルで美しい条件分岐コードを書くことができます。

</details>

---

### 練習問題 5: 「ボタンが消えるバグ」のデバッグ
以下のコードは、ボタンを押した際に「注文を受け付けました！」と画面に緑の文字で表示し、その下でスライダーを使ってトッピングの量を調整するアプリです。

```python
import streamlit as st

st.title("注文システム")

if st.button("この内容で注文する"):
    # 注文処理
    st.success("🎉 注文を受け付けました！")

# トッピング量
topping_qty = st.slider("トッピングの量 (g)", 0, 100, 10)
```

このアプリを実行し、ユーザーが以下の操作を行いました。
*   手順①：画面を開き、「この内容で注文する」ボタンをクリックした。
    ➡ 画面に「🎉 注文を受け付けました！」と正しく表示された。
*   手順②：次に、その下にあるスライダーを動かして `20g` に変更した。
    ➡ その瞬間、画面から「🎉 注文を受け付けました！」という緑のメッセージが**消えてしまった。**

なぜ手順②の操作をした瞬間に、メッセージが消えてしまったのでしょうか？ 理由を説明してください。

<details>
<summary>👉 練習問題 5 の解答と解説を見る</summary>

*   **解答例**:
    *   スライダーを動かした瞬間、**アプリ全体の再実行（Rerun）が1行目から走り出すため。**
    *   再実行が走った際、今回の実行トリガーは「スライダーの移動」であり「ボタンのクリック」ではないため、`st.button` の判定は **`False`**（押されていない）に戻ってしまう。
    *   その結果、`if st.button` の中の `st.success` 処理がスキップされ、画面から消滅してしまう。
*   **解説**:
    *   これがStreamlit開発における「最大の罠」です。ボタンは「押したその一瞬だけしか True にならない」という性質をしっかりと理解しておきましょう。

</details>

---
## 💻 総合演習課題（難易度別5問：時間外用）

ここからは、皆さんがこれまでに身につけたすべてのPythonおよびStreamlitのスキルを試す「最終評価課題」です。
すべての課題は、**「Google Colabのセルに記述して実行すれば、その場で `app.py` というファイルに書き出され、手順Cを実行することでブラウザ上で即座に動作テストを行える」**ように設計されています。

1行ずつコードを確認しながら、穴埋め（`# TODO:` の部分）を完成させてください。

---

### 課題 1: 割引率調整スライダーと価格シミュレーター（難易度：易）
カフェのお会計において、サイドバーに設置したスライダーで「割引率（%）」を自由に調整し、その割引が適用された「割引後の支払金額」をメイン画面にリアルタイム表示するアプリを作成してください。

*   **【要件】**
    1.  サイドバー（`st.sidebar.slider`）に、割引率を設定するスライダーを配置（最小: 0%, 最大: 50%, 初期値: 10%）。
    2.  基本価格は **500円** とする。
    3.  合計支払金額は `基本価格 ✕ (1 - 割引率 ÷ 100)` で求め、整数（`int`）にキャストする。
    4.  計算結果を画面に `st.metric` を用いて美しく表示する。

```python
%%writefile app.py
import streamlit as st

st.title("カフェ割引シミュレーター ☕")

# 基本価格
base_price = 500

# TODO: 1. サイドバーに 0%〜50% の範囲を1%刻みで選べるスライダー「st.sidebar.slider」を設置し、変数 discount に代入する。初期値は 10% とすること。
discount = 

# TODO: 2. 割引後の金額を計算する（基本価格 ✕ (1 - 割引率/100)）。最後に int() で整数値に変換すること。
discount_price = 

# 画面への出力
st.write(f"通常価格: {base_price} 円")

# TODO: 3. st.metric を使い、ラベルを「割引適用後の価格」、値を「〇〇 円」の形式で表示する。
st.metric(                               )
```

#### 🛠️ 課題1：教員用採点基準（10点満点）
*   **10点**: 完璧に動作する。サイドバーへのスライダー配置、計算ロジック、metricの表示がすべて正しい。
*   **8点**: スライダーは配置できているが、割引率の計算ロジックが誤っており、価格が正しく引かれない。
*   **6点**: スライダーをサイドバー（`st.sidebar.slider`）ではなく、メイン画面（`st.slider`）に配置してしまっている。
*   **4点**: 計算後の `int()` キャストを忘れており、金額が `450.0 円` のように少数表示になってしまっている（減点-2点）。

---

### 課題 2: カテゴリ選択に応じたメニューの動的絞り込み（難易度：易）
あらかじめ用意されたデータ（カフェのメニュー）から、ユーザーがセレクトボックスで選択したカテゴリ（ドリンク、またはデザート）に属する商品だけを画面に動的に表示する、簡易データベース検索アプリを作成してください。

*   **【要件】**
    1.  セレクトボックス（`st.selectbox`）で、「ドリンク」または「デザート」を選択できるようにする。
    2.  選択されたカテゴリに応じて、表示するリストを切り替える。
        *   ドリンクの場合：`["ブレンドコーヒー", "宇治抹茶ラテ", "キャラメルマキアート"]`
        *   デザートの場合：`["ベイクドチーズケーキ", "窯焼きアップルパイ", "特製クラシックプリン"]`
    3.  切り替えたリストを、画面に `st.write`（またはリスト表示）で綺麗に表示する。

```python
%%writefile app.py
import streamlit as st

st.title("メニュー検索カタログ 📖")
st.write("カテゴリを選ぶと、おすすめ商品の一覧を自動で切り替えます。")

# TODO: 1. セレクトボックス「st.selectbox」を配置し、ユーザーに「ドリンク」か「デザート」を選択させる。選ばれた文字をカテゴリ変数「category」に代入。
category = 

# TODO: 2. if-else文を使い、categoryが「ドリンク」の場合と「デザート」の場合で、表示するリストを変数「items」に代入する。
if                     :
    items = ["ブレンドコーヒー", "宇治抹茶ラテ", "キャラメルマキアート"]
else:
    items = ["ベイクドチーズケーキ", "窯焼きアップルパイ", "特製クラシックプリン"]

# 3. 画面に選ばれたおすすめ商品を表示
st.header(f"✨ おすすめの{category}一覧")

# リストの商品を1つずつ表示
for item in items:
    st.write(f"- {item}")
```

#### 🛠️ 課題2：教員用採点基準（10点満点）
*   **10点**: セレクトボックスが正しく設置され、if文によるリストの切り替え、forループによる個別描画が完璧に動作している。
*   **8点**: リストの切り替えはできているが、for文のインデントミスやタイポがあり、画面描画にエラーが発生する。
*   **6点**: セレクトボックスの選択肢に「ドリンク」「デザート」以外の不要な文字が混ざっており、if文の判定（完全一致）が機能せずにデザート側しか表示されない。

---

### 課題 3: 店舗売上目標達成判定チェッカー（難易度：中）
その日の「実績売上」をスライダーで入力し、あらかじめ設定した「目標売上」に到達しているかどうかをAI（プログラム）が自動で判定し、目標値までの「残り金額」と、達成・未達成のステータスを表示するアプリを作成してください。

*   **【要件】**
    1.  目標売上は固定で **200,000円** とする。
    2.  画面中央のスライダー（`st.slider`）で、本日の売上実績を入力（範囲: 0円〜300,000円, 1万刻み、初期値: 150,000円）。
    3.  もし、実績売上が目標売上（200,000円）以上の場合は、画面に `st.success("🎉 目標達成！おめでとうございます！")` と表示。
    4.  目標売上未満の場合は、画面に `st.warning("⚠️ 目標未達成。目標まであと〇〇円足りません。")` と引き算した不足金額を表示。

```python
%%writefile app.py
import streamlit as st

st.title("店舗売上目標達成チェッカー 📈")

# 目標売上（固定値）
target_sales = 200000

st.subheader(f"🎯 本日の目標売上: {target_sales:,} 円")

# TODO: 1. スライダーを設置し、本日の売上実績を 0〜300,000円、10,000円刻みで入力させる。初期値は 150,000円 とし、変数 actual_sales に入れる。
# ※ヒント: step=10000 オプションを指定すると、1万刻みの調整ができます。
actual_sales = 

# 画面への現在の状況表示
st.write(f"現在の実績売上: {actual_sales:,} 円")

# TODO: 2. if-else文を使い、目標を達成しているかどうかの自動判定と、不足額の計算・警告表示を行う
if actual_sales >= target_sales:
    # TODO: 達成時のメッセージを st.success で出力
    
else:
    # TODO: 不足金額（目標 - 実績）を計算する
    deficit = 
    # TODO: 不足時の警告メッセージを st.warning で出力。メッセージ内に deficit のカンマ区切り値を入れること。
    
```

#### 🛠️ 課題3：教員用採点基準（10点満点）
*   **10点**: 1万刻みのスライダーの設定、金額計算、if文によるメッセージの切り替えが美しく動作している。カンマ表記（`:,`）も適切に扱えている。
*   **8点**: 1万刻みの設定（`step=10000`）を忘れているため、1円単位でのスライダー調整になり、非常に操作しづらくなっている（減点-2点）。
*   **6点**: `st.success` と `st.warning` を逆に使っている、または不足額の計算が `actual_sales - target_sales` になっており、マイナスの金額が表示されてしまっている。

---

### 課題 4: チェックボックスによる複数トッピング複数加算POSレジ（難易度：中）
基本の「パンケーキ（600円）」に対し、3つのトッピング（ホイップ、チョコ、アイス）をチェックボックスで複数自由にトッピングし、数量を掛け合わせた「お見積もり合計金額」をリアルタイムで算出するオシャレなPOSシステムを構築してください。

*   **【要件】**
    1.  基本価格は **600円** とする。
    2.  3つのチェックボックス（`st.checkbox`）を設置する。
        *   「バニラアイス (+150円)」
        *   「チョコレートシロップ (+80円)」
        *   「完熟バナナスライス (+120円)」
    3.  チェックが入ったトッピングの料金を加算し、最後にスライダーで選択された「数量（1〜10個）」を掛け算して、総支払金額を算出する。
    4.  計算結果を `st.metric` で見やすくメイン画面に表示する。

```python
%%writefile app.py
import streamlit as st

st.title("🥞 プレミアム・パンケーキPOSレジ 🥞")
st.write("トッピングを自由に組み合わせて、オリジナルのカスタムパンケーキをお見積もりできます。")

base_price = 600
st.write(f"プレーンパンケーキ基本価格: {base_price} 円")

st.header("🧀 トッピングの選択")

# TODO: 1. 3つのトッピングチェックボックスを設置し、それぞれ変数に代入する（戻り値は True/False）
add_ice = st.checkbox("バニラアイス (+150円)")
add_chocolate = 
add_banana = 

# TODO: 2. if文を組み合わせ、チェックが入っている（True）のトッピング料金を合計して「topping_total」を計算する
topping_total = 0
if add_ice:
    topping_total += 150
if add_chocolate:
    
if add_banana:
    

st.header("🔢 数量の選択")
# TODO: 3. 数量（1〜10個、初期値1個）を選択するスライダー「quantity」を設置する
quantity = 

# TODO: 4. 総支払金額（(基本価格 ＋ トッピング合計単価) ✕ 数量）を計算する
total = 

# 画面表示
st.header("💳 お見積書（合計金額）")
st.metric(label="パンケーキセット 合計支払金額", value=f"{total:,} 円")
```

#### 🛠️ 課題4：教員用採点基準（10点満点）
*   **10点**: 3つのチェックボックスの値が適切に加算ロジックに反映され、数量の掛け算も含めて完璧な見積もりPOSアプリに仕上がっている。
*   **8点**: トッピングの加算処理で `topping_total += 150` とすべきところを、`topping_total = 150` と代入（`=`）で書いてしまっているため、最後にチェックを入れたトッピング代金しか計算に反映されない（減点-2点）。
*   **6点**: 計算順序（優先順位）を間違えて `base_price + topping_total * quantity` と括弧なしで計算したため、トッピング代金にだけ数量が掛けられ、パンケーキの基本代金が1個分しか支払いに加算されないバグがある（減点-4点）。

---

### 課題 5: 【最上級・集大成】武蔵大学学園祭総合POS見積システム（難易度：難）
これまでに学んだすべてのStreamlit要素（テキスト入力、セレクトボックス、スライダー、チェックボックス、カラム分割、KPIメーター表示、お祝い/エラー表示）を完全合体させ、学園祭のカフェ出店でiPadレジとしてそのまま実戦投入できる「総合POSシステム」を構築してください。

*   **【要件】**
    1.  **顧客名の入力**: `st.text_input` を使い、お客様のお名前（例: `"山田様"`)を入力させる（初期値: `"ゲスト様"`）。
    2.  **メインメニューの選択**: `st.selectbox` で3つの商品から選ばせる。
        *   「タピオカミルクティー (500円)」
        *   「自家製レモネード (450円)」
        *   「宇治抹茶宇治金時 (600円)」
        （商品名に応じて、Python側で「基本価格」を自動判定する）
    3.  **数量の選択**: `st.slider` で、1個から20個（初期値: 1個）の範囲で個数を選ばせる。
    4.  **会員割引特典（チェックボックス）**: `st.checkbox` を設置し、「武蔵大学の学生・教職員割引 (10%OFF)」にチェックが入った場合、お会計総額から一律10%（0.9倍）を割引する。
    5.  **深夜営業割増（チェックボックス）**: `st.checkbox` を設置し、「夜間営業対応 (+100円/個)」にチェックが入った場合、トッピングのように1個あたり100円を加算する。
    6.  **画面の構築（Columns）**: `st.columns(3)` を使って画面を3分割し、主要な指標（①お名前と商品詳細、②会員割引ステータス、③割引適用後の最終お支払総額 [st.metricでカンマ区切り表示]）を美しく横一列に並列表示する。
    7.  **エラーハンドリング**: もし、お客様のお名前が空欄（空の文字列 `""`）のまま注文しようとした場合、画面に `st.error("🚨 エラー: お客様のお名前を入力してください（空欄は不可です）。")` という警告を表示して、お会計画面を非表示（または処理を一時停止）にする。

```python
%%writefile app.py
import streamlit as st

# ページのタイトルとウェルカムメッセージ
st.title("🎪 武蔵大学学園祭 総合POSシステム ☕")
st.write("学園祭カフェ出店「しらきじカフェ」のiPad POSレジお見積もりシステムです。")

# ==========================================
# 1. 入力部品の配置（データの入力）
# ==========================================
st.header("📋 注文入力")

# TODO: ① st.text_input を使い、お客様の名前を「お客様名を入力してください」というラベルで入力させる。初期値は "ゲスト様" とする。
customer_name = 

# TODO: ② st.selectbox を使い、商品を「メニューの選択」というラベルでドロップダウン選択させる。
menu_item = st.selectbox(
    "メニューを選択してください:",
    ["タピオカミルクティー (500円)", "自家製レモネード (450円)", "宇治抹茶宇治金時 (600円)"]
)

# メニュー名から単価の自動判定
if "タピオカ" in menu_item:
    base_price = 500
elif "レモネード" in menu_item:
    base_price = 450
else:
    base_price = 600

# TODO: ③ st.slider を使い、個数を 1個〜20個（初期値 1個）の範囲で入力させる。
qty = 

st.subheader("⚙️ 特典・追加オプション設定")
# TODO: ④ st.checkbox を使い、「武蔵大学の学生・教職員割引 (10%OFF)」のチェックボックスを設置。変数「is_student」に代入。
is_student = 

# TODO: ⑤ st.checkbox を使い、「夜間営業割増 (+100円/個)」のチェックボックスを設置。変数「is_night」に代入。
is_night = 

# ==========================================
# 2. 計算ロジック（データの処理）
# ==========================================
# 夜間割増単価の加算
night_charge = 100 if is_night else 0
subtotal = (base_price + night_charge) * qty

# 会員（学割）10%OFFの適用
if is_student:
    total_amount = int(subtotal * 0.9)
else:
    total_amount = subtotal

# ==========================================
# 3. 画面のレイアウトと出力（データの表示）
# ==========================================
st.header("🧾 お会計明細票")

# TODO: ⑥ 名前が空欄（""）の場合のエラーハンドリングを記述する
if customer_name.strip() == "":
    # エラーメッセージを表示して、処理をストップ
    st.error("🚨 エラー：お名前が空欄です。お会計明細を処理できません。")
else:
    # 正常にお名前が入力されている場合のみ、お見積書をcolumnsで表示
    st.success(f"--- {customer_name} のご注文お見積書を正常に出力しました ---")
    
    # columnsで3等分割
    col1, col2, col3 = st.columns(3)
    
    with col1:
        st.write("**👤 お客様情報**")
        st.write(f"お名前: {customer_name}")
        st.write(f"選択商品: {menu_item}")
        st.write(f"単価: {base_price} 円 ✕ {qty}個")
        
    with col2:
        st.write("**🎟️ 特典・割増ステータス**")
        st.write(f"学割10%OFF: {'適用(10%引き)' if is_student else 'なし'}")
        st.write(f"夜間営業割増: {'適用(+100円/個)' if is_night else 'なし'}")
        
    with col3:
        st.write("**💰 お支払総額**")
        # TODO: ⑦ st.metric を使い、ラベル「最終ご請求金額」、値「〇〇 円」をカンマ区切りで表示する。
        st.metric(                                            )
```

#### 🛠️ 課題5：教員用採点基準（10点満点）
*   **10点**: エラーハンドリング（お名前が空欄の際の判定・処理分岐）、基本ウィジェットの配置、学割計算、夜間割増加算、カラム分割配置、すべての要件が1マスのバグもなく完璧に実装されている。
*   **8点**: カラム分割や計算ロジックはできているが、お名前の空欄判定で `.strip() == ""` などの例外処理をしていないため、スペースだけが入力された際に見積書がスカスカの状態で出力されてしまう（減点-2点）。
*   **6点**: 学割の割引率計算で、`int(subtotal * 0.9)` とキャストするのを忘れたため、割り引いた合計金額が小数点付き（小数型）でmetricに表示されてしまい、見た目の美しさが損なわれている（減点-4点）。

---
## 💡 総合演習課題の解答例（教員用・自習用）

（自分でたくさんタイポし、エラー画面と睨み合い、AIにアドバイスをもらってデバッグできたプロセスこそが、あなたの「開発力」を急成長させます。答えをコピーする前に、一度じっくり自力で試行錯誤しましょう！）

<details>
<summary>👉 課題1〜5の模範解答コードを見る（ここをクリックして展開）</summary>

### 課題 1 の解答例
```python
%%writefile app.py
import streamlit as st

st.title("カフェ割引シミュレーター ☕")

base_price = 500

# 1. スライダーの設定
discount = st.sidebar.slider("割引率(%)を設定してください:", 0, 50, 10)

# 2. 割引後価格の計算
discount_price = int(base_price * (1 - discount / 100))

# 3. 画面の出力
st.write(f"通常価格: {base_price} 円")
st.metric(label="割引適用後の価格", value=f"{discount_price} 円")
```

---

### 課題 2 の解答例
```python
%%writefile app.py
import streamlit as st

st.title("メニュー検索カタログ 📖")
st.write("カテゴリを選ぶと、おすすめ商品の一覧を自動で切り替えます。")

# 1. セレクトボックスの配置
category = st.selectbox("カテゴリを選択してください:", ["ドリンク", "デザート"])

# 2. 条件分岐によるリストの切り替え
if category == "ドリンク":
    items = ["ブレンドコーヒー", "宇治抹茶ラテ", "キャラメルマキアート"]
else:
    items = ["ベイクドチーズケーキ", "窯焼きアップルパイ", "特製クラシックプリン"]

# 3. 画面表示
st.header(f"✨ おすすめの{category}一覧")
for item in items:
    st.write(f"- {item}")
```

---

### 課題 3 の解答例
```python
%%writefile app.py
import streamlit as st

st.title("店舗売上目標達成チェッカー 📈")

target_sales = 200000
st.subheader(f"🎯 本日の目標売上: {target_sales:,} 円")

# 1. 1万刻みの調整用スライダーを配置
actual_sales = st.slider("実績売上を入力してください (円):", 0, 300000, 150000, step=10000)

st.write(f"現在の実績売上: {actual_sales:,} 円")

# 2. 目標判定ロジック
if actual_sales >= target_sales:
    st.success("🎉 目標達成！おめでとうございます！素晴らしい営業成果です！")
else:
    deficit = target_sales - actual_sales
    st.warning(f"⚠️ 目標未達成。目標まであと 【{deficit:,} 円】 足りません。")
```

---

### 課題 4 の解答例
```python
%%writefile app.py
import streamlit as st

st.title("🥞 プレミアム・パンケーキPOSレジ 🥞")
st.write("トッピングを自由に組み合わせて、オリジナルのカスタムパンケーキをお見積もりできます。")

base_price = 600
st.write(f"プレーンパンケーキ基本価格: {base_price} 円")

st.header("🧀 トッピングの選択")

add_ice = st.checkbox("バニラアイス (+150円)")
add_chocolate = st.checkbox("チョコレートシロップ (+80円)")
add_banana = st.checkbox("完熟バナナスライス (+120円)")

# トッピング金額の動的な一括加算
topping_total = 0
if add_ice:
    topping_total += 150
if add_chocolate:
    topping_total += 80
if add_banana:
    topping_total += 120

st.header("🔢 数量の選択")
quantity = st.slider("数量を選んでください:", 1, 10, 1)

# お見積総額の計算（優先順位カッコを忘れないこと！）
total = (base_price + topping_total) * quantity

st.header("💳 お見積書（合計金額）")
st.metric(label="パンケーキセット 合計支払金額", value=f"{total:,} 円")
```

---

### 課題 5 の解答例（最上級・集大成アプリ）
```python
%%writefile app.py
import streamlit as st

st.title("🎪 武蔵大学学園祭 総合POSシステム ☕")
st.write("学園祭カフェ出店「しらきじカフェ」のiPad POSレジお見積もりシステムです。")

st.header("📋 注文入力")

# 1. テキスト入力の配置
customer_name = st.text_input("お客様のお名前を入力してください:", value="ゲスト様")

# 2. セレクトボックスの配置
menu_item = st.selectbox(
    "メニューを選択してください:",
    ["タピオカミルクティー (500円)", "自家製レモネード (450円)", "宇治抹茶宇治金時 (600円)"]
)

if "タピオカ" in menu_item:
    base_price = 500
elif "レモネード" in menu_item:
    base_price = 450
else:
    base_price = 600

# 3. 数量スライダーの配置
qty = st.slider("数量を選択してください:", 1, 20, 1)

st.subheader("⚙️ 特典・追加オプション設定")
# 4. 特典チェックボックス
is_student = st.checkbox("武蔵大学の学生・教職員割引 (10%OFF)")
# 5. 夜間割増チェックボックス
is_night = st.checkbox("夜間営業対応 (+100円/個)")

# 6. 計算ロジック
night_charge = 100 if is_night else 0
subtotal = (base_price + night_charge) * qty

if is_student:
    total_amount = int(subtotal * 0.9)
else:
    total_amount = subtotal

# 7. 表示とエラーハンドリング
st.header("🧾 お会計明細票")

if customer_name.strip() == "":
    st.error("🚨 エラー：お名前が空欄です。お会計明細を処理できません。")
else:
    st.success(f"--- {customer_name} のご注文お見積書を正常に出力しました ---")
    
    col1, col2, col3 = st.columns(3)
    
    with col1:
        st.write("**👤 お客様情報**")
        st.write(f"お名前: {customer_name}")
        st.write(f"選択商品: {menu_item}")
        st.write(f"合計点数: {qty} 個")
        
    with col2:
        st.write("**🎟️ 特典・割増ステータス**")
        st.write(f"学割10%OFF: {'適用(10%引き)' if is_student else 'なし'}")
        st.write(f"夜間営業割増: {'適用(+100円/個)' if is_night else 'なし'}")
        
    with col3:
        st.write("**💰 お支払総額**")
        st.metric(label="最終ご請求金額 (税込)", value=f"{total_amount:,} 円")
```

</details>

---
## 📚 第5回のまとめと、次回の「データ・グラフ連動」へ向けて

200分の集中講義、本当にお疲れ様でした！
これまでは「黒い実行結果画面」に味気ない文字や画像が表示されるだけだったプログラムが、本日の授業を通じて、自分たちの手で「誰もがグリグリ動かせるお洒落なWebアプリ」に進化しました。

### ✨ 本日の重要キーワード

*   **Streamlit**: HTML/CSS/JS不要で、Pythonコードのみでプロ級のUIを自動構築する革新的Webフレームワーク。
*   **localtunnel**: クラウドの奥深く（Colab）で動いているWebサービスを、外部へ直結させるトンネリング技術。
*   **8501ポート**: Streamlitが外部からのアクセスを待機している、Colab内の標準の窓口番号。
*   **Rerun（再実行）システム**: ユーザーが操作を行うたびに、Pythonのコードが1行目から最後まで毎回すべて最初から実行され直す、Streamlitの最も重要なお約束ルール。
*   **st.buttonの罠**: ボタンは「押された瞬間だけTrueになり、他の操作をするとFalseに戻る」という、Rerunが原因で起きる初心者定番の仕様。
*   **st.metric / st.columns**: 画面を並列に並べ、KPIメーターを表示する、ダッシュボード制作のレイアウト手法。

---
## ❓ よくあるエラーと対処法（FAQ）

*   **Q. localtunnel のURLを踏むと、真っ白な画面のまま「504 Gateway Timeout」と表示されてアプリが開きません。**
    *   **A.** Colab上でStreamlitの起動セルが一時停止してしまっているか、途中でエラー停止している可能性があります。Colabに戻り、アプリ起動セル（手順C）をもう一度停止ボタンを押してから再起動し、新しく発行されたURLを踏み直してみてください。
*   **Q. app.pyを書き換えても、画面が前の状態のまま変わりません。**
    *   **A.** 手順A（`%%writefile app.py`）のセルを再実行し忘れているか、Web画面の右上にある「Rerun（再読み込み）」をクリックし忘れています。コードを書き換えたら、必ず「Colabでセル実行」➡「Web画面でRerun」の2アクションを行う癖をつけましょう！
*   **Q. スライダーを動かそうとすると、画面全体が「TypeError: ...」や「NameError: ...」という赤いエラーで埋め尽くされます。**
    *   **A.** 鋭いバグですね！それはスライダーが動いたことによる「Rerun（再実行）」の際、コードのどこかで定義し忘れた変数や、データ型の不整合（文字列に数値を掛け算しようとするなど）が発生し、Pythonがパニックを起こしたためです。エラーメッセージに表示されている「何行目でエラーが起きているか」をじっくり読み、お掃除しましょう。分からなければ、そのエラーを丸ごと生成AIに投げるのも賢い解決策です！

---
### 🌟 次回の「本格データ・グラフダッシュボード構築」への架け橋

本日学んだこの美しいUIシステムは、まだ手書きのダミーデータ（450円など）を使っているだけで、これまでに学んだ「Pandasによる大量データの処理」とは繋がっていません。

次回（第6回）の **「Webサービスの活用2」** では、
「セレクトボックスで店舗名を選ぶ」➡「Pandasが自動でその店舗の売上CSVをフィルタリングする」➡「Matplotlibが売上推移グラフを自動で描き、Streamlit上にリアルタイム埋め込みする」という、**「データ ✕ UI ✕ グラフ」がすべて一本の論理回路で連動する、プロ仕様のデータ分析ダッシュボード**を完成させます！

本日手に入れた「アプリを作る翼」を活かして、さらにエキサイティングな領域へ進みましょう。次回も元気にお会いしましょう、本当にお疲れ様でした！
