<!-- Markdownセル -->
# 第12回 Pythonとバイブコーディング（Vibe Coding）

皆さん、こんにちは！プログラミング応用A、第12回の講義です。担当の教員です。
これまでに、Pandasでのデータ加工、記述統計、GIS（動く地図アプリfolium）、Streamlit（Webアプリ開発）、そして前回と前々回のscikit-learnを用いた「AI・機械学習モデルの構築」まで、ものすごいスピードで高度な技術を習得してきました。本当に素晴らしい学習態度です。

さて、今日学ぶテーマは、今プログラミング界隈で最もホットで、未来の働き方をガラリと変える最新の開発パラダイム**「バイブコーディング（Vibe Coding）」**です！

「バイブコーディング」とは、一言で言えば**「コードの細かな文法は1文字も書かず、生成AIと会話しながら、作りたいアプリの『雰囲気（Vibe）』や『大まかな設計図』だけを伝えて、一瞬で高度なシステムを組み上げる開発スタイル」**のことです。

「自分が書けないはずの難しいコード」も、AIと呼吸（バイブ）を合わせることで、驚くほど動くシステムになって現れます。
今回は、これまでに学んだ「Pandasデータ処理 ✕ Matplotlibグラフ ✕ scikit-learn売上予測AI ✕ Streamlit WebUI」という、まともに書けば数日はかかる**「AI売上予測Webダッシュボード」を、AIの力を借りてわずか15分で完成させる実習**を行います。

文法暗記の苦しみから解放され、プログラミングが「最高に楽しいクリエイティブ活動」に変わる瞬間を、100分間かけて体感していきましょう！

---
## 🎯 到達目標 (約5分)
この授業が終わるころには、以下のことができるようになります。
1. **バイブコーディング（Vibe Coding）**の定義と、開発における役割（指示者としての人間、実装者としてのAI）を説明できる。
2. 生成AIから一発で動く完璧なコードを引き出すための**「プロンプトエンジニアリング（指示の書き方）」**の基本パターンを習得する。
3. AIが生成したコードにエラーやバグがあった際、**エラーメッセージをAIに正しくフィードバックして自己解決（デバッグ）**できる。
4. これまでに学んだ技術（Pandas, ML, Streamlit）をAIに指示して「合体」させ、実用的なWebダッシュボードを構築できる。

---
## 12.1 バイブコーディングとは何か？（プログラミングのパラダイムシフト） (約15分)

これまでのプログラミング学習は、「スペルミスをしないように、コロン（`:`）やインデントを1マスずつ気にして、文法を丸暗記して書く」のが普通でした。
しかし、ChatGPTやClaudeなどの優秀な生成AIの登場により、その常識がひっくり返りました。

### 「コードを書く人（実装者）」から「システムを設計する人（ディレクター）」へ
現代のプログラミングでは、役割分担が以下のように変わっています。

*   **AI（プログラマー・実装者）**：
    指示されたコードを、一瞬で正確に書く。バグの修正も得意。ただし、「何を作れば人間が喜ぶか」「社会のどんな課題を解決するか」は自分で考えられない。
*   **人間（設計者・ディレクター）**： 🌟**あなたの役割！**
    「どんな画面にしたいか」「何のデータを読み込ませたいか」「どういう人がこのアプリを使うか」という、**全体の雰囲気（Vibe）と設計図を考え、AIを指揮する。**

**【バイブコーディングの開発ループ（イメージ図）】**
```text
  [ 🧠 人間：アイデアとVibe ]
         │  (「こんな売上予測アプリをStreamlitで作って！」)
         ▼
  [ 🤖 AI：爆速コーディング ]
         │  (一瞬で200行の完璧なコードを吐き出す)
         ▼
  [ 💻 実行＆テスト ] ──▶ [ 🐛 エラー発生！ ] ──▶ [ 🤖 AIにエラー文を貼る ]
         │                                               │
         │                                               ▼
         │                                       [ 🛠️ AIが即座に修正 ]
         ▼
  [ 🎉 わずか15分で完成！ ]
```
文法を覚えることよりも、**「何を作りたいかをAIに分かりやすく伝える力」**と、**「AIが書いたコードが本当に正しく動いているかを見破る検証力」**が、これからの時代に最も求められるプログラミングスキルなのです。

---
## 12.2 AIから完璧なコードを引き出す「プロンプトの極意」 (約20分)

AIに「売上予測アプリのコードを書いて」とだけ頼むと、高確率で「自分が使っている環境（Colabなど）では動かないコード」や「中身がスカスカの適当なコード」が返ってきます。
AIを最高の部下（ペアプログラマー）として動かすための、プロンプトの4大要素を覚えましょう。

1.  **役割（ペルソナ）の指定**：「あなたは世界最高峰のPythonデータサイエンティストです。」
2.  **前提条件（環境）の指定**：「Google Colab上で、Streamlitとlocaltunnelを使って開発しています。」
3.  **具体的な要件（インプットとアウトプット）**：「説明変数に客数(X)、目的変数に売上(y)を使い、決定係数(R²)も画面に表示してください。」
4.  **出力フォーマットの指定**：「コピペするだけで動く、1つの Python ファイル（`app.py`）にまとめて出力してください。」

---
## 12.3 実践デモ：15分で「AI売上予測Webアプリ」をバイブコーディング！ (約30分)

それでは、実際にAIに指示を出して、前々回（Streamlit）と前回（scikit-learn 機械学習）の技術が全て合体した「AI売上予測Webダッシュボード」を一撃で構築するデモを行います。

今回は、授業用の「特製プロンプト」を用意しました。これをAIに投げると、以下のようなプロ級のコードが自動生成されます。

### 🌟 AIに投げたプロンプト（Vibe）
```text
あなたは優秀なデータサイエンティストです。
Google Colab上で、Streamlitとlocaltunnelを使って起動する「売上予測AIダッシュボードアプリ」のコードを Python で書いてください。
【要件】
1. サイドバーで「客数（スライダー）」を動かせるようにしてください。
2. スライダーの客数から、線形回帰モデルを使って「予測売上高」を計算し、画面にデカデカとメーター（Metric）で表示してください。
3. メイン画面には、過去の模擬データ（客数と売上）の点々と、AIが引いた「予測直線」を Matplotlib で描画して表示してください。
4. コピペするだけで動く、1つの「app.py」ファイルとして完結するコードにしてください。
```

上記をAIに投げ、返ってきたコードを Colab の `/workspace/scratch/app.py` に保存し、localtunnelで起動します。

```python
<!-- コードセル -->
# （準備）AIが生成したコード（app.py）を書き込みます。
# 通常の授業では、学生が生成AI（ChatGPTなど）にプロンプトを投げて返ってきたコードをここにコピペします。
# 今回は、100%一撃で動く「AIが生成した最高水準のダッシュボードコード」を事前に用意して書き込みます。

app_code = """
import streamlit as st
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from sklearn.linear_model import LinearRegression

st.set_page_config(page_title="AI売上予測システム", layout="wide")

# タイトル
st.title("📊 カフェ売上予測 AIダッシュボード")
st.write("過去の客数データからAIが自動で学習し、客数スライダーの動きに連動して『未来の売上高』を一瞬で予測します。")

# 1. 模擬データの作成（客数と売上）
np.random.seed(42)
customers = np.random.randint(50, 300, 50)
sales = customers * 350 + np.random.normal(0, 10000, 50) # 1人あたり350円 + ノイズ

df = pd.DataFrame({"客数": customers, "売上高": sales})

# AIモデル（線形回帰）の学習（このアプリが起動した瞬間に一瞬で学習します！）
X = df[["客数"]]
y = df["売上高"]
model = LinearRegression()
model.fit(X, y)

# 2. サイドバーでの入力（UI）
st.sidebar.header("🔮 未来の客数をシミュレーション")
input_customers = st.sidebar.slider("予測したい明日の客数（人）", min_value=10, max_value=400, value=150)

# 3. AIによる予測
pred_sales = model.predict(np.array([[input_customers]]))[0]

# 4. KPIメーターの表示
col1, col2 = st.columns(2)
with col1:
    st.metric(label="明日の予測客数", value=f"{input_customers} 人")
with col2:
    st.metric(label="💰 AI予測売上高", value=f"{int(pred_sales):,} 円", delta=f"客単価換算: {int(pred_sales/input_customers)}円")

# 5. グラフの描画（Matplotlib）
fig, ax = plt.subplots(figsize=(10, 5))
ax.scatter(df["客数"], df["売上高"], color="blue", alpha=0.6, label="過去の営業データ")
# 予測直線の描画
line_X = np.linspace(10, 400, 100).reshape(-1, 1)
line_y = model.predict(line_X)
ax.plot(line_X, line_y, color="red", linestyle="--", linewidth=2, label="AIが学習した売上予測ライン")
# 現在のスライダーの位置を黄色い星でプロット！
ax.scatter([[input_customers]], [pred_sales], color="yellow", edgecolor="black", s=300, marker="*", zorder=5, label="明日の予測位置")

ax.set_xlabel("客数（人）")
ax.set_ylabel("売上高（円）")
ax.legend()
ax.grid(True)

# Streamlitにグラフを配置
st.pyplot(fig)
"""

# ファイルに書き出す
with open("app.py", "w", encoding="utf-8") as f:
    f.write(app_code)

print("AIが生成した app.py をローカル環境に書き出しました！")
```

それでは、第5回で学んだ **「localtunnel」** を使って、このAIアプリを起動してみましょう！
（以下のコードセルを実行し、表示されるIPアドレスをコピーして、青いリンクをクリックして貼り付けます。）

```python
<!-- コードセル -->
# 1. あなたのColab環境のグローバルIPアドレスを確認します（これがlocaltunnelの『鍵（パスワード）』になります）
!wget -qO- ipv4.icanhazip.com
```

```python
<!-- コードセル -->
# 2. バックグラウンドでStreamlitを起動し、同時にlocaltunnelを繋ぎます！
# ※星マーク [*] が回転し続けますが、アプリが起動している証拠ですので止めないでください。
!streamlit run app.py & npx localtunnel --port 8501
```

ブラウザで開いたアプリを動かしてみてください！
左側のサイドバーで客数スライダーを動かすと、**一瞬で予測売上高の数値が変わり、同時にグラフの中の黄色い星（★）が売上予測ラインの上をヌルヌルと移動する**はずです。

これが、人間が設計（Vibe）し、AIが実装（Coding）する、現代の最先端にして究極の開発スタイルです！

---
## 💻 演習課題 (約25分)

さあ、今日の演習は特別です。
これまでの「この通りにコードを書き換えてください」という演習ではありません。**「生成AIにどんなプロンプトを送れば、自分が求めるシステムを1回で正しく作ってくれるか」という、プロンプトの設計力を鍛える演習**です。

AIとチャットしながら、以下の課題をクリアする Python コードを吐き出させ、実際に実行して確かめてみましょう！
```

```python
# 演習1: 【プロンプト設計】特定の店舗だけを抽出するアプリ（難易度：易）
# あなたはAIに「店舗のデータフレームから、ユーザーが選択した店舗だけを抽出する Streamlit のセレクトボックス（st.selectbox）のコード」を書いてもらいたいです。
# TODO: AIが一発であなたの求める完璧なコードを出力してくれるような「プロンプトの文章」を日本語で考えて、
#       以下の文字列変数 prompt_sjis に代入してください。
# ※ヒント：役割、環境（Colab/Streamlit）、入力（df）、出力（selectboxとテーブル表示）を明記しましょう。

prompt_1 = """
# TODO: ここにAIに送る「最高のプロンプト」を書いてください！



"""
print("あなたが設計した演習1のプロンプト:\n", prompt_1)
```

```python
# 演習2: 【プロンプト実践】AIが作ったコードのコピペ実行（難易度：易）
# 演習1で設計したプロンプトを、実際にChatGPTなどの生成AIに投げてください。
# 返ってきたコードをコピーして、以下の `"""` の間に貼り付けて（コピペして）実行し、
# エラーなく動作する Python ファイル `ex2.py` を作成してください。

ex2_code = """
# TODO: AIが返してきたコードをここに丸ごと貼り付けてください！



"""

# ファイルに保存
with open("ex2.py", "w", encoding="utf-8") as f:
    f.write(ex2_code)
print("演習2のファイルex2.pyが作成されました！")
```

```python
# 演習3: 【デバッグバイブ】エラーをAIに直させる技術（難易度：中）
# AIにコードを書いてもらった時、以下のようなエラー(NameError)が発生してしまいました。
# ----------------------------------------------------
# NameError: name 'plt' is not defined
# ----------------------------------------------------
# TODO: このエラーを最速で直してもらうために、AIにチャットで送る「指示文（プロンプト）」を書いてください。
# ※ヒント：「直してください」だけでなく、エラーメッセージそのものを貼り付けるのがコツです。

prompt_debug = """
# TODO: AIに送るエラー修正指示をここに書いてください！



"""
print("あなたが設計したデバッグ用のプロンプト:\n", prompt_debug)
```

```python
# 演習4: 【仕様の合体】GIS ✕ Streamlit のバイブプロンプト（難易度：中）
# 第9回で学んだ「動く地図（folium）」と「Streamlit」を合体させたアプリを作りたいです。
# ユーザーがセレクトボックスで「東京」「大阪」を選ぶと、
# 地図の中心がその場所に自動でジャンプする動的マップアプリです。
# TODO: この高度なシステムをAIに一撃で作ってもらうための「仕様書付きプロンプト」を書いてください。

prompt_gis_app = """
# TODO: AIに送る「地図✕Streamlit合体アプリ」のプロンプトを書いてください！



"""
print("あなたが設計したGISアプリのプロンプト:\n", prompt_gis_app)
```

```python
# 演習5: 【総仕上げ】「Vibe Coding」で自作した世界に一つのオリジナルダッシュボード（難易度：難）
# これまでに学んだ「すべての知識」と「AIの力」を総動員して、
# あなただけのオリジナル・データ分析Webアプリを1つ、バイブコーディングで作成してください！
# （例：自分の家計簿分析、好きなアニメのキャラクター能力値比較、飲食店の売上と雨の関係分析など）
# TODO: AIに書いてもらったコードを以下の `my_dashboard_code` に貼り付けて実行し、
#       `my_app.py` というファイルを作成して、実際に起動して動かしてみましょう！

my_dashboard_code = """
# TODO: AIと対話して完成させた、あなただけのオリジナルダッシュボードのコードを貼り付けてください！



"""

# ファイルに保存
with open("my_app.py", "w", encoding="utf-8") as f:
    f.write(my_dashboard_code)
print("おめでとうございます！！！あなただけのオリジナルアプリ『my_app.py』が誕生しました！")
```

---
## 💡 演習問題の解答例と「プロのプロンプト」

（プロンプトに唯一の絶対的な正解はありません。AIに「意図が伝わり、欲しいものが返ってきた」なら、あなたのプロンプトは大正解です！プロが書くプロンプトの構成例を確認してみましょう。）

<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

**演習1（プロンプト設計）の解答例**
> あなたはプロのPythonエンジニアです。
> Google Colab環境上で起動する Streamlit のWebアプリコードを書いてください。
> 【インプット】
> 商品名（コーヒー、ケーキ、紅茶）と、価格（400, 500, 450）を持つPandasのデータフレーム(df)をコード内で定義してください。
> 【処理・アウトプット】
> 1. `st.selectbox` を使って、ユーザーが画面上で商品名を選択できるようにしてください。
> 2. 選択された商品の「価格」を、`st.write` を使って「〇〇の価格は 〇〇円です」と画面に分かりやすく表示するコードにしてください。
> コピペだけで動くコードにしてください。

**演習3（エラーデバッグ）の解答例**
> 提供されたコードを実行したところ、以下のエラーが発生しました。
> ```
> NameError: name 'plt' is not defined
> ```
> おそらく、matplotlib.pyplot を plt としてインポートするコード（`import matplotlib.pyplot as plt`）がファイルの上部で抜けていることが原因だと思います。このバグを修正した、完全なコードを再出力してください。

**演習4（GIS ✕ Streamlit）の解答例**
> あなたはGIS（地理情報システム）に強いデータサイエンティストです。
> Streamlit と folium（もしくは streamlit_folium）を使って、Google Colab上で起動する地図アプリのコードを書いてください。
> 【仕様】
> 1. セレクトボックス（`st.selectbox`）で「東京駅」と「大阪駅」を選択できるようにしてください。
> 2. 東京駅が選ばれたら 緯度 35.6812, 経度 139.7671、大阪駅が選ばれたら 緯度 34.7024, 経度 135.4959 を中心にした folium の地図を描画し、画面に表示してください。
> 3. 地図上には、その場所に赤いピンを1つ立ててください。
> 4. コピペするだけで動く、1つの Python コードにしてください。

</details>

---
## 📚 第12回のまとめ

お疲れ様でした！本日は、これまでのプログラミングの「文法を覚える学習」から、「AIを指揮して、素晴らしいシステムを瞬時に生み出す設計学習」へのパラダイムシフトを体験しました。

### ✨ 今日の重要ポイントまとめ
*   **バイブコーディング（Vibe Coding）**：文法の細かなスペルにとらわれず、生成AIに「作りたいものの全体像（Vibe）」を伝えて、爆速でシステムを実装してもらう現代最強の開発スタイル。
*   **プロンプトの4大要素**：AIから1回で動くコードを出すためには、「役割（ペルソナ）」「前提環境」「具体的な要件（入力と出力）」「出力フォーマット」をプロンプトに盛り込む。
*   **デバッグ・フィードバックループ**：エラーが出ても諦めず、エラーメッセージをそのままAIに貼り付けて「ここを直して」と頼むことで、最速でエラーを自己解決できる。

### ❓ よくある質問と回答 (FAQ)
*   **Q. AIが全部コードを書いてくれるなら、私たちはもうプログラミングの勉強をしなくていいのでは？**
    *   **A.** **いいえ、実は逆です！** AIが吐き出したコードの中に「致命的な計算ミス」や「データの解釈間違い（バグ）」があったとき、プログラミングの基礎知識（変数、リスト、Pandasの仕組み、決定係数の意味など）がない人は、そのバグにすら気づけず、間違ったデータ分析結果を世の中に垂れ流してしまいます。
    *   「1文字ずつ手で書く」必要はなくなりましたが、**「AIが書いたコードの中身を読み解き、正しさを保証（レビュー）する力」**を持つために、私たちがこの全13回で学んできた「プログラミング基礎」と「応用」の知識が、絶対に不可欠なのです。

### 🌟 Colabで試すおすすめコード（バイブコーディングへの挑戦）
あなたが「お、このアプリ面白そうだな！」と思うアイデアがあれば、今日の `app.py` を書き換えて、どんどん生成AIにプロンプトを投げてみてください。
「白山店と江古田店の売上を合体させるアプリ」「アンケートを自動分析するアプリ」など、AIはあなたのバイブ（情熱）に必ず応えてくれます。

```python
<!-- コードセル -->
# （参考）作成した Streamlit アプリや、localtunnel プロセスを安全に終了させるコマンドです。
# 授業が終わる際、または次の新しいアプリを起動する前に実行してください。
!pkill streamlit
!pkill lt
print("すべての起動中プロセスを安全にシャットダウンしました。")
```
```markdown
<!-- Markdownセル -->
次回（第13回）は、ついに最終回！
これまでに学んだすべての技術（Pandas、データ加工、統計解析、GIS地図、Streamlit、そしてAI予測）を組み合わせ、さらに生成AIの力（バイブコーディング）も借りながら、社会の課題を解決する「あなただけのWebサービス・データ分析ダッシュボード」を完成させて発表する、総合卒業演習＆ピッチ大会です。

皆さんの集大成となる素晴らしいアイデアに会えるのを、楽しみにしています。今日もお疲れ様でした！
```
