# プログラミング応用A（コンピューティング応用G）最終実技試験問題

本ファイルは、1学期間で学んだ「データ加工・統計解析・GIS（地理空間情報）・AI（機械学習）・Webアプリ開発」の実力をフラットに測定するための**最終実技試験問題（全10問）**です。

---

## 🧭 受験者（学生）への案内
1. すべての問題は、Google Colab上でプログラムを実行しながら解答してください。
2. 外部からCSVファイルをアップロードする必要はありません。各問題のセルを実行すると、自動的にテスト用のデータフレームやGeoDataFrameが生成されます。
3. コード内の `# TODO:` と書かれた部分を適切に書き換え、指示された通りの結果を出力するプログラムを完成させてください。
4. インターネット、講義資料、自作のColabノートブック、および生成AI（ChatGPT等）を活用したデバッグ・相談を許可します（※授業の実施方針に従ってください）。ただし、出力される結果が要件を満たしているか、必ず自分で最終確認を行ってください。

---

## 📊 採点者（教員）への案内・配点基準
各問10点満点、合計100点満点で採点します。各問題には「採点基準」と「減点ポイント」を記載しています。
部分点（コードが未完成でも、ロジックが合っている場合は部分点を与えるなど）の基準も設けてありますので、採点時の参考にしてください。

---

## ⏱️ 試験時間
推奨される試験時間：**60分〜90分**（AI併用（Vibe Coding）を前提とする場合は60分、自力での手書きコーディングを前提とする場合は90分が目安です）。

---

# 📝 試験問題（全10問）

## 【テーマ1：データ前処理とデータ加工】（問1〜問3）

### 📊 問1: Pandasでのデータ結合(merge)と欠損値補完（配点: 10点）
カフェの「1日の注文履歴（`orders_df`）」と「顧客マスタ（`customers_df`）」があります。
1. `orders_df` をベースにし、共通するキー列「顧客ID」を使って、`customers_df` を**左結合（left join）**で合体させてください。
2. 結合すると、一部の顧客（マスタに登録がないゲスト購入者）の「顧客名」列が `NaN`（データなし）になります。`.fillna()` を使って、この `NaN` の部分を「ゲスト会員」という文字列で埋めて補完してください。

#### 📥 初期コード（Colabに貼り付けて実行するコード）
```python
import pandas as pd
import numpy as np

# 1. 注文履歴（ベースデータ）
orders_df = pd.DataFrame({
    "注文ID": [101, 102, 103, 104],
    "顧客ID": ["M01", "M02", "G01", "M01"], # G01はマスタ未登録
    "売上金額": [1200, 850, 2000, 1500]
})

# 2. 顧客マスタ
customers_df = pd.DataFrame({
    "顧客ID": ["M01", "M02", "M03"],
    "顧客名": ["山田", "佐藤", "鈴木"]
})

# TODO: 1. orders_df に customers_df を「顧客ID」をキーにして左結合(left)する
merged_df = 

# TODO: 2. 「顧客名」列の欠損値（NaN）を「ゲスト会員」で穴埋めする
merged_df["顧客名"] = 

print("--- 処理完了後のデータフレーム ---")
display(merged_df)
```

#### 🎯 期待される出力結果
```text
--- 処理完了後のデータフレーム ---
   注文ID 顧客ID  売上金額   顧客名
0   101  M01  1200    山田
1   102  M02   850    佐藤
2   103  G01  2000  ゲスト会員
3   104  M01  1500    山田
```

<details>
<summary>🔑 【教員用】模範解答コード</summary>

```python
# 1. 左結合の実行
merged_df = pd.merge(orders_df, customers_df, on="顧客ID", how="left")

# 2. 欠損値補完
merged_df["顧客名"] = merged_df["顧客名"].fillna("ゲスト会員")
```
</details>

#### 📐 採点基準（10点満点）
*   **【10点】**: 1および2の要件が完全に満たされ、期待通りのデータフレームが出力されている。
*   **【6点】**: 左結合（`how="left"`）は正しくできているが、欠損値補完（`.fillna`）ができていない、あるいは上書き保存されていない。
*   **【4点】**: 結合方式を誤って `how="inner"`（デフォルト）にしてしまい、データが3行に減少してしまっている（`NaN`が消滅）。
*   **【0点】**: 結合すらできていない、あるいは文法エラー（SyntaxError）で動作しない。

---

### ⚙️ 問2: apply ✕ lambda を用いた条件付きの列追加（配点: 10点）
カフェの顧客満足度アンケートのデータフレームがあります。
アンケートの「スコア」（1〜5の5段階評価）に基づき、以下の基準で「評価」列を新しく追加してください。
*   スコアが **4以上**（`>= 4`）の場合 ➡ **「満足」**
*   スコアが **3**（`== 3`）の場合 ➡ **「普通」**
*   スコアが **2以下**（`<= 2`）の場合 ➡ **「改善が必要」**

※ `apply()` と `lambda`、および if-elif-else 文（あるいは入れ子のif文）を組み合わせて実装してください。

#### 📥 初期コード
```python
import pandas as pd

survey_df = pd.DataFrame({
    "回答者": ["Aさん", "Bさん", "Cさん", "Dさん"],
    "スコア": [5, 2, 3, 4]
})

# TODO: apply と lambda を使い、「評価」列を作成する
# ※ lambda row: ... の形、または判定用の簡単な自作関数を定義して apply する形でも可
survey_df["評価"] = 

print("--- 判定結果 ---")
display(survey_df)
```

#### 🎯 期待される出力結果
```text
--- 判定結果 ---
   回答者  スコア     評価
0  Aさん    5     満足
1  Bさん    2  改善が必要
2  Cさん    3     普通
3  Dさん    4     満足
```

<details>
<summary>🔑 【教員用】模範解答コード</summary>

```python
# パターン1: 1行でlambdaを書く場合（入れ子の if-else）
survey_df["評価"] = survey_df["スコア"].apply(
    lambda x: "満足" if x >= 4 else ("普通" if x == 3 else "改善が必要")
)

# パターン2: 関数を定義して渡す場合（学生にとって読みやすく、実務でも推奨される書き方）
def judge_score(score):
    if score >= 4:
        return "満足"
    elif score == 3:
        return "普通"
    else:
        return "改善が必要"

survey_df["評価"] = survey_df["スコア"].apply(judge_score)
```
</details>

#### 📐 採点基準（10点満点）
*   **【10点】**: 条件分岐が正しく行われ、期待通りの「評価」列が追加されている。
*   **【7点】**: `apply` と関数（あるいはlambda）は使えているが、一部の条件（例：スコア3の「普通」）の判定ロジックが漏れている。
*   **【3点】**: 手書きの for ループを使って行を追加している（動くが、Pandasの高速処理に則っていないため大幅減点）。
*   **【0点】**: エラーで動作しない。

---

### 📅 問3: 時系列データの型変換と特定曜日の売上集計（配点: 10点）
カフェの売上履歴データがあります。「注文日時」列は現在、ただの文字列型になっています。
1. 「注文日時」列を `pd.to_datetime()` を使って**日付型（datetime64）に変換**してください。
2. 日付データから曜日（0=月曜日, 1=火曜日... 6=日曜日）を取得できるプロパティ（`.dt.weekday`）を活用し、**「土曜日（5）と日曜日（6）のデータだけ」を条件抽出**して `weekend_sales_df` に代入し、表示してください。

#### 📥 初期コード
```python
import pandas as pd

sales_df = pd.DataFrame({
    "注文ID": [5001, 5002, 5003, 5004, 5005],
    "注文日時": ["2026-08-01 12:00:00", "2026-08-03 15:30:00", "2026-08-05 10:15:00", "2026-08-08 18:00:00", "2026-08-09 11:00:00"],
    "売上額": [1500, 800, 1200, 3500, 4200]
})
# ※ 2026年8月1日は「土曜日」、8月3日は「月曜日」、8月5日は「水曜日」、8月8日は「土曜日」、8月9日は「日曜日」です。

# TODO: 1. 「注文日時」列を日付型に変換する
sales_df["注文日時"] = 

# TODO: 2. 日付データから曜日情報を判定し、土日（曜日コードが 5 または 6、あるいは .dt.day_name() が Saturday/Sunday）の行だけを抽出する
# ヒント: sales_df["注文日時"].dt.weekday を使うと、各行の曜日コードが得られます。
weekend_sales_df = 

print("--- 土日の売上データのみ抽出 ---")
display(weekend_sales_df)
```

#### 🎯 期待される出力結果
```text
--- 土日の売上データのみ抽出 ---
   注文ID                注文日時   売上額
0  5001 2026-08-01 12:00:00  1500
3  5004 2026-08-08 18:00:00  3500
4  5005 2026-08-09 11:00:00  4200
```

<details>
<summary>🔑 【教員用】模範解答コード</summary>

```python
# 1. datetime型への変換
sales_df["注文日時"] = pd.to_datetime(sales_df["注文日時"])

# 2. 土日（5 or 6）の抽出
weekend_sales_df = sales_df[sales_df["注文日時"].dt.weekday.isin([5, 6])]

# （別解：個別に or 条件を書く方法）
# weekend_sales_df = sales_df[(sales_df["注文日時"].dt.weekday == 5) | (sales_df["注文日時"].dt.weekday == 6)]

# （別解：曜日名を取り出す方法）
# weekend_sales_df = sales_df[sales_df["注文日時"].dt.day_name().isin(["Saturday", "Sunday"])]
```
</details>

#### 📐 採点基準（10点満点）
*   **【10点】**: 日付型への変換が正しく行われ、土曜日（8/1, 8/8）と日曜日（8/9）の3行が正確に抽出されている。
*   **【6点】**: 日付型への変換（`pd.to_datetime`）はできているが、土日の抽出条件式が間違っている、または平日（月・水）が混ざって出力されている。
*   **【3点】**: 曜日をプログラムで判定せず、手作業で「0, 3, 4行目をスライス」して取り出している（日付が変わった時に自動化できないため、大幅減点）。
*   **【0点】**: 動作しない。

---

## 【テーマ2：記述統計とクロス集計（データ解析）】（問4〜問5）

### 📊 問4: 記述統計（中央値・四分位数・標準偏差の算出）（配点: 10点）
カフェの「顧客の滞在時間（分）」のデータがリストで記録されています。
このデータを用いて、以下の3つの統計量をPandasの機能を使って算出し、print文で表示してください。
1. データの **中央値（50%点）**
2. データの **第3四分位数（75%点）**（※これを超える人はかなりの長居顧客です）
3. データのばらつきを表す **標準偏差**

#### 📥 初期コード
```python
import pandas as pd

# 顧客20人の滞在時間（分）
stay_times = pd.Series([15, 30, 45, 120, 20, 35, 50, 10, 180, 25, 40, 55, 60, 90, 15, 30, 45, 75, 80, 200])

# TODO: 1. 中央値（median）を算出する
median_val = 

# TODO: 2. 第3四分位数（75%パーセンタイル）を算出する（.quantile() を使います）
q3_val = 

# TODO: 3. 標準偏差（std）を算出する
std_val = 

print(f"中央値: {median_val} 分")
print(f"第3四分位数 (75%): {q3_val} 分")
print(f"標準偏差: {std_val:.2f}")
```

#### 🎯 期待される出力結果
```text
中央値: 42.5 分
第3四分位数 (75%): 82.5 分
標準偏差: 54.78
```

<details>
<summary>🔑 【教員用】模範解答コード</summary>

```python
# 1. 中央値の計算
median_val = stay_times.median()

# 2. 第3四分位数（0.75を指定します）
q3_val = stay_times.quantile(0.75)

# 3. 標準偏差の計算
std_val = stay_times.std()
```
</details>

#### 📐 採点基準（10点満点）
*   **【10点】**: 3つの指標（42.5, 82.5, 54.78）がすべて正確に算出され、表示されている。
*   **【6点】**: `describe()` などを呼び出して数値を確認することはできているが、個々の変数（`median_val`, `q3_val`, `std_val`）に正しく代入し、個別に出力するプログラムになっていない。
*   **【3点】**: いずれか1つの指標しか算出できていない。
*   **【0点】**: 算出できていない。

---

### 🧮 問5: ピボットテーブルを用いた店舗別・曜日別のクロス集計（配点: 10点）
「江古田店」と「白山店」の、各曜日の売上データがあります。
`pd.pivot_table()` を用いて、**「縦軸（行）に『店舗名』、横軸（列）に『曜日』、集計値に『売上』の【合計（sum）】」** を配置した、店舗ごとの曜日別売上傾向が一目でわかるクロス集計表（ピボットテーブル）を作成してください。

#### 📥 初期コード
```python
import pandas as pd

sales_data = pd.DataFrame({
    "店舗名": ["江古田店", "江古田店", "白山店", "白山店", "江古田店", "白山店", "江古田店", "白山店"],
    "曜日": ["月", "水", "月", "金", "水", "月", "金", "水"],
    "売上": [15000, 18000, 20000, 35000, 12000, 22000, 42000, 25000]
})

# TODO: pd.pivot_table を使い、指定された行・列・集計関数でクロス集計表を作成する
# ※値のないセルは fill_value=0 で埋める設定も加えてください。
pivot_df = pd.pivot_table(
    
)

print("--- 店舗別・曜日別の売上集計表 ---")
display(pivot_df)
```

#### 🎯 期待される出力結果
```text
--- 店舗別・曜日別の売上集計表 ---
曜日       金      月      水
店舗名                     
江古田店  42000  15000  30000
白山店   35000  42000  25000
```
*(※インデックスやカラムの表示順序は、環境により月・水・金の順などで前後する場合がありますが、数値が合っていれば正解です)*

<details>
<summary>🔑 【教員用】模範解答コード</summary>

```python
pivot_df = pd.pivot_table(
    data=sales_data,
    values="売上",
    index="店舗名",
    columns="曜日",
    aggfunc="sum",
    fill_value=0
)
```
</details>

#### 📐 採点基準（10点満点）
*   **【10点】**: `pd.pivot_table` の引数（`values`, `index`, `columns`, `aggfunc=\"sum\"`）が完璧に設定され、集計結果の数値（江古田店の金曜売上42,000円など）が一致している。
*   **【6点】**: ピボットテーブルは作れているが、集計関数を誤って平均（デフォルトの `mean`）にしたため、合計値ではなく平均値が表示されている。
*   **【3点】**: `groupby` を用いて集計している（表形式のクロス集計になっていないため減点）。
*   **【0点】**: エラーで動作しない。

---

## 【テーマ3：地理空間情報（GIS）データの分析】（問6〜問7）

### 🗺️ 問6: 緯度経度からのGeoDataFrame作成と座標系(CRS)変換（配点: 10点）
3箇所の観光スポットの「緯度・経度」が Pandas のデータフレームで与えられています。
1. `geopandas` と `shapely.geometry.Point` を使って、経度（Lng）と緯度（Lat）から `geometry` 列を作成し、最初の空間座標系（CRS）を **「世界測地系（経緯度：`EPSG:4326`）」** に設定した GeoDataFrame を作成してください。
2. 作成した GeoDataFrame を、平面直角座標系である **「JGD2011/9系（関東地域：`EPSG:6677`）」** に `.to_crs()` メソッドを用いて座標変換してください。

#### 📥 初期コード
```python
import pandas as pd
import geopandas as gpd
from shapely.geometry import Point

# 観光地データ（経度 Lng, 緯度 Lat）
spots_df = pd.DataFrame({
    "スポット名": ["スカイツリー", "浅草寺", "上野動物園"],
    "Lng": [139.8107, 139.7967, 139.7711],
    "Lat": [35.7100, 35.7148, 35.7164]
})

# TODO: 1. 経度(Lng)と緯度(Lat)から Point オブジェクトのリストを作成する
# ※ Point(X, Y) の順、つまり Point(経度, 緯度) の順にすること！
geometry = 

# TODO: GeoDataFrameを作成し、初期CRSを "EPSG:4326" に設定する
gdf_spots = gpd.GeoDataFrame(spots_df, geometry=geometry, crs="EPSG:4326")

print("【変換前: 経緯度座標系 (EPSG:4326)】")
display(gdf_spots)

# TODO: 2. 平面直角座標系「EPSG:6677」に変換する
gdf_spots_meter = 

print("\n【変換後: 平面直角座標系 (EPSG:6677)】")
# geometry の Point が、大きなメートル単位の数値（X座標, Y座標）に変わっているか確認してください
display(gdf_spots_meter)
```

#### 🎯 期待される出力結果
```text
【変換前: 経緯度座標系 (EPSG:4326)】
   スポット名       Lng      Lat                     geometry
0  スカイツリー  139.8107  35.7100  POINT (139.81070 35.71000)
...

【変換後: 平面直角座標系 (EPSG:6677)】
   スポット名       Lng      Lat                         geometry
0  スカイツリー  139.8107  35.7100  POINT (10486.208 -11648.740)
...
```

<details>
<summary>🔑 【教員用】模範解答コード</summary>

```python
# 1. geometry列の作成
geometry = [Point(xy) for xy in zip(spots_df["Lng"], spots_df["Lat"])]

# 2. CRSのメートル変換
gdf_spots_meter = gdf_spots.to_crs(epsg=6677)
```
</details>

#### 📐 採点基準（10点満点）
*   **【10点】**: 1および2の要件が完全に満たされ、`geometry` 列がメートル単位（例：`POINT (10486.208 -11648.740)` 等の大きな負数/正数）に変換されている。
*   **【6点】**: GeoDataFrameの作成（`EPSG:4326`）はできているが、`.to_crs(epsg=6677)` への変換が行われていない、あるいは上書き代入されていない。
*   **【4点】**: Pointオブジェクトを作成する際に `Point(Lat, Lng)` と、緯度と経度の順番を逆（Y, Xの順）にしてしまったため、スポットが東京から遠く離れた別の国（あるいは南極など）にプロットされてしまっている（GIS最大の定番エラー）。
*   **【0点】**: 動作しない。

---

### 🛰️ 問7: 空間結合（Spatial Join）を用いた行政区の自動判定（配点: 10点）
店舗のピン（`gdf_shops`：Pointデータ）が、どの市区町村エリア（`gdf_wards`：Polygonデータ）に物理的に含まれているかを、空間結合（`gpd.sjoin`）を使って自動判定してください。
結合後のデータフレームに、各店舗が属する「区名」列が正しくマージされて表示されている必要があります。

#### 📥 初期コード
```python
import geopandas as gpd
from shapely.geometry import Point, Polygon

# 1. 行政区ポリゴンデータ（簡易版ダミー：経緯度座標系 EPSG:4326）
poly_shinjuku = Polygon([(139.68, 35.68), (139.71, 35.68), (139.71, 35.71), (139.68, 35.71)]) # 新宿区エリア
poly_shibuya  = Polygon([(139.68, 35.64), (139.71, 35.64), (139.71, 35.68), (139.68, 35.68)]) # 渋谷区エリア

gdf_wards = gpd.GeoDataFrame({
    "区名": ["新宿区", "渋谷区"]
}, geometry=[poly_shinjuku, poly_shibuya], crs="EPSG:4326")

# 2. 店舗Pointデータ
shop_points = [Point(139.7005, 35.6902), Point(139.7015, 35.6585)] # 店舗A, 店舗B
gdf_shops = gpd.GeoDataFrame({
    "店舗名": ["店舗A (新宿エリア)", "店舗B (渋谷エリア)"]
}, geometry=shop_points, crs="EPSG:4326")

print("--- 行政区画ポリゴン ---")
display(gdf_wards)
print("\n--- 店舗GPSポイント ---")
display(gdf_shops)

# TODO: gpd.sjoin を使って、店舗データ(gdf_shops)に区画データ(gdf_wards)を空間結合する
# 引数には、結合方法(how)や条件(predicate=\"intersects\" または \"within\")を指定してください。
joined_df = 

print("\n--- 空間結合（自動エリア判定）の結果 ---")
# 各店舗名に対し、自動的に正しい「区名」がマージされて表示されるか確認してください
display(joined_df[["店舗名", "区名", "geometry"]])
```

#### 🎯 期待される出力結果
```text
--- 空間結合（自動エリア判定）の結果 ---
              店舗名   区名                    geometry
0  店舗A (新宿エリア)  新宿区  POINT (139.70050 35.69020)
1  店舗B (渋谷エリア)  渋谷区  POINT (139.70150 35.65850)
```

<details>
<summary>🔑 【教員用】模範解答コード</summary>

```python
joined_df = gpd.sjoin(gdf_shops, gdf_wards, how="left", predicate="intersects")
# ※ predicate は "within" でも正しく動作します。
```
</details>

#### 📐 採点基準（10点満点）
*   **【10点】**: `gpd.sjoin` が正しく実行され、店舗Aに「新宿区」、店舗Bに「渋谷区」が正確に紐づいて出力されている。
*   **【6点】**: 空間結合（`sjoin`）は実行されているが、引数の `how` を誤って `right` にしたため、店舗データではなく区画データがベースになり表の構造が壊れている。
*   **【3点】**: Pandasの `pd.merge()`（属性結合）を無理やり使おうとして、空間的な位置判定が行われていない（経緯度が合致しないためエラーまたは空になる）。
*   **【0点】**: エラーで動作しない。

---

## 【テーマ4：機械学習（scikit-learn）】（問8〜問9）

### 🤖 問8: scikit-learnによる線形回帰AIモデルの構築と評価（配点: 10点）
客数から売上高を自動で予測するAIを作ります。
以下の手順に沿って、`scikit-learn` を用いた回帰分析のプログラムを完成させてください。
1. `train_test_split()` を使い、客数（`X`）と売上（`y`）を、**学習用（`X_train`, `y_train`）に80%、テスト用（`X_test`, `y_test`）に20%** の比率で分割してください。
2. 線形回帰（`LinearRegression`）のモデルを定義し、学習用データを使って学習（`.fit()`）させてください。
3. テストデータ（`X_test`）に対する予測値（`y_pred`）を算出し、モデルの精度（決定係数 \\(R^2\\)）を `r2_score()` を使って計算して表示してください。

#### 📥 初期コード
```python
import numpy as np
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import r2_score

# ダミーの過去の客数(X)と1日の売上高(y)
np.random.seed(42)
X = np.random.randint(40, 220, (100, 1)) # 客数
y = X * 2300 + np.random.normal(0, 15000, 100) # 売上

# TODO: 1. データを「学習用80%」「テスト用20%」に分割する（random_state=42を指定してください）
X_train, X_test, y_train, y_test = 

# TODO: 2. 線形回帰モデルを定義し、学習用データで学習させる
model = 


# TODO: 3. テストデータを使って売上を予測し、決定係数(R2)を算出する
y_pred = 
r2 = 

print(f"AIモデルの学習が完了しました。")
print(f"テストデータに対する決定係数 (R2): {r2:.4f}") # 0.95前後の高い精度が出ます
```

#### 🎯 期待される出力結果
```text
AIモデルの学習が完了しました。
テストデータに対する決定係数 (R2): 0.9856
```
*(※ `random_state=42` を正しく指定していれば、決定係数は `0.9856` になります)*

<details>
<summary>🔑 【教員用】模範解答コード</summary>

```python
# 1. データの分割
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.20, random_state=42)

# 2. モデルの定義と学習
model = LinearRegression()
model.fit(X_train, y_train)

# 3. 予測と精度測定
y_pred = model.predict(X_test)
r2 = r2_score(y_test, y_pred)
```
</details>

#### 📐 採点基準（10点満点）
*   **【10点】**: データ分割（`test_size=0.20`）、AIの学習（`.fit`）、予測（`.predict`）、および決定係数の算出（0.9856）がすべて正しく実装されている。
*   **【7点】**: 分割や学習は正しくできているが、予測（`.predict`）の段階で、引数に `X_test` ではなく誤って `X_train` を渡してしまい、カンニング状態（過学習の測定）になっている。
*   **【4点】**: 分割割合（`test_size`）の指定が間違っている（デフォルトの25%になっているなど）。
*   **【0点】**: 動作しない。

---

### 🧩 問9: KMeansによる顧客データの自動クラスタリング（配点: 10点）
100人のお客様の「客単価」と「来店頻度」のデータがあります。
`scikit-learn` の `KMeans`（非階層クラスタリング）を使い、お客様を特徴ごとに **「4つのグループ（クラスタ）」** に自動分類し、さらにAIが決定した各グループの中心点（重心座標）を表示してください。

#### 📥 初期コード
```python
import numpy as np
import pandas as pd
from sklearn.cluster import KMeans

# 100人の顧客データ（0列目: 客単価(円), 1列目: 年間来店回数(回)）
np.random.seed(10)
customer_data = np.random.randint(300, 3000, (100, 2))

# TODO: 1. KMeansモデルを定義し、クラスタ数「4」に設定する（random_state=0を指定してください）
kmeans = 

# TODO: 2. データを学習させ、顧客ごとの所属グループ番号(0〜3)を予測して labels に代入する
# ※ .fit_predict(customer_data) メソッドを使うと一撃で完了します。
labels = 

# TODO: 3. 各グループの中心点（重心）の座標を取得して centroids に代入する
# ※モデルの .cluster_centers_ 属性から取得できます。
centroids = 

print("最初の5人のお客様の所属グループ:", labels[:5])
print("\n--- AIが決定した4つのグループの中心点座標（[客単価(円), 来店回数(回)]） ---")
for i, coord in enumerate(centroids):
    print(f"グループ {i} の中心点: 客単価 {coord[0]:.1f} 円 / 来店回数 {coord[1]:.1f} 回")
```

#### 🎯 期待される出力結果
```text
最初の5人のお客様の所属グループ: [1 0 0 2 0]

--- AIが決定した4つのグループの中心点座標（[客単価(円), 来店回数(回)]） ---
グループ 0 の中心点: 客単価 1599.9 円 / 来店回数 1045.1 回
グループ 1 の中心点: 客単価 1500.5 円 / 来店回数 2337.3 回
グループ 2 の中心点: 客単価 2496.2 円 / 来店回数 788.1 回
グループ 3 の中心点: 客単価 871.2 円 / 来店回数 605.3 回
```
*(※ `random_state=0` が正しく指定されていれば、出力される所属グループ番号や中心点座標の数値が完全に一致します)*

<details>
<summary>🔑 【教員用】模範解答コード</summary>

```python
# 1. モデル定義（n_clusters=4）
kmeans = KMeans(n_clusters=4, random_state=0, n_init=10)

# 2. 学習とグループラベル予測
labels = kmeans.fit_predict(customer_data)

# 3. 中心点（重心）の取得
centroids = kmeans.cluster_centers_
```
</details>

#### 📐 採点基準（10点満点）
*   **【10点】**: クラスタ数（`n_clusters=4`）、学習と予測（`fit_predict`）、および重心取得（`.cluster_centers_`）がすべて正確に実装され、数値が一致している。
*   **【6点】**: クラスタ数を誤って「3」や「5」にしている、あるいは重心座標が取得できていない。
*   **【3点】**: モデルの定義（`KMeans()`）はできているが、学習（`fit`）させておらず、ラベルも重心も出力が空のままである。
*   **【0点】**: 動作しない。

---

## 【テーマ5：WebアプリケーションとAIの統合】（問10）

### 🚀 問10: AI予測 ✕ UI連動 Streamlitダッシュボード構築のプロンプト設計（配点: 10点）
本応用クラスの集大成です。
これまでに学んだ「Pandasデータ処理、Matplotlibグラフ描画、scikit-learn回帰予測AI、StreamlitのUI」をすべて合体させ、**「客数をスライダーで動かすと、AIが売上高をリアルタイムで自動計算し、グラフの上に予測点をマッピングし直す、インタラクティブな売上シミュレーションWebアプリ」** を構築します。

このアプリの完璧なPythonプログラムを生成AI（ChatGPT等）に「1行もバグのない完全な形」で出力させるための、**「前提条件や要件を正確に記述したプロンプト（指示テキスト）」** を設計して記述してください。

#### 📥 初期コード
```python
# TODO: 以下のトリプルクォーテーション「\"\"\"」の中に、
#       AIに1回で完璧なStreamlitダッシュボードアプリを書いてもらうための
#       プロンプト（開発要件、動作環境、出力形式の指示などを含む）を
#       データサイエンスのディレクターになったつもりで日本語で記述してください。

final_exam_prompt = """
ここにあなたの設計した「プロンプト（指示書）」を記述してください。
"""

print("--- 設計されたプロンプト ---")
print(final_exam_prompt)
```

<details>
<summary>🔑 【教員用】模範解答例（プロンプトの構成要素）</summary>

優れたプロンプトには、以下の**5つの構成要素**が網羅されている必要があります。

1. **役割の定義（Persona）**: 「あなたはPythonとStreamlitのシニアエンジニアです」等。
2. **前提・実行環境（Context/Environment）**: 「Google Colab上で動作し、japanize_matplotlibやscikit-learnを使用します」等。
3. **データ仕様（Data）**: 「客数と売上高を持つ、店舗AのダミーデータをPandasで自動作成してください」等。
4. **機能・UI要件（Requirements）**:
   - サイドバーに `st.slider` を配置し、「明日の客数（50〜250人、初期値150人）」を入力できるようにする。
   - スライダーの値をもとに、scikit-learnの線形回帰AIで「明日の売上」を予測し、画面に `st.metric` でKPI表示する。
   - メイン画面に、過去実績の散布図、AIが引いた予測直線（回帰直線）、および「現在のスライダー入力に基づく明日のシミュレーション予測点（星マーク ★ などの目立つマーカー）」を重ねたMatplotlibのグラフを表示する。
5. **出力形式（Output Format）**: 「余計な文章や部分的な解説は一切省き、Colabのセルに貼り付けてそのまま動く1つの完成されたPythonコードブロックのみを出力してください」等。

---

### 🌟 実際にこのプロンプトから生成される「完成アプリコード」（動作確認用）
教員の方が動作を確認する、あるいは学生に「動いたときの感動」を体験させる際に、このコードをColabに貼り付けて実行してください。

#### セル1：インストール（Colabでの実行用）
```bash
!pip install streamlit japanize-matplotlib
```

#### セル2：アプリコード（app.py）の書き出し
```python
%%writefile app.py
import streamlit as st
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import japanize_matplotlib
from sklearn.linear_model import LinearRegression

# 1. 過去データの自動生成（100日分）
np.random.seed(42)
visitors = np.random.randint(50, 220, (100, 1))
sales = visitors * 2500 + np.random.normal(0, 20000, 100) # 単価2500円

df_sales = pd.DataFrame({"客数": visitors.flatten(), "売上": sales})

# 2. アプリ画面ヘッダー
st.title("📈 AI売上予測シミュレーター")
st.write("過去実績データからAI（線形回帰）が売上予測モデルを作成。明日の客数を入力してシミュレーションできます。")

# 3. UIの配置（サイドバー）
st.sidebar.header("⚙️ シミュレーション設定")
next_visitors = st.sidebar.slider("明日の予想客数（人）", min_value=50, max_value=250, value=150)

# 4. AIのその場学習
X = df_sales[["客数"]].values
y = df_sales["売上"].values
model = LinearRegression()
model.fit(X, y)

# 明日の売上予測
predicted_sales = model.predict([[next_visitors]])[0]

# 5. 予測結果のKPI表示
st.subheader("🔮 AI予測結果")
st.metric(
    label=f"明日の売上予測額 (予想客数: {next_visitors}人)",
    value=f"{int(predicted_sales):,} 円"
)

# 6. グラフの描画
fig, ax = plt.subplots(figsize=(8, 5))
# 過去データの散布図
ax.scatter(X, y, color="skyblue", label="過去の実績（100日間）", alpha=0.7)

# AIの予測直線（回帰直線）
X_line = np.linspace(50, 250, 100).reshape(-1, 1)
y_line = model.predict(X_line)
ax.plot(X_line, y_line, color="red", linestyle="--", label="AI売上予測ライン")

# 明日のシミュレーション予測点（オレンジの星）
ax.scatter(next_visitors, predicted_sales, color="orange", marker="*", s=250, zorder=5, label="明日のシミュレーション（予測）")

ax.set_title("客数と売上高の関係およびAI予測", fontsize=14)
ax.set_xlabel("客数（人）")
ax.set_ylabel("売上高（円）")
ax.legend()
ax.grid(True, linestyle=":")

# Streamlitにグラフをプロット
st.pyplot(fig)
```

#### セル3：アプリ起動（localtunnel経由）
```bash
# パスワードとなるIPアドレスを表示
!wget -qO- ipv4.icanhazip.com
# バックグラウンドで起動し、外部公開
!streamlit run app.py & npx localtunnel --port 8501
```
</details>

#### 📐 採点基準（10点満点）
*   **【10点】**: 「シニアエンジニアの役割（1点）」「Colab/Streamlit等の実行環境（1点）」「Pandasダミーデータの作成（2点）」「スライダーによる明日の客数入力要件（2点）」「AIでの売上予測とst.metric表示要件（2点）」「散布図・予測直線・明日の予測点★を重ねたMatplotlibグラフ描画の要件（2点）」のすべての重要項目が、プロンプトの中で明確に言語化・指示されている。
*   **【6点】**: アプリの要件は書かれているが、前提環境（Colabで動かすこと、ダミーデータが必要なこと）の指示が抜けているため、AIに投げるとそのままでは動かないコード（ローカルのファイルを読み込もうとするコードなど）が出力されてしまう。
*   **【3点】**: 「売上予測のダッシュボードをStreamlitで作ってください」などの、具体的な要件が何もない極端に短い指示になっている（AIが判断を誤り、使い物にならないアプリを出力するため、大幅減点）。
*   **【0点】**: 白紙、あるいは関係のない記述。

---

# 🎓 採点お疲れ様でした！
この最終課題10問をすべてクリアした学生は、データのクレンジング（Pandas）から、統計的解釈、地図連携（GIS）、AI予測（機械学習）、そしてそれを誰でも使えるようにサービス化するWebエンジニアリングまで、**実務の即戦力として最も求められるデータサイエンスの一連のパイプラインを完全にマスターした証**です。自信を持って、社会やゼミのデータ研究に送り出してください！
