<!-- Markdownセル -->
# 第11回 PythonとAI (scikit-learn) 2：分類モデルとクラスタリング（200分集中講義用）

皆さん、こんにちは！プログラミング応用A（コンピューティング応用G）、第11回の講義です。担当の教員です。

前回（第10回）は、機械学習の王道である「教師あり学習」の第一歩として、数値を予測する「回帰分析（線形回帰・重回帰）」を学びました。過学習を防ぐためにデータを「学習用」と「テスト用」に分ける黄金の儀式や、AIモデルの賢さを測る「決定係数（$R^2$）」などの指標を自分の手で動かして計算できるようになりました。

第11回となる今日は、AI・機械学習シリーズの第2弾として、さらにエキサイティングな2つの領域へとステップアップします！

1. **教師あり学習「分類（Classification）」**：
   「数値」ではなく、未来の顧客が「リピーターになるか、ならないか（1か0か）」、このメールは「迷惑メールか、普通メールか」という**カテゴリ（グループ）を予測するAI**を作ります。ビジネスの意思決定やターゲティング広告の心臓部となる技術です。
2. **教師なし学習「クラスタリング（Clustering）」**：
   これまでは「正解（売上額やリピーターフラグ）」があらかじめ分かっているデータを扱ってきました。しかし現実のビジネスでは、「正解のラベルなんて付いていない、大量の顧客データ」しか手元にないことがほとんどです。
   クラスタリングは、正解のないデータからコンピュータが**「自動的に似たもの同士をグループ分けしてプロファイリングしてくれる」**魔法のような技術です。

「文系の自分には、数式やロジックが難解すぎるのではないか」と身構える必要は一切ありません！
本日の200分の集中講義では、AIが条件を分岐させていく「決定木の図解」や、データ同士が引き寄せ合う「クラスタリングのアニメーション的図解」など、すべて身近なカフェの顧客分析に例えて徹底的に直感的な解説を行います。

これまでに学んだPandasやMatplotlibの知識が、AIの中でどのように実務的な輝きを放つのか、200分間じっくりと楽しんでいきましょう！

---
## ⏱️ 本日の200分スケジュール（タイムテーブル）

2コマ連続の集中講義において、学生の集中力を持続させ、理論の納得と手を動かすハンズオンを高密度に融合させた詳細なスケジュールです。
※授業時間（200分）を圧迫しないよう、宿題・レポート・評価用の「総合演習課題5問」は、この200分枠からは切り離し、時間外に取り組む設計にしています。

| 時間（目安） | セクションと活動内容 | 指導のポイントと教員の役割 |
| :--- | :--- | :--- |
| **00:00 - 00:15**<br>（15分） | **オープニング ＆ 前回の復習**<br>・機械学習の地図の再確認<br>・「回帰」と「分類」の違い | ・前回学んだ「数値を当てる回帰」の全体像をおさらいします。<br>・本日学ぶ「カテゴリを当てる分類」が、日常のどのようなサービス（推薦、スパムフィルター、離脱予測）に使われているかを語り、興味を引きつけます。 |
| **00:15 - 00:55**<br>（40分） | **11.1 教師あり学習「分類」と「決定木」**<br>・決定木（Decision Tree）の仕組み<br>・Yes/No質問を繰り返す直感図解 | **【講義①】**<br>・人間が直感的に理解しやすい最強の分類アルゴリズム「決定木（Decision Tree）」を解説します。<br>・情報利得（ジニ係数）という難しい数学用語を一切使わず、「データを最もきれいに2分割できる最高の質問をAIが自動探索する」プロセスを徹底的にかみ砕いて説明します。 |
| **00:55 - 01:35**<br>（40分） | **11.2 実践：決定木の構築と可視化**<br>・`DecisionTreeClassifier` の実装<br>・過学習（Max Depthの調整） | **【ハンズオン①】**<br>・模擬の顧客データ（年齢、支払額、同伴者有無など）から、リピーターになるか否かを予測する決定木モデルを構築します。<br>・「木の深さ（`max_depth`）」を深くしすぎると、過去データを丸暗記してテストデータで爆死する「過学習（オーバーフィッティング）」の様子をビジュアルで実験します。 |
| **01:35 - 01:45**<br>（10分） | **中間ブレイク（10分小休止）** | 脳を休め、質問を受け付けるためのリフレッシュタイムを挟みます。 |
| **01:45 - 02:25**<br>（40分） | **11.3 分類モデルの「評価指標」**<br>・不均衡データにおける正解率の罠<br>・混同行列（Confusion Matrix）<br>・適合率（Precision）と再現率（Recall） | **【講義②：最大のつまずきポイント】**<br>・「100人中1人の難病を診断するAIが『全員健康』と答えるだけで正解率99%になる大バグ」を例に、Accuracyの限界を解説します。<br>・「オオカミ少年を減らす適合率」と「見落としを減らす再現率」のトレードオフをビジネスの場面（DM送付 vs 不正検知）と結びつけてロジカルに解き明かします。 |
| **02:25 - 03:05**<br>（40分） | **11.4 教師なし学習「クラスタリング」**<br>・K-Means（K平均法）の仕組み<br>・エルボー法による最適なKの決定 | **【講義＆ハンズオン②】**<br>・「正解ラベルが1つもないデータ」から、顧客を自動的にグループ分けするK-Meansの4ステップ（重心のランダム配置 ➡ 割り当て ➡ 重心再計算 ➡ 収束）をアスキーアートで図解します。<br>・「グループ数は何個にすればいい？」を解決するエルボー法（Elbow Method）のメカニズムを解説し、Matplotlibで肘の折れ曲がり図を描画します。 |
| **03:05 - 03:20**<br>（15分） | **11.5 授業内確認練習問題（5問）**<br>・理論＆記述クイズ大会 | クラス全員で、本日の最重要ポイントを振り返るミニクイズ5問に挑戦。教員がユーモアを交えて各問の解説を行い、本日の学びを完全に脳に定着させます。 |
| **03:20 - 03:30**<br>（10分） | **総括 ＆ 次回（Vibe Coding）への接続**<br>・本日のまとめと振り返り | ・これにて「前処理 ➡ 統計解析 ➡ アプリ ➡ 地図 ➡ AI」というPythonデータ活用の全技術の習得が完了したことを讃えます。<br>・次回（第12回）は、これらの力を武器に、生成AIとペアになって口頭の指示だけで一気にシステムを作る「バイブコーディング（AIペアプログラミング）」に入ることを予告して終了します。 |

---
## 🎯 到達目標
この200分の集中講義が終わるころには、以下のことができるようになります。

1. **「回帰（数値を当てる）」** と **「分類（グループを当てる）」** の違いを、実例を交えて説明できる。
2. **決定木（Decision Tree）** アルゴリズムが、データを最もきれいに2分割する「Yes/Noの質問」を自動で組み立てていく仕組みを直感的に理解できる。
3. 単純な「正解率（Accuracy）」だけを信じる危険性を理解し、ビジネスの目的に応じて **「適合率（Precision）」** と **「再現率（Recall）」** を使い分けられる。
4. **「教師あり学習（正解あり）」** と **「教師なし学習（正解なし）」** の決定的な違いを、用途と併せて説明できる。
5. **K-Means（K平均法）** を使って、ラベルのない顧客データを自動でグループ分け（セグメンテーション）し、**エルボー法**で最適なグループ数を科学的に導き出せる。

---
## 前半：教師あり学習「分類」モデルの真髄（100分）

## 11.1 分類（Classification）とは何か？（回帰との違い）

前回学んだ「回帰（Regression）」は、アイスコーヒーの売上杯数（28杯、45杯、52杯...）といった**「途切れなく連続する数値（連続値）」**を当てるのが目的でした。

それに対し、本日学ぶ**「分類（Classification）」**は、「このメールは迷惑メール（1）か、普通メール（0）か」「このお客様はリピーターになってくれる（1）か、もう来ない（0）か」という、**「あらかじめ決められたカテゴリ（離散値）」**のどれに当てはまるかを予測します。

```text
▼ 回帰（Regression）の目的：
  [ 気温 32度 ]  ──▶  【 AIモデル 】  ──▶  「今日の売上は 235,000円 です！」（具体的な数値をピンポイントで予測）

▼ 分類（Classification）の目的：
  [ 年齢: 21歳, 来店3回 ] ──▶  【 AIモデル 】  ──▶  「リピーターになる確率 85% ＝ 【リピーター(1)】に分類！」
```

### 決定木（Decision Tree）の直感的イメージ

分類を行うアルゴリズムにはたくさんの種類がありますが、その中で最も文系の学生の皆さんに優しく、かつ実務でも強力なのが**「決定木（Decision Tree）」**です。

決定木は、データの中に隠れているパターンを分析し、コンピュータが自動的に**「最もきれいにデータを仕分けられるYes/Noの質問リスト」**を作ってくれるアルゴリズムです。その形が、木が枝分かれしているように見えるため「決定木（意思決定の木）」と呼ばれます。

例えば、あるカフェの顧客データから「リピーターになる人（1）」と「1回きりの人（0）」を見分ける決定木をAIが作ると、以下のようなフローチャートが自動生成されます。

**【決定木の枝分かれ（イメージ図）】**
```text
                  [ 🌳 ルートノード（出発点） ]
                   お客様はリピーターになるか？
                               │
                 ❓ 質問1: 来店回数は 3回以上？
                     /                       \
               (Yes) /                       \ (No)
                    /                         \
      ❓ 質問2: ケーキを注文した？          【 1回きりの顧客(0) 】
            /               \               (※来店回数が少ないため)
      (Yes) /               \ (No)
           /                 \
  【 超VIP(1) 】        【 リピーター(1) 】
  (高確率で再来店)      (普通のリピーター)
```

AIは、過去のデータをすべて見比べながら、「どういう順番で、どのような質問をすれば、最もすっきりと1と0を仕分けることができるか」を自動的に学習（`.fit()`）してくれるのです。

---
## 11.2 実践：決定木の構築と過学習（木の深さ）のコントロール

それでは、実際にGoogle Colabで決定木モデルを構築してみましょう！
今回は、カフェの「年齢」と「1回あたりの購入金額」という2つの情報から、そのお客様が「リピーター（1）」になるか「1回きりのライト顧客（0）」になるかを予測する決定木モデルを作成します。

まずは、おなじみの「ダミーデータ自動生成」を行い、データを学習用とテスト用に分割します。

```python
# =================================================================─
# 1. 模擬データの作成とデータ分割（Colabのセルで実行してください）
# =================================================================─
import numpy as np
import pandas as pd
from sklearn.model_selection import train_test_split

np.random.seed(12)
n_customers = 100

# 100人分のお客様の「年齢」と「平均支払金額（円）」
age = np.random.randint(18, 65, n_customers)
spend = np.random.randint(300, 2500, n_customers)

# 年齢が若くて支払金額が高い人、または年齢が高くて支払金額がそこそこの人が
# リピーター（1）になりやすいというルール（パターンのベース）を定義
repeater = []
for a, s in zip(age, spend):
    if a < 35 and s > 1200:
        repeater.append(1)
    elif a >= 35 and s > 800:
        repeater.append(1)
    else:
        # 多少のノイズ（ランダムさ）を加えて現実らしくする
        repeater.append(1 if np.random.rand() > 0.85 else 0)

# データフレーム化
df_customer = pd.DataFrame({
    "年齢": age,
    "支払金額": spend,
    "リピーター": repeater
})

print("--- カフェの顧客データ（先頭5名分） ---")
display(df_customer.head())

# 説明変数 X（年齢、支払金額）と目的変数 y（リピーター：1か0か）に分ける
X = df_customer[["年齢", "支払金額"]]
y = df_customer["リピーター"]

# データを「学習用（75%）」と「テスト用（25%）」に分割
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.25, random_state=42)
print(f"データ分割完了：学習用 {len(X_train)}名分, テスト用 {len(X_test)}名分")
```

### 決定木モデルの学習と「木の深さ」の制限

決定木を作る際、最も注意しなければならないパラメータが**「木の最大深さ（`max_depth`）」**です。

もし、この制限をかけずにAIに学習させると、AIは「100%完全に1と0を仕分けられるまで、無限に細かく複雑な質問」を作り続けてしまいます。
これは、過去のデータの細かいノイズ（たまたまその日だけ機嫌が悪かったお客様、など）まで丸暗記してしまい、新しいデータ（テストデータ）が来たときに全く予測が当たらなくなる**過学習（オーバーフィッティング）**を引き起こします。

そのため、実務では `max_depth`（質問を繰り返す回数の上限）を **3回や4回程度に制限する** のが鉄則です。

```python
# =================================================================─
# 2. 決定木モデルの定義・学習・予測
# =================================================================─
from sklearn.tree import DecisionTreeClassifier # 決定木分類器

# max_depth=3（質問は最大3階層まで）に制限して過学習を防ぎます
clf_tree = DecisionTreeClassifier(max_depth=3, random_state=42)

# 【超重要】学習用データを使って、AIに「最適な質問リスト」を考えさせます
clf_tree.fit(X_train, y_train)

# 勉強した成果を使い、一度も見たことがないテスト用の客層に対して予測を実行
y_pred = clf_tree.predict(X_test)

print("AIによるリピーター予測（最初の5名分）：", y_pred[:5])
print("実際のテスト用データの正解（最初の5名分）：", y_test[:5].values)
```

### 決定木の最大のメリット：「なぜ？」が説明できる（可視化）

決定木の素晴らしいところは、他のAIモデル（複雑なニューラルネットワークなど）と違い、**「なぜその予測をしたのか」という思考のプロセスを、人間が完全に見て納得できる**という点にあります。これを目で見える図（ツリー構造）として書き出してみましょう。

```python
# =================================================================─
# 3. 決定木の構造を綺麗にビジュアル表示する
# =================================================================─
from sklearn.tree import plot_tree
import matplotlib.pyplot as plt
import japanize_matplotlib

fig, ax = plt.subplots(figsize=(14, 8))
plot_tree(
    clf_tree,
    feature_names=["年齢", "支払金額"], # 質問に使った説明変数名
    class_names=["リピートなし", "リピートあり"], # 分類結果のグループ名
    filled=True, # 確率が高い方に色を塗って分かりやすくする
    rounded=True, # 角を丸くする
    ax=ax
)
plt.title("AIが自動構築した『リピーター判定』決定木フローチャート", fontsize=16)
plt.show()
```

描画された図の一番上のボックス（根っこ）を見てみましょう。
AIが「データを最もきれいに2分割できる最高の最初の質問」として、例えば「支払金額 <= 973.5円」といった具体的な数値を自動で弾き出し、そこから見事に枝分かれ（ツリー）を構築している様子が視覚的に確認できたはずです！

---
## 11.3 分類モデルの成績表：「評価指標」の正しい読み解き方

予測モデルが完成したら、そのモデルの「実力テスト（評価）」を行います。
しかし、分類モデルの評価は、前回の回帰（MAEや$R^2$）とは異なる、非常に重要な**ビジネス上のトラップ**が潜んでいます。

### ① 単純な「正解率（Accuracy）」だけを信じてはいけない罠（不均衡データ）

多くの人は、AIの評価として「正解率（Accuracy）＝全体の何％の予測が当たったか」だけを見てしまいがちです。しかし、これは実務において**最悪の意思決定**を招く原因になります。

例えば、100人中1人だけが感染している「超難病（確率1%）」を発見する診断AIを作るとします。
このAIのプログラムを極めてシンプルにし、**「中身を一切見ず、誰が来ても全員に『あなたは健康（陰性）です』と答えるだけの怠け者AI」**にしたとします。

この怠け者AIの正解率はどうなるでしょうか？
100人中99人は本当に健康なので、なんと**「正解率 99.0%」の超優秀に見えるAI**が誕生してしまいます。しかし、一番見つけなければならない「1名の難病患者」を完全に見落としているため、医療現場では使い物にならないどころか、人命に関わる欠陥品です。

このように、データの比率に偏りがある（不均衡データ）場合、正解率（Accuracy）は使い物になりません。そこで、実務では以下の**「混同行列（Confusion Matrix）」**を頭に叩き込む必要があります。

### ② 混同行列（Confusion Matrix）の直感的4分類

混同行列とは、実際の正解（1か0か）と、AIの予測（1か0か）の組み合わせを2✕2の表にまとめたものです。

```text
                  【 AIの予測結果 】
                 予測＝1           予測＝0
            ┌─────────────────┬─────────────────┐
  本物＝1   │  ① 真陽性 (TP)  │  ② 偽陰性 (FN)  │
            │  （大アタリ！） │ （見落とし！）  │
実際の正解  ├─────────────────┼─────────────────┤
  本物＝0   │  ③ 偽陽性 (FP)  │  ④ 真陰性 (TN)  │
            │ （オオカミ少年）│  （安全クリア） │
            └─────────────────┴─────────────────┘
```

*   **① 真陽性 (TP: True Positive)**：本物が「1」で、AIも「1」と予測。文句なしの**大アタリ**！
*   **② 偽陰性 (FN: False Negative)**：本物は「1」なのに、AIは「0」と予測。病気や解約をスルーしてしまった最も危険な**「見落とし（見逃し）」**。
*   **③ 偽陽性 (FP: False Positive)**：本物は「0」なのに、AIは「1」と予測。何もないのに「狼が来たぞ！」と騒いでしまった**「空振り（オオカミ少年・誤検知）」**。
*   **④ 真陰性 (TN: True Negative)**：本物が「0」で、AIも「0」と予測。何もないことを正しく見破った**「安全クリア」**。

---

### ③ 適合率（Precision）と再現率（Recall）のビジネス・トレードオフ

この混同行列をもとに、不均衡データでも騙されないための「2大指標」が定義されます。この2つは**天秤（トレードオフ）の関係**にあり、ビジネスの目的によってどちらを重視するかを私たちが選ぶ必要があります。

#### 1. 適合率 (Precision) ＝ 「空振りを減らしたい！」
*   **意味**：AIが「1（リピーターになる）」と予測した人たちの中で、**「本当に1だった本物の割合」**。
*   **式**： $\text{Precision} = \frac{\text{TP}}{\text{TP} + \text{FP}}$
*   **重視すべきビジネス場面**：
    *   **「無駄なコストを極限まで減らしたいとき」**。
    *   *例*：予算が限られているため、リピート確率が極めて高い人だけに限定して「2000円割引の超高額クーポン」をDM郵送したい。もしAIの予測が空振り（偽陽性：リピートしない人に送ってしまう）すると、郵送費とクーポン代が丸ごと赤字になってしまいます。このような時は、空振りを減らすために「適合率」を最重視します。

#### 2. 再現率 (Recall) ＝ 「見落としを減らしたい！」
*   **意味**：本当に「1（リピーターになるはず）」だった人たちの中で、AIがどれだけ**「見落とさずに1だと見破れたかの割合」**（検出率とも呼ばれます）。
*   **式**： $\text{Recall} = \frac{\text{TP}}{\text{TP} + \text{FN}}$
*   **重視すべきビジネス場面**：
    *   **「見落とした時のリスクやダメージが、取り返しのつかないほど巨大なとき」**。
    *   *例①*：**がんの早期発見AI**。空振り（健康なのに『再検査です』と脅してしまう）は後で検査すれば誤解が解けますが、見落とし（がんがあるのに『健康です』とスルーする）は患者の命に関わります。
    *   *例②*：**クレジットカードの不正利用検知や、顧客の解約予兆検知**。「怪しい行動」を少しでも多くあぶり出し、多少の空振りがあっても、本物の不正や解約を1件残らず食い止めたいときは、「再現率」を限界まで高める必要があります。

それでは、先ほど作ったカフェの決定木モデルの成績表を、scikit-learnを使って一瞬で計算してみましょう。

```python
# =================================================================─
# 4. 評価指標を一括計算する
# =================================================================─
from sklearn.metrics import accuracy_score, precision_score, recall_score, confusion_matrix

print("--- AIモデルの成績表（実力テスト結果） ---")
# 1. 正解率（Accuracy）
acc = accuracy_score(y_test, y_pred)
print(f"① 正解率 (Accuracy) : {acc:.3f}  (全体のうち当たった割合)")

# 2. 適合率（Precision：空振りを防ぐ力）
prec = precision_score(y_test, y_pred)
print(f"② 適合率 (Precision): {prec:.3f}  (AIが『1』と予言した中の本物率)")

# 3. 再現率（Recall：見落としを防ぐ力）
rec = recall_score(y_test, y_pred)
print(f"③ 再現率 (Recall)   : {rec:.3f}  (本物の『1』をどれだけ検知できたか)")

# 4. 混同行列（表の形式で出力）
cm = confusion_matrix(y_test, y_pred)
print("\n▼ 混同行列（Confusion Matrix）の実数内訳:")
cm_df = pd.DataFrame(cm, index=["実際の0（非リピ）", "実際の1（リピ）"], columns=["予測の0", "予測の1"])
display(cm_df)
```

テスト結果の適合率や再現率の数値を見ながら、「このAIは、クーポンの誤送付を防ぐのに向いているか？それとも優良顧客の流出を食い止めるのに向いているか？」を考えることこそが、文系ビジネスパーソンに求められるAI翻訳者（AIディレクター）としての真のスキルです。

---
## 後慢：教師なし学習「クラスタリング」の衝撃（100分）

## 11.4 教師なし学習（Unsupervised Learning）の役割

これまでの学習は、データの中に必ず「売上杯数」や「リピーターフラグ（1/0）」といった**「正解ラベル（答え）」**がセットされていました。
しかし、現実社会にあるデータの9割以上には、そのような都合の良い正解ラベルは付いていません。

例えば、あなたは新しくカフェの店長に就任し、手元に「過去1万人分の購入データ（来店回数と支払金額）」という大きなエクセルシートがあるとします。
正解ラベルはありませんが、ここから「うちのカフェには、どのようなタイプのお客さんたちが集まっているのか？」という**顧客のプロファイリング（顧客セグメンテーション）**を行いたいとします。

ここで活躍するのが、**教師なし学習（Unsupervised Learning）**です。
教師なし学習のAIは、正解を与えられなくても、自らデータの点々の「距離感（特徴）」をじっくり観察し、**「似た性質を持つ者同士のグループ」を自動的にあぶり出して分類**してくれます。

**【教師あり（分類）と教師なし（クラスタリング）の違い】**
```text
▼ 教師あり学習（分類）：あらかじめ「正解の境界線」を教わって分ける
  [ 🔴と🔵が混ざった過去データ ]  ──(学習)──▶  「ここが赤と青の境目（境界線）だ！」

▼ 教師なし学習（クラスタリング）：正解はない。なんとなく「集まっている塊」を見つけて分ける
  [ ⚫と⚫が混ざった過去データ ]  ──(学習)──▶  「なんとなく、左上に1つの塊、右下に1つの塊があるな！」
```

---
## 11.5 K-Means（K平均法）の直感的なステップ

教師なし学習の中で、最も有名で強力なグループ分けアルゴリズムが **K-Means（K平均法 / ケー・ミーンズ）** です。
K-Meansの「K」とは、**「データを何個のグループに分けたいか」というグループの数**を表します（私たちが事前に設定します）。

K-Meansは、まるで学校の席替えでリーダーがクラスメイトを自分のグループに引き寄せるように、以下の4つのステップを高速で繰り返すことで、完璧なグループ分けを自動的に完成させます。

### K-Meansがグループを決める「4つのステップ」

#### ステップ 1：リーダー（重心）をランダムに配置する
あなたが「3つのグループに分けたい（K=3）」と命令すると、AIは真っ白なデータ空間の上に、3つの「リーダーのピン（重心：Centroid）」をランダムにグサッと立てます。

```text
  [ データの点々 ]
     *    *              * 
        *     ▲(赤)        * *
                ▲(青)
     * *   ▲(緑)
```

#### ステップ 2：一番近いリーダーのグループに所属する（割り当て）
すべてのデータの点々は、自分から一番距離が近いリーダーを見つけ、「私は赤グループに入ります」「僕は緑グループ！」と、自動的に所属を決めます。

```text
     🔴   🔴             🔵 
        🔴               🔵 🔵
             
     🟢 🟢   
```

#### ステップ 3：グループの中心地にリーダーが引っ越す（重心の再計算）
所属が決まったら、リーダーは「よし、みんなの真ん中の位置（平均値）を計算して、そこに私が引っ越そう！」と、メンバーの中心地へと移動します。これにより、リーダーの位置がブラッシュアップされます。

```text
       *   *(新赤リーダー)   * 
        *                *(新青リーダー)*
             
     * *(新緑リーダー)
```

#### ステップ 4：リーダーが動かなくなるまで「2」と「3」を繰り返す！
リーダーが動いたので、もう一度ステップ2に戻り、データの点々は「あ、リーダーの位置が変わったから、私は今度は青グループの方が近いな」と所属を決め直します。
このステップ2と3を何度も何度も繰り返すと、やがてリーダーが全く動かなくなります。ここで**「クラスタリング完了」**となります。

---
## 11.6 K-Meansの実践ハンズオンと「エルボー法」

それでは、K-Meansを使って、カフェの顧客100名を自動的にグループ分けしてみましょう！
データは「来店回数」と「平均支払金額（客単価）」の2つの特徴だけを持つ、ラベルのない散布図データです。

```python
# =================================================================─
# 1. データの作成（来店回数と客単価）
# =================================================================─
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

np.random.seed(42)

# カフェに集まる、性質の異なる3つのグループの顧客層を模擬的に生成します
# グループA: 来店は少ないが、1回の支払額が非常に高い「たまの贅沢・ご褒美層」
g_a_freq = np.random.normal(3, 1, 30)
g_a_spend = np.random.normal(1800, 200, 30)

# グループB: 毎日のように来るが、支払額は安い「お仕事・ノマド常連層」
g_b_freq = np.random.normal(15, 2, 40)
g_b_spend = np.random.normal(450, 80, 40)

# グループC: 来店も支払額もそこそこの「標準的なライト層」
g_c_freq = np.random.normal(8, 1.5, 30)
g_c_spend = np.random.normal(1000, 150, 30)

# 合体して1つの顧客データフレームにします
df_customer_raw = pd.DataFrame({
    "来店回数": np.concatenate([g_a_freq, g_b_freq, g_c_freq]),
    "平均支払金額": np.concatenate([g_a_spend, g_b_spend, g_c_spend])
})

# データの可視化（最初は誰がどのグループか分からない真っ黒な点々です）
plt.scatter(df_customer_raw["来店回数"], df_customer_raw["平均支払金額"], color="gray")
plt.title("カフェ顧客データ（初期状態：正解ラベルなし）")
plt.xlabel("来店回数 (回)")
plt.ylabel("平均支払金額 (円)")
plt.grid(True)
plt.show()
```

### 【超重要】K-Meansを動かす前の「標準化（スケーリング）」の絶対ルール

クラスタリングを動かす前に、前回の重回帰の最後でも紹介した**「標準化（データのスケールを整える）」**を実行しなければなりません。

なぜなら、K-Meansは点と点の**「物理的な距離」**を測ってグループ分けするからです。
今回のデータを見ると、横軸の「来店回数」は高々 `1〜20` 程度の狭い範囲ですが、縦軸の「支払金額」は `300〜2500` という巨大な桁数の範囲を動きます。
このままの状態で距離を計算すると、AIは「縦軸の支払金額のちょっとのズレ（例えば100円の差）」をもの凄く遠い距離だと勘違いし、横軸の来店回数の違いをほぼ無視して、縦方向の細長いスライスだけでグループ分けをしてしまいます。

これを防ぐために、両方の列を「平均が0、標準偏差（ばらつき）が1」の平等なスケールに変換（標準化）してから、K-Meansに入力します。

```python
# =================================================================─
# 2. データの標準化と K-Means の実行
# =================================================================─
from sklearn.preprocessing import StandardScaler
from sklearn.cluster import KMeans

# ① データを平等なスケールに標準化します
scaler = StandardScaler()
X_scaled = scaler.fit_transform(df_customer_raw)

# ② K-Meansモデルの定義（3つのグループに分けたいので n_clusters=3）
kmeans = KMeans(n_clusters=3, random_state=42, n_init=10)

# ③ 学習とグループ割り当ての実行
# 教師なし学習なので、引数に y (正解) は渡さず、X_scaled（ヒント）だけを渡します！
# .fit_predict() を使うと、学習と同時に各データのグループ番号（0, 1, 2）を返してくれます。
customer_clusters = kmeans.fit_predict(X_scaled)

# 元のデータフレームに「AIが自動で決めたグループ番号」を追加
df_customer_raw["グループ番号"] = customer_clusters

print("--- AIが自動でグループ分けした結果（先頭5名分） ---")
display(df_customer_raw.head())
```

### AIが色付けした顧客セグメントの可視化

AIが自動的に導き出したグループ番号ごとに、色を変えて散布図を再描画してみましょう。

```python
# =================================================================─
# 3. クラスタリング結果のプロット
# =================================================================─
plt.figure(figsize=(8, 6))
# グループ番号（0, 1, 2）ごとに色を変えてプロット
for i in range(3):
    cluster_data = df_customer_raw[df_customer_raw["グループ番号"] == i]
    plt.scatter(cluster_data["来店回数"], cluster_data["平均支払金額"], label=f"グループ {i}")

plt.title("K-Meansによる顧客自動セグメンテーション（3グループ）")
plt.xlabel("来店回数 (回)")
plt.ylabel("平均支払金額 (円)")
plt.legend()
plt.grid(True)
plt.show()
```

人間が「この人はVIP、この人はライト」と教えることすらしていないのに、コンピュータが距離感から見事に「来店は少ないが高額なグループ」「常連だけど安価なグループ」「平均的なグループ」の3つの塊を自動で看破し、色分けしたのが確認できたでしょうか？これが教師なし学習の絶大なパワーです。

---

### 🤔 グループ数はいくつに設定すればいいの？「エルボー法（Elbow Method）」

K-Meansを使うとき、誰もが「グループ数（K）は本当に3個でいいのか？2個や4個の方がいいのでは？」と悩みます。正解のラベルがないため、客観的に評価するのが難しいためです。

この疑問を解決するアプローチが、**エルボー法（Elbow Method）**です。

K-Meansモデルには、学習が終わると **`inertia_`（インナーシャ：グループ内の歪み・データのバラツキの合計値）** という数値が自動で保存されます。
グループ数を「1個、2個、3個……」と増やしていくと、当然グループが細分化されるため、グループ内のバラツキ（`inertia_`）はどんどん小さくなっていきます。

この「グループ数」と「バラツキの数値」をグラフにプロットした時、**「これ以上グループを増やしても、大してバラツキが減らなくなる境界線（グラフがガクッと折れ曲がり、まるで人間の『肘（Elbow）』のように見える部分）」**があります。この折れ曲がりポイントを最適なグループ数（K）と判断するのがエルボー法です。

```python
# =================================================================─
# 4. エルボー法による最適なグループ数の探索
# =================================================================─
inertia_list = []
k_range = range(1, 8) # グループ数Kを 1から7まで試してみる

for k in k_range:
    # 各Kでモデルを作り、学習させます
    km_test = KMeans(n_clusters=k, random_state=42, n_init=10)
    km_test.fit(X_scaled)
    # 歪み（inertia_）をリストに格納
    inertia_list.append(km_test.inertia_)

# エルボー図を描画
plt.plot(k_range, inertia_list, marker="o", color="purple")
plt.title("最適なグループ数を見極める『エルボー図』")
plt.xlabel("設定したグループ数 (K)")
plt.ylabel("グループ内のデータのバラツキ合計 (Inertia)")
plt.grid(True)
plt.show()
```

グラフを見てください。
Kが「3」の部分を境目にして、それ以降（4, 5, 6）は傾きが緩やかになり、バラツキがあまり減らなくなっている（肘のように曲がっている）のが見つかったでしょうか？
エルボー法により、「このカフェの顧客データは、科学的に『3つ』に分類するのが最も美しいグループ数である」という客観的な根拠を、説得力を持って上司やクライアントにプレゼンすることができます！

---
## 11.7 授業内確認練習問題（ミニクイズ5問） (約15分)

本日の講義で学んだコアとなる知識をおさらいし、頭の中を整理するための理論確認クイズです。全員で解いて、正解を導き出してみましょう！

### クイズ 1: 「回帰」と「分類」の仕分け（理論）
以下のデータ予測タスクのうち、解くべき問題が**「分類（Classification）」**であるものを「すべて」選んでください。
1. 各顧客が「今月の新商品を注文する（1）か、しない（0）か」を予測する。
2. カフェの「来週の土曜日の売上金額（円）」を予測する。
3. アップロードされた写真が「ショートケーキ」「モンブラン」「プリン」のどれであるかを予測する。
4. カフェに滞在する「お客様の平均時間（分）」を予測する。

<details>
<summary>👉 クイズ 1 の解答と解説を見る</summary>

*   **正解**: **1 と 3**
*   **解説**:
    *   1 は「注文する／しない」という2カテゴリの予測、3 は3種類のケーキというカテゴリの予測なので、これらはすべて**分類（Classification）**の問題です。
    *   2 は「売上金額（円）」という連続する数値を当てるため、4 は「滞在時間（分）」という連続する数値を当てるため、これらは**回帰（Regression）**の問題に該当します。

</details>

---

### クイズ 2: 正解率（Accuracy）の甘い罠（理論）
あなたはカフェの会員登録アプリで「退会しそうな人を事前に発見して引き止めるAI」を構築しました。
このカフェの会員の退会率は非常に低く、年間でわずか「0.5%（200人に1人）」しか退会しません。

あなたが作った予測モデルの精度を測定したところ、**「正解率（Accuracy）が 99.5%」**という驚異的な数値が出ました。
しかし、実際に中身を詳しく見ると、このAIは「退会しそうな人を1人も発見できていない」ことが分かりました。なぜこのような現象が起きてしまったのか、理由を説明してください。

<details>
<summary>👉 クイズ 2 の解答と解説を見る</summary>

*   **解答例**:
    *   このデータは退会する人が0.5%、退会しない人が99.5%という極端に偏った「不均衡データ」である。
    *   AIが全員に対して単に「この人は退会しません（0）」と、何一つ考えずにただ同じ予測を繰り返す（怠け者モデル）だけで、全体の99.5%の予測が当たってしまい、見かけ上の正解率（Accuracy）が99.5%になってしまった。
    *   実際のビジネスで本当に見つけなければならない「退会しそうなわずか0.5%の人（見落とし）」を一切捉えられていないため、このAIは実務的には価値がまったくない。
*   **解説**:
    *   不均衡データに対して正解率（Accuracy）だけを評価に使うと、こうした見かけの数値に騙される「不均衡データの罠」に陥ります。こうした場合は、後述する適合率や再現率、混同行列を必ずセットで評価する必要があります。

</details>

---

### クイズ 3: 適合率と再現率、どっちを優先？（データリテラシー）
あなたが開発した「カフェのクレジットカードの不正利用を自動的に検知・ブロックするAIモデル」があるとします。
この不正検知システムを実務で運用するにあたり、以下の2つのリスクを考えました。

*   **リスクA（空振りのリスク）**：普通の健全なお客様の決済を、AIが「不正利用かもしれない！」と誤検知（空振り）してしまい、一時的に決済をロックして不快な思いをさせてしまう。
*   **リスクB（見落としのリスク）**：本物の泥棒がクレジットカードを不正に大金を使ってしまっているのに、AIが「普通のお客様の決済です」とスルー（見落とし）してしまい、店とカード会社に数百万の損害が出る。

1. 今回のクレジットカード不正検知ビジネスにおいて、極限まで減らさなければならないのは**リスクAとリスクBのどちら**でしょうか？
2. そのリスクを避けるために、私たちが最大まで引き上げるべきAIモデルの評価指標は、**「適合率（Precision）」と「再現率（Recall）」のどちら**でしょうか？

<details>
<summary>👉 クイズ 3 の解答と解説を見る</summary>

*   **正解**:
    1.  極限まで減らすべきリスク：**リスクB（本物の不正利用の見落とし）**
    2.  最大まで高めるべきAIの評価指標：**「再現率（Recall）」**
*   **解説**:
    *   カードの不正利用は、1件見落とすだけで数百万の壊滅的な実害が発生します。多少の空振り（健全なお客様を疑って確認の手間を取らせてしまう：リスクA）をしてでも、本物の泥棒を「絶対に1件も見落とさない」ようにモデルを設定する必要があります。
    *   したがって、見落としを限界までゼロに近づける指標である **「再現率（Recall、検出率）」** を最優先で引き上げるチューニングを行うのが、実務における正しいデータリテラシーです。

</details>

---

### クイズ 4: 教師あり学習と教師なし学習の決定的な違い（理論）
データサイエンスのプロジェクトを進める上で、「教師あり学習」と「教師なし学習（クラスタリング）」の最も決定的なデータの持ち方の違い（何が存在し、何が存在しないか）を説明してください。

<details>
<summary>👉 クイズ 4 の解答と解説を見る</summary>

*   **正解**: データのなかに**「正解ラベル（ターゲット・答えとなるy）」**が含まれているか、含まれていないかの違い。
*   **解説**:
    *   **教師あり学習**：データの中に「売上高」や「リピーター判定（1/0）」といった明確な**正解の列（y）**があらかじめ用意されており、それをヒントから当てるルールを学びます。
    *   **教師なし学習**：データの中には「年齢」「来店回数」といったヒント（X）しかなく、**正解（y）が存在しません**。コンピュータが自らデータのバラツキや距離の偏りを見つけて、自動的に似たもの同士をグルーピングします。

</details>

---

### クイズ 5: K-Meansにおける「標準化」の必要性（理論）
K-Means（K平均法）を動かす前に、データの各列のスケールを揃える「標準化（平均0、標準偏差1にする変換）」を行う必要があるのはなぜですか？
「距離」というキーワードを必ず含めて説明してください。

<details>
<summary>👉 クイズ 5 の解答と解説を見る</summary>

*   **解答例**:
    *   K-Meansは、データ同士の**「距離（空間的な直線距離）」**を測ることによってグループ分けを計算するアルゴリズムだから。
    *   標準化をせずに「単位や桁数が著しく異なるデータ（例：1〜20の来店回数と、300〜2500の支払金額）」をそのまま計算に入れると、桁数の大きい支払金額のわずかな差だけで「距離がとても遠い」とAIが過剰に判定してしまい、来店回数の違いをほとんど無視した歪んだグループ分けになってしまうため、データのばらつきのスケールを均等に揃える必要がある。

</details>

---
## 💻 総合演習課題（実技5問）

※この演習課題は、200分の講義時間内には含みません。授業後の宿題やレポート、自習用、期末の実技試験問題としてフルにご活用ください。
すべてGoogle Colab上で100%動くダミー生成データを内蔵して書き下ろしています。

### 📋 演習課題の概要
*   **課題 1（決定木の学習）**: 顧客アンケートから「新メニューのリピート意向」を予測する決定木AIモデルの構築（難易度：易）
*   **課題 2（過学習の制御）**: 決定木の最大深さ（`max_depth`）を変更し、テストデータの精度変化をスコアリングする過学習制御実験（難易度：中）
*   **課題 3（評価指標の算出）**: 予測結果から、混同行列（Confusion Matrix）を可視化し、Accuracy, Precision, Recallを算出（難易度：中）
*   **課題 4（K-Meansセグメント）**: カフェの「平均滞在時間」と「年間支払総額」の未ラベルデータから、K-Meansで3つの顧客クラスタを自動検出（難易度：中）
*   **課題 5（エルボー最適解の探索）**: K-Meansの `inertia_`（歪み）を取り出すforループを書き、最適なKを見極めるエルボー図を描く実践（難易度：難）

---

### 演習課題 1: 顧客アンケートから「リピート意向」を予測する決定木分類器の作成

ここに、カフェで新しく発売した「特製アサイーボウル」を注文した顧客50人のアンケート結果があります。
説明変数（X）として「年齢」「評価（1〜5の5段階評価）」の2つを用い、目的変数（y）として「リピート意向（1: また買う、0: もう買わない）」を予測する決定木モデルを作成してください。

*   **条件**:
    1.  `DecisionTreeClassifier` のパラメータは `max_depth=2`（質問は最大2回まで）に設定してください。
    2.  `random_state=42` を指定して、結果を固定してください。
    3.  モデルを学習（`.fit()`）させる際は、事前に用意された学習用データ（`X_train`, `y_train`）を使用してください。

```python
import numpy as np
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.tree import DecisionTreeClassifier

# --- データの自動生成 (このセルをそのまま実行してください) ---
np.random.seed(100)
n_samples = 80
survey_age = np.random.randint(18, 60, n_samples)
survey_rating = np.random.randint(1, 6, n_samples) # 5段階評価

# 評価が4以上、または年齢が30代以下の評価3の人がまた買いやすいパターン
survey_repeat = []
for a, r in zip(survey_age, survey_rating):
    if r >= 4:
        survey_repeat.append(1)
    elif a <= 39 and r >= 3:
        survey_repeat.append(1)
    else:
        survey_repeat.append(0)

df_survey = pd.DataFrame({
    "年齢": survey_age,
    "満足度評価": survey_rating,
    "リピート": survey_repeat
})

# データ分割
X_survey = df_survey[["年齢", "満足度評価"]]
y_survey = df_survey["リピート"]
X_train, X_test, y_train, y_test = train_test_split(X_survey, y_survey, test_size=0.25, random_state=42)

# --- ここからコードを記述してください ---

# TODO: 1. DecisionTreeClassifier のインスタンスを作成（max_depth=2, random_state=42）
clf_survey = 

# TODO: 2. 学習用データ（X_train, y_train）を使って学習を実行（.fit）


# TODO: 3. テストデータ（X_test）を使って予測を実行し、結果を y_pred_survey に代入する
y_pred_survey = 

print("学習完了後のAIの予測結果:")
print(y_pred_survey)
```

<details>
<summary>👉 演習課題 1 の模範解答と解説を見る</summary>

#### 【模範解答】
```python
# 1. インスタンスの作成
clf_survey = DecisionTreeClassifier(max_depth=2, random_state=42)

# 2. モデルの学習
clf_survey.fit(X_train, y_train)

# 3. 予測の実行
y_pred_survey = clf_survey.predict(X_test)
```

#### 【教員による採点・部分点ガイド（10点満点）】
*   **【10点】**: 決定木のインスタンスを `max_depth=2`, `random_state=42` を指定して正しく作り、`.fit` に `X_train`, `y_train` を渡し、`.predict` で `X_test` に対する予測を実行できている。
*   **【7点】**: 決定木の作成はできたが、`max_depth=2` のパラメータの記述を忘れて、制限をかけずにモデルを作ってしまった（過学習のリスクがあるため）。
*   **【5点】**: モデルの学習（`.fit`）の際に、テスト用のデータ（`X_test`）を誤って渡してしまった（試験問題を見て勉強したことになり、不正な評価[データリーケージ]になるため大幅減点）。
*   **【0点】**: 決定木ではないモデル（LinearRegressionなど）を使用している、または未記入。

</details>

---

### 演習課題 2: 木の深さ（max_depth）の変更による過学習の制御実験

決定木を構築する際、`max_depth`（木の深さの上限）をいくらに設定するかで予測の精度が激変します。
演習課題1で使用した顧客アンケートデータ（`X_train`, `y_train`, `X_test`, `y_test`）を使い、木の深さを「制限なし（最大まで伸ばす）」にした場合の決定木モデルを構築し、テストデータの正解率（Accuracy）を算出してください。

*   **条件**:
    1.  `DecisionTreeClassifier` を定義する際、`max_depth` パラメータを指定せずに（Noneのまま）作成し、`random_state=42` のみを指定して、変数 `clf_deep` に代入してください。
    2.  学習用データで学習を実行してください。
    3.  `accuracy_score` をインポートし、テストデータに対する正解率を計算して画面に出力してください。

```python
# 演習課題1のデータ（X_train, y_train, X_test, y_test）がColabのメモリに残っている状態で実行します。
from sklearn.tree import DecisionTreeClassifier
from sklearn.metrics import accuracy_score

# --- ここからコードを記述してください ---

# TODO: 1. max_depthを指定せず、random_state=42のみで決定木モデルを作成
clf_deep = 

# TODO: 2. 学習用データで学習を実行


# TODO: 3. テストデータで予測を実行し、y_deep_pred に代入
y_deep_pred = 

# TODO: 4. テストデータの実際の正解(y_test)と予測(y_deep_pred)から、正解率(Accuracy)を算出
deep_accuracy = 

print(f"深さ制限なしの決定木のテストデータ正解率: {deep_accuracy:.4f}")
```

<details>
<summary>👉 演習課題 2 の模範解答と解説を見る</summary>

#### 【模範解答】
```python
# 1. 制限なしの決定木作成
clf_deep = DecisionTreeClassifier(random_state=42)

# 2. 学習
clf_deep.fit(X_train, y_train)

# 3. 予測
y_deep_pred = clf_deep.predict(X_test)

# 4. 正解率の算出
deep_accuracy = accuracy_score(y_test, y_deep_pred)
```

#### 【教員による採点・部分点ガイド（10点満点）】
*   **【10点】**: `max_depth` の引数を何も書かずにインスタンスを作り、学習・予測させて `accuracy_score` を用いて正解率を正しく計算できている。
*   **【7点】**: 正解率を計算する `accuracy_score` の引数に渡す順番（正解, 予測）を誤っている、または計算結果を格納できていない。
*   **【5点】**: インスタンス定義の際、誤って `max_depth=2` などの引数を残してしまい、制限なしの実験ができていない。
*   **【0点】**: 決定木の作成ができていない、または未記入。

</details>

---

### 演習課題 3: 分類成績表（混同行列、適合率、再現率）の一括算出

あなたが作成したAIモデルの予測精度をより正確に測定するため、演習課題1で作成した「深さ制限あり（max_depth=2）のモデル」の予測結果（`y_pred_survey`）を使い、以下の評価指標をすべて計算して表示するプログラムを完成させてください。

*   **算出するもの**:
    1.  混同行列（実数内訳）：`confusion_matrix()`
    2.  正解率：`accuracy_score()`
    3.  適合率（Precision）：`precision_score()`
    4.  再現率（Recall）：`recall_score()`

```python
# 演習課題1が正常に完了し、予測結果 y_pred_survey と y_test がメモリにある状態で実行します。
from sklearn.metrics import confusion_matrix, accuracy_score, precision_score, recall_score
import pandas as pd

# --- ここからコードを記述してください ---

# TODO: 1. 混同行列を計算し、変数 cm_survey に代入
cm_survey = 

# TODO: 2. 正解率を計算し、変数 acc_survey に代入
acc_survey = 

# TODO: 3. 適合率(Precision)を計算し、変数 prec_survey に代入
prec_survey = 

# TODO: 4. 再現率(Recall)を計算し、変数 rec_survey に代入
rec_survey = 


# 結果表示（ここはそのまま実行して確認してください）
print("==== 決定木（max_depth=2）評価レポート ====")
print(f"正解率 (Accuracy) : {acc_survey:.4f}")
print(f"適合率 (Precision): {prec_survey:.4f}")
print(f"再現率 (Recall)   : {rec_survey:.4f}")
print("\n▼ 混同行列の実数内訳:")
cm_df_survey = pd.DataFrame(cm_survey, index=["実際0（非リピ）", "実際1（リピ）"], columns=["予測0", "予測1"])
display(cm_df_survey)
```

<details>
<summary>👉 演習課題 3 の模範解答と解説を見る</summary>

#### 【模範解答】
```python
# 1. 混同行列の計算
cm_survey = confusion_matrix(y_test, y_pred_survey)

# 2. 正解率の計算
acc_survey = accuracy_score(y_test, y_pred_survey)

# 3. 適合率の計算
prec_survey = precision_score(y_test, y_pred_survey)

# 4. 再現率の計算
rec_survey = recall_score(y_test, y_pred_survey)
```

#### 【教員による採点・部分点ガイド（10点満点）】
*   **【10点】**: 4つのすべての評価関数を正しくインポートして呼び出し、正しい引数（本物のテスト用正解 `y_test` と、AIの予測結果 `y_pred_survey`）を渡して計算できている。
*   **【7点】**: 4つのうち一部の計算（例えば混同行列のみなど）で引数の順番を誤っている。
*   **【5点】**: 関数の引数に、誤って学習用の正解データ（`y_train`）を渡して評価を計算してしまった（実力テストになっていないため大幅減点）。
*   **【0点】**: 関数が正しく動作していない、または未記入。

</details>

---

### 演習課題 4: K-Means を使った「滞在時間と客単価」による未ラベル顧客グループ分け

ここに、正解ラベル（答え）が全く付いていない、カフェの顧客120名分のデータフレーム `df_cafe_unlabeled` があります。
記録されている特徴量は、お客様の**「滞在時間（分）」**と**「年間支払総額（円）」**の2つです。

K-Meansを使って、この120名のお客様を**「4つのグループ（n_clusters=4）」**に自動的にセグメンテーション（グループ分け）してください。

*   **条件**:
    1.  距離を正しく計算させるため、K-Meansにデータを入れる前に、`StandardScaler` を用いてすべてのデータを**標準化**してください（変数 `X_unlabeled_scaled` に代入）。
    2.  `KMeans` のインスタンスを作る際は、`n_clusters=4`、`random_state=42` を指定してください。
    3.  `fit_predict()` を実行して、各顧客のグループ番号（0, 1, 2, 3）を取得し、データフレームの新しい列 **「顧客タイプ」** に追加して中身を表示してください。

```python
import numpy as np
import pandas as pd
from sklearn.preprocessing import StandardScaler
from sklearn.cluster import KMeans

# --- データの自動生成 (このセルをそのまま実行してください) ---
np.random.seed(80)
n_guests = 120
# 120名の「滞在時間(分)」と「年間支払総額(円)」
stay_time = np.concatenate([
    np.random.normal(20, 5, 40),   # サクッと短時間層
    np.random.normal(120, 25, 40), # 長居する勉強・仕事層
    np.random.normal(60, 15, 40)   # 標準のおしゃべり層
])
annual_spend = np.concatenate([
    np.random.normal(5000, 1200, 40),
    np.random.normal(30000, 5000, 40),
    np.random.normal(15000, 3000, 40)
])

df_cafe_unlabeled = pd.DataFrame({
    "滞在時間": stay_time,
    "年間支払総額": annual_spend
})

# --- ここからコードを記述してください ---

# TODO: 1. StandardScaler のインスタンスを作成し、データを標準化する
scaler_unlabeled = 
X_unlabeled_scaled = 

# TODO: 2. KMeans のインスタンスを作成（n_clusters=4, random_state=42）
kmeans_unlabeled = 

# TODO: 3. fit_predict を使って標準化データからグループ番号（0〜3）を取得し、新列「顧客タイプ」に追加
df_cafe_unlabeled["顧客タイプ"] = 

print("--- 自動グループ分け完了後のデータフレーム（先頭10名分） ---")
display(df_cafe_unlabeled.head(10))
```

<details>
<summary>👉 演習課題 4 の模範解答と解説を見る</summary>

#### 【模範解答】
```python
# 1. StandardScaler によるデータの標準化
scaler_unlabeled = StandardScaler()
X_unlabeled_scaled = scaler_unlabeled.fit_transform(df_cafe_unlabeled)

# 2. KMeansモデルの定義（n_clusters=4, random_state=42）
kmeans_unlabeled = KMeans(n_clusters=4, random_state=42, n_init=10)

# 3. 学習と同時にクラスタラベルを予測し、データフレームに列追加
df_cafe_unlabeled["顧客タイプ"] = kmeans_unlabeled.fit_predict(X_unlabeled_scaled)
```

#### 【教員による採点・部分点ガイド（10点満点）】
*   **【10点】**: `StandardScaler` で正しく標準化を実行し、`KMeans` インスタンスを `n_clusters=4` で定義、`fit_predict` を使って元のデータフレームの列に格納できている。
*   **【7点】**: 標準化の処理はできたが、K-Meansを動かす引数に、誤って「標準化する前（生データ）」の `df_cafe_unlabeled` を直接渡してしまった（滞在時間と年間支払総額の桁数が全く違うため、距離計算がバグってしまい、実用に耐えない細長セグメントになるため3点減点）。
*   **【5点】**: K-Meansのクラスター数を `n_clusters=4` 以外の値（3や5など）で作成してしまっている。
*   **【0点】**: 標準化もクラスタリングもできていない、または未記入。

</details>

---

### 演習課題 5: 実践！エルボー法（Elbow Method）による最適なグループ数の科学的探索

K-Meansで最も頭を悩ませる「最適なグループ数（K）の数」を決定するために、forループと `inertia_`（グループ内の歪み・ばらつき合計）を組み合わせた**「エルボー図」**を自力で描画する、応用レベルのプログラムを作成してください。

*   **条件**:
    1.  グループ数 $K$ を **「1 から 8 まで（1以上9未満）」** 変化させる for ループを作成してください。
    2.  ループ内で各 $K$ に応じた `KMeans(n_clusters=k, random_state=42, n_init=10)` を定義・学習（`.fit()`）させてください。
    3.  学習が終わったモデルから、ばらつき合計値である **`test_model.inertia_`** を取得し、リスト `my_inertia_list` に `.append()` で追加していってください。
    4.  ループ終了後、横軸に設定したグループ数（1〜8）、縦軸に `my_inertia_list` の値をプロットした線グラフ（マーカー付き）を Matplotlib で描画してください。

```python
# 演習課題4の標準化されたデータ「X_unlabeled_scaled」がメモリにある状態で実行します。
from sklearn.cluster import KMeans
import matplotlib.pyplot as plt

my_inertia_list = []
k_range = range(1, 9) # 1から8まで試す

# --- ここからコードを記述してください ---

# TODO: 1. forループを使って k_range (1から8) の値をループ処理する
for k in 

    # TODO: 2. K-Meansモデルを各 k で定義 (n_clusters=k, random_state=42, n_init=10)
    test_model = 
    
    # TODO: 3. 標準化データ X_unlabeled_scaled を使って学習させる（教師なしなので fit のみでOK）
    
    
    # TODO: 4. my_inertia_list に、モデルの「inertia_」属性の値をアペンドする
    

# TODO: 5. Matplotlib を用いて、横軸 k_range、縦軸 my_inertia_list のエルボー図を描く
plt.figure(figsize=(8, 5))
plt.plot(                               , marker="o", color="blue")
plt.title("顧客データの最適なKを見つけるエルボー図")
plt.xlabel("グループ数 (K)")
plt.ylabel("インナーシャ (Inertia)")
plt.grid(True)
plt.show()
```

<details>
<summary>👉 演習課題 5 の模範解答と解説を見る</summary>

#### 【模範解答】
```python
# 1. forループを回す
for k in k_range:
    # 2. KMeansモデルを定義
    test_model = KMeans(n_clusters=k, random_state=42, n_init=10)
    # 3. 学習の実行
    test_model.fit(X_unlabeled_scaled)
    # 4. インナーシャ（歪み）の蓄積
    my_inertia_list.append(test_model.inertia_)

# 5. エルボー図のプロット
plt.figure(figsize=(8, 5))
plt.plot(k_range, my_inertia_list, marker="o", color="blue")
plt.title("顧客データの最適なKを見つけるエルボー図")
plt.xlabel("グループ数 (K)")
plt.ylabel("インナーシャ (Inertia)")
plt.grid(True)
plt.show()
```

#### 【教員による採点・部分点ガイド（10点満点）】
*   **【10点】**: ループ内で各Kの `KMeans` モデルを作って `.fit` させ、`.inertia_` を正しくアペンドし、最終的に肘が折れ曲がる美しいグラフを描画できている。
*   **【8点】**: グラフの描画まで出来たが、横軸や縦軸に渡すリストが不一致でエラーが出た、またはループの範囲設定を誤り、K=1しか計算していない。
*   **【5点】**: K-Meansの学習時に、標準化されたデータではなく、標準化前の生データを渡してループを回したため、歪みが極端に異なるスケールで計算されている（論理的に重大なミスとみなし減点）。
*   **【0点】**: ループ処理が全く書けていない、または未記入。

</details>
