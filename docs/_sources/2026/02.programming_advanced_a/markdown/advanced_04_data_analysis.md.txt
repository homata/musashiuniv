<!-- Markdownセル -->
# 第4回 データ解析（100分授業用）

皆さん、こんにちは！プログラミング応用A（コンピューティング応用G）、第4回の講義です。担当の教員です。
前回の第3回では、「データの種類（尺度）」を整理し、複数の表を連結・結合したり、時系列データから曜日を抽出したりする高度なデータ加工（前処理）を学びました。

ここまでの授業で、皆さんは「汚いデータを綺麗にし、分析しやすいように並べるスキル」を完全にマスターしました。データ分析の全作業のうち、実に約8割がこの「前処理」だと言われています。つまり、皆さんはすでに山頂のすぐ手前まで来ているのです！

今日のテーマは、いよいよデータ分析の本番、**「データ解析（Data Analysis）」**です。

綺麗に整ったデータから、「売上の平均やばらつき（記述統計）」を計算し、「何と何が関係しているのか（相関分析）」を紐解き、データを二次元の表で多角的に集計する「クロス集計（ピボットテーブル）」の手法を学びます。
「標準偏差」や「相関係数」といった一見難しそうな数学の言葉が出てきますが、**大丈夫です！** 「カフェに来る大富豪の罠」や「アイスの売上と気温の関係」など、身近な例を交えて、数式なしで直感的に理解できるように解説します。

それでは、データからビジネスや社会の課題を解決する「データアナリスト」への扉を開きましょう！

---
## 🎯 到達目標 (約5分)
この100分の授業が終わるころには、以下のことができるようになります。
1. **記述統計**（平均値、中央値、最頻値、分散、標準偏差）のビジネスにおける使い分けと意味を説明できる。
2. 平均値の落とし穴（外れ値の罠）を理解し、**中央値**を適切に選んで評価できる。
3. **クロス集計とピボットテーブル（pivot_table）** を使って、2つの属性（例：顧客タイプ×注文商品）を掛け合わせた複雑なデータ分析ができる。
4. **相関分析（相関係数）** の仕組みを理解し、2つの変数の間にある関係（正の相関、負の相関）をデータから見つけ出せる。

---
## ⏱️ 本日のタイムスケジュール (100分授業用)

1.  **4.1 平均値の罠と記述統計の基本 (約20分)**
    *   平均値、中央値、最頻値、標準偏差の直感的な理解。
2.  **4.2 クロス集計とピボットテーブル (約15分)**
    *   2つのデータをクロスさせて隠れたトレンドをあぶり出す。
3.  **4.3 相関分析：データ同士の関係性を数値化する (約15分)**
    *   相関係数（$r$）の意味と、相関 vs 因果の落とし穴。
4.  **💻 演習課題（難易度別5問） (約45分)**
    *   実際のカフェデータ、顧客アンケートデータを使った実践解析。
5.  **💡 解答確認と本日のまとめ (約5分)**

---
## 4.1 平均値の罠と記述統計の基本 (約20分)

データを分析する際、最初にデータの全体像（代表値とばらつき）を把握します。これを**記述統計（Descriptive Statistics）**と呼びます。

### ① 代表値（平均値、中央値、最頻値）の使い分け
*   **平均値 (Mean)**：すべてのデータの合計を件数で割ったもの。最も一般的ですが、**「極端な値（外れ値）」にめちゃくちゃ引っ張られる**という最大の弱点があります。
*   **中央値 (Median)**：データを順番に並べたときに、ちょうど**真ん中にくる値**。外れ値の影響を受けません。
*   **最頻値 (Mode)**：データの中で**最も多く登場する値**（人気投票の1位のようなもの）。アンケートの回答などに使います。

**【大富豪来店！平均値の罠（イメージ）】**
```text
  [ カフェのお客さん5人の所持金 ]
   佐藤さん: 3000円  │
   鈴木さん: 5000円  │  この4人の「平均所持金」は、約 4,750円。 (納得のいく数字)
   田中さん: 6000円  │
   高橋さん: 5000円  │
      ＋
   💰【大富豪】ビル・ゲイツ氏：1兆円！
  
  [ 5人集まった結果 ]
   平均所持金 ──▶  約 2000億円！ (「このカフェの平均顧客はみんな超大金持ち！」となってしまい、現実と激しく乖離する)
   中央値     ──▶  5000円！ (ちょうど真ん中の佐藤・高橋さんの金額。これが現実の実態を正しく示している)
```

```python

<!-- コードセル -->
import pandas as pd
import numpy as np

# カフェのお客さんの滞在時間（分）のデータ
# 4人は普通にゆっくりしていますが、1人だけ「朝から夕方まで居座った人（480分）」が混ざっています（外れ値）。
stay_times = [30, 45, 60, 40, 480]

df_stay = pd.DataFrame(stay_times, columns=["滞在時間"])

# 平均値と中央値を計算して比較してみましょう！
print("平均値 (Mean):", df_stay["滞在時間"].mean(), "分")
print("中央値 (Median):", df_stay["滞在時間"].median(), "分")

# 平均だと「1人あたり131分」となり、みんな2時間以上いるように見えますが、
# 中央値の「45分」の方が、大部分のお客さんの実感値（代表値）に近いですね！
```

### ② データの「ばらつき」を表す「標準偏差（Standard Deviation）」
「平均滞在時間は45分です」と言われても、「全員が40〜50分の間に収まっている」のか、「5分で帰る人と90分いる人が半分ずつ」なのか分かりません。
この**「データのばらつき（散らばり具合）」を数値化したものが、標準偏差（Std）**です。
*   標準偏差が**小さい** ➡ みんな平均値の近くに集まっている（安定している）。
*   標準偏差が**大きい** ➡ データがバラバラに散らばっている。

---
## 4.2 クロス集計とピボットテーブル (約15分)

データの中に複数のカテゴリがある場合、それらを掛け合わせて集計すると「意外な組み合わせ」が見えてきます。これを**クロス集計**と呼びます。
Pandasでは、エクセルの機能でおなじみの **ピボットテーブル（`pivot_table`）** が使えます。

**【ピボットテーブルのイメージ】**
```text
  [ 生データ ] ──▶ [ ピボットテーブル集計 ] ──▶ [ 二次元のクロス表 ]
  顧客区分, 注文商品                              
  学生,   コーヒー                                     学生    社会人
  一般,   ケーキ                               コーヒー  12杯     5杯
  学生,   コーヒー                               ケーキ    2個    15個
  一般,   コーヒー                               (学生はコーヒー、社会人はケーキを好むと一目でわかる)
```

```python

<!-- コードセル -->
# カフェの顧客タイプ別の売上履歴データ
df_cafe_sales = pd.DataFrame([
    ["学生", "コーヒー", 450],
    ["一般", "コーヒー", 450],
    ["学生", "ケーキ", 500],
    ["一般", "ケーキ", 500],
    ["学生", "コーヒー", 450],
    ["一般", "ケーキ", 500],
    ["一般", "ケーキ", 500],
], columns=["顧客区分", "商品名", "売上"])

# ピボットテーブルを使って「顧客区分」を横（列）に、「商品名」を縦（行）にして、売上の「個数（count）」を集計します！
df_pivot = df_cafe_sales.pivot_table(
    values="売上",            # 集計したい数値の列
    index="商品名",           # 縦軸（行）に置く項目
    columns="顧客区分",       # 横軸（列）に置く項目
    aggfunc="count"           # 集計方法（今回はデータの個数を数える count。合計なら sum、平均なら mean）
)

print("▼ クロス集計（ピボットテーブル）の結果")
display(df_pivot)
```

---
## 4.3 相関分析：データ同士の関係性を数値化する (約15分)

データ分析の最もエキサイティングな瞬間、それは「2つのデータがどう関係しているか（相関）」を見つけるときです。

### ① 相関係数（Correlation Coefficient: $r$）
2つのデータの関係性を **`-1.0` 〜 `+1.0`** の間の数字で表したものです。
*   **正の相関（0 から 1.0）**：一方が増えると、もう一方も増える関係。
    *   *例*：気温が高くなると、アイスコーヒーの売上が増える（相関係数は +0.8 など強い正の相関）。
*   **負の相関（-1.0 から 0）**：一方が増えると、もう一方は減る関係。
    *   *例*：気温が高くなると、ホットコーヒーの売上が減る（相関係数は -0.7 など強い負の相関）。
*   **無相関（0付近）**：お互いに関係がない。
    *   *例*：店長の体重と、コーヒーの売上（相関係数は 0.05 など、無関係）。

**【相関係数（$r$）の基準値】**
*   `0.7 〜 1.0`（または `-0.7 〜 -1.0`）：**強い相関**がある。信頼できる強い関係。
*   `0.4 〜 0.7`（または `-0.4 〜 -0.7`）：**中程度の相関**がある。
*   `0.0 〜 0.2`：**ほとんど相関がない**。

```python

<!-- コードセル -->
# 「その日の最高気温」と「冷たい飲み物の売上数」「温かい飲み物の売上数」のデータ
df_weather_sales = pd.DataFrame([
    [15, 10, 80],
    [20, 25, 60],
    [25, 45, 40],
    [30, 70, 20],
    [35, 95, 5]
], columns=["最高気温", "冷たい飲料", "温かい飲料"])

# 相関行列（すべてのペアの相関係数を一瞬で計算する .corr()）
correlation_matrix = df_weather_sales.corr()

print("▼ 相関行列（自分自身との相関は 1.0 になります）")
display(correlation_matrix)

# 気温と冷たい飲料の相関係数は「0.99」➡ ほぼ完璧な「強い正の相関」
# 気温と温かい飲料の相関係数は「-0.99」➡ ほぼ完璧な「強い負の相関」であることがわかります！
```

### 🚨 注意！「相関関係」と「因果関係」は違う！
データ分析者が一生忘れてはならない超重要な鉄則があります。
**「相関関係があるからといって、原因と結果（因果関係）があるとは限らない」**

*   *有名な例*：「交番の数が多い地域ほど、犯罪発生数が多い」というデータがあります。
    *   これを「交番を建てたから、おまわりさんが犯罪を増やしたんだ！交番を壊そう！」と判断するのは間違いですよね。
    *   実際には「人口が非常に多い大都市だから、交番も多く、必然的に犯罪も多い」という**「人口」という隠れた原因（交絡因子）**が裏に潜んでいるだけです。

データ分析の数字を見る際は、常に「本当にこれが原因なのか？」を論理的に疑う癖をつけましょう。

---
## 💻 演習課題 (約45分)

さあ、今日学んだ「記述統計・代表値、ピボットテーブル、相関分析」を使いこなし、カフェのデータ分析レポートを作成しましょう！
講義と演習の時間は約半分ずつです。もし「エラーで相関が出ない（数値以外の文字が混ざっている）」等のトラブルが起きたら、第3回で学んだデータ加工（キャスト）を思い出すか、生成AIにエラーを相談してみましょう。

以下の `# TODO: ` の部分を書き換えて、プログラムを完成させてください。
```

```python
# 演習1: 平均値と中央値の算出と比較（難易度：易）
# カフェに設置した「ご意見箱」に寄せられた、お客様の満足度（5点満点）のデータです。
# ほとんどの人は満足(4点や5点)していますが、1人だけ「激怒したクレーマー(1点)」がいます。
# TODO: 満足度の「平均値」と「中央値」を計算して表示し、どちらが全体の「実感（代表値）」に近いか考えてください。

import pandas as pd

satisfaction_scores = [5, 4, 5, 4, 5, 1, 5, 4] # 1点という外れ値が混ざっている

df_satisfaction = pd.DataFrame(satisfaction_scores, columns=["満足度"])

# TODO: 平均値を計算
mean_score = 

# TODO: 中央値を計算
median_score = 

print("平均値:", mean_score)
print("中央値:", median_score)
```

```python
# 演習2: クロス集計による男女のメニュー好みの分析（難易度：易）
# 顧客の「性別（Gender）」と「注文メニュー（Menu）」のデータフレームがあります。
# TODO: pd.crosstab() を使って、性別ごとにどのメニューが何回注文されたかを集計する 
#       クロス集計表 df_cross を作成し、表示してください。
# ※ヒント： pd.crosstab(縦軸に置く列, 横軸に置く列)

df_customers = pd.DataFrame([
    ["女性", "パンケーキ"],
    ["男性", "コーヒー"],
    ["女性", "コーヒー"],
    ["女性", "パンケーキ"],
    ["男性", "コーヒー"],
    ["男性", "パンケーキ"],
    ["女性", "コーヒー"]
], columns=["性別", "メニュー"])

# TODO: 性別とメニューのクロス集計（度数分布表）を作成
df_cross = 

print("▼ 性別×メニューのクロス集計表")
display(df_cross)
```

```python
# 演習3: ピボットテーブルを使った店舗別売上平均の集計（難易度：中）
# 複数店舗の売上データがあります。
# TODO: df_sales.pivot_table() を使って、
#       縦軸（index）を「店舗名」、横軸（columns）を「商品カテゴリ」にし、
#       「売上」の【平均値（mean）】を集計するピボットテーブル df_sales_pivot を作ってください。

df_sales = pd.DataFrame([
    ["新宿店", "ドリンク", 450],
    ["新宿店", "フード", 800],
    ["渋谷店", "ドリンク", 500],
    ["渋谷店", "フード", 900],
    ["新宿店", "ドリンク", 400],
    ["渋谷店", "ドリンク", 480]
], columns=["店舗名", "商品カテゴリ", "売上"])

# TODO: pivot_tableを使って、店舗名×カテゴリの売上「平均（mean）」を集計する
df_sales_pivot = df_sales.pivot_table(
    values= ,
    index= ,
    columns= ,
    aggfunc= 
)

print("▼ 店舗別・カテゴリ別の売上平均ピボットテーブル")
display(df_sales_pivot)
```

```python
# 演習4: 売上相関行列の算出（難易度：中）
# 店舗の「客数（Customers）」「客単価（Avg_Spend）」「天気（Rainy_Days：雨の日の数）」「売上（Total_Sales）」の月次データです。
# TODO: .corr() メソッドを使い、すべての項目同士の相関係数を計算した 
#       相関行列 df_corr を作成して表示してください。

df_monthly_data = pd.DataFrame({
    "客数": ,
    "客単価": ,
    "雨の日数": ,
    "総売上": 
})

# TODO: 相関行列を計算
df_corr = 

print("▼ 月次売上データの相関行列")
display(df_corr)
```

```python
# 演習5: 実践！最も強い相関の特定と散布図可視化（難易度：難）
# 演習4で計算した相関行列（df_corr）を眺めると、ある項目同士に「強い正の相関」があることがわかります。
# TODO: 1. 相関行列の「客数」と「総売上」の相関係数を目視で確認し、以下の print の空欄に数値を書いてください。
# TODO: 2. matplotlib.pyplot (plt) の plt.scatter() を使い、
#          X軸に「客数」、Y軸に「総売上」を指定した散布図を描画してください。
#          （タイトル「客数と総売上の関係」をつけ、plt.show() で画面に表示すること）

import matplotlib.pyplot as plt
import japanize_matplotlib # 日本語表示用

# TODO: 1. 相関係数の値を手動（またはコード）で確認し、表示してください。
# ※「客数」と「総売上」の相関係数を確認して、書き換えてみてください。
print("客数と総売上の相関係数は約 [ TODO: ここに数値を書く ] です。")

# TODO: 2. 散布図を描く
# X軸に df_monthly_data["客数"]、Y軸に df_monthly_data["総売上"] を指定して散布図を描く
plt.

# タイトルと軸ラベルの設定
plt.title("客数と総売上の関係（散布図）")
plt.xlabel("客数（人）")
plt.ylabel("総売上（円）")

# 表示
plt.show()
# 右上がりのきれいな直線状の点々が描画できれば大正解です！
```

---
## 💡 演習問題の解答例

（自分で苦労してコードを書き、エラーを乗り越えた経験こそが、あなたの「データ分析のセンス」を劇的に向上させます。答え合わせをして、理解を深めましょう！）

<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

**演習1の解答**
```python
mean_score = df_satisfaction["満足度"].mean()
median_score = df_satisfaction["満足度"].median()
# 平均値: 4.125, 中央値: 4.5
# 1人だけのクレーマー(1点)がいるため平均は4.1点に下がっていますが、
# 中央値は4.5点となり、大半のお客様が満足（4点か5点）している実態をよく表しています。
```

**演習2の解答**
```python
df_cross = pd.crosstab(df_customers["性別"], df_customers["メニュー"])
```

**演習3の解答**
```python
df_sales_pivot = df_sales.pivot_table(
    values="売上",
    index="店舗名",
    columns="商品カテゴリ",
    aggfunc="mean" # 平均値を算出します
)
```

**演習4の解答**
```python
df_corr = df_monthly_data.corr()
```

**演習5の解答**
```python
# 1. 相関係数の確認（演習4の実行結果を見ると、客数と総売上の交点は 0.985... となっています）
print("客数と総売上の相関係数は約 0.99 です。") # ほぼ完璧な強い正の相関！

# 2. 散布図の描画
plt.scatter(df_monthly_data["客数"], df_monthly_data["総売上"], color="purple")
```

</details>

---
## 📚 第4回のまとめ

お疲れ様でした！今日は、ただのデータ加工から、データの裏に眠るストーリー（真実）を暴き出す「データ解析」の基礎を学びました。

### ✨ 今日の重要ポイントまとめ
*   **記述統計**: 平均値は外れ値（大富豪）に弱いため、実態を正しく掴むには**中央値**も併せて確認する。データのばらつきは**標準偏差**で測る。
*   **クロス集計 (crosstab / pivot_table)**: カテゴリ同士を掛け合わせる（例：性別×好み）ことで、特定の層に人気のある商品や、店舗ごとの得意・不得意の傾向をあぶり出せる。
*   **相関分析 (.corr)**: 2つのデータの一方が増えればもう一方も増える（正の相関）、あるいは減る（負の相関）という関係を `-1.0 〜 +1.0` の数値で表す。
*   **相関 vs 因果の鉄則**: 「相関があるからといって、因果関係（原因と結果）があるとは限らない」を常に意識する。

### ❓ よくある質問と回答 (FAQ)
*   **Q. 相関係数が「0.3」でした。これは関係がないということですか？**
    *   **A.** 統計学的には「非常に弱い相関（あるいはほとんど無相関）」と判断されることが多いです。ただし、「本当は2つのデータに関係があるのに、データ件数が少なすぎるために、たまたま低い相関係数になってしまっている（サンプルサイズの問題）」ということもあります。分析をする際は、データ件数（サンプル数）が十分に足りているかも意識しましょう。
*   **Q. `df.corr()` を実行したらエラー（`ValueError: could not convert string to float`）が出ました。**
    *   **A.** 相関係数は「数値データ」同士でしか計算できません。データフレームの中に「店舗名」や「日付」などの文字列（テキスト）の列が含まれていませんか？最新のPandasでは、自動的に数値列だけを選んで計算してくれますが、古いバージョンではエラーになるため、`df[["客数", "総売上"]].corr()` のように、**あらかじめ数値列だけを絞り込んでから `.corr()` を呼ぶ**とエラーを確実に回避できます。

### 🌟 Colabで試すおすすめコード
ピボットテーブルと並ぶPandasの超強力なクロス集計メソッド **`pd.crosstab`** に `normalize="index"` を指定することで、人数ではなく「割合（％）」で男女の好みの差を一撃で比較するプロのテクニックです。
```python

<!-- コードセル -->
# 100人のお客様のアンケート（ダミーデータ）
df_survey_large = pd.DataFrame({
    "年代": ["20代", "30代", "20代", "40代", "30代", "20代", "40代", "20代"] * 10,
    "利用満足度": ["満足", "不満", "満足", "普通", "満足", "不満", "普通", "満足"] * 10
})

# normalize="index" を指定すると、各年代ごとに合計が 1.0 (100%) になる割合で集計してくれます！
ratio_crosstab = pd.crosstab(df_survey_large["年代"], df_survey_large["利用満足度"], normalize="index")

print("▼ 年代別の満足度割合（%）")
display(ratio_crosstab * 100) # 100を掛けて％表記にします
```
```markdown
<!-- Markdownセル -->
年代別の満足度割合がきれいに％で算出されたでしょうか？これで「どの年代が最も不満を抱えているか」が割合で一目でわかり、具体的な対策が立てられます。

次回（第5回）は、今回学んだ統計量や相関の概念をさらに一歩進め、データから未来を予測する**「相関・回帰・重回帰分析（AI・機械学習の入り口）」**に突入します。
いよいよデータ分析が「予測の科学」に変わっていきますよ。次回もどうぞお楽しみに！
```
