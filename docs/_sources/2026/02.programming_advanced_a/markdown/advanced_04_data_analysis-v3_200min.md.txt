<!-- Markdownセル -->
# 第4回 データ解析の基礎とデータ評価（200分集中講義用・完全版）

皆さん、こんにちは！プログラミング応用A（コンピューティング応用G）、第4回の講義です。担当の教員です。
前回の第3回では「データの種類（尺度）と高度なデータ加工」を学びました。名義・順序・間隔・比例という4つの尺度を見極め、バラバラの表を連結（`pd.concat`）したり結合（`pd.merge`）したり、日付データ（`datetime`）から曜日を取り出すといった、データ分析の「土台」となる高度な前処理（データエンジニアリング）を習得しました。

今日からはいよいよ、綺麗にお掃除されたデータを使って、データの裏側に隠された「真実」をあぶり出し、客観的な評価を下す**「データ解析とデータ評価（Data Analysis & Evaluation）」**の本番に突入します！

本日は、2コマ連続（計200分）の集中講義枠を活用し、統計学の基礎理論、Pandasによる多角的な集計、そしてデータ解析の現場で極めて重要視される**「定量的評価（Quantitative Evaluation）」と「定性的評価（Qualitative Evaluation）」の両面アプローチ**を深く学びます。

教材として、データサイエンスの世界で非常に有名で愛されている**「南極半島に生息するペンギンの体格データ（2007年〜2009年調査）」**をフルに活用します。
ただ数字を機械的に計算する（定量的）だけでなく、なぜ欠損値があるのか、なぜこの生息地なのかといった背景（定性的）にまで想いを馳せることで、AI時代に本当に必要とされる「データを見極める審美眼（データリテラシー）」を体得していきましょう！

---
## ⏱️ 本日の200分スケジュール（タイムテーブル）

2コマ連結講義（計200分）を、学生が集中力を途切れさせず、深い理論納得と実践的なデバッグ力を身につけられるよう設計したタイムテーブルです。
※授業外での評価・宿題用である「総合演習課題5問（評価基準付き）」は、講義の200分枠内には含めず、時間外の課題として切り離しています。

### 【前半：記述統計とデータ評価の基本アプローチ（100分）】
*   **00:00 - 00:15 (15分) 導入 ＆ 本日の目標確認**
    *   本日の目標と、データ解析における「評価」の重要性について。
*   **00:15 - 00:55 (40分) 4.1 記述統計の基本：平均値の罠とデータのばらつき（標準偏差・外れ値）**
    *   ビル・ゲイツ氏の来店で破綻する平均値、中央値（メディアン）の重要性。
    *   数式なしで直感的に理解する「標準偏差」の概念と、Pandasでの計算。
*   **00:55 - 01:35 (40分) 4.2 データ評価の新概念：定量的評価と定性的評価 ＆ 南極ペンギンデータの導入**
    *   数値で語る「定量的評価」と、背景やコンテキストを探る「定性的評価」の組み合わせ。
    *   南極ペンギン調査データ（Palmer Penguins：2007〜2009年）のインポートとデータ構造確認。
*   **01:35 - 01:45 (10分) 前半のまとめ ＆ 10分間の中間小休止**

### 【後半：データの切り分け・関係性分析とペンギンデータ評価の実践（100分）】
*   **01:45 - 02:15 (30分) 4.3 データを多角的に切り分ける：クロス集計とピボットテーブル**
    *   `pd.crosstab` によるカテゴリ度数の割合。
    *   `pd.pivot_table` による複数グループの平均・合計集計。
*   **02:15 - 02:45 (30分) 4.4 相関分析と疑似相関の罠、データの罠（シンプソンのパラドックス）**
    *   相関係数 \\(r\\) （-1.0〜+1.0）の読み解き。
    *   疑似相関（交番と犯罪）および、全体を合算すると関係が逆転する「シンプソンのパラドックス」の解説。
*   **02:45 - 03:10 (25分) 4.5 ペンギンデータを用いた「定量的・定性的データ評価」の実践ハンズオン**
    *   フリッパーの長さと体重の極めて強い正の相関、欠損値（NaN）が発生した現場コンテキストの定性考察。
*   **03:10 - 03:25 (15分) 4.6 授業内確認練習問題（ミニクイズ5問）**
    *   穴埋め・選択式の理論問題。各自で解いた後、教員が解説。
*   **03:25 - 03:30 (5分) まとめと次回（第5回：StreamlitによるWebアプリ開発の基礎）への接続**

---
## 🎯 本日の到達目標
この200分の授業が終わるころには、以下のことができるようになります。
1. **「平均値の罠」**を理解し、データに極端な値（外れ値）が含まれる場合に「中央値」を正しく選択・算出できる。
2. データのばらつき度合いを示す**「標準偏差（Standard Deviation）」**の直感的な意味を説明でき、Pandasで算出できる。
3. 位置情報や属性の背景にアプローチする**「定性的評価」**と、統計量で裏付ける**「定量的評価」**の2つのアプローチを組み合わせたデータ評価ができる。
4. 2つのグループの属性を掛け合わせた**「クロス集計（`pd.crosstab`）」**や、複数条件をマトリクス表で集計する**「ピボットテーブル（`pd.pivot_table`）」**を自在に作成できる。
5. 2つのデータの関係性の強さを表す**「相関係数（\\(r\\)）」**を正しく解釈し、データ分析最大のタブーである**「疑似相関」**や**「シンプソンのパラドックス」**の罠を見破ることができる。

---
## 4.1 記述統計の基本：平均値の「甘い罠」とデータの「ばらつき」 (約40分)

データを入手した際、まず最初に行うのが「データ全体のざっくりとした特徴（要約統計量）」を計算することです。これを統計学では**「記述統計（Descriptive Statistics）」**と呼びます。
しかし、ここで「とりあえず平均値を出しておけば大丈夫」と考えていると、データに騙されることになります。

### 4.1.1 平均客単価の罠：ビル・ゲイツ氏が来店した日
例えば、あなたが江古田でオシャレな学生カフェを経営しているとします。
ある日、来店したお客様10人の「購入金額」は、普段は 300円〜500円 の範囲でした。しかし、そのうちの1人がたまたま超大富豪（ビル・ゲイツ氏）で、お店に飾ってあった2億円のオブジェをキャッシュで購入していきました。

この日の客単価データは以下のようになります。
`[350円, 400円, 380円, 450円, 300円, 420円, 390円, 360円, 410円, 200,000,000円（ゲイツ氏）]`

このデータをPandasに入力し、「平均値（Mean）」と「中央値（Median）」を計算して比較してみましょう。

```python
# ------------------------------------------------------------------
# コードセル：平均値と中央値の比較
# ------------------------------------------------------------------
import pandas as pd

# 10人のお客様の購入金額データ
sales_data = pd.Series([350, 400, 380, 450, 300, 420, 390, 360, 410, 200000000])

# 平均値（Mean）の計算
mean_val = sales_data.mean()

# 中央値（Median）の計算
median_val = sales_data.median()

print(f"💰 平均客単価: {mean_val:,.1f} 円 (えっ！？全員が2000万円も使ったの！？)")
print(f"🎯 中央客単価: {median_val:,.1f} 円 (なるほど、普段通り約395円だね！)")
```

**【解説】**
*   **平均値（`.mean()`）**は、「全員の合計金額を人数で割る」という計算をするため、1件でも異常に巨大な値（**外れ値：Outlier**）が混ざると、強烈にその数値に引っ張られてしまい、実態を全く表さなくなります。
*   **中央値（`.median()`）**は、データを小さい順に並べ替えたときに**「ちょうど真ん中の順位（50%目）にくる値」**です。どれほど極端な外れ値が混ざっていても、順位自体は1つしかズレないため、外れ値の影響を完全にシャットアウトし、「普段の一般的なお客様の姿」をきれいに映し出すことができます。

> **⚠️ データリテラシー**:
> ニュースで「日本の平均年収は450万円です」と言われた際、多くの人が「自分は平均以下だ…」と落ち込みます。しかし、実際は一部の超高所得層（大企業の社長やスポーツ選手など）が平均値を強烈に引き上げています。実態に近い年収（日本人のちょうど真ん中の人の年収＝中央値）を調べると、約360万円〜380万円付近になり、平均値より約100万円近く低くなります。データを見るときは、常に「平均値の罠」を疑い、中央値も同時に確認する癖をつけましょう！

---

### 4.1.2 データのばらつきを測る「標準偏差（Standard Deviation）」
平均値や中央値といった「真ん中の値」が分かったら、次に知りたいのは**「データはどのくらい散らばっているのか？（個人差が大きいのか、全員同じくらいなのか）」**という情報です。

ここに、1日の総売上高がどちらも「ぴったり30万円」である2つの店舗があります。

*   **A店（安定型）**：来店するお客様全員が、ほぼ均一に「300円〜400円」のコーヒーとパンを頼んでいく。
*   **B店（波乱万丈型）**：100円のガム1個だけを買う極端な学生もいれば、一度に2万円分のギフト用コーヒー豆をまとめ買いする近所のマダムもいる。

この2つの店の「個人差（ばらつき）」を数値化したものが**「標準偏差（Standard Deviation）」**です。
数式は難しそうに見えますが、直感的な意味は極めてシンプルです。

> **💡 標準偏差の直感的な定義**：
> **「全員のデータが、平均値から平均して『どれくらいの距離（金額）』離れているかを表す指標」**

**【標準偏差（ばらつき）のビジュアル図解】**
```text
  【 A店 (標準偏差が小さい) 】             【 B店 (標準偏差が大きい) 】
      
       |      ●●●                      |   ●
       |    ●●●●●                     |   ●      ●       ●
  ─────┼──────●───────▶             ─────┼──────●───────▶
       |    (平均350円)                |    (平均350円)
    (全員が平均のすぐ近くにいる)            (平均から遠く離れた人が多い)
```

Pandasでこの「ばらつき」を計算してみましょう。

```python
# ------------------------------------------------------------------
# コードセル：標準偏差（ばらつき）の計算
# ------------------------------------------------------------------
# A店とB店の顧客ごとの購入額データ（各10人）
shop_A = pd.Series([350, 360, 340, 350, 370, 330, 350, 360, 340, 350]) # 平均350円
shop_B = pd.Series([100, 500,  50, 800, 150, 100, 350,  50, 400, 1000]) # 平均350円

print(f"🏪 A店の平均値: {shop_A.mean():.1f} 円 / 標準偏差(std): {shop_A.std():.1f}")
print(f"🏪 B店の平均値: {shop_B.mean():.1f} 円 / 標準偏差(std): {shop_B.std():.1f}")
```

**【解説】**
*   **A店**の標準偏差は **11.5** です。これは「顧客全員の金額が、平均350円からプラスマイナス11.5円程度の非常に狭い範囲にギュッと集まっている（ばらつきが極めて小さい）」ことを示しています。
*   **B店**の標準偏差は **333.3** です。これは「顧客全員 of 金額が、平均350円から平均して約333円も遠く離れた、非常に激しいバラバラな状態にある（ばらつきが極めて大きい）」ことを意味します。
*   ビジネスにおいて、標準偏差を知ることは「仕入れや在庫の安定性」を予測する上で、平均値と同じくらい極めて重要な意味を持ちます。

---
## 4.2 データ評価の新概念：「定量的評価」と「定性的評価」の融合 (約40分)

データ分析から実社会に役立つ知見（意思決定）を導き出すとき、私たちは**「データ評価（Data Evaluation）」**というステップを踏みます。
この評価には、全く異なる2つのアプローチが存在します。

```text
       【 📊 データ評価の2大アプローチ 】
        ├── ① 定量的評価 (Quantitative): 「数値」で測る。客観的、全体像の把握。
        └── ② 定性的評価 (Qualitative): 「背景・文脈」を探る。主観、理由・動機の探索。
```

### 4.2.1 定量的評価（Quantitative Evaluation）
*   **特徴**：すべてのデータを「数値（数量）」に変換し、統計量やグラフを用いて客観的に評価します。
*   **メリット**：主観を排除し、誰が見ても同じ結論に至ります。大規模なデータの「全体像」を捉えるのが大得意です。
*   **よく使う道具**：平均値、中央値、標準偏差、相関係数、売上高、パーセント比率など。
*   **限界**：「なぜその数値になったのか」という、人間の感情や偶発的な現場のトラブルなどの**「裏の理由」**を説明することはできません。

### 4.2.2 定性的評価（Qualitative Evaluation）
*   **特徴**：数値にできない「言語、状況、背景、感情、歴史的コンテキスト」に注目し、深く評価します。
*   **メリット**：「なぜそうなったのか」という根本的な原因やプロセス、データの収集過程における現場の制約などを解き明かします。
*   **よく使う道具**：インタビュー、行動観察、歴史的背景の調査、エラーや欠損データの発生原因の推測など。
*   **限界**：客観的な比較や、大規模なデータ全体に対して「本当にそれが代表的な意見か」を一般化することが難しく、評価者の主観が入りやすいです。

---

### 4.2.3 実践データの導入：「南極パルマー半島のペンギン調査データ」
この「定量的」「定性的」な2つのアプローチを融合させて評価する極上のお手本として、2007年〜2009年にかけて南極半島で観測された**「ペンギンの体格調査データ（Palmer Penguins）」**を導入します。

このデータは、アデリー（Adelie）、ヒゲ（Chinstrap）、ジェンツー（Gentoo）という3種類のペンギンの体格を測定した、世界中のデータサイエンス教材で絶大な人気を誇る実データです。

まずはGoogle Colab上にデータをインポートし、データ構造を定量的・定性的に確認しましょう。
※ネットワーク環境が不安定な場合を考慮し、自動でバックアップデータを作成する「セーフティネットコード」を搭載しています。

```python
# ------------------------------------------------------------------
# コードセル：ペンギンデータのインポートとお掃除
# ------------------------------------------------------------------
import pandas as pd
import numpy as np

# インターネットからパルマーペンギンデータを読み込みます
try:
    url = "https://raw.githubusercontent.com/mwaskom/seaborn-data/master/penguins.csv"
    df_penguins = pd.read_csv(url)
    print("🐧 オンラインのSeabornリポジトリからペンギンデータを読み込みました！")
except Exception as e:
    # 読み込めない場合は、Colabローカルで本物とほぼ同じダミーデータをその場で構築します
    print("⚠️ 接続エラーのため、ローカルで模擬ペンギンデータを生成します。")
    np.random.seed(42)
    n = 344
    species_choice = np.random.choice(["Adelie", "Chinstrap", "Gentoo"], n, p=[0.44, 0.20, 0.36])
    island_choice = np.where(species_choice == "Adelie", 
                             np.random.choice(["Torgersen", "Biscoe", "Dream"], n),
                             np.where(species_choice == "Gentoo", "Biscoe", "Dream"))
    
    bill_len = np.where(species_choice == "Adelie", np.random.normal(38.8, 2.6, n),
               np.where(species_choice == "Chinstrap", np.random.normal(48.8, 3.3, n), np.random.normal(47.5, 3.1, n)))
    bill_depth = np.where(species_choice == "Adelie", np.random.normal(18.3, 1.2, n),
                 np.where(species_choice == "Chinstrap", np.random.normal(18.4, 1.1, n), np.random.normal(15.0, 1.0, n)))
    flipper_len = np.where(species_choice == "Adelie", np.random.normal(190, 6.5, n),
                  np.where(species_choice == "Chinstrap", np.random.normal(195, 7.1, n), np.random.normal(217, 6.5, n)))
    mass = np.where(species_choice == "Adelie", np.random.normal(3700, 450, n),
           np.where(species_choice == "Chinstrap", np.random.normal(3730, 380, n), np.random.normal(5070, 500, n)))
    
    df_penguins = pd.DataFrame({
        "species": species_choice,
        "island": island_choice,
        "bill_length_mm": np.round(bill_len, 1),
        "bill_depth_mm": np.round(bill_depth, 1),
        "flipper_length_mm": np.round(flipper_len, 0),
        "body_mass_g": np.round(mass, 0),
        "sex": np.random.choice(["MALE", "FEMALE", np.nan], n, p=[0.48, 0.48, 0.04])
    })
    # 意図的にいくつかの測定値を欠損（NaN）にする
    for col in ["bill_length_mm", "bill_depth_mm", "flipper_length_mm", "body_mass_g"]:
        df_penguins.loc[df_penguins.sample(frac=0.01).index, col] = np.nan

# データの基本情報の表示
print(f"データの行数: {df_penguins.shape[0]}行, 列数: {df_penguins.shape[1]}列")
display(df_penguins.head(5))
```

---

### 4.2.4 ペンギンデータの定性的評価：欠損値「NaN」に隠された現場の真実
このデータフレームを眺めると、いくつかのデータが **`NaN`（欠損値：データが入っていない状態）** になっていることが分かります。

ここで、**「定性的評価」**の出番です。
単に「データが欠損しているからプログラムで削除（クレンジング）しよう」と機械的に処理する前に、**「なぜ2007年〜2009年の南極の現場で、このデータが欠損したのか？」という背景を定性的に想像・考察**してみましょう。

**【欠損が発生した南極の調査現場の定性的考察（仮説）】**
1.  **野生ペンギンが暴れた可能性**：
    ペンギンの「体重（body_mass_g）」を量るには、ペンギンをネット（網）に入れて吊り下げる必要があります。その際、野生のペンギン（特に大型のジェンツーペンギンなど）が激しく暴れて暴動状態になり、安全のために測定を断念したのかもしれません。
2.  **極寒と吹雪による気象条件の悪化**：
    測定が行われたパルマー半島は、時に強烈なブリザード（吹雪）が吹き荒れます。気温がマイナス数十度になり、測定用の電子機器がフリーズしてしまったり、研究者の指がかじかんでノギス（くちばしの長さを測る道具）が動かせなくなり、泣く泣く測定をスキップしてテントに避難した光景が目に浮かびます。
3.  **「性別（sex）」の判定の難しさ**：
    ペンギンは外見からオスとメスを区別することが極めて困難な動物です（外見的な性差がほとんどありません）。性別を確定させるためには、血液を採取してDNA検査を行う必要がありますが、すべてのペンギンから血液を採取できる時間や医療資材の余裕が現場になかったため、「性別：欠損（NaN）」として記録せざるを得なかった可能性があります。

このように、データが生まれた「背景（定性的な物語）」に目を向けることで、欠損値はただのゴミではなく、**「過酷な南極で命がけで調査した研究者たちの戦いの歴史」**として見えてくるようになります。

---
## 4.3 クロス集計とピボットテーブル：データをグループで評価する (約30分)

それでは、データを属性（ペンギンの「種別」や「生息する島」）ごとに細かく切り分けて評価していきましょう。

### 4.3.1 `pd.crosstab` による生息地の分布評価
どの島（island）に、どの種別（species）のペンギンが何羽生息しているかをクロス集計してみましょう。

```python
# ------------------------------------------------------------------
# コードセル：島と種別のクロス集計（度数）
# ------------------------------------------------------------------
# 島（行）と種別（列）のクロス集計
island_species = pd.crosstab(df_penguins["island"], df_penguins["species"])

print("▼ 島別 ✕ ペンギン種別 の生息羽数（クロス集計）")
display(island_species)
```

**【解説と定性的評価への接続】**
クロス集計を実行すると、非常に面白い「定性的な事実」が浮かび上がります。
*   **アデリーペンギン（Adelie）**：Torgersen, Biscoe, Dream という**3つの島すべて**に広く均等に分布して生息しています。
*   **ヒゲペンギン（Chinstrap）**：**Dream島**にしか生息していません。
*   **ジェンツーペンギン（Gentoo）**：**Biscoe島**にしか生息していません。

ここから、定性的な仮説として「アデリーペンギンは環境適応力や食性が広く、どこでも生きていけるタフな性質があるが、ジェンツーペンギンやヒゲペンギンは、その島にしかない特殊なエサ（特定のオキアミ）や地形（巣作りに必要な石の種類や傾斜）に依存した繊細な生態を持っているのではないか？」という評価を導くことができます。

---

### 4.3.2 `pd.pivot_table` による体格の定量的評価
次に、ピボットテーブルを使って「種別ごと・性別ごとの平均体重（body_mass_g）」をマトリクス集計してみましょう。

```python
# ------------------------------------------------------------------
# コードセル：ピボットテーブルによる平均体重集計
# ------------------------------------------------------------------
# 縦軸に「species（種別）」、横軸に「sex（性別）」、値に「body_mass_g（体重）」の平均を配置
pivot_mass = pd.pivot_table(
    df_penguins,
    values="body_mass_g",
    index="species",
    columns="sex",
    aggfunc="mean"
)

print("▼ ペンギン種別 ✕ 性別 の平均体重（グラム）ピボットテーブル")
display(pivot_mass)
```

**【解説と定量的評価の結論】**
*   すべての種において、**「メス（FEMALE）よりもオス（MALE）の方が一回り体重が重い（体格が大きい）」**ということが、具体的な数値として客観的に証明（定量的評価）されました。
*   また、種別ごとに見ると、アデリー（約3,700g）とヒゲ（約3,730g）はほぼ同じ体格ですが、**ジェンツーペンギン（Gentoo）はオス・メスともに5,000g（5kg）を超える圧倒的な大型種**であることがはっきりと分かります。

---
## 4.4 相関分析とデータの「甘い罠」 (約30分)

ペンギンの体格データ（くちばしの長さ、深さ、フリッパー（翼）の長さ、体重）には、お互いにどのような関係性（相関）があるでしょうか？
Pandasの `.corr()` を使って、相関係数マトリクス（相関行列）を算出してみましょう。

```python
# ------------------------------------------------------------------
# コードセル：ペンギンデータの相関行列
# ------------------------------------------------------------------
# 数値列だけを選択して相関行列を計算
corr_matrix = df_penguins.corr(numeric_only=True)

print("▼ ペンギン体格データの相関係数マトリクス")
display(corr_matrix.style.background_gradient(cmap="bwr", vmin=-1, vmax=1))
```

**【定量的評価の解釈】**
*   **「フリッパーの長さ（flipper_length_mm）」と「体重（body_mass_g）」の相関係数は約 0.87**。極めて強い正の相関関係があります。「翼（フリッパー）が長いペンギンほど、体重も重い（体格が比例して大きい）」という、生物学的な法則がクリアな数値として評価できました。

---

### ⚠️ 【超重要】データ分析の高度な罠：シンプソンのパラドックス
相関行列を注意深く見ると、不思議な数値に気づきます。
*   **「くくちばしの長さ（bill_length_mm）」と「くちばしの深さ（bill_depth_mm）」の相関係数が、約 -0.23（弱い負の相関）**になっているのです。

「くちばしが長いペンギンほど、くちばしが薄く（浅く）なる」ということでしょうか？直感的には、体格が大きくなれば、くくちばしは「長く、かつ分厚く（深く）」なるはずです。

ここに、データ分析者全員が気をつけなければならない大罠**「シンプソンのパラドックス（Simpson's Paradox）」**が潜んでいます。

> **💡 シンプソンのパラドックスとは**：
> **「全体のデータをまとめて相関を計算したときの関係性が、データをグループ（種別など）に細かく切り分けて計算したときに、真逆の関係性に反転してしまう現象」**

百聞は一見に如かず。ペンギンの「種別（species）」ごとにグループ化して、種ごとの相関係数を再計算してみましょう！

```python
# ------------------------------------------------------------------
# コードセル：種別ごとの相関関係の計算
# ------------------------------------------------------------------
print("--- 種別を分けない全体での相関 ---")
overall_corr = df_penguins["bill_length_mm"].corr(df_penguins["bill_depth_mm"])
print(f"全体での『長さ』と『深さ』の相関係数: {overall_corr:.3f} (負の相関！？)\n")

print("--- ペンギンの『種別ごと』に切り分けた相関 ---")
for spec in df_penguins["species"].unique():
    # 各種ごとにデータを絞り込んで相関を計算
    sub_df = df_penguins[df_penguins["species"] == spec]
    spec_corr = sub_df["bill_length_mm"].corr(sub_df["bill_depth_mm"])
    print(f"🐧 {spec}種内での『長さ』と『深さ』の相関係数: {spec_corr:.3f} (ええっ！？強い正の相関に大反転！)")
```

**【解説とデータ評価としての教訓（定量的 ✕ 定性的）】**
*   全体のデータをまとめて計算した定量的評価では、**「-0.23（負の相関）」**に見えた関係が、種別という定性的なカテゴリでデータを切り分けて再集計すると、それぞれの種内では **「+0.5 〜 +0.7（強い正の相関＝くちばしが長くなれば、深さも分厚くなる）」という真逆の関係**に大反転しました！
*   なぜこんなことが起きるかというと、アデリーペンギンは「くちばしが短くて分厚い」、ジェンツーペンギンは「くちばしが長くて薄い」という、種ごとの「生物学的な特徴（定性情報）」が、全体の平均位置をズラしてしまっていたからです。
*   全体をまとめて「定量的」に計算しただけの数値がいかに嘘をつきやすいか、そして「種別ごとの特徴」という「定性的」な視点を持ってデータを切り分けて精緻に再評価することがいかに大切かを教えてくれる、極上のお手本です。

---
## 4.5 ペンギンデータを用いた「定量的・定性的データ評価」の実践ハンズオン (約25分)

それでは、本日の学びを1つのストーリーとして完成させましょう。
以下のコードセルを実行し、「フリッパーの長さ」と「体重」の散布図を描画します。今回は、全体を一色で描くのではなく、**「種別（species）」ごとに色を塗り分けて**描画してみましょう！

```python
# ------------------------------------------------------------------
# コードセル：種別ごとに色分けした美しい体格散布図の描画
# ------------------------------------------------------------------
import matplotlib.pyplot as plt
import japanize_matplotlib # 日本語文字化け対策

plt.figure(figsize=(10, 6))

# ペンギンの種別ごとに色と名前を分けてプロットします
species_colors = {"Adelie": "orange", "Chinstrap": "purple", "Gentoo": "blue"}
species_labels = {"Adelie": "アデリー (Adelie)", "Chinstrap": "ヒゲ (Chinstrap)", "Gentoo": "ジェンツー (Gentoo)"}

for spec, color in species_colors.items():
    # その種だけのデータを取り出す
    sub_df = df_penguins[df_penguins["species"] == spec]
    
    # 散布図のプロット (X軸: フリッパーの長さ, Y軸: 体重)
    plt.scatter(
        sub_df["flipper_length_mm"], 
        sub_df["body_mass_g"], 
        color=color, 
        label=species_labels[spec], 
        alpha=0.7, 
        edgecolors="white"
    )

# グラフの装飾
plt.title("南極ペンギンの『フリッパーの長さ』と『体重』の相関散布図")
plt.xlabel("フリッパー（翼）の長さ (mm)")
plt.ylabel("体重 (g)")
plt.legend(title="ペンギンの種類")
plt.grid(True, linestyle=":", alpha=0.6)

# 画面に表示
plt.show()
```

**【このグラフを用いた総合的なデータ評価（定量 ✕ 定性）】**

1.  **定量的評価の結論**：
    *   グラフは全体として美しい「右上がり」の形状をなしており、3種すべてにおいて「翼が長い個体ほど、体重が重い（大型である）」という関係が視覚的にも、相関係数（0.87）としても実証されました。
    *   特に青色の**ジェンツーペンギン**は、他の2種と比べて「フリッパーの長さ（210mm以上）」も「体重（4,500g以上）」も、重なりが全くないほどに明確に巨大であることが一目で定量評価できます。
2.  **定性的評価の結論（生物学的背景・生存戦略の考察）**：
    *   なぜジェンツーペンギンだけがこれほど巨大に進化したのでしょうか？
        定性的な調査によると、ジェンツーペンギンは、他の2種よりも**「より深く、遠い海まで潜水して魚を捕獲する」**という生態を持っています。冷たい海の深海まで潜るためには、体温を保つための「大きな体（厚い皮下脂肪）」と、強い水圧に負けずに深海まで一気に推進力を生み出す「長く強力な翼（フリッパー）」が必要不可欠だった、という、過酷な南極海を生き抜くための**美しい生存戦略（ストーリー）**が見えてきます。

このように、**「定量的（数字とグラフで客観的事実を固める）」**と**「定性的（生物学的な背景や生存戦略というストーリーで理由を説明する）」**の2つの車輪を回すことで、初めてデータの裏にある「真の評価」に到達することができます。

---
## 4.6 授業内確認練習問題（理論・記述：5問） (約15分)

本日の講義（200分）の理解度を確かめるための確認クイズです。全員で一緒に考え、理解を定着させましょう！

### 練習問題 1: データのばらつき度合いの読み取り
データAとデータBの記述統計を計算したところ、平均値はどちらも同じ「100」でしたが、標準偏差はデータAが「5」、データBが「35」でした。このデータの「ばらつき具合」として正しい解釈を説明してください。

<details>
<summary>👉 練習問題 1 の解答と解説を見る</summary>

*   **正解**: **データBの方が、データAに比べて数値のばらつき（個人差）が極めて大きい。**
*   **解説**:\\n    *   標準偏差は「平均値からの平均的な距離（離れ具合）」を表します。
    *   データAは標準偏差が「5」と小さいため、ほとんどのデータが「95〜105」の平均値のすぐ近くに固まっています。
    *   データBは標準偏差が「35」と大きいため、平均値から遠く離れた小さな値や大きな値がバラバラに散らばっていることを示します。

</details>

---

### 練習問題 2: クロス集計とピボットテーブルの使い分け
Pandasにおいて、「店舗別の会員100人の性別比率（人数割合）」を出したい時と、「店舗別・曜日別の売上金額の平均値」を出したい時、それぞれ最も適したPandasのメソッド（関数）の名称を答えてください。

<details>
<summary>👉 練習問題 2 の解答と解説を見る</summary>

*   **正解**:
    *   店舗別の性別人数比率：**`pd.crosstab()`** (度数・割合の集計に最適)
    *   店舗別・曜日別の売上金額平均：**`pd.pivot_table()`** (任意の数値列の合計・平均のマトリクス集計に最適)
*   **解説**:\\n    *   `crosstab` は件数を数えたりパーセント割合を出すのが得意です。
    *   `pivot_table` はExcelのピボットと同じく、売上や客数といった特定の「数値列」に対して合計や平均などの複雑な計算（aggfunc）を縦横の表で処理するのが得意です。

</details>

---

### 練習問題 3: 相関係数 \\(r = -0.85\\) の解釈
あるアイスクリーム店で「1日の最低気温（X）」と「おでんの売上（Y）」の相関係数を算出したところ、\\(r = -0.85\\) でした。この2つのデータの間にはどのような関係があると言えるでしょうか？最も適切なものを選んでください。

1.  気温が上がると、おでんの売上も強烈に上昇する関係（強い正の相関）。
2.  気温と、おでんの売上には全く何の関係も見られない（無相関）。
3.  気温が上がると、おでんの売上が強烈に低下する関係（強い負の相関）。

<details>
<summary>👉 練習問題 3 の解答と解説を見る</summary>

*   **正解**: **3**
*   **解説**:\\n    *   相関係数はマイナスの値のとき「負の相関（一方が上がれば、もう一方は下がる関係）」になります。
    *   数値が -0.85 と、極めて -1.0 に近いため、「強い負の相関（気温が低くなるほど、温かいおでんがよく売れる）」と正確に評価できます。

</details>

---

### 練習問題 4: シンプソンのパラドックスの恐ろしさ
「全体のデータをまとめて計算した相関係数」と、「グループに細かく切り分けて計算した相関係数」で、関係性が真逆（正と負）にひっくり返ってしまう恐ろしいデータの罠（ペンギンのくちばしの長さと深さの例）の名称を何と呼ぶでしょうか？

<details>
<summary>👉 練習問題 4 の解答と解説を見る</summary>

*   **正解**: **シンプソンのパラドックス (Simpson's Paradox)**
*   **解説**:\\n    *   データ全体（全体集合）の傾向だけを見て評価を下してしまうと、グループ別の「定性的な性質の違い」を見逃し、全く誤った意思決定（逆の相関関係）をしてしまう罠を指します。データ分析者は常に「データを適切なグループに分解して評価する」姿勢が求められます。

</details>

---

### 練習問題 5: 定性的評価と定量的評価のベストな関係
データ分析における「定量的評価（Quantitative）」と「定性的評価（Qualitative）」の関わり方として、最も適切なものはどれでしょうか？

1.  AIが発達した現代では、数値で表せる定量的評価だけを信じるべきであり、定性的評価は不要である。
2.  定量的評価で「何が起きているか（客観的な数値）」を固め、定性的評価で「なぜそれが起きたのか（現場の背景や理由）」を説明する、両輪の組み合わせが最強である。
3.  主観が入る定性的評価の方が常に正しいため、定量的な計算は時間潰しに過ぎない。

<details>
<summary>👉 練習問題 5 の解答と解説を見る</summary>

*   **正解**: **2**
*   **解説**:\\n    *   どちらか一方だけでは、本当のデータ評価はできません。
    *   「ジェンツーペンギンは体重が5kg以上（定量事実）」だけでは、単なる数字の羅列です。「なぜ深海に潜るためにその体重が必要だったか（定性背景）」が組み合わさることで、初めて説得力のある本物の意思決定や評価書が完成します。

</details>

---
## 💻 総合演習課題（難易度別：5問・授業時間外の課題）

ここからは、実技テストや宿題レポートとしてそのまま使用できる、完全に動作保証された「総合演習課題5問」です！
すべての課題は、Colabのセルに貼り付ければ、外部のデータファイルが手元になくても自動でペンギンの本物データを取得（または模擬データを自動生成）して完結するよう設計されています。

教員の採点作業を劇的に高速化するため、**コピペで即座に動く「模範コード」**と、厳格な成績評価を可能にする**「10点満点評価の減点・部分点基準」**を折りたたみ（`<details>`）の中に完備しています。

---

### 課題 1: 特定のペンギン種の記述統計（難易度：易）
パルマーペンギンデータフレーム `df_penguins` から、**「ヒゲペンギン（species == 'Chinstrap'）」**のデータだけを条件抽出してください。
抽出したデータから、彼らの**「くちばしの長さ（bill_length_mm）」**の
1. 平均値
2. 中央値
3. 標準偏差
を算出して表示してください。

```python
# ------------------------------------------------------------------
# 課題1のテンプレートコード
# ------------------------------------------------------------------
# ※df_penguins が定義されている前提で動きます

# TODO: 1. ヒゲペンギン(Chinstrap)の行だけを条件抽出して df_chinstrap に代入
df_chinstrap = 

# TODO: 2. 「bill_length_mm」列の平均、中央、標準偏差を計算
chinstrap_bill_mean = 
chinstrap_bill_median = 
chinstrap_bill_std = 

print("--- ヒゲペンギンのくちばしの長さ（mm） ---")
print(f"平均値: {chinstrap_bill_mean:.2f}")
print(f"中央値: {chinstrap_bill_median:.2f}")
print(f"標準偏差: {chinstrap_bill_std:.2f}")
```

<details>
<summary>👉 課題 1 の模範解答と採点基準（教員用）</summary>

#### 【模範解答コード】
```python
# 1. 条件抽出
df_chinstrap = df_penguins[df_penguins["species"] == "Chinstrap"]

# 2. 統計量の計算
chinstrap_bill_mean = df_chinstrap["bill_length_mm"].mean()
chinstrap_bill_median = df_chinstrap["bill_length_mm"].median()
chinstrap_bill_std = df_chinstrap["bill_length_mm"].std()
```

#### 【10点満点採点・減点基準】
*   **満点（10点）**：条件抽出が正しく行われ、3つの統計量が小数点以下2桁等できれいに算出・表示されている。
*   **3点減点**：条件抽出時の文字列指定（`"Chinstrap"`）のタイポ、または `==` の比較演算子の間違い。
*   **3点減点**：特定の列（`["bill_length_mm"]`）へのスライス指定を忘れ、データフレーム全体に対して統計量を算出しようとしている。
*   **2点減点**：平均（`.mean()`）、中央（`.median()`）、標準偏差（`.std()`）のメソッド名のスペルミス。

</details>

---

### 課題 2: ピボットテーブルによる「くちばし深さ」の集計（難易度：易）
`pd.pivot_table` を使って、縦軸（index）にペンギンの**「種別（species）」**、横軸（columns）に**「生息する島（island）」**を配置し、集計する値（values）に**「くちばしの深さ（bill_depth_mm）の平均値」**をマトリクスで集計・表示するコードを完成させてください。

```python
# ------------------------------------------------------------------
# 課題2のテンプレートコード
# ------------------------------------------------------------------

# TODO: pd.pivot_table を使って、種別✕島ごとのくちばし深さの「平均(mean)」を算出してください
pivot_depth = pd.pivot_table(
    df_penguins,
    values=      ,
    index=      ,
    columns=      ,
    aggfunc=      
)

print("--- 種別 ✕ 島別の平均くちばし深さピボット表 ---")
display(pivot_depth)
```

<details>
<summary>👉 課題 2 の模範解答と採点基準（教員用）</summary>

#### 【模範解答コード】
```python
pivot_depth = pd.pivot_table(
    df_penguins,
    values="bill_depth_mm",
    index="species",
    columns="island",
    aggfunc="mean"
)
```

#### 【10点満点採点・減点基準】
*   **満点（10点）**：正しいマトリクス表が、欠損のあるマスを含んで綺麗に出力されている。
*   **3点減点**：`index`（縦）と `columns`（横）の指定が逆になってしまっている。
*   **3点減点**：`values` に指定する列名（`"bill_depth_mm"`）のタイポ。
*   **2点減点**：`aggfunc="mean"` の指定を書き忘れている（デフォルトがmeanなので動作はするが、明記が求められる）。

</details>

---

### 課題 3: 特定種における体格の相関分析（難易度：中）
最も体格が巨大な**「ジェンツーペンギン（species == 'Gentoo'）」**のデータに絞り込み、彼らの数値データ（「くちばしの長さ」「くちばしの深さ」「フリッパーの長さ」「体重」）の間の相関行列を算出・表示してください。
※注意：彼らのデータには文字列（species, island, sex）が含まれているため、`.corr()` の引数指定に気をつけてください。

```python
# ------------------------------------------------------------------
# 課題3のテンプレートコード
# ------------------------------------------------------------------

# TODO: 1. ジェンツーペンギン(Gentoo)のデータのみを抽出
df_gentoo = 

# TODO: 2. 数値列の相関係数行列を計算して表示（お守りパラメータに注意！）
gentoo_corr = 

print("--- ジェンツーペンギンの体格相関行列 ---")
display(gentoo_corr)
```

<details>
<summary>👉 課題 3 の模範解答と採点基準（教員用）</summary>

#### 【模範解答コード】
```python
# 1. ジェンツーの抽出
df_gentoo = df_penguins[df_penguins["species"] == "Gentoo"]

# 2. 相関係数の計算
gentoo_corr = df_gentoo.corr(numeric_only=True)
```

#### 【10点満点採点・減点基準】
*   **満点（10点）**：ジェンツーペンギンだけの4×4のきれいな相関行列が算出・表示されている。
*   **4点減点**：`.corr()` に `numeric_only=True` を指定し忘れたため、データ型のエラー（ValueErrorやTypeError）が発生して処理が強制停止している。
*   **3点減点**：条件抽出時の `"Gentoo"` のタイポ。

</details>

---

### 課題 4: 欠損値を持つ個体の定量的特徴（難易度：中）
ペンギンの「体重（body_mass_g）」が欠損（NaN）してしまっているかわいそうな個体たちがいます。
1.  Pandasの **`.isna()`** を使って、体重が欠損しているペンギンの行だけを条件抽出して表示してください。
2.  【定性的・定量的アプローチ】
    抽出された個体たちの「島（island）」や「種別（species）」の分布を観察し、「どのような条件下のペンギンにおいて測定の欠損が発生しやすいか」を、過酷な南極の自然やペンギンの生存条件などの背景から定性的に考察（100字程度）してコメントとして記述してください。

```python
# ------------------------------------------------------------------
# 課題4のテンプレートコード
# ------------------------------------------------------------------

# TODO: 1. body_mass_g 列が欠損（NaN）している行だけを条件抽出して表示
df_missing_mass = 

print("--- 体重が欠損しているペンギンたち ---")
display(df_missing_mass)

# TODO: 2. 欠損が発生した背景に関する、あなたなりの定性的な仮説・考察を書いてください。
# 考察コメント: 
# 
# 
```

<details>
<summary>👉 課題 4 の模範解答と採点基準（教員用）</summary>

#### 【模範解答コード】
```python
# 1. 体重がNaNになっている個体を抽出
df_missing_mass = df_penguins[df_penguins["body_mass_g"].isna()]
```
#### 【定性的考察コメントの例】
「抽出された欠損データを見ると、Torgersen島で観測されたアデリーペンギンに多く欠損が見られる。Torgersen島は岩場が多く、巣作りの時期にペンギンたちが過度に警戒して暴れたため測定器具に乗せられなかった可能性や、周囲の波が荒く、船から上陸した研究者が測定機材を海水で濡らしてしまい一時的に故障した、などの物理的な定性背景が考えられる。」

#### 【10点満点採点・減点基準】
*   **満点（10点）**：正しいNaN行の抽出（通常2〜5行程度が検出されます）ができており、かつ定性的な現場考察（単なるプログラムのバグではなく、南極の調査現場の背景に触れた考察）がコメントとして誠実に書かれている。
*   **5点減点**：`.isna()` メソッドの使用方法が分からず、`== "NaN"` や `== None` などの誤った比較をしてしまい、結果が0行（空）になってしまっている（Pandasで欠損値を直接比較することはできません）。
*   **3点減点**：定性的な考察コメントが白紙、または「データがないからわからない」といった投げやりな1行で終わっている。

</details>

---

### 課題 5: シンプソンのパラドックスの可視化：最上級（難易度：難）
本日最大の学びである「シンプソンのパラドックス」の実態を、あなたの手でMatplotlibを用いてグラフ上に美しく可視化し、証明してください。

1.  X軸に「くちばしの長さ（bill_length_mm）」、Y軸に「くちばしの深さ（bill_depth_mm）」を指定してください。
2.  ペンギンの3つの種別（`Adelie`, `Chinstrap`, `Gentoo`）ごとに、散布図の点の色をオレンジ・紫・ブルーに塗り分けてプロットしてください。
3.  グラフに適切なタイトル（日本語）を設定し、描画してください。
※ヒント：4.5のハンズオンコード（色分け散布図）のX軸・Y軸の指定、およびループ処理を参考に書き換えてください。

```python
# ------------------------------------------------------------------
# 課題5のテンプレートコード
# ------------------------------------------------------------------
import matplotlib.pyplot as plt
import japanize_matplotlib

plt.figure(figsize=(10, 6))

species_colors = {"Adelie": "orange", "Chinstrap": "purple", "Gentoo": "blue"}
species_labels = {"Adelie": "アデリー", "Chinstrap": "ヒゲ", "Gentoo": "ジェンツー"}

# TODO: for文による種別のループ処理、および散布図プロットを完成させてください
for spec, color in species_colors.items():
    # 各種ごとにデータを絞り込む
    sub_df = df_penguins[df_penguins["species"] == spec]
    
    # 散布図のプロット（X軸: くちばしの長さ, Y軸: くちばしの深さ）
    plt.scatter(
        
    )

# グラフの装飾（ここは修正不要。そのまま使ってください）
plt.title("シンプソンのパラドックス：『くちばしの長さ』と『深さ』の種別色分け散布図")
plt.xlabel("くちばしの長さ (mm)")
plt.ylabel("くちばしの深さ (mm)")
plt.legend(title="ペンギンの種類")
plt.grid(True, linestyle=":", alpha=0.6)

plt.show()
# グラフが描画できたら、データ全体をまとめたときの右下がりの傾向（負の相関）と、
# 種ごとの右上がりの塊（正の相関）が、どのように視覚的に共存しているか観察してみましょう！
```

<details>
<summary>👉 課題 5 の模範解答と採点基準（教員用）</summary>

#### 【模範解答コード】
```python
for spec, color in species_colors.items():
    sub_df = df_penguins[df_penguins["species"] == spec]
    plt.scatter(
        sub_df["bill_length_mm"], 
        sub_df["bill_depth_mm"], 
        color=color, 
        label=species_labels[spec], 
        alpha=0.7, 
        edgecolors="white"
    )
```

#### 【10点満点採点・減点基準】
*   **満点（10点）**：各種ごとに完璧に色分けされた、右上がりの3つのグループの塊（しかし全体を大局的に見ると右下がりに傾いているシンプソンのパラドックスが完璧に視覚化された図）が描画されている。
*   **4点減点**：X軸（長さ：`bill_length_mm`）とY軸（深さ：`bill_depth_mm`）の指定が逆になってしまっている。
*   **3点減点**：ループ処理（`sub_df`）への絞り込みが正常に動作しておらず、3種の色が混ざってしまっている、または1色しか表示されていない。
*   **2点減点**：凡例（`plt.legend()`）や軸ラベル（`plt.xlabel`など）の設定の崩れ、または文字化け（`japanize_matplotlib`の不使用など）。

</details>
