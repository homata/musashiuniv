# 第5回 Webサービスの活用1：StreamlitによるWebアプリ開発の基礎（100分授業用）

皆さん、こんにちは！プログラミング応用A（コンピューティング応用G）、第5回の講義です。担当の教員です。
前回（第4回）は、Pandasを使って「平均値の罠」を見破り、データのばらつき（標準偏差）や変数間の関係性の強さ（相関係数）を可視化する「データ解析」の基本を学びました。皆さんのデータリテラシーは確実にプロのレベルへと近づいています。

今回からはいよいよ、データ分析をさらに一歩進めて、**「Webサービスの活用（Webアプリケーション開発）」**というエキサイティングな領域に突入します！
具体的には、Pythonだけで本格的かつモダンなWebアプリケーションを構築できる魔法のライブラリ **Streamlit（ストリームリット）** を使い、あなた専用の「データダッシュボード」を作っていきます。

「HTMLやCSSは難しそう…」「Webサーバーって何？」と思うかもしれませんが、**大丈夫です！**
Streamlitを使えば、そうした難しいデザインのコードは一切書く必要がありません。Pythonのコードを数行書くだけで、オシャレなボタンやスライダー、グラフが画面に自動的に配置されます。

今日は、Google Colab上でWebアプリを起動するための特別な手順（トンネリング）から、基本的なボタンや入力欄（ウィジェット）の配置方法まで、100分間かけて丁寧に学んでいきます。自分で作ったプログラムがWebブラウザ上で「アプリ」として動く感動を、ぜひ味わってください！

---
## 🎯 本日の到達目標 (約5分)
この100分の授業が終わるころには、以下のことができるようになります。
1. **Webアプリ化（ダッシュボード化）の目的と意義**（なぜデータをアプリとして公開するのか、誰が使うのか）を説明できる。
2. **Google Colab上でStreamlitを安全に起動**し、ローカルトンネル（localtunnel）を使ってスマホやブラウザからアクセスできる。
3. **Streamlitの基本コマンド**（タイトル、テキスト出力、マークダウン、インジケーター表示）を使い分けてWeb画面を構成できる。
4. **入力用ウィジェット**（ボタン、スライダー、テキスト入力、セレクトボックス、ラジオボタン）を配置し、ユーザーの操作に応じて画面が変わるインタラクティブ（対話型）なアプリが作れる。
5. **Streamlit特有の「上から下へ毎度再実行される（Rerun）」仕組み**を理解し、ボタンを押した時の挙動の罠をデバッグ・回避できる。

---
## ⏱️ 本日のタイムテーブル

1コマ100分の授業時間を最大限に活かすためのタイムテーブルです。Colab特有の「環境構築」をクリアすれば、あとは楽しいアプリ開発の世界が待っています！

| 時間 | セクション | 学習内容と教員のアドバイス |
| :--- | :--- | :--- |
| **00:00 - 00:05**<br>（5分） | **導入** | 本日のゴール確認、データ共有をアプリ化することの意義。 |
| **00:05 - 00:25**<br>（20分） | **5.1 なぜデータを「アプリ」にするのか？** | 静的なデータ共有から双方向アプリへのパラダイムシフト。Streamlitの特徴。 |
| **00:25 - 00:55**<br>（30分） | **5.2 ColabでのStreamlit環境構築** | localtunnel（ローカルトンネル）の仕組み。Hello Worldアプリの起動ハンズオン。 |
| **00:55 - 01:20**<br>（25分） | **5.3 基本ウィジェットと挙動の理解** | ボタン、スライダー、セレクトボックスの配置。**「上から下への再実行（Rerun）」の罠**の解明。 |
| **01:20 - 01:35**<br>（15分） | **5.4 授業内練習問題（5問）** | 講義の理解度をチェックする理論＆クイックコードクイズ（解説付き）。 |
| **01:35 - 01:55**<br>（20分） | **💻 総合演習課題（5問）** | TODO形式のコード穴埋め実装。割引シミュレーター、会員レジアプリ開発。 |
| **01:55 - 02:00**<br>（5分） | **まとめと次回への架け橋** | 本日の振り返りと、次回「Pandas/グラフをWebに合体させるダッシュボード開発」への展望。 |

---
## 5.1 なぜデータを「アプリ」にするのか？ (約20分)

### 5.1.1 データ分析を「社会に届ける」ための課題
これまでの授業で、私たちはデータを収集し、Pandasで集計し、Matplotlibでグラフを描くことができるようになりました。
しかし、その素晴らしい分析結果を「プログラミングを全く知らない会社の上司、クライアント、あるいは一般のお客様」に見せたいとき、どのように共有すればいいでしょうか？

*   **「Pythonのコード（Colabのノートブック）をそのまま送る」**
    *   ❌ 相手はPythonのコードを読めませんし、Colabの実行方法も知りません。余計なコードが並んでいるだけで「難しそうだ」と敬遠されてしまいます。
*   **「グラフを画像（PNG）にして、メールやスライドで送る」**
    *   ❌ 画像は「静止画」です。「客数を150人から200人に増やしたときの売上はどうなるの？」とか「江古田店だけの売上グラフを見たい」といった、相手の「もしも（シミュレーション）」にその場で応えることができません。
*   **「Excelファイルを共有する」**
    *   ❌ 相手が数式や関数をうっかり書き換えて壊してしまう危険があります。また、スマートフォンやタブレットからだと画面が崩れて操作しづらいという難点があります。

一番良い解決策は、**「誰でもスマホやパソコンのブラウザからアクセスして、ボタン一つでデータを絞り込んだり、スライダーで数値を動かして未来を予測できる『Webアプリ』にして渡すこと」**です。これを**「データダッシュボード」**と呼びます。

**【データ共有の進化ステップ】**
```text
  [ 段階 1: 生データ ] ──▶ [ 段階 2: 静止画グラフ ] ──▶ [ 段階 3: Webアプリ（ダッシュボード）]
   エクセルの数字の羅列        報告用スライドの画像         スライダーを動かすとグラフが
   (見ても意味がわからない)    (自分で条件を変えられない)   リアルタイムに自動変形する！(双方向)
```

### 5.1.2 Streamlitとは何か？
通常、Webアプリケーション（YouTubeやTwitter、企業のダッシュボードなど）をゼロから作成するためには、非常に多くのプログラミング言語を習得しなければなりません。

1.  **HTML**（画面の骨組みを作る）
2.  **CSS**（画面の色やデザイン、レイアウトを整える）
3.  **JavaScript**（ボタンを押した時の「動き」をつける）
4.  **Webサーバーの構築**（バックエンド：PythonやNode.js、Rubyなど）

これらをすべて一人で習得するには、何百時間もの学習コストが必要です。データ分析者や文系学生が「ちょっとデータをオシャレに見せたい」だけなのに、これらを学ぶのは現実的ではありません。

そこで登場したのが、Pythonライブラリ **Streamlit（ストリームリット）** です！

**【伝統的な開発方法 vs Streamlit】**
```text
▼ 従来のWeb開発（挫折率：極大 😱）
  Python（分析） ＋ HTML/CSS（デザイン） ＋ JavaScript（挙動） ＋ サーバー構築 ＝ 複雑すぎる！

▼ Streamlitを使った開発（効率性：極大 😍）
  [ 🐍 Pythonコードを数行書くだけ ] ──▶ [ 🎨 Streamlitが自動変換 ] ──▶ [ 💻 プロ級のオシャレWebアプリ ]
```

### 5.1.3 Streamlitの画期的な3大特徴
*   **HTML/CSS/JavaScriptが一切不要**
    *   Pythonのコードを書くだけで、Streamlitが裏側で自動的に美しいWebデザイン（マテリアルデザイン）に変換してブラウザに表示してくれます。スマホ画面の自動対応（レスポンシブデザイン）も最初から完備されています。
*   **直感的なUI（ユーザーインターフェース）コンポーネント**
    *   `st.slider()` と1行書くだけでオシャレなスライダーが配置され、`st.selectbox()` と書くだけで綺麗なドロップダウンメニューが生成されます。
*   **リアクティブ（即時反応）な実行システム**
    *   スライダーを「10」から「11」に動かした瞬間、プログラムが自動で瞬時に再計算を行い、画面のグラフや文字を滑らかに更新します。JavaScriptの「お固い同期処理」を書く必要は全くありません。

---
## 5.2 Google Colab上でStreamlitを動かす魔法（環境構築） (約30分)

通常、Streamlitは自分のパソコン（ローカル環境）のコマンドプロンプトやターミナルで起動します。
しかし、この授業では誰もがインストールなしで同じ環境で学べるよう **Google Colab** を使っています。

Colabは「Googleのクラウド上にある仮想のパソコン」なので、そこで動かしたWebアプリはそのままでは私たちのパソコンの画面には表示されません。
そこで、クラウド上のColabと、皆さんの手元のブラウザを繋ぐ**「一時的な通り道（トンネル）」**を作ってあげる必要があります。

このトンネリングツールとして **localtunnel（ローカルトンネル）** という無料のサービスを使います。

**【localtunnelによる通信のイメージ図】**
```text
  [ ☁️ Google Colab ]                           [ 🌐 localtunnel サーバー ]                 [ 💻 あなたのブラウザ ]
    ・Streamlitが起動中                           一時的な公開URLを発行する                  Colabの画面を
    ・ポート: 8501番   ──(トンネル開通)──▶     (https://xxxx.loca.lt)   ──(アクセス)──▶  手元のPC/スマホで
    ・IPアドレス(鍵)                                                                         直接操作できる！
```

まずは、Colabのセルに以下の「おまじない」を貼り付けて、環境構築の準備をしましょう。

```python
# ------------------------------------------------------------------
# ステップ 1: 必要なライブラリのインストール（Colabのセルで実行してください）
# ------------------------------------------------------------------
# Streamlitと、トンネリング用のツールをインストールします。
!pip install -q streamlit
!npm install -g localtunnel
```

### ⚠️ 【最重要】ColabでStreamlitを起動する「3つの手順（黄金の儀式）」

Colab上でアプリを起動し、表示させるためには、これから紹介する**3つのセルを順番に実行する「儀式」**が必ず必要になります。1つでも手順を抜かすと繋がりませんので、丁寧に確認していきましょう。

#### 手順①：アプリのソースコードを書き出す（`%%writefile`）
Colabのセルの1行目に **`%%writefile app.py`** と記述すると、そのセルの中身がそのまま `app.py` という名前のPythonファイルとしてColabの中に保存されます。

*   ※以下のコードセルを実行してください。
```python
%%writefile app.py
# これがあなたの最初のWebアプリのプログラムになります！
import streamlit as st

st.title("武蔵大学データ分析アプリ ☕")
st.subheader("プログラミング応用A（第5回実習）")
st.write("こんにちは！私が自分で作って起動した、最初のStreamlitアプリへようこそ！")
st.markdown("---")
st.success("おめでとうございます！アプリのソースコードが正常に作成されました。")
```

#### 手順②：トンネルの鍵（あなたのIPアドレス）を確認する
localtunnelを使って外部からアプリにアクセスする際、いたずら防止（セキュリティ確認）のために「Colabの仮想パソコンのIPアドレス（4つの数字の組み合わせ）」を求められます。
以下のコマンドを実行して、出力されたIPアドレスをコピーして手元にメモしておきましょう。

*   ※以下のコードセルを実行してください。
```python
# あなたのColab環境のIPアドレスを表示します（トンネルを開くためのパスワードになります）
!wget -qO- ipv4.icanhazip.com
```
*出力例：`34.125.12.34`（※実行するたびに毎回異なる数字が出ます）*

#### 手順③：アプリをバックグラウンドで起動し、トンネルを開通する！
以下のコマンドを実行すると、StreamlitがColabの裏側（バックグラウンド）でひっそりと動き続け、localtunnelが手元のブラウザからアクセスするための一時的なURLリンクを発行してくれます。

*   ※以下のコードセルを実行してください。
```python
# 1. StreamlitをColabの裏側で静かに起動（ポート8501番を使用）
!streamlit run app.py &>/dev/null &

# 2. ポート8501番を localtunnel を使って外部に公開します
!npx localtunnel --port 8501
```

*   **ブラウザでの開き方（接続の完了まで）**:
    1. 手順③を実行すると、画面に `url: https://xxxx.loca.lt` という青いリンクが表示されます。このリンクをクリックしてください。
    2. 以下のような英語の画面（セキュリティ警告）が表示されます。
       ```text
       Friendly Reminder
       Submit your IP address to access this localtunnel:
       [                  ] (ここにIPアドレスを入れる入力欄)
       ```
    3. 入力欄に、先ほど**手順②で確認したIPアドレス（例：`34.125.x.x`）**をそのまま貼り付け、**「Submit」**ボタンを押してください。
    4. 画面がパッと切り替わり、あなたが手順①で作った「武蔵大学データ分析アプリ ☕」が表示されれば大成功です！🎉

---
## 5.3 基本ウィジェットと挙動の理解 (約25分)

無事にアプリが起動したら、今度はアプリの中身を豪華にしていきましょう！

### 5.3.1 最重要：Streamlit特有の「Rerun（再実行）」システム
Streamlitでコードを書く際、従来のプログラミングと大きく異なる「お約束」があります。
それは、**「ユーザーがボタンを押したりスライダーを動かしたりするたびに、StreamlitはPythonのコードを上から下まで、毎回超高速で丸ごと再実行（Rerun）する」**という仕組みです。

**【Streamlitの処理サイクル】**
```text
  [ 💻 画面でユーザーが操作 ] （例：スライダーを 150 に動かす）
             │
             ▼ （Streamlitが検知！）
  [ 🔄 Pythonコードを上から下まで全部再実行 ] （app.pyの1行目から読み直し）
             │
             ▼ （現在のスライダーの値を読み込み、売上高を再計算）
  [ 🎨 画面を滑らかに更新 ]
```

このお約束を理解していないと、次の「ボタンの罠」に引っかかってしまいます。

### ⚠️ 【初心者が100%はまる】ボタンの罠（st.button）
以下のようなコードを書いたとします。
```python
if st.button("秘密のメッセージを見る"):
    st.write("秘密のメッセージ：いつも授業を頑張ってくれてありがとう！")
```
ボタンを押した瞬間はメッセージが表示されます。しかし、その後、別の場所にあるスライダーなどを動かすと、せっかく表示されたメッセージが**一瞬で消えてしまいます。**

なぜでしょうか？
スライダーが動かされた瞬間、Streamlitはコードを上から下まで再実行します。その再実行の際、「今、まさにこの瞬間ボタンがカチッとクリックされたか？」と判定されますが、クリックされた瞬間ではない（スライダーが動いただけ）ため、`st.button` の結果は `False`（押されていない）と判定され、if文の中のメッセージ表示処理がスキップされてしまうのです。

この「ボタンは押された瞬間だけ True になり、次のアクションでは False に戻る」という性質は、Streamlitの最も重要なルールです。覚えておきましょう！

---

### 5.3.2 画面を構成する代表的なコマンド
ソースコードを上書きして保存し、ブラウザの画面を「リロード（F5）」することで、瞬時にアプリが更新されます。

```python
%%writefile app.py
import streamlit as st

# ==========================================
# 1. 画面表示系（テキストや装飾）
# ==========================================
st.title("カフェ自動レジアプリ ☕") # タイトル（最大サイズ）
st.header("お会計シミュレーター") # ヘッダー（中サイズ）
st.write("商品の選択と数量を入力すると、自動でお会計を計算します。") # 標準テキスト

# ==========================================
# 2. 入力系ウィジェット（ユーザーからデータを受け取る部品）
# ==========================================
# (1) セレクトボックス（ドロップダウンメニュー）
# ユーザーが選んだ値が、変数 selected_item に代入されます！
selected_item = st.selectbox(
    "ご注文の商品を選んでください:",
    ["コーヒー (450円)", "紅茶 (400円)", "チョコレートケーキ (550円)"]
)

# (2) スライダー（数値の範囲入力）
# st.slider("ラベル名", 最小値, 最大値, 初期値)
amount = st.slider("数量（個数）を選んでください:", 1, 10, 1)

# ==========================================
# 3. 連動した計算処理（Pythonのロジック）
# ==========================================
# 商品名から価格を取り出す
if "コーヒー" in selected_item:
    price = 450
elif "紅茶" in selected_item:
    price = 400
else:
    price = 550

# 合計金額の計算
total_price = price * amount

# ==========================================
# 4. インジケーター・結果表示
# ==========================================
st.markdown("---") # 区切り線
st.subheader("🧾 お会計結果")

# st.metric(ラベル, 値) を使うと、プロのダッシュボードのような綺麗な数字メーターが表示されます
st.metric(label="合計金額 (税込み)", value=f"{total_price} 円")

# お礼のメッセージ（ st.success を使うと、緑色の綺麗なボックスで表示されます）
st.success(f"ご注文ありがとうございます！【{selected_item}】を {amount} 個、レジでご用意いたします。")
```

*   **動作確認の方法**: 
    1. 上のセルを実行して、`app.py` を上書き保存します。
    2. 先ほど localtunnel で開いたブラウザのタブに移動します。
    3. 右上の「︙」（メニュー）から **「Always rerun（常時再実行）」** を選択するか、ブラウザをリロード（再読み込み）します。
    4. ドロップダウンを変更したり、スライダーを動かした瞬間に、合計金額がリアルタイムにピタッと計算し直される快感を確かめてください！

---
## 5.4 授業内練習問題（理論・確認用：5問） (約15分)

ここまでの講義内容がしっかりと理解できているか、5つの確認問題に挑戦してみましょう！答えは折りたたまれていますので、自分なりの答えを頭に思い浮かべてから開いてみてくださいね。

### 練習問題 1: データの「アプリ化」のメリット
データ分析の結果を、静止画のグラフやPDFではなく「Webアプリ（ダッシュボード）」として共有することの最大のメリットは何でしょうか？ 「相手の行動（操作）」に焦点を当てて説明してください。

<details>
<summary>👉 練習問題 1 の解答と解説を見る</summary>

*   **解答**: 相手が自分の見たい条件（店舗、期間、客数など）に合わせて、**「ブラウザ上でパラメータを直接操作し、リアルタイムにシミュレーションや絞り込み（双方向の対話処理）が行える」**点。
*   **解説**: 静止画やPDFは「一方的な報告」で終わってしまいますが、アプリにすることで「相手が自分でデータを探索して納得する」という自発的な意思決定を促すことができます。

</details>

---

### 練習問題 2: %%writefile マジックコマンドの役割
Google ColabでStreamlitを開発する際、セルの1行目に書く `%%writefile app.py` というコマンドは、具体的に何をするためのものでしょうか？

<details>
<summary>👉 練習問題 2 の解答と解説を見る</summary>

*   **解答**: そのセルの中に書かれたプログラムコードを、**`app.py` という名前のテキストファイル（Pythonファイル）としてColabの仮想環境内に書き出して上書き保存する**コマンド。
*   **解説**: Colabのセルは通常その場でPythonを実行しますが、Streamlitを動かすためには「ファイルに保存されたコードを裏で起動する」必要があるため、このコマンドを使ってセルをエディタ（書き出し機）として利用します。

</details>

---

### 練習問題 3: Rerun（再実行）システムの罠
Streamlitのアプリ画面で、ユーザーがスライダーのつまみを動かしたとき、裏側で動いているPythonのプログラム（`app.py`）はどのような挙動をしますでしょうか？ 「実行される範囲」に注目して答えてください。

<details>
<summary>👉 練習問題 3 の解答と解説を見る</summary>

*   **解答**: **プログラムの1行目（最初）から最後の行まで、すべてのコードが丸ごと超高速で再実行（Rerun）される。**
*   **解説**: これがStreamlitの最も重要で特徴的な仕組みです。ユーザーが何か操作するたびに、すべてのコードが上から下に再実行され、その時点でのウィジェットの値を読み取って画面全体を再描画します。

</details>

---

### 練習問題 4: ボタン（st.button）の挙動の真実
以下のコードを実行し、ブラウザでボタンを押した瞬間は「お祝い」と表示されましたが、その直後、別のスライダーを動かした瞬間、画面から「お祝い」の文字が消えてしまいました。この現象が起きる原因を説明してください。

```python
if st.button("合格発表を見る"):
    st.write("🎉 おめでとうございます！合格です！")
```

<details>
<summary>👉 練習 representation 4 の解答と解説を見る</summary>

*   **解答**: `st.button` は「ボタンがクリックされた瞬間（再実行）だけ `True` を返し、その後に別のウィジェット（スライダー等）が動いて再実行されたときには、クリックされた瞬間ではないため `False`（押されていない状態）に戻ってしまう」から。
*   **解説**: ボタンの判定は「一瞬の打ち上げ花火」のようなものです。次の瞬間（再実行）にはリセットされて `False` に戻るため、if文の中の処理は消えてしまいます。この消したくないデータを保存する高度な技術（`st.session_state`）は、今後の授業で学びます！

</details>

---

### 練習問題 5: インジケーター st.metric の役割
Streamlitで用意されている `st.metric()` というコマンドは、どのようなデータを画面に表示するのに適していますか？ 具体的な例を2つ挙げてみてください。

<details>
<summary>👉 練習問題 5 の解答と解説を見る</summary>

*   **解答**: 売上高、客数、気温、進捗率など、**「ダッシュボードの最上部に、パッと一目でわかるように大きな太字でアピールしたい『重要な数値指標（KPI）』」**を表示するのに適している。
*   **解説**: 
    *   例：「本日総売上：150,000円」「前日比：+15%」
    *   普通のテキスト（st.write）に比べて、文字が非常に大きく、枠線やカラー表示もオシャレに整理されるため、ビジネスダッシュボードの「主役」として多用されます。

</details>

---
## 💻 総合演習課題（難易度別：5問） (約20分)

さあ、ここからはあなたがデータエンジニアになって、実際にアプリを拡張・構築する番です！
以下の5つの課題は、TODO形式で記述されています。

**【開発のコツ】**
1. 課題ごとに、Colabのコードセルを `%%writefile app.py` で上書き保存します。
2. ブラウザのアプリ画面をリロード（または「Always rerun」をオン）して、動くか確かめます。
3. エラーが出たら、Colabの出力やブラウザの画面に書いてあるエラー内容をコピーし、生成AIに「このStreamlitコードでエラーが出たよ。どう直せばいい？」と相談して解決してみましょう！

---

### 課題 1: 【初級】カフェの追加トッピング判定（難易度：易）
お会計シミュレーターに、トッピングの有無を判定する「チェックボックス（`st.checkbox`）」を追加します。
チェックボックスにチェックを入れたら、合計金額に **「ホイップ追加料金：50円」** がプラスされ、チェックを外したら追加されない、というプログラムを完成させてください。

```python
%%writefile app.py
import streamlit as st

st.title("カフェトッピングレジ ☕")

base_price = 450 # コーヒーの基本価格
st.write(f"コーヒーの価格: {base_price} 円")

# TODO: st.checkbox("ラベル") を使って、チェックボックスを配置し、結果を変数 add_whip に代入してください。
add_whip = 

# TODO: もし add_whip が True なら、価格に50円をプラスしてください。
if 
    final_price = base_price + 50
    st.write("🍦 ホイップを追加しました（+50円）")
else:
    final_price = base_price

st.markdown("---")
st.metric("お支払い合計", f"{final_price} 円")
```

---

### 課題 2: 【初級】会員ランクによる割引率の変動（難易度：易）
お客様の「会員ランク」をラジオボタン（`st.radio`）で選択させ、ランクに応じて割引率（％）が自動で適用される仕組みを作ります。
*   「一般会員」：割引なし（割引率 0%）
*   「ゴールド会員」：10% 割引（割引率 10%）
*   「VIP会員」：20% 割引（割引率 20%）
この条件分岐を完成させ、割引後の合計金額を表示させてください。

```python
%%writefile app.py
import streamlit as st

st.title("Musashi Member POS 💳")

total_amount = 1000 # 元の合計金額
st.write(f"お買い物元の金額: {total_amount} 円")

# TODO: st.radio("ラベル", 選択肢リスト) を使って、会員ランクを決定し、変数 rank に代入してください。
rank = 

# TODO: rank の値に応じて割引率(rate)を 0.0, 0.1, 0.2 に決定してください。
if 
    rate = 0.0
elif 
    rate = 0.1
else:
    rate = 0.2

# 割引後の価格計算
discounted_price = int(total_amount * (1 - rate))

st.markdown("---")
st.metric("割引後お支払い金額", f"{discounted_price} 円")
st.info(f"現在の適用割引率: {int(rate * 100)} %")
```

---

### 課題 3: 【中級】深夜の割増＋テイクアウト複数条件レジ（難易度：中）
少し実務的な、複数条件が絡み合うPOSレジアプリです。
1.  「イートイン（店内飲食）」か「テイクアウト（持ち帰り）」かをセレクトボックスで選択します。店内なら消費税10%（1.1倍）、持ち帰りなら8%（1.08倍）を基本価格に掛け算します。
2.  チェックボックスで「深夜22時以降の注文」にチェックが入っている場合、さらに深夜割増料金として **「深夜サービス料：100円」** を最後に足し算します。
この二重の条件計算を完成させてください。

```python
%%writefile app.py
import streamlit as st

st.title("深夜対応カフェスマートレジ 🌙")

# 基本価格
food_price = 600
st.write(f"商品の本体価格: {food_price} 円")

# TODO: 1. セレクトボックスで「店内飲食」か「持ち帰り」を選択させ、変数 eating_style に代入してください。
eating_style = 

# TODO: 2. チェックボックスで「深夜22時以降の注文ですか？」と問いかけ、結果を変数 is_late_night に代入してください。
is_late_night = 

# 税率の決定
if eating_style == "店内飲食":
    tax_rate = 1.10
else:
    tax_rate = 1.08

# 税込み価格の計算
tax_included_price = int(food_price * tax_rate)

# TODO: 3. 深夜チェックが入っている場合は、税込み価格に100円をプラスしてください。
if 
    final_bill = tax_included_price + 100
else:
    final_bill = tax_included_price

st.markdown("---")
st.metric("最終お支払い金額", f"{final_bill} 円")
st.write(f"内訳: 税率 {int((tax_rate-1)*100)}% 適用後 ({tax_included_price}円) ＋ 深夜サービス料")
```

---

### 課題 4: 【中級】スタッフ専用：簡易目標達成度チェッカー（難易度：中）
カフェのスタッフが今日の「売上実績（円）」と「今日の目標（円）」を数値入力（`st.number_input`）し、ボタンを押すと、目標達成率（％）を計算して画面に表示するツールを作ります。
*   目標達成率（％） ＝ （売上実績 ÷ 今日の目標） ✕ 100
*   もし達成率が 100% 以上の場合は、お祝いのメッセージ（`st.success`）を表示し、100%未満の場合は応援のメッセージ（`st.warning`）を表示するロジックを完成させてください。

```python
%%writefile app.py
import streamlit as st

st.title("📊 今日の店舗目標チェッカー")

# st.number_input("ラベル", 最小値, 最大値, 初期値, ステップ幅)
goal = st.number_input("本日の売上目標（円）を入力してください:", 1000, 1000000, 100000, 10000)
actual = st.number_input("本日の売上実績（円）を入力してください:", 0, 1000000, 80000, 10000)

# TODO: ボタン「達成度を計算する」がクリックされた時の処理を書いてください。
if 
    # TODO: 達成率を計算する
    achievement_rate = 
    
    st.write(f"本日の目標達成率: **{achievement_rate:.1f} %**")
    
    # TODO: 100%以上の場合は st.success、未満の場合は st.warning で表示を分けてください。
    if 
        st.success("🎉 素晴らしい！本日の目標を完全に達成しました！スタッフの皆さん、お疲れ様でした！")
    else:
        st.warning("💪 あと少し！目標達成まであと一歩です。明日も頑張りましょう！")
```

---

### 課題 5: 【上級】武蔵カフェ・総合自動お見積りPOSシステム（難易度：難）
本日学んだ全ての知識（テキスト入力、セレクトボックス、スライダー、チェックボックス、条件分岐、インジケーター）を完全に統合した、実用に耐えうる「本格POSお見積りアプリ」をゼロから完成させます。

以下のソースコードの空欄をすべて埋め、動くフル機能アプリを完成させてください！

```python
%%writefile app.py
import streamlit as st

st.title("🎪 武蔵野フェスティバル・総合POS見積システム")
st.write("学園祭のカフェ屋台で、お客様の注文をその場でiPadから入力し、即座に見積もり領収書を発行するシステムです。")

# 1. お客様のお名前を入力（ st.text_input ）
customer_name = st.text_input("お客様のお名前を入力してください（必須）:", "ゲスト")

# 2. メイン商品を選択（ st.selectbox ）
main_dish = st.selectbox(
    "メインのフードを選んでください:",
    ["タピオカミルクティー (500円)", "武蔵焼きそば (600円)", "大学いもソフト (450円)"]
)

# 3. メイン商品の個数をスライダーで決定（ st.slider ）
quantity = st.slider("メイン商品の個数を選んでください:", 1, 10, 1)

# 4. セットドリンクの追加（ st.checkbox ）
# ドリンクを追加する場合は、1杯につき「+150円」とします。
add_drink = st.checkbox("セットドリンクを追加しますか？ (+150円/杯)")

# 5. 価格の計算ロジック
# メインフードの単価決定
if "タピオカ" in main_dish:
    base_unit_price = 500
elif "焼きそば" in main_dish:
    base_unit_price = 600
else:
    base_unit_price = 450

# フードの小計
food_subtotal = base_unit_price * quantity

# TODO: ドリンクがチェックされている場合は、ドリンク料金（150 * 個数）を算出し、
#       そうでない場合は 0 円として合計金額（final_total）を計算してください。
if 
    drink_subtotal = 150 * quantity
else:
    drink_subtotal = 0

# 最終お支払い金額
final_total = food_subtotal + drink_subtotal

# 6. 見積書・領収書画面の出力（ st.metric や st.success を活用）
st.markdown("---")
st.subheader("🧾 発行されたお見積書")

# 領収書の宛名表示
st.write(f"宛名: **{customer_name} 様**")

# 金額のKPI表示
st.metric("合計お支払い総額 (税込み)", f"{final_total} 円")

# 内訳の詳細表示
st.write(f"【内訳】")
st.write(f"- {main_dish} : {base_unit_price}円 × {quantity}個 ＝ {food_subtotal}円")
if add_drink:
    st.write(f"- セットドリンク追加 : 150円 × {quantity}個 ＝ {drink_subtotal}円")

st.success("📝 上記の内容でご注文を承りました。厨房へデータを送信しました。")
```

---
## 💡 演習課題の解答例

（エラーが起きても諦めずに、デバッグを繰り返したプロセスそのものがあなたの実力になります。確認が終わったら、答えを展開して復習しましょう！）

<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

**課題 1 の解答**
```python
# チェックボックスを配置
add_whip = st.checkbox("ホイップを追加しますか？ (+50円)")

# トッピング追加判定
if add_whip:
    final_price = base_price + 50
    st.write("🍦 ホイップを追加しました（+50円）")
else:
    final_price = base_price
```

**課題 2 の解答**
```python
# 会員ランクを決定
rank = st.radio("会員ランクを選択してください:", ["一般会員", "ゴールド会員", "VIP会員"])

# 割引率の決定
if rank == "一般会員":
    rate = 0.0
elif rank == "ゴールド会員":
    rate = 0.1
else:
    rate = 0.2
```

**課題 3 の解答**
```python
# 1. 飲食スタイル
eating_style = st.selectbox("飲食スタイルを選択:", ["店内飲食", "持ち帰り"])

# 2. 深夜フラグ
is_late_night = st.checkbox("深夜22時以降の注文ですか？")

# 3. 深夜料金計算
if is_late_night:
    final_bill = tax_included_price + 100
else:
    final_bill = tax_included_price
```

**課題 4 の解答**
```python
# 計算ボタンのクリック判定
if st.button("達成度を計算する"):
    # 達成率の計算
    achievement_rate = (actual / goal) * 100
    st.write(f"本日の目標達成率: **{achievement_rate:.1f} %**")
    
    # 判定ボックス
    if achievement_rate >= 100.0:
        st.success("🎉 素晴らしい！本日の目標を完全に達成しました！スタッフの皆さん、お疲れ様でした！")
    else:
        st.warning("💪 あと少し！目標達成まであと一歩です。明日も頑張りましょう！")
```

**課題 5 の解答**
```python
# ドリンク追加判定
if add_drink:
    drink_subtotal = 150 * quantity
else:
    drink_subtotal = 0
```

</details>

---
## 📚 第5回のまとめと次回への架け橋

お疲れ様でした！本日は、データ分析の結果を「プログラムの知識がない誰か」に最も伝わりやすいカタチで届けるための技術、**「StreamlitによるWebアプリ開発の基礎」**を学びました。

### 🌟 本日の重要ポイントまとめ
*   **Webアプリ化の価値**: 静止画のグラフとは違い、相手がブラウザ上で直接操作して、手元で条件を変えて試せる「双方向のシミュレーション環境」を提供できる。
*   **localtunnelによる公開**: Google Colabの裏側で動いているStreamlitサーバー（8501番）を、一時的な公開リンクとパスワード（ColabのIPアドレス）を用いてブラウザに表示させる魔法。
*   **Rerun（再実行）システム**: ボタンを押したりスライダーを動かしたりするたびに、Pythonのプログラムは「上から下まで、最初から毎回実行され直す」という特徴を持つ。
*   **基本ウィジェット**: `st.title`, `st.write`（表示系）と `st.button`, `st.slider`, `st.selectbox`（入力系）、`st.metric`（インジケーター）の組み合わせでオシャレなUIが一瞬で作れる。

---
### 🗺️ 次回への架け橋：データ ✕ グラフ ✕ Webアプリの完全合体！
今回は、自分で数値を決め打ちして計算するシンプルな「レジアプリ」を作りました。
次回（第6回）の**「Webサービスの活用2」**では、今日学んだStreamlitの画面上に、第3回・第4回で学んだ**「Pandasによる売上CSVデータの読み込み」**と**「Matplotlibによるきれいな統計グラフ」**を丸ごと合体させます！

「サイドバーで『江古田店』を選ぶと、Pandasが裏で売上データを瞬時に絞り込み、Matplotlibが自動でグラフを描き直し、画面の売上合計メーター（st.metric）が連動して更新される」という、実務で数千万円の価値を生み出すレベルの**「完全自動連動型売上ダッシュボード」**を、あなたの手で完成させます！

どうぞお楽しみに。次回も元気にアプリ開発を楽しみましょう。お疲れ様でした！
