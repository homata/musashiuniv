# 第5回 データ構造（数値、文字列、リスト、辞書）

前回までは「1つの箱（変数）に1つのデータ」を入れてきました。  
でも、現実のデータ分析では、何千、何万というデータを扱います。  
「カフェの1ヶ月分の売上」を記録するのに、変数を30個も作るのは大変ですよね。

そこで今日は、**「たくさんのデータを1つにまとめて管理する魔法の箱（データ構造）」**を学びます！  
「リスト」「タプル」「辞書」「セット」と色々な名前が出てきますが、

それぞれ「レシート」「鍵付きの箱」「メニュー表」「重複なし名簿」と例えていくので、一緒に楽しく整理していきましょう。これが使えるようになると、プログラミングが一気に便利に、そして強力になります

---

## 🎯 到達目標
1. **リスト（list）** を使って、たくさんのデータを順番に管理・操作できる。
2. **辞書（dictionary）** を使って、「商品名と価格」のようなペアのデータを管理できる。
3. **タプル（tuple）とセット（set）** の特徴を理解し、目的に応じて使い分けられる。
4. 「等しい（値が同じ）」と「同一（モノが同じ）」の違いなど、データ構造特有の**エラーや落とし穴**を回避できる。

---

### 練習問題のGoogle Colab起動
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](
https://colab.research.google.com/github/homata/musashiuniv/blob/master/docs/exercises/2026/01.programming_basic_a/basic_05.ipynb)

1. 上記の「Open In Colab」をクリックして、Google Colabを起動させてください
2. [ファイル] → [ドライブにコピーを保存] を選択して、Googleドライブにコピーを保存してください
3. 保存が成功すると，Googleドライブの「マイドライブ」 →「Colab Notebooks」にコピーしたファイルが保存されます。
4. GoogleドライブにコピーしたGoogle ColabのNotebooksを開いてください
5. 開いたGoogle ColabのNotebooksで作業をしてください

---

## イントロダクション：データ構造とは何か？

データ分析を行うためには、バラバラのデータを「構造化（整理整頓）」する必要があります。  
Pythonには、用途に合わせて様々な「まとめ方（データ構造）」が用意されています。

| 名前 | 括弧の記号 | 特徴（カフェの例え） | データ分析での用途 |
| :--- | :---: | :--- | :--- |
| **リスト** | `[ ]` | **レシート（順番がある）**。追加や削除が自由自在。 | 時系列の売上データ、顧客リスト |
| **タプル** | `( )` | **確定申告書（変更不可）**。中身を書き換えられない。 | 変更してはいけない座標や設定値 |
| **辞書** | `{ : }`| **メニュー表（ラベル付き）**。名前（キー）で探せる。 | 商品名と価格、顧客IDと名前の紐付け |
| **セット** | `{ }` | **来店者名簿（重複なし）**。順番はないが重複を消せる。| アンケートのユニーク（重複なし）回答者数の確認 |

まずは、一番よく使う「リスト」と「辞書」を中心に学んでいきましょう！

---

## リスト (list) ：データを順番に並べよう

リストは、データを一列に並べて管理する箱です。角括弧 `[ ]` を使います。

### リストとインデックス・スライス
前回、文字列で「0番目から数える」ことを学びましたね。リストも全く同じです！

リスト
```{figure} images/python_slides/スライド60.PNG
:width: 70%
```


**【リストの図解】**
```text
  インデックス:    0        1        2
  リスト(sales): [ 450,     500,     400 ]
```

```python
sales = [ 450, 500, 400 ]
print(sales[0])
print(sales[1])
print(sales[2])
```

```
# カフェの売上をリスト（配列）にまとめます
sales = 

# インデックス（番号）でデータを取り出す
print("最初の売上:", sales)

# リストに対する関数・演算子・メソッド
print("データの個数(len):", len(sales))
print("合計金額(sum):", sum(sales))
print("売上データの連結:", sales + ) # リスト同士は + で繋げます
```

マイナスのインデックス
```{figure} images/python_slides/スライド61.PNG
:width: 70%
```

```python
sales = [ 450, 500, 400 ]
print(sales[-1])
print(sales[-2])
print(sales[-3])
```

リスト内の値の変更
```{figure} images/python_slides/スライド62.PNG
:width: 70%
```

```python
a = [10, 20, 30, 40, 50]
print(a)

a[0] = 99
print(a)

a[-1] = 'A'
print(a)
```

リストの要素数の確認
```{figure} images/python_slides/スライド63.PNG
:width: 70%
```

```python
a = [10, 20, 30, 40, 50]
len(a)
```

### リストを操作するメソッドなど（追加・削除・スライス）
リストは、後からデータを追加したり削除したりできます（これを「ミュータブル（変更可能）」と言います）。

* **破壊的（インプレース）な操作**: 元のリストそのものを書き換えてしまう操作。
* **非破壊的な生成**: 元のリストはそのままにして、新しいリストを作る操作（スライスなど）。

```
menu = ["コーヒー", "紅茶", "ケーキ"]

# 破壊的な操作（元のリストが変化します！）
menu.append("サンドイッチ")  # append: 最後に「追加」
print("追加後:", menu)

menu.remove("紅茶")          # remove: 指定したデータを「削除」
print("削除後:", menu)

# 非破壊的な操作（元のリストは変わりません）
sub_menu = menu[0:2]         # スライスで一部を切り出す
print("スライスした結果:", sub_menu)
print("元のリストはそのまま:", menu)
```

### 多重リスト
リストの中に、さらにリストを入れることができます（エクセルの表のようなイメージです）。
`[行][列]` のようにしてデータを取り出します。

### リストと文字列の相互変換
データクレンジング（データの掃除）でよく使います。
* `文字列.split()`: 文字列を分割してリストにする。
* `結合文字.join(リスト)`: リストの文字をくっつけて1つの文字列にする。

```
# 多重リスト（月曜と火曜の、午前・午後の客数）
visitors = [
   , # 月曜 [午前, 午後] (インデックス0)
     # 火曜 [午前, 午後] (インデックス1)
]
print("火曜の午後の客数:", visitors) # 1番目のリストの、1番目のデータ

# 文字とリストの変換
csv_data = "コーヒー,400,ケーキ,500"
data_list = csv_data.split(",") # カンマで分割！
print("リスト化:", data_list)
```

---

listクラスの主なメソッド
```{figure} images/python_slides/スライド149.PNG
:width: 70%
```

メソッドを使ったリストの操作
```{figure} images/python_slides/スライド150.PNG
:width: 70%
```

```
a = [10, 20, 30, 40]
a.append(100)
a
```

```
a.pop()
a
```

```
a.reverse()
a
```

```
a.index(20)
```

メソッド以外のリストの操作
```{figure} images/python_slides/スライド151.PNG
:width: 70%
```

```
a = [10, 20, 30, 40]
del a[1]
a
```

```
a = a + [0, 1]
a
```

```
1 in a
2 in a
len(a)
```

```
sorted(a)
sorted(a, reverse=True)
```

リストを含むリスト
```{figure} images/python_slides/スライド154.PNG
:width: 70%
```

---

### 内包表記

内包表記
```{figure} images/python_slides/スライド152.PNG
:width: 70%
```

```
data = [2**n for n in range(1, 11)]
```

```
[str(n)+'月' for n in range(1, 13)]
```

if 構文を含む内包表記
```{figure} images/python_slides/スライド153.PNG
:width: 70%
```

```
data0 = ['apple', 'orange', 'banana', 'avocado']
data1 = [s for s in data0 if s[0] == 'a']
print(data1)
```


---

## タプル (tuple)

タプルは、丸括弧 `( )` を使います。リストとそっくりですが、最大の決定的な違いがあります。
それは**「イミュータブル（変更不可能）」**であることです。一度作ったら、追加も削除も書き換えも絶対にできません！「絶対に間違えて書き換えたくない重要なデータ」を守るために使います。

タプル
```{figure} images/python_slides/スライド155.PNG
:width: 70%
```

( )の省略とアンパック代入
```{figure} images/python_slides/スライド156.PNG
:width: 70%
```

### 多重代入
タプルは、複数の変数に一気にデータを入れる（多重代入）ときによく隠れて使われています。

### オブジェクトの等価性(==)と同一性(is)
* `==`（等価性）: 箱の**「中身（値）」**が同じかどうか。
* `is`（同一性）: 箱**「そのもの（メモリ上の場所）」**が同じかどうか。
データ分析では基本的に `==` を使えば大丈夫です。

```
# タプルの作成
tax_rate = (0.08, 0.10) # 軽減税率と標準税率（変わってはいけない値！）
print("標準税率:", tax_rate)

# エラーになります！（タプルは書き換えられないため、TypeErrorが出ます）
# tax_rate = 0.15 

# 多重代入（カンマで区切ると、実はタプルとして処理されています）
morning_sales, afternoon_sales = 12000, 15000
print("午後の売上:", afternoon_sales)

# 等価性と同一性の違い（リストやタプルの比較演算）
list_A =
list_B =
list_C = list_A

print("中身は同じ？(==):", list_A == list_B) # True (中身は同じ)
print("箱も同じ？(is):", list_A is list_B) # False (別の箱として作られた)
print("CはAと同じ箱？(is):", list_A is list_C) # True (同じ箱を指している)
```

---

## 辞書 (dictionary) ：名前（キー）でデータを管理

辞書は、波括弧 `{ }` を使います。
リストが「番号」でデータを管理するのに対し、辞書は**「名前（キー）」と「値（バリュー）」をペア**にして管理します。

**【辞書の図解】**
```text
  [ キー (Key) ] ━▶ [ 値 (Value) ]
  "コーヒー"     ━▶   450
  "ケーキ"       ━▶   500
```

辞書
```{figure} images/python_slides/スライド158.PNG
:width: 70%
```

辞書の要素の取得
```{figure} images/python_slides/スライド159.PNG
:width: 70%
```

辞書の操作
```{figure} images/python_slides/スライド160.PNG
:width: 70%
```

```
info = {'firstname':'太郎', 'lastname':'山田', 'address': '茨城県つくば市 99-99'}
info.get('firstname')
info.get('tel')
info['tel'] = '090-000-0000'
info.get('tel')
del info['tel']
len(info)
'age' in info
'firstname' in info
```

辞書の要素の並べ替え
```{figure} images/python_slides/スライド161.PNG
:width: 70%
```

### 辞書の操作（メソッド）と、辞書とリストの組み合わせ
辞書は、キーを指定して値を取り出したり、新しいキーを作ってデータを追加したりできます。
また、リストの中に辞書を入れることで、複雑な顧客データなども綺麗に表現できます。

```
# 辞書の作成
menu_dict = {"コーヒー": 450, "ケーキ": 500}

# データの取り出し（番号ではなく、名前を使います！）
print("コーヒーの値段:", menu_dict["コーヒー"])

# データの追加と変更
menu_dict["紅茶"] = 400         # 新しいキーと値を追加
menu_dict["コーヒー"] = 480     # 既存のキーの値を変更（値上げ！）
print("変更後:", menu_dict)

# 辞書のメソッド（キーだけ、値だけを取り出す）
print("メニュー一覧:", menu_dict.keys())
print("価格一覧:", menu_dict.values())

# 辞書とリストの組み合わせ（顧客データの表現）
customers = [
    {"name": "山田", "age": 20},
    {"name": "佐藤", "age": 35}
]
print("2人目のお客様の名前:", customers["name"])
```

---

## set（セット）の特徴と活用

セットも波括弧 `{ }` を使いますが、辞書と違って「キーと値のペア」ではなく、単なるデータの集まりです。
最大の特徴は**「重複を許さない（同じデータは1つにまとめられる）」**ことです！

```
# アンケートで「好きな飲み物」を聞いた結果のリスト（重複があります）
survey_results = ["コーヒー", "紅茶", "コーヒー", "ジュース", "紅茶"]

# setに変換すると、重複が一瞬で消えます！（データ分析でよく使います）
unique_results = set(survey_results)
print("重複なしの回答:", unique_results)
```

セット
```{figure} images/python_slides/スライド162.PNG
:width: 70%
```

セットの操作
```{figure} images/python_slides/スライド163.PNG
:width: 70%
```

```
data = [1, 2, 3]
a = set(data)
a
set1 = {'A', 'B', 'C'}
set2 = {'B', 'C'}
set1.issubset(set2)
set2.issubset(set1)
```

セットどうしの演算（集合演算）
```{figure} images/python_slides/スライド164.PNG
:width: 70%
```

セットどうしの演算（集合演算）
```{figure} images/python_slides/スライド165.PNG
:width: 70%
```

```
set1 = {'A', 'B', 'C'}
set2 = {'A', 'B', 'D'}
set1 | set2
set1 & set2
set1 - set2
set1 ^ set2
```

---

## イミュータブル

基本型の性質
* 変更可能（ミュータブル）
* 反復可能（イテラブル）
* 順序を持つ（シーケンス

基本型の性質
```{figure} images/python_slides/スライド167.PNG
:width: 70%
```

ミュータブルな型とイミュータブルな型
```{figure} images/python_slides/スライド168.PNG
:width: 70%
```

反復可能なオブジェクト
```{figure} images/python_slides/スライド170.PNG
:width: 70%
```

順序を持つオブジェクト
```{figure} images/python_slides/スライド171.PNG
:width: 70%
```

---
## for文による繰り返しとデータ構造

**※この内容は第8回で詳しくやりますが、データ構造と相性が抜群なので少しだけ紹介します！**

リストや辞書の中身を1つずつ取り出して処理したいとき、`for` という魔法の言葉を使います。
「箱（リスト）の中から、1つずつデータを取り出して、一時的な変数に入れる」という処理を全自動でやってくれます。

```
orders = ["コーヒー", "ケーキ", "紅茶"]

# for文による繰り返しとリスト・タプル（文字列も同じように1文字ずつ繰り返せます）
print("=== 注文の読み上げ開始 ===")
for item in orders:
    print(item, "が入りました！") # インデント（字下げ）を忘れずに！
print("=== 読み上げ終了 ===")

# for文によるリスト初期化の短縮記法（内包表記とも呼ばれます）
# 値段リストのすべての要素を税込み(1.1倍)にする、といった時に1行で書けます。
prices = 
tax_in_prices = [p * 1.1 for p in prices]
print("税込み価格リスト:", tax_in_prices)
```

---

## 💻 演習課題

今日学んだリストと辞書を使って、カフェのデータ処理ツールを作ってみましょう。
エラーが出ても大丈夫。「あれ、丸括弧だっけ？角括弧だっけ？」と迷ったら、図解を見直してみてくださいね。

以下の `# TODO: ` の部分を書き換えて、プログラムを完成させてください。

```
# 演習1: リストの基本操作（難易度：易）
# カフェの今週の来客数データです。
# TODO: 客数に120を設定してください。
# TODO: 月曜日（一番最初）の客数を print してください。
# TODO: 日曜日（一番最後）に 150人 のデータを追加（append）して print してください。

visitors = 

# TODO: 月曜日（0番目）の客数を表示
print("月曜日の客数:", )

# TODO: append を使って 150 を追加
visitors.

print("日曜追加後:", visitors)
```

```
# 演習2: 文字列からリストへの変換（難易度：易）
# ネットショップから送られてきたカンマ区切りのデータがあります。
# TODO: split() メソッドを使って、カンマ(,)で区切ったリストに変換してください。

raw_csv = "抹茶ラテ,550,ホット,Mサイズ"

# TODO: split(",") を使ってリスト化する
item_data = 

print("リスト化されたデータ:", item_data)
```

```
# 演習3: 辞書を使った価格検索ツール（難易度：中）
# カフェのメニューと価格が辞書に登録されています。
# TODO: 辞書から「サンドイッチ」の価格を取り出して変数 price に入れてください。
# TODO: 新メニュー「スムージー」（600円）を辞書に追加してください。

menu_db = {"コーヒー": 400, "紅茶": 400, "サンドイッチ": 500}

# TODO: 辞書からキーを指定して値を取得する
price = 

print("サンドイッチの価格は", price, "円です。")

# TODO: 辞書に新しいキー「スムージー」と値「600」を追加する
menu_db[   ] = 

print("新メニュー追加後:", menu_db)
```

```
# 演習4: セット(set)を使った重複データの削除（難易度：中）
# ポイントカードの利用履歴から、今日来店した「ユニークな（重複のない）顧客ID」を抽出します。
# TODO: set() を使って duplicates_list から重複を消した unique_users を作成してください。
# TODO: len() 関数を使って、ユニークな顧客が「何人」いるか表示してください。

duplicates_list = ["ID01", "ID02", "ID01", "ID03", "ID02", "ID04"]

# TODO: 重複を削除する
unique_users = 

# TODO: len() を使って人数を数える
user_count = 

print("今日のユニークな来店者数は", user_count, "人です。")
```

```
# 演習5: 実践！多重リストと辞書の組み合わせ（難易度：難）
# これまでの総決算です。顧客の注文履歴が「辞書が入ったリスト」になっています。
# TODO: 2番目のお客様（佐藤さん）が注文した「item（商品名）」を取り出して print してください。
# ヒント：まずリストのインデックスを指定し、その後に辞書のキーを指定します。 orders[番号]["キー"]

order_history = [
    {"name": "山田", "item": "コーヒー", "amount": 1},
    {"name": "佐藤", "item": "ケーキ", "amount": 2},
    {"name": "鈴木", "item": "紅茶", "amount": 1}
]

# TODO: 佐藤さんの item を取り出す
sato_item = 

print("佐藤さんのご注文は", sato_item, "です。")
```

---
## 💡 演習問題の解答例

（どうしても分からない時や、答え合わせをしたい時に展開して見てくださいね。）

<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

**演習1の解答**
```python
print("月曜日の客数:", visitors) # 120 と出れば正解！
visitors.append(150)
```

**演習2の解答**
```python
item_data = raw_csv.split(",")
```

**演習3の解答**
```python
price = menu_db["サンドイッチ"]
menu_db["スムージー"] = 600
```
※ 辞書に追加するときは `append` ではなく、新しいキーを指定して代入します。

**演習4の解答**
```python
unique_users = set(duplicates_list)
user_count = len(unique_users) # 4人 と出れば大正解！
```

**演習5の解答**
```python
sato_item = order_history["item"]
# 「ケーキ」と表示されれば完璧です！リストので佐藤さんの辞書を取り出し、["item"]で商品名を取り出します。
```

</details>

---
## 📚 第5回のまとめ

### ✨ 今日の重要ポイントまとめ
*   **リスト `[ ]`**: 順番のあるデータの集まり。インデックスで取り出し、`append` で追加。（ミュータブル＝変更可能）
*   **タプル `( )`**: 変更してはいけないデータの集まり。（イミュータブル＝変更不可能）
*   **辞書 `{ }`**: キー（名前）と値のペア。「名前」で検索できる強力なメニュー表。
*   **セット `{ }`**: 重複を許さないデータの集まり。ユニークな（重複のない）データ数を知りたい時に便利。

### ❓ よくある質問と回答 (FAQ)
*   **Q. `TypeError: 'tuple' object does not support item assignment` というエラーが出ました。**
    *   **A.** タプル（丸括弧 `()` で作ったデータ）の中身を、後から書き換えようとしていませんか？ タプルは「イミュータブル（変更不可能）」という頑固な性質を持っています。書き換えたい場合は、リスト `[]` を使うようにコードを修正しましょう！
*   **Q. リストと辞書、どちらを使えばいいか迷います。**
    *   **A.** 「順番」に意味があるか、「名前（ラベル）」に意味があるかで決めます。例えば「1日目、2日目、3日目の気温」なら順番に並べるリストが便利です。「国語の点数、数学の点数」なら、数字だけ並べても後から分からなくなるので、科目名をキーにした辞書が便利です。

---

### 🌟 Colabで試すおすすめコード
リスト、辞書、for文を組み合わせた「カフェの売上自動集計プログラム」です。
データ分析の現場で実際に使う処理の基本形です。実行してみてください！
```

<!-- コードセル -->
# 1日の注文履歴（辞書のリスト）
daily_orders = [
    {"item": "コーヒー", "price": 450},
    {"item": "ケーキ", "price": 500},
    {"item": "コーヒー", "price": 450}
]

# 合計金額を入れる箱（最初は0円）
total_sales = 0

# for文で1つずつ注文を取り出して、値段を足していく
for order in daily_orders:
    total_sales = total_sales + order["price"]

print("本日の合計売上は", total_sales, "円です！")
```
