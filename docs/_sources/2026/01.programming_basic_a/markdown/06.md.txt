かしこまりました。第6回の講義資料（Google Colab用）を作成いたしました。

今回は、プログラムが自ら判断を下す「条件分岐（if文）」と、外部のデータを読み書きする「ファイル入出力」がテーマです。データ分析において「特定の条件のデータだけを抽出する」という必須スキルを身につける重要な回となります。

前回までの内容を踏まえ、今回もカフェの身近な例やアスキーアートを豊富に取り入れ、文系の学生が安心して学べるよう設計しています。以下のテキストをコピーし、Google Colabのセルに貼り付けてご利用ください。

---

### 第6回の講義資料（Google Colab用）

```markdown
<!-- Markdownセル -->
# 第6回 条件分岐、ファイル入出力の基本

皆さん、こんにちは！プログラミング基礎A、第6回の講義です。担当の教員です。
ここまでよく頑張ってきましたね！前回までは、すでに用意されたデータを計算したり、整理したりする方法を学びました。

今回は、いよいよ**「プログラムに自分で判断させる力（条件分岐）」**と、**「自分のパソコンにある外部データを読み込む力（ファイル入出力）」**を身につけます。
この2つが使えるようになると、「数万行の売上データ（ファイル）を読み込んで、雨の日のデータ（条件）だけを抽出して計算する」といった、本格的なデータ分析の自動化ができるようになります。

「再帰」や「with文」など新しい言葉も出てきますが、**大丈夫です！** カフェの「雨の日割引のルール」や「自動ドア」に例えて一つずつ丁寧に解説します。一緒に一歩ずつ進んでいきましょう！

---
## 🎯 到達目標 (約5分)
この授業が終わるころには、以下のことができるようになります。
1. **if / elif / else 文** を使って、条件によって異なる処理を行うプログラムを作れる。
2. **論理演算子 (and, or, not)** や比較演算子を用いて、複雑な条件式を作れる。
3. **ファイル入出力 (with文)** を使って、外部データの読み込みや書き込みができる。
4. ファイル読み込み時の**文字コード指定**や、不要な**改行文字の削除**など、データ分析の下準備（前処理）ができる。

---
## 6.1 イントロダクション：プログラムに「判断」と「記憶」をさせよう (約5分)

これまで私たちが書いてきたプログラムは、上から下へ「一直線」に進むだけでした。しかし、実際のカフェの業務やデータ分析では、条件によって対応を変える必要があります。

*   **条件分岐（if文）**: 「もしお客様が会員なら、5%割引にする」「もし気温が30度以上なら、アイスコーヒーの売上としてカウントする」といった**判断の分かれ道**を作ります。
*   **ファイル入出力**: これまでは毎回コードの中にデータを直接書き込んでいましたが、現実のデータはExcelやテキストファイルとして存在します。それをPythonの世界に**「読み込み（インプット）」**、分析結果をファイルとして**「書き出す（アウトプット）」**方法を学びます。

---
## 6.2 条件分岐（if文）と論理・比較演算 (約20分)

### if / elif / else による条件分岐
Pythonでは、`if`（もし〜なら）というキーワードを使って条件分岐を書きます。
ポイントは、第3回で学んだ**インデント（字下げ）**です！条件に当てはまった時だけ実行したい処理は、必ずインデントを下げて書きます。

**【条件分岐のイメージ図】**
```text
          [スタート：お会計]
                  │
      ╭───── (if: 会員ですか？) ─────╮
   【Yes】                        【No】
      │                             │
 [ 5%割引！ ]              [ 割引なし（通常価格）]
      │                             │
      ╰─────────── v ───────────────╯
                  │
           [エンド：お支払い]
```

```

<!-- コードセル -->
# 基本的な if / else 文
is_member = True  # 会員かどうか（True または False）
price = 1000

if is_member:
    # ここは is_member が True の時だけ実行されます
    print("会員様ですね。50円割引します！")
    price = price - 50
else:
    # それ以外の時（False の時）はここが実行されます
    print("一般のお客様ですね。通常価格です。")

print("お会計は", price, "円です。")

<!-- Markdownセル -->
### 様々な条件（比較演算子）
数値の大小などを比べるための記号です。
*   `A == B` : AとBは等しい（※ `=` が2つなことに注意！1つは代入です）
*   `A != B` : AとBは等しくない
*   `A > B`  : AはBより大きい
*   `A >= B` : AはB以上（含む）

### 3つ以上の分岐（elif）
「もし〜なら、そうでなくて〜なら、それ以外は」というように、複数の条件を繋ぐには `elif` (else if の略) を使います。

```

<!-- コードセル -->
# if / elif / else 文と比較演算子
temperature = 28

if temperature >= 30:
    print("猛暑です！フラペチーノをたくさん仕入れましょう。")
elif temperature >= 25:
    print("夏日です。アイスコーヒーがよく売れそうです。")
else:
    print("涼しいです。ホットコーヒーの準備をしましょう。")

<!-- Markdownセル -->
### 論理演算子（and, or, not）で条件を組み合わせる
データ分析では、「20代」**かつ**「女性」のデータを抽出したい！といった場面がよくあります。
*   `A and B`（かつ）: AもBも両方TrueならTrue
*   `A or B`（または）: AかBのどちらかがTrueならTrue
*   `not A`（〜ではない）: AがFalseならTrueになる（ひっくり返す）

```

<!-- コードセル -->
# 論理演算子 (and, or)
day = "水曜"
weather = "雨"

# 水曜日「または」雨の日はポイント2倍！
if day == "水曜" or weather == "雨":
    print("本日はポイント2倍デーです！")

<!-- Markdownセル -->
### 真理値を返す関数、オブジェクト、None、条件として使われる他の値
*   **真理値を返す関数**: `is_member()` のように、呼び出すと `True` か `False` を返す関数を作ることができます。
*   **None（ノン）**: 「データが空っぽ（何もない）」ことを表す特別なオブジェクトです。データ分析の欠損値処理などで活躍します。
*   **条件として使われる他の値**: 実はPythonでは、`True/False` 以外の値も if 文の条件として使えます。**「0」や「空の文字列("")」「空のリスト([])」は、自動的に `False` として扱われます**。それ以外は `True` です。

### 3項演算子（条件式）
簡単な条件分岐なら、1行でスッキリ書くことができます（`値1 if 条件 else 値2`）。

### 再帰（さいき）について
※少し高度な概念として「再帰（関数の中で自分自身の関数を呼び出すこと）」がありますが、初心者は無限ループに陥りやすいため、まずは通常の if 文や次回学ぶ for 文をマスターすれば大丈夫です！

```

<!-- コードセル -->
# None と 空のデータの判定
stock_list = []  # 空のリスト（在庫なし）

# リストが空っぽだと、False 扱いになる性質を利用します！
if not stock_list:
    print("在庫が空っぽです！発注してください。")

# 3項演算子（1行で書くif文）
age = 20
# 年齢が20以上なら"大人"、そうでないなら"未成年"を変数 status に入れる
status = "大人" if age >= 20 else "未成年"
print("このお客様は", status, "です。")

<!-- Markdownセル -->
---
## 6.3 ファイル入出力と文字列操作 (約25分)

ここからは、Pythonに外部のテキストファイルを読み込ませる方法です。データ分析の第一歩ですね！

### ファイルとは？ ファイルのオープンとクローズ
パソコンの中にあるデータは、本棚の本のようなものです。
読むためには、**「開く（オープン）」**し、読み終わったら**「閉じる（クローズ）」**必要があります。閉じ忘れると、他のアプリがそのファイルを触れなくなったり、パソコンのメモリを無駄遣いしてしまいます（これが「編集中のファイルの動作」の問題です）。

### `open` と `with` 文（自動ドアの魔法！）
昔は手動で `f.close()` と書いて閉じていましたが、今は**`with` 文**という便利な書き方があります。これは**「処理が終わったら自動でファイルを閉じてくれる自動ドア」**のようなものです。絶対に `with` 文を使いましょう！

**【with文のイメージ図】**
```text
 🚪 [ 自動ドア (with open...) ] ──╮ 
 │                               │ インデントが下がっている間だけ、
 │  ファイルの中身を読み書きする │ ファイルが開いた状態になる
 │                               │
 ╰── (インデントが終わると自動でドアが閉まる！) 
```

### ファイルの読み書きにおける文字コード指定
第1回で「文字コード（UTF-8 など）」について触れましたね。ファイルを読み込む時、Pythonに「このファイルは UTF-8 という言語で書かれているよ」と教えてあげる必要があります。これを忘れると、**文字化け**やエラー（`UnicodeDecodeError`）の原因になります。

```

<!-- コードセル -->
# （準備）Colabの仮想環境に、練習用のテキストファイルを作成します。
# 以下のコードを実行して、"sales_memo.txt" というファイルを作ってください。
sample_text = "コーヒー,450\nケーキ,500\n紅茶,400\n"
with open("sales_memo.txt", "w", encoding="utf-8") as f:
    f.write(sample_text)
print("練習用ファイルを作成しました！")

<!-- Markdownセル -->
### ファイルの読み込み（行の読み込み、全体の読み込み）
*   `f.read()` : ファイル全体を一気に1つの文字列として読み込みます。
*   `f.readlines()` : ファイルを1行ずつリストにして読み込みます。

### 改行文字の削除と文字列メソッド
ファイルを読み込むと、行の終わりに「目に見えない改行マーク (`\n`)」がくっついてきます。
データ分析ではこれが非常に邪魔になるため、第4回で学んだ文字列メソッド **`strip()`**（前後の空白や改行を消す）を使って掃除（データクレンジング）をするのが鉄則です！

```

<!-- コードセル -->
# with文を使ったファイルの読み込み（"r" は読み込み専用という意味です）
with open("sales_memo.txt", "r", encoding="utf-8") as f:
    # readlines() で1行ずつリストにして読み込む
    lines = f.readlines()

print("▼ 読み込んだ生のデータ（改行記号 \\n がついている）")
print(lines)

print("\n▼ データクレンジング後")
# リストから最初の1行目（インデックス0）を取り出す
first_line = lines

# strip() メソッドで不要な改行記号を削除し、split(",") でリストに分割する！
clean_data = first_line.strip().split(",")
print(clean_data)

<!-- Markdownセル -->
### ファイルへの書き込み
新しくファイルを作ったり、結果を保存したりするには、モードを **`"w"`** (write: 上書き) または **`"a"`** (append: 追記) にして開きます。

```

<!-- コードセル -->
# "a" モードで開いて、新しいデータを一番下に追加します
with open("sales_memo.txt", "a", encoding="utf-8") as f:
    f.write("抹茶ラテ,550\n") # \n（改行）を忘れずに入れる！

print("新しいメニューをファイルに書き込みました！")
# ※画面左側のフォルダアイコン📁を開くと、「sales_memo.txt」があるのが確認できます。

<!-- Markdownセル -->
---
## 💻 演習課題 (約50分)

さあ、今日学んだ「条件分岐」と「ファイル操作」を組み合わせて、実践的な課題に挑戦しましょう！
講義と演習の時間は約半分ずつです。エラーが出ても焦らず、どこで躓いているのか一緒に考えていきましょう。

以下の `# TODO: ` の部分を書き換えて、プログラムを完成させてください。
```

<!-- コードセル -->
# 演習1: if / else による条件分岐（難易度：易）
# カフェのスタンプカードは、10個貯まると「無料ドリンクチケット」がもらえます。
# TODO: stamps の値が 10 以上なら "チケット獲得！"、そうでないなら "あと少し！" と print してください。

stamps = 8

# TODO: if文を使って条件分岐を書こう（インデントに気をつけて！）
if 
    print("チケット獲得！")
else:
    

<!-- コードセル -->
# 演習2: 論理演算子と elif（難易度：易）
# ケーキ（500円）の賞味期限による値引きシステムです。
# 期限まで 1日 なら 20%引き、0日（今日まで）または マイナス（期限切れ） なら 廃棄（0円）にします。
# TODO: 条件式を完成させてください。

days_left = 0
price = 500

# TODO: days_left が 0以下（0またはマイナス）なら廃棄
if 
    price = 0
    print("期限切れのため廃棄します。")
# TODO: そうではなく、days_left が 1 と等しいなら 20%引き (price * 0.8)
elif 
    price = price * 0.8
    print("割引価格になります。")
else:
    print("通常価格です。")

print("販売価格:", price, "円")

<!-- コードセル -->
# 演習3: with文を使ったファイルの読み込み（難易度：中）
# 事前準備で作成した "sales_memo.txt" を読み込みます。
# TODO: with open を使ってファイルを開き、f.read() で中身をすべて読み込んで表示してください。

# TODO: with open(...) を完成させる（文字コード encoding="utf-8" も忘れずに！）
with 
    # TODO: f.read() の結果を変数 all_data に入れる
    all_data = 

print("ファイルの内容:\n" + all_data)

<!-- コードセル -->
# 演習4: 文字列の掃除と条件判定（難易度：中）
# ファイルから読み込んだような、不要な空白や改行が混ざった文字列があります。
# TODO: strip() で掃除した後、中身が "コーヒー" と一致するか if文で判定してください。

raw_text = "  コーヒー \n"

# TODO: strip() メソッドで空白と改行を消す
clean_text = 

# TODO: clean_text が "コーヒー" と等しいか判定する
if 
    print("これはコーヒーのデータです！")
else:
    print("コーヒーではありません。")

<!-- コードセル -->
# 演習5: 実践！ファイルへの書き込みと3項演算子（難易度：難）
# 本日の売上目標（10000円）を達成したかどうかを判定し、その結果を新しいファイルに記録します。
# TODO: 3項演算子を使って、売上が10000以上なら "達成"、未満なら "未達成" を result に入れてください。
# TODO: その result の文字を、"report.txt" に書き込んで保存してください。

today_sales = 12500

# TODO: 3項演算子 ( 値1 if 条件 else 値2 ) を使う
result = 

# TODO: with open を "w" モードで使い、report.txt に result を書き込む(write)
with 
    

print("レポートを出力しました！（左のフォルダ📁から report.txt を確認してみましょう）")

<!-- Markdownセル -->
---
## 💡 演習問題の解答例

（どうしても分からない時や、答え合わせをしたい時に展開して見てくださいね。エラーメッセージが出たらAIに質問してみましょう！）

<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

**演習1の解答**
```python
if stamps >= 10:
    print("チケット獲得！")
else:
    print("あと少し！")
```

**演習2の解答**
```python
if days_left <= 0:  # または days_left == 0 or days_left < 0
    price = 0
    print("期限切れのため廃棄します。")
elif days_left == 1:
    price = price * 0.8
    print("割引価格になります。")
```

**演習3の解答**
```python
with open("sales_memo.txt", "r", encoding="utf-8") as f:
    all_data = f.read()
```

**演習4の解答**
```python
clean_text = raw_text.strip()
if clean_text == "コーヒー":
    print("これはコーヒーのデータです！")
```
※ `strip()` をしないと、「  コーヒー \n」と「コーヒー」は違うものだと判断され、一致しません！

**演習5の解答**
```python
result = "達成" if today_sales >= 10000 else "未達成"

with open("report.txt", "w", encoding="utf-8") as f:
    f.write(result)
```

</details>

---
## 📚 第6回のまとめ

お疲れ様でした！今日は、プログラムを賢くする「条件分岐」と、データを外の世界とやり取りする「ファイル入出力」を学びました。これができると、データ分析の幅がぐっと広がります！

### ✨ 今日の重要ポイントまとめ
*   **if / elif / else**: インデントを使って、条件ごとに処理を分けることができる。
*   **比較演算子と論理演算子**: `==`, `>=`, `and`, `or` を使って、複雑な抽出条件を作る。
*   **with open()**: ファイルを読み書きする時は、閉じ忘れを防ぐ「自動ドア」の `with` 文を使う。
*   **文字コードと改行**: 読み込む時は `encoding="utf-8"` を指定し、読み込んだ文字列は `strip()` で綺麗にする（データクレンジング）。

### ❓ よくある質問と回答 (FAQ)
*   **Q. `SyntaxError: expected ':'` というエラーが出ました。**
    *   **A.** 文系学生がif文で一番よくやるミスです！ `if` や `elif`, `else`, そして `with` の行の最後には、**必ずコロン（`:`）が必要**です。コロンを忘れていないかチェックしてください。
*   **Q. `FileNotFoundError: [Errno 2] No such file or directory` というエラーが出ました。**
    *   **A.** 「そんな名前のファイルは見つからないよ！」というエラーです。ファイル名のスペルミスがないか、あるいは「読み込む前に、ちゃんとファイルを作成（アップロード）しているか」を確認しましょう。Colabでは左側のフォルダアイコンから確認できます。
*   **Q. Macで作ったファイルを読み込むとエラーになります…。**
    *   **A.** 文字コードの問題かもしれません。MacやWindowsによって標準の文字コードが違うことがあるため、`open()` の中に必ず `encoding="utf-8"` を入れるクセをつけておくと安心です。

### 🌟 Colabで試すおすすめコード
今日学んだことを全て組み合わせた「売上データの自動集計プログラム」です。
ファイルからデータを読み込み、コーヒーの売上だけを合算します。
```

<!-- コードセル -->
# （準備）売上データファイルを作成します
with open("daily_sales.txt", "w", encoding="utf-8") as f:
    f.write("コーヒー,450\nケーキ,500\nコーヒー,450\n紅茶,400\n")

# コーヒーだけの売上合計を入れる箱
coffee_total = 0

# ファイルを読み込みます
with open("daily_sales.txt", "r", encoding="utf-8") as f:
    lines = f.readlines() # 1行ずつリストにする

# まだ学んでいませんが、次回詳しくやる for文（繰り返し）を使います！
for line in lines:
    clean_line = line.strip()       # 1. 改行などのゴミを消す
    data_list = clean_line.split(",") # 2. カンマで区切ってリストにする
    
    item_name = data_list # 商品名
    item_price = int(data_list) # 値段（計算したいので int で数値に変換！）
    
    # 3. もし商品名がコーヒーなら、合計に足す！
    if item_name == "コーヒー":
        coffee_total = coffee_total + item_price

print("本日のコーヒーの売上合計は", coffee_total, "円です！")
```
```markdown
<!-- Markdownセル -->
次回（第7回）は、「テスト・デバッグ、コードスタイル」です。エラーが出た時の上手な直し方や、誰もが読みやすい美しいコードの書き方を学びます。今日はお疲れ様でした！
```

---

### 【教員（AI）からの工夫ポイント解説】
1. **概念の日常化とアスキーアート**: 「if文」をカフェの割引判定ルートとして、「with文」を自動で閉まる自動ドアとしてアスキーアートで図解し、抽象的な概念を視覚的・直感的に把握できるよう工夫しました。
2. **データクレンジングへの接続**: テキストファイルの読み込みにおいて、ただ文法を教えるだけでなく、「読み込んだデータには目に見えない改行(`\n`)が含まれており、`strip()`を使って掃除しないと正しい条件判定ができない」という、実際のデータ分析で必ず直面する罠を講義と演習（演習4）で体験させる構成にしました。
3. **初学者の典型的なエラーの先回り**: `if`文の末尾のコロン(`:`)忘れによる`SyntaxError`や、ファイルの読み込み失敗による`FileNotFoundError`、文字コードによるエラーなど、初心者がパニックになりやすいポイントをFAQで詳しく解説し、自己解決能力を養えるようにしています。
4. **トピックの網羅と時間配分の厳守**: `Weekly Topics Summary.md` に指定された非常に多くのトピック（3項演算子、真理値、None、再帰、with文など）を、105分という枠（講義約55分・演習約50分）の中で消化不良を起こさないよう、実務で必須なもの（with文、if文）は手厚く、概念的なもの（再帰など）は軽く触れる程度に濃淡をつけてすべて網羅しました。
5. **自己肯定感の向上**: 各所で「大丈夫です」「ここまでよく頑張りました」といった寄り添う声かけ（ペルソナ）を徹底しています。

内容をご確認いただき、修正等のご要望がなければ、続いて**第7回「テスト・デバッグ、コードスタイル」**の教材を出力いたします。いかがでしょうか？