# 第13回 総合演習

皆さんの手でゼロからプログラムを作り上げる**「ミニプロジェクト（総合演習）」**に挑戦していただきます。
完成させることが目的ではなく、エラーと格闘しながら「試行錯誤するプロセス」を体験してください

---

## 🎯 到達目標 

1. **プロジェクト設計・実装** の流れを理解し、自力で簡単なプログラムを組み立てることができる。
2. 他の人が読んでも分かりやすいよう、**コードの整理とコメント** を書くことができる。
3. エラーが出ても、これまで学んだ**テストとデバッグ**の知識を使って自己解決（AIの活用含む）ができる。
4. 全13回の**学習の振り返り**を行い、「プログラミング応用A」への**次の一歩**を踏み出せる。

---

### 練習問題のGoogle Colab起動
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](
https://colab.research.google.com/github/homata/musashiuniv/blob/master/docs/exercises/2026/01.programming_basic_a/basic_13.ipynb)

1. 上記の「Open In Colab」をクリックして、Google Colabを起動させてください
2. [ファイル] → [ドライブにコピーを保存] を選択して、Googleドライブにコピーを保存してください
3. 保存が成功すると，Googleドライブの「マイドライブ」 →「Colab Notebooks」にコピーしたファイルが保存されます。
4. GoogleドライブにコピーしたGoogle ColabのNotebooksを開いてください
5. 開いたGoogle ColabのNotebooksで作業をしてください

---

## イントロダクション：なぜデータ分析を学ぶのか？

演習に入る前に、私たちがこの全13回で学んできた「データ分析」が、これからの社会でいかに重要かをお話しします。

### データは「21世紀の新しい石油」
皆さんが文系であっても、将来どんな仕事（営業、企画、マーケティング、公務員など）に就くとしても、今は「エビデンス（根拠）に基づく意思決定」が求められる時代です。
「なんとなく最近この商品が売れている気がする」ではなく、「データ分析の結果、20代女性のリピート率が先月比で15%上昇しているため、ここに予算を投じましょう」と言える人材が、どこの組織でも喉から手が出るほど欲しがられています。

**【データ分析の価値（イメージ図）】**
```text
[ 混沌としたデータ (原油) ]
  売上、気温、アンケート結果... そのままでは使えない
       │
       ▼ (Pythonの魔法: pandas, NumPy)
[ 📊 整理・集計 (精製) ]
  欠損値を消し、グループ化して集計する
       │
       ▼ (Pythonの魔法: matplotlib, seaborn)
[ 💡 価値ある洞察 (ガソリン) ]
  グラフ化することで「傾向」が見え、ビジネスの意思決定に繋がる！
```

---
## プロジェクト設計・実装のステップ

いきなりコードを書き始めるのは失敗のもとです。プロは必ず「設計」から始めます。
ミニプロジェクトを進めるときは、以下の3ステップを意識しましょう。

1. **設計（要件定義）**: 「どんなデータ（変数）を扱うか？」「どんな結果を出力したいか？」を日本語でメモします。
2. **実装（コーディング）**: メモをもとに、1行ずつコードを書いていきます。一気に全部書かず、少し書いたら実行して `print()` で中身を確認するのがコツです。
3. **テスト・デバッグ**: わざと変なデータ（マイナスの金額など）を入れてみて、エラーで止まらないか（例外処理ができているか）を確認します。

```
# （例）カフェの割引計算システムの「設計から実装」

# 1. 設計（コメントで書く）
# - 価格(price)と割引率(rate)を受け取る関数を作る
# - 割引後の価格を計算して返す
# - もし割引率が0未満ならエラーを防ぐ

# 2. 実装（コードを書く）
def calculate_discount(price, rate):
    # 3. テスト・デバッグ（安全装置を入れる）
    if rate < 0:
        print("エラー：割引率は0以上にしてください")
        return price
    
    final_price = price * (1 - rate)
    return final_price

# テスト実行
print(calculate_discount(1000, 0.2)) # 正常
print(calculate_discount(1000, -0.1)) # 異常値への対応
```

---
## コードの整理とコメント

**「動けばいい」から卒業し、「美しいコード」を目指しましょう。**

*   **コメントは「何をしているか（Why）」を書く**: `a = a + 1` の上に `# aに1を足す` と書くのは不要です（見ればわかるため）。`# 来客カウンターを更新する` のように、**なぜその処理をしているか**を書きましょう。
*   **マジックナンバーを避ける**: コードの中に突然 `0.1` などの数字を書かず、`TAX_RATE = 0.1` のように変数にして意味を持たせます。

```
# ❌ 悪い例（意味がわからない）
p = 1000
r = p * 1.1

# ⭕ 良い例（コードの整理とコメントができている）
TAX_RATE = 1.1
coffee_price = 1000

# 税込み価格を計算し、レジに表示するため変数に保存する
final_price = coffee_price * TAX_RATE
```

---
## 演習課題の説明と評価について

これから、皆さんに難易度別のミニプロジェクト（演習1〜5）に取り組んでいただきます。

### 難易度と採点について
*   **初級（基本文法中心）**: 変数、リスト、関数などの基礎。
*   **中級（データ処理中心）**: pandasやmatplotlibを使った、実務に近いデータ分析。
*   **上級（応用）**: クラス（オブジェクト指向）や例外処理を使った本格的なシステム。

**【重要】難易度によって成績（採点）は変わりません！**
上級が解けなかったからといって減点されることはありません。初級を丁寧に完璧に仕上げるのも、上級に果敢に挑んでエラーと格闘するのも、どちらも素晴らしい「学習のプロセス」として高く評価します。自分のレベルに合わせて、自由に選んで（もちろん全部やってもOK！）挑戦してください。

---

## 💻 総合演習（ミニプロジェクトに挑戦！）

それでは、いよいよ本番です。時間をたっぷり使って取り組んでください。

```
# 演習1: 【初級】簡易TODOリスト管理ツール（難易度：易）
# 辞書のリストを使って、タスクを管理します。
# TODO: 未完了（status が "未"）のタスクだけを取り出し、画面に表示するプログラムを書いてください。

tasks = [
    {"task": "Pythonの復習", "status": "済"},
    {"task": "レポート提出", "status": "未"},
    {"task": "カフェでバイト", "status": "未"}
]

print("--- 今日の未完了タスク ---")
# TODO: for文とif文を組み合わせて、未完了のタスク名だけを print する
for 
    if 
        print("・" + )
```

```
# 演習2: 【初級】電卓＋単位変換ツール（難易度：易）
# 気温を「摂氏（℃）」から「華氏（℉）」に変換する関数を作ります。
# 華氏 = (摂氏 × 1.8) + 32 という計算式になります。
# TODO: 計算式を完成させ、気温のリスト temperatures_c をすべて華氏に変換したリストを作ってください。

def convert_c_to_f(celsius):
    # TODO: 変換の計算式を書く
    fahrenheit = 
    return fahrenheit

temperatures_c = # 摂氏のリスト
temperatures_f = []           # 華氏を入れる空リスト

# TODO: for文を使って、temperatures_c の中身を一つずつ変換し、temperatures_f に追加(append)する
for 
    
    

print("摂氏:", temperatures_c)
print("華氏:", temperatures_f) # [68.0, 77.0, 86.0] になれば正解！
```

```
# 演習3: 【中級】家計簿管理アプリ（入力→集計）（難易度：中）
# これまでの買い物の履歴（辞書のリスト）をpandasを使って集計します！
# TODO: pandasのデータフレームに変換し、「カテゴリごと」の「金額の合計」を出してください。

import pandas as pd

kakeibo_data = [
    {"日付": "7/1", "カテゴリ": "食費", "金額": 1200},
    {"日付": "7/2", "カテゴリ": "交際費", "金額": 3000},
    {"日付": "7/3", "カテゴリ": "食費", "金額": 800},
    {"日付": "7/4", "カテゴリ": "交通費", "金額": 500}
]

# TODO: リストをデータフレーム df に変換する
df = 

# TODO: groupby を使って、「カテゴリ」ごとの「金額」の合計(sum)を出す
category_sum = 

print("--- カテゴリ別の支出合計 ---")
print(category_sum)
```

```
# 演習4: 【中級】CSVデータを読み込んで統計量を表示＋グラフ化（難易度：中）
# 店舗の売上データを集計し、グラフで可視化する本格的なデータ分析です。
# TODO: DataFrameの売上データを棒グラフにしてください。

import matplotlib.pyplot as plt
import japanize_matplotlib # 日本語化

# （準備）売上データフレーム
sales_df = pd.DataFrame({
    "店舗": ["東京", "大阪", "名古屋", "福岡"],
    "売上": 
})

print("基本統計量:")
# TODO: describe() を使って売上の統計量（平均や最大値など）を表示する
print(sales_df["売上"].         )

# TODO: plt.bar() を使って、X軸に「店舗」、Y軸に「売上」を指定して棒グラフを描く
plt.

plt.title("店舗別売上")
plt.ylabel("売上（円）")
plt.show()
```

```
# 演習5: 【上級】簡単な在庫管理システム（クラス使用）（難易度：難）
# クラスと例外処理を使った、少し高度なプログラミングに挑戦します！
# TODO: Itemクラスを完成させ、在庫(stock)が足りない場合は ValueError を起こすようにしてください。

class Item:
    # 設計図（初期化）
    def __init__(self, name, stock):
        self.name = name
        self.stock = stock

    # 商品を売る（在庫を減らす）メソッド
    def sell(self, amount):
        # TODO: もし amount が 現在の在庫(self.stock) より大きいなら、
        # raise ValueError("在庫が足りません！") を実行する
        if 
            
            
        # 在庫から amount を引く
        self.stock = self.stock - amount
        print(self.name, "を", amount, "個売りました。残り:", self.stock)

# --- テスト実行 ---
cake = Item("チーズケーキ", 10)

try:
    cake.sell(3)  # 3個売れるはず
    cake.sell(10) # 在庫7個に対して10個売ろうとするのでエラーになるはず！
except ValueError as e:
    print("エラー発生:", e)
```

---

## 💡 演習問題の解答例

（エラーが出てもすぐに諦めず、ぜひ「なぜ動かないのか」を考えてみてください。それが一番の勉強になります！）

<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

**演習1の解答**
```python
for item in tasks:
    if item["status"] == "未":
        print("・" + item["task"])
```

**演習2の解答**
```python
def convert_c_to_f(celsius):
    fahrenheit = (celsius * 1.8) + 32
    return fahrenheit

for c in temperatures_c:
    f = convert_c_to_f(c)
    temperatures_f.append(f)
```
※ リスト内包表記を使って `temperatures_f = [convert_c_to_f(c) for c in temperatures_c]` と1行で書けた方は、パーフェクトです！

**演習3の解答**
```python
df = pd.DataFrame(kakeibo_data)
category_sum = df.groupby("カテゴリ")["金額"].sum()
# 交際費 3000、食費 2000、交通費 500 と表示されていれば大正解です。
```

**演習4の解答**
```python
print(sales_df["売上"].describe())
plt.bar(sales_df["店舗"], sales_df["売上"], color="skyblue")
# きれいな棒グラフが表示されましたね！
```

**演習5の解答**
```python
    def sell(self, amount):
        if amount > self.stock:
            raise ValueError("在庫が足りません！")
        # (以下略)
```
※ クラス（設計図）の中で自分自身のデータを使う時は `self.` をつけるのがポイントでしたね。

</details>

---
## 📚 第13回のまとめと、次の一歩

### 🔄 学習の振り返りとQ&A
*   **Q. エラーが出た時、最初はパニックになっていましたが、少し読めるようになりました！**
    *   **A.** それこそが、この授業で得た最大の財産です！「エラーはコンピュータからの優しいお手紙」です。エラーを怖がらずに読み解く力（デバッグ力）は、どんな仕事でも役立つ論理的思考力そのものです。
*   **Q. 授業が終わった後も勉強を続けたいですが、どうすればいいですか？**
    *   **A.** 今日作った「家計簿」や「TODOリスト」を、自分なりに改造してみてください。例えば「グラフの色を変えてみる」「新しい機能を追加してみる」といった遊び心が、一番成長を促してくれます。

### 🚀 次の一歩を示す：「プログラミング応用A」へ！

この「プログラミング基礎A」で、皆さんはデータの「収集・整理・可視化」という強力な武器を手に入れました。

秋学期の**「プログラミング応用A（コンピューティング応用G）」**では、この基礎を活かして、以下のようにより高度でワクワクする世界へ進みます！

*   **デジタルツイン / 地理空間情報 (leafmap)**: 地図の上にデータを重ね合わせて、現実世界を可視化する。
*   **機械学習の基礎**: 教師なし学習などを使い、AI（人工知能）の裏側の仕組みを自分で作る。

皆さんがこの授業で培った「試行錯誤する力」があれば、応用クラスでも必ず活躍できます。
これからもPythonを楽しみながら、データを使って社会の課題を解決できる人材になってくださいね。応援しています！
