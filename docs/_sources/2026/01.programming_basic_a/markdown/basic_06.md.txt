# 第6回 条件分岐、ファイル入出力の基本

いよいよ**「プログラムに自分で判断させる力（条件分岐）」**と、**「自分のパソコンにある外部データを読み込む力（ファイル入出力）」**を身につけます。
この2つが使えるようになると、「数万行の売上データ（ファイル）を読み込んで、雨の日のデータ（条件）だけを抽出して計算する」といった、本格的なデータ分析の自動化ができるようになります。

---

## 🎯 到達目標

1. **if / elif / else 文** を使って、条件によって異なる処理を行うプログラムを作れる。
2. **論理演算子 (and, or, not)** や比較演算子を用いて、複雑な条件式を作れる。
3. **ファイル入出力 (with文)** を使って、外部データの読み込みや書き込みができる。
4. ファイル読み込み時の**文字コード指定**や、不要な**改行文字の削除**など、データ分析の下準備（前処理）ができる。

---

### 練習問題のGoogle Colab起動
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](
https://colab.research.google.com/github/homata/musashiuniv/blob/master/docs/exercises/2026/01.programming_basic_a/basic_06.ipynb)

1. 上記の「Open In Colab」をクリックして、Google Colabを起動させてください
2. [ファイル] → [ドライブにコピーを保存] を選択して、Googleドライブにコピーを保存してください
3. 保存が成功すると，Googleドライブの「マイドライブ」 →「Colab Notebooks」にコピーしたファイルが保存されます。
4. GoogleドライブにコピーしたGoogle ColabのNotebooksを開いてください
5. 開いたGoogle ColabのNotebooksで作業をしてください

---

## イントロダクション：プログラムに「判断」と「記憶」をさせよう

これまで私たちが書いてきたプログラムは、上から下へ「一直線」に進むだけでした。しかし、実際のカフェの業務やデータ分析では、条件によって対応を変える必要があります。

*   **条件分岐（if文）**: 「もしお客様が会員なら、5%割引にする」「もし気温が30度以上なら、アイスコーヒーの売上としてカウントする」といった**判断の分かれ道**を作ります。
*   **ファイル入出力**: これまでは毎回コードの中にデータを直接書き込んでいましたが、現実のデータはExcelやテキストファイルとして存在します。それをPythonの世界に**「読み込み（インプット）」**、分析結果をファイルとして**「書き出す（アウトプット）」**方法を学びます。

---
## 条件分岐（if文）と論理・比較演算

### if / elif / else による条件分岐
Pythonでは、`if`（もし〜なら）というキーワードを使って条件分岐を書きます。
ポイントは、第3回で学んだ**インデント（字下げ）**です！条件に当てはまった時だけ実行したい処理は、必ずインデントを下げて書きます。

**【条件分岐のイメージ図】**
```text
          [スタート：お会計]
                  │
      ╭───── (if: 会員ですか？) ─────╮
   【Yes】                        【No】
      │                             │
 [ 5%割引！ ]              [ 割引なし（通常価格）]
      │                             │
      ╰─────────── v ───────────────╯
                  │
           [エンド：お支払い]
```

if文による条件分岐
```{figure} images/python_slides/スライド96.PNG
:width: 70%
```

if文による条件分岐
```{figure} images/python_slides/スライド97.PNG
:width: 70%
```

if～else文による条件分岐
```{figure} images/python_slides/スライド100.PNG
:width: 70%
```

if～else文による条件分岐
```{figure} images/python_slides/スライド101.PNG
:width: 70%
```

```
# 基本的な if / else 文
is_member = True  # 会員かどうか（True または False）
price = 1000

if is_member:
    # ここは is_member が True の時だけ実行されます
    print("会員様ですね。50円割引します！")
    price = price - 50
else:
    # それ以外の時（False の時）はここが実行されます
    print("一般のお客様ですね。通常価格です。")

print("お会計は", price, "円です。")
```

### 様々な条件（比較演算子）
数値の大小などを比べるための記号です。
*   `A == B` : AとBは等しい（※ `=` が2つなことに注意！1つは代入です）
*   `A != B` : AとBは等しくない
*   `A > B`  : AはBより大きい
*   `A >= B` : AはB以上（含む）

条件式と関係演算子
```{figure} images/python_slides/スライド99.PNG
:width: 70%
```

### 3つ以上の分岐（elif）
「もし〜なら、そうでなくて〜なら、それ以外は」というように、複数の条件を繋ぐには `elif` (else if の略) を使います。

if～elif～else文による条件分岐
```{figure} images/python_slides/スライド102.PNG
:width: 70%
```

if～elif～else文による条件分岐
```{figure} images/python_slides/スライド103.PNG
:width: 70%
```

```
# if / elif / else 文と比較演算子
temperature = 28

if temperature >= 30:
    print("猛暑です！フラペチーノをたくさん仕入れましょう。")
elif temperature >= 25:
    print("夏日です。アイスコーヒーがよく売れそうです。")
else:
    print("涼しいです。ホットコーヒーの準備をしましょう。")
```

----

### 論理演算子（and, or, not）で条件を組み合わせる
データ分析では、「20代」**かつ**「女性」のデータを抽出したい！といった場面がよくあります。
*   `A and B`（かつ）: AもBも両方TrueならTrue
*   `A or B`（または）: AかBのどちらかがTrueならTrue
*   `not A`（〜ではない）: AがFalseならTrueになる（ひっくり返す）

論理演算子による条件の組み合わせ
```{figure} images/python_slides/スライド105.PNG
:width: 70%
```

論理演算子
```{figure} images/python_slides/スライド106.PNG
:width: 70%
```

論理演算子による条件の組み合わせ
```{figure} images/python_slides/スライド107.PNG
:width: 70%
```

```
# 論理演算子 (and, or)
day = "水曜"
weather = "雨"

# 水曜日「または」雨の日はポイント2倍！
if day == "水曜" or weather == "雨":
    print("本日はポイント2倍デーです！")
```

----

### 演算子の優先度

演算子の優先度とカッコ
```{figure} images/python_slides/スライド108.PNG
:width: 70%
```

### 比較演算子の連結
比較演算子の連結
```{figure} images/python_slides/スライド109.PNG
:width: 70%
```

### 真理値を返す関数、オブジェクト、None、条件として使われる他の値
*   **真理値を返す関数**: `is_member()` のように、呼び出すと `True` か `False` を返す関数を作ることができます。
*   **None（ノン）**: 「データが空っぽ（何もない）」ことを表す特別なオブジェクトです。データ分析の欠損値処理などで活躍します。
*   **条件として使われる他の値**: 実はPythonでは、`True/False` 以外の値も if 文の条件として使えます。**「0」や「空の文字列("")」「空のリスト([])」は、自動的に `False` として扱われます**。それ以外は `True` です。

真理値（True / False）を返す関数
```
def is_member(age):
    """年齢が18歳以上なら会員登録可能（True）とする関数"""
    return age >= 18
```

None（データが空・値がない状態）
```
user_name = None  # まだ名前が登録されていない状態

if user_name is None:
    print("ユーザー名が設定されていません。")
```

条件式として使われる True/False 以外の値  
空の文字列("")」「0」「空のリスト([])」は False として扱われます
```
item_list = []  # 空のリスト
cart_count = 0  # 0
```

リストが「空（False扱い）」かどうかで条件分岐
```
if item_list:
    print("カートに商品が入っています。")
else:
    print("カートは空です。")  # こちらが実行される
```

関数を使った条件分岐の組み合わせ例
```
current_age = 20
if is_member(current_age):
    print("会員サービスを利用できます！")
```

### if文と真偽値

```{figure} images/python_slides/スライド110.PNG
:width: 70%
```

### 3項演算子（条件式）

簡単な条件分岐なら、1行でスッキリ書くことができます（`値1 if 条件 else 値2`）。

三項演算子
```{figure} images/python_slides/スライド104.PNG
:width: 70%
```

```
a = 1
b = 2
c = a if a < b else b
```

----

### 再帰（さいき）について
※少し高度な概念として「再帰（関数の中で自分自身の関数を呼び出すこと）」がありますが、初心者は無限ループに陥りやすいため、まずは通常の if 文や次回学ぶ for 文をマスターすれば大丈夫です！

```
# None と 空のデータの判定
stock_list = []  # 空のリスト（在庫なし）

# リストが空っぽだと、False 扱いになる性質を利用します！
if not stock_list:
    print("在庫が空っぽです！発注してください。")

# 3項演算子（1行で書くif文）
age = 20
# 年齢が20以上なら"大人"、そうでないなら"未成年"を変数 status に入れる
status = "大人" if age >= 20 else "未成年"
print("このお客様は", status, "です。")
```

---

## 練習問題

練習問題 1
```
次の条件を、関係演算子を使って記述してください。
    問題例　aはbより大きい
    解答例　a > b

（1） aはbと等しい
（2） aはbと等しくない
（3） bはcより小さい
（4） aはb以下である
（5） cはb以上である
```
<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

練習問題 1（解答）
```
次の条件を、関係演算子を使って記述してください。
    問題例　aはbより大きい
    解答例　a > b

（1） aはbと等しい      a == b
（2） aはbと等しくない  a != b
（3） bはcより小さい    b < c
（4） aはb以下である    a <= b
（5） cはb以上である    c >= b
```
</details>

練習問題 2
```text
次のプログラムコードにある空欄を埋めて、変数aの値が3で割り切れるときには「3で割り切れます」、
そうでないときには「3で割り切れません」とprint出力するプログラムを完成させてください。

a = 2021
if ○○○：
    ○○○
else:
    ○○○
```

<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

練習問題 2（解答）
```
a = 2021
if a % 3 == 0:
    print('3で割り切れます')
else:
    print('3で割り切れません')
```
</details>


練習問題 4
```text
次の条件を、論理演算子と関係演算子を使って記述してください。

（1） aは5または8と等しい

（2） aとcは両方ともb以下

（3） aは1より大きくて10より小さいが、5ではない

（4） aはbまたはcと等しいが、aとdは等しくない
```

<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

練習問題 4（解答）
```
（1） aは5または8と等しい
    a == 5 or a == 8
（2） aとcは両方ともb以下
    a <= b and c <= b
（3） aは1より大きくて10より小さいが、5ではない
    a > 1 and a < 10 and a != 5
（4） aはbまたはcと等しいが、aとdは等しくない
    (a == b or a == c) and a != d
```
</details>


練習問題 5
```text
次に示すものは、scoresという変数名のリストに格納されている要素のうち、
値が60より大きいものの数をカウントするプログラムコードです。
○○○○を埋めて、完成させてください。
```
```
scores = [65, 80, 40, 92, 76, 52]
count = 0  # 値が60よりも大きな要素の数
for i in socores:
    ○○○○
print(count)  # 結果出力
```

<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

練習問題 5（解答）
```
scores = [65, 80, 40, 92, 76, 52]
count = 0  # 値が60よりも大きな要素の数
for i in socores:
    if i > 60:
        count += 1
print(count)  # 結果出力
```
</details>


---

## ファイル入出力と文字列操作

ここからは、Pythonに外部のテキストファイルを読み込ませる方法です。  

### テキストファイルとバイナリファイル

テキストファイルとバイナリファイルは、「データを人間が直接読める形式で保存しているか、コンピュータが効率よく処理できる形式で保存しているか」という違いがあります。

一番大きな違いは「メモ帳などのテキストエディタで開いたときに、意味のある文字として読めるかどうか」です。

---

#### 主な違いの比較

| 項目 | テキストファイル | バイナリファイル |
| --- | --- | --- |
| **中身のデータ** | 文字コード（UTF-8など）で変換された「文字」 | 0と1のデータ（バイナリ）そのもの |
| **メモ帳で開くと** | **読める**（意味のある文章が表示される） | **文字化けする**（記号や乱数が並ぶ） |
| **主な用途** | ソースコード、設定ファイル、メモ | 画像、音声、動画、実行ファイル（.exe） |
| **データの構造** | 行ごとに「改行コード」が入る | 決まったデータ構造（ヘッダー情報など）を持つ |
| **主な拡張子** | `.txt`, `.py`, `.html`, `.json`, `.csv` | `.png`, `.jpg`, `.mp3`, `.mp4`, `.pdf`, `.exe` |

---

#### わかりやすいイメージ

##### テキストファイル

* 人が読める文章
* 文字データだけで構成されています。
* メモ帳やVS Codeなどで開くだけで、誰でも内容を理解・編集できます。

##### バイナリファイル

* コンピュータ用のデータ
* 画像のピクセル情報や、音の波形データ、プログラムの実行命令などが効率的な配置で詰め込まれています。
* 文字として表示できないため、メモ帳で無理やり開くと「文字化け（ぐちゃぐちゃの表示）」になります。専用のアプリ（画像閲覧ソフトや音楽プレイヤーなど）を使って読み込みます。

----

### テキストファイル

```{figure} images/python_slides/スライド270.PNG
:width: 70%
```

----

### ファイルとは？ ファイルのオープンとクローズ
パソコンの中にあるデータは、本棚の本のようなものです。
読むためには、**「開く（オープン）」**し、読み終わったら**「閉じる（クローズ）」**必要があります。閉じ忘れると、他のアプリがそのファイルを触れなくなったり、パソコンのメモリを無駄遣いしてしまいます（これが「編集中のファイルの動作」の問題です）。


### `open` と `with` 文

昔は手動で `f.close()` と書いて閉じていましたが、今は**`with` 文**という便利な書き方があります。これは**「処理が終わったら自動でファイルを閉じてくれる自動ドア」**のようなものです。絶対に `with` 文を使いましょう！

**【with文のイメージ図】**
```text
 🚪 [ 自動ドア (with open...) ] ──╮ 
 │                                │ インデントが下がっている間だけ、
 │  ファイルの中身を読み書きする    │ ファイルが開いた状態になる
 │                               │
 ╰── (インデントが終わると自動でドアが閉まる！) 
```

### ファイルの読み込み

ファイルの読み込み
```{figure} images/python_slides/スライド271.PNG
:width: 70%
```

with文を使ったファイルの読み込み
```{figure} images/python_slides/スライド273.PNG
:width: 70%
```

### ファイルの読み書きにおける文字コード指定
ファイルを読み込む時、Pythonに「このファイルは UTF-8 という言語で書かれているよ」と教えてあげる必要があります。これを忘れると、**文字化け**やエラー（`UnicodeDecodeError`）の原因になります。

```
# （準備）Colabの仮想環境に、練習用のテキストファイルを作成します。
# 以下のコードを実行して、"sales_memo.txt" というファイルを作ってください。

sample_text = "コーヒー,450\nケーキ,500\n紅茶,400\n"
with open("sales_memo.txt", "w", encoding="utf-8") as f:
    f.write(sample_text)

print("練習用ファイルを作成しました！")
```

### ファイルの読み込み（行の読み込み、全体の読み込み）
*   `f.read()` : ファイル全体を一気に1つの文字列として読み込みます。
*   `f.readlines()` : ファイルを1行ずつリストにして読み込みます。

ファイルを1行ずつ読む
```{figure} images/python_slides/スライド272.PNG
:width: 70%
```

### 改行文字の削除と文字列メソッド
ファイルを読み込むと、行の終わりに「目に見えない改行マーク (`\n`)」がくっついてきます。
データ分析ではこれが非常に邪魔になるため、第4回で学んだ文字列メソッド **`strip()`**（前後の空白や改行を消す）を使って掃除（データクレンジング）をするのが鉄則です！

```
# with文を使ったファイルの読み込み（"r" は読み込み専用という意味です）
with open("sales_memo.txt", "r", encoding="utf-8") as f:
    # readlines() で1行ずつリストにして読み込む
    lines = f.readlines()

print("▼ 読み込んだ生のデータ（改行記号 \\n がついている）")
print(lines)

print("\n▼ データクレンジング後")
# リストから最初の1行目（インデックス0）を取り出す
first_line = lines

# strip() メソッドで不要な改行記号を削除し、split(",") でリストに分割する！
clean_data = first_line.strip().split(",")
print(clean_data)
```

### ファイルへの書き込み
新しくファイルを作ったり、結果を保存したりするには、モードを **`"w"`** (write: 上書き) または **`"a"`** (append: 追記) にして開きます。

テキストファイルの書き出し
```
f = open(ファイルパス, 'w', encoding='UTF-8')
    for i in range(0,100):
        f.write(str(i) + '¥n')  # <-- 改行も書き出す必要がある

close(f) # <-- close()を呼び出す必要がある
```

with文を使う場合
```
with open(ファイルパス, 'w', encoding='UTF-8') as f:
    for i in range(0,100):
        f.write(str(i) + '¥n') # <-- 改行も書き出す必要がある

# close()を呼ばなくてOK
```

"a" モード
```
# "a" モードで開いて、新しいデータを一番下に追加します
with open("sales_memo.txt", "a", encoding="utf-8") as f:
    f.write("抹茶ラテ,550\n") # \n（改行）を忘れずに入れる！

print("新しいメニューをファイルに書き込みました！")
# ※画面左側のフォルダアイコン📁を開くと、「sales_memo.txt」があるのが確認できます。
```

ファイルの読み書き
with文を使って、出力先のファイルと読み込むファイルの両方を開く
入力ファイルを1行ずつ読み込み、'東京都'という文字列が含まれる場合は出力ファイルに書き出す

```
with open(出力ファイルのパス, 'w', encoding='UTF-8') as out_f:
    with open(入力ファイルのパス, 'r', encoding='UTF-8') as in_f:
        for line in in_f:
            if '東京都' in line:
                out_f.write(line)
```
---

## Pythonでのテキストファイルとバイナリファイルの扱い方

Pythonなどのプログラミング言語でファイルを扱う際も、読み書きのモードを使い分けます。

```python
# --- テキストファイルの読み込み ---
# 'r' (read) モードを使用。自動的に文字として読み込まれる
with open("example.txt", "r", encoding="utf-8") as f:
    text_content = f.read()
    print(text_content)  # "こんにちは" などの文字列が出力される


# --- バイナリファイルの読み込み ---
# 'rb' (read binary) モードを使用。生データのバイト列として読み込む
with open("sample_image.png", "rb") as f:
    binary_content = f.read()
    print(binary_content[:10])  # b'\x89PNG\r\n\x1a\n...' のようにバイト列が表示される

```

> **💡 ポイント**
> `CSV` や `JSON` は数字やデータが入っていますが、テキストエディタで開いて読めるため**テキストファイル**に分類されます。一方で、`Excel(.xlsx)` や `Word(.docx)` は装飾情報や構造データが特殊な形式で圧縮されて入っているため、**バイナリファイル**です。


---

## 💻 演習課題

「条件分岐」と「ファイル操作」を組み合わせて、実践的な課題に挑戦しましょう！  
エラーが出ても焦らず、どこで躓いているのか一緒に考えていきましょう。

以下の `# TODO: ` の部分を書き換えて、プログラムを完成させてください。

```
# 演習1: if / else による条件分岐（難易度：易）
# カフェのスタンプカードは、10個貯まると「無料ドリンクチケット」がもらえます。
# TODO: stamps の値が 10 以上なら "チケット獲得！"、そうでないなら "あと少し！" と print してください。

stamps = 8

# TODO: if文を使って条件分岐を書こう（インデントに気をつけて！）
if 
    print("チケット獲得！")
else:
```
    

```
# 演習2: 論理演算子と elif（難易度：易）
# ケーキ（500円）の賞味期限による値引きシステムです。
# 期限まで 1日 なら 20%引き、0日（今日まで）または マイナス（期限切れ） なら 廃棄（0円）にします。
# TODO: 条件式を完成させてください。

days_left = 0
price = 500

# TODO: days_left が 0以下（0またはマイナス）なら廃棄
if 
    price = 0
    print("期限切れのため廃棄します。")
# TODO: そうではなく、days_left が 1 と等しいなら 20%引き (price * 0.8)
elif 
    price = price * 0.8
    print("割引価格になります。")
else:
    print("通常価格です。")

print("販売価格:", price, "円")
```

```
# 演習3: with文を使ったファイルの読み込み（難易度：中）
# 事前準備で作成した "sales_memo.txt" を読み込みます。
# TODO: with open を使ってファイルを開き、f.read() で中身をすべて読み込んで表示してください。

# TODO: with open(...) を完成させる（文字コード encoding="utf-8" も忘れずに！）
with 
    # TODO: f.read() の結果を変数 all_data に入れる
    all_data = 

print("ファイルの内容:\n" + all_data)
```

```
# 演習4: 文字列の掃除と条件判定（難易度：中）
# ファイルから読み込んだような、不要な空白や改行が混ざった文字列があります。
# TODO: strip() で掃除した後、中身が "コーヒー" と一致するか if文で判定してください。

raw_text = "  コーヒー \n"

# TODO: strip() メソッドで空白と改行を消す
clean_text = 

# TODO: clean_text が "コーヒー" と等しいか判定する
if 
    print("これはコーヒーのデータです！")
else:
    print("コーヒーではありません。")
```

```
# 演習5: 実践！ファイルへの書き込みと3項演算子（難易度：難）
# 本日の売上目標（10000円）を達成したかどうかを判定し、その結果を新しいファイルに記録します。
# TODO: 3項演算子を使って、売上が10000以上なら "達成"、未満なら "未達成" を result に入れてください。
# TODO: その result の文字を、"report.txt" に書き込んで保存してください。

today_sales = 12500

# TODO: 3項演算子 ( 値1 if 条件 else 値2 ) を使う
result = 

# TODO: with open を "w" モードで使い、report.txt に result を書き込む(write)
with 
    

print("レポートを出力しました！（左のフォルダ📁から report.txt を確認してみましょう）")
```

---

## 💡 演習問題の解答例

（どうしても分からない時や、答え合わせをしたい時に展開して見てくださいね。）

<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

**演習1の解答**
```python
if stamps >= 10:
    print("チケット獲得！")
else:
    print("あと少し！")
```

**演習2の解答**
```python
if days_left <= 0:  # または days_left == 0 or days_left < 0
    price = 0
    print("期限切れのため廃棄します。")
elif days_left == 1:
    price = price * 0.8
    print("割引価格になります。")
```

**演習3の解答**
```python
with open("sales_memo.txt", "r", encoding="utf-8") as f:
    all_data = f.read()
```

**演習4の解答**
```python
clean_text = raw_text.strip()
if clean_text == "コーヒー":
    print("これはコーヒーのデータです！")
```
※ `strip()` をしないと、「  コーヒー \n」と「コーヒー」は違うものだと判断され、一致しません！

**演習5の解答**
```python
result = "達成" if today_sales >= 10000 else "未達成"

with open("report.txt", "w", encoding="utf-8") as f:
    f.write(result)
```

</details>

---
## 📚 第6回のまとめ

プログラムを賢くする「条件分岐」と、データを外の世界とやり取りする「ファイル入出力」を学びました。  
これができると、データ分析の幅がぐっと広がります！

### ✨ 今日の重要ポイントまとめ
*   **if / elif / else**: インデントを使って、条件ごとに処理を分けることができる。
*   **比較演算子と論理演算子**: `==`, `>=`, `and`, `or` を使って、複雑な抽出条件を作る。
*   **with open()**: ファイルを読み書きする時は、閉じ忘れを防ぐ「自動ドア」の `with` 文を使う。
*   **文字コードと改行**: 読み込む時は `encoding="utf-8"` を指定し、読み込んだ文字列は `strip()` で綺麗にする（データクレンジング）。

### ❓ よくある質問と回答 (FAQ)
*   **Q. `SyntaxError: expected ':'` というエラーが出ました。**
    *   **A.** 文系学生がif文で一番よくやるミスです！ `if` や `elif`, `else`, そして `with` の行の最後には、**必ずコロン（`:`）が必要**です。コロンを忘れていないかチェックしてください。
*   **Q. `FileNotFoundError: [Errno 2] No such file or directory` というエラーが出ました。**
    *   **A.** 「そんな名前のファイルは見つからないよ！」というエラーです。ファイル名のスペルミスがないか、あるいは「読み込む前に、ちゃんとファイルを作成（アップロード）しているか」を確認しましょう。Colabでは左側のフォルダアイコンから確認できます。
*   **Q. Macで作ったファイルを読み込むとエラーになります…。**
    *   **A.** 文字コードの問題かもしれません。MacやWindowsによって標準の文字コードが違うことがあるため、`open()` の中に必ず `encoding="utf-8"` を入れるクセをつけておくと安心です。

---

### 🌟 Colabで試すおすすめコード

今日学んだことを全て組み合わせた「売上データの自動集計プログラム」です。
ファイルからデータを読み込み、コーヒーの売上だけを合算します。

```
# （準備）売上データファイルを作成します
with open("daily_sales.txt", "w", encoding="utf-8") as f:
    f.write("コーヒー,450\nケーキ,500\nコーヒー,450\n紅茶,400\n")

# コーヒーだけの売上合計を入れる箱
coffee_total = 0

# ファイルを読み込みます
with open("daily_sales.txt", "r", encoding="utf-8") as f:
    lines = f.readlines() # 1行ずつリストにする

# まだ学んでいませんが、次回詳しくやる for文（繰り返し）を使います！
for line in lines:
    clean_line = line.strip()       # 1. 改行などのゴミを消す
    data_list = clean_line.split(",") # 2. カンマで区切ってリストにする
    
    item_name = data_list # 商品名
    item_price = int(data_list) # 値段（計算したいので int で数値に変換！）
    
    # 3. もし商品名がコーヒーなら、合計に足す！
    if item_name == "コーヒー":
        coffee_total = coffee_total + item_price

print("本日のコーヒーの売上合計は", coffee_total, "円です！")
```
