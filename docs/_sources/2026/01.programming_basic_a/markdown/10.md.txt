かしこまりました。第10回の講義資料（Google Colab用）を作成いたしました。

今回はデータ分析において必須となる「拡張機能（ライブラリ）」の導入と、数値計算の最強ツールである「NumPy（ナムパイ）」について学びます。新しいツールが入ることで専門用語（モジュール、パッケージなど）が出てきますが、文系初学者でもイメージしやすいように「カフェの拡張キット」や「腕利きの職人」に例えて解説しています。

以下のテキストをコピーし、Google Colabのセルに貼り付けてご利用ください。

---

### 第10回の講義資料（Google Colab用）

```markdown
<!-- Markdownセル -->
# 第10回 Pythonのライブラリ・パッケージ（演算系）

皆さん、こんにちは！プログラミング基礎A、第10回の講義です。担当の教員です。
いよいよ今回から、Pythonを「データ分析のプロ」にするための拡張機能（ライブラリ）の学習に入ります！

今回は **NumPy（ナムパイ）** という、数値計算に特化した強力なツールを学びます。
「配列」や「線形代数」など数学的な言葉が出てきますが、**大丈夫です！** カフェの売上データ（客単価や合計売上）を、今までよりずっと少ないコードで、しかも一瞬で計算するための便利な道具として使っていきます。「数学のテスト」ではなく「便利な電卓の使い方」を学ぶつもりで、一緒に楽しくやっていきましょう！

---
## 🎯 到達目標 (約5分)
この授業が終わるころには、以下のことができるようになります。
1. **ライブラリ・モジュールの概念**を理解し、`import`文を使って正しく読み込むことができる。
2. **NumPy配列（ndarray）** を作成し、リストよりも効率的なデータ管理ができる。
3. NumPyを使って、**要素ごとの演算や統計量の算出（平均、合計など）** ができる。
4. **真理値配列** を用いて、「売上が目標を超えた日だけ抽出する」といったデータ分析特有の処理ができる。

---
## 10.1 イントロダクション：ライブラリとNumPyとは？ (約10分)

### モジュール・パッケージ・ライブラリ
Pythonのすごいところは、世界中の天才たちが作った「便利な道具箱（拡張キット）」が無料で公開されていることです。

*   **モジュール**: 便利な関数などを1つのファイルにまとめたもの。（例：カフェの「レジ打ち機能」）
*   **パッケージ / ライブラリ**: 複数のモジュールをさらに大きくまとめたもの。（例：カフェの「店舗運営まるごとセット」）

### なぜリストではなくNumPy（ナムパイ）なのか？
これまで複数のデータは「リスト `[ ]`」に入れて計算（for文など）してきましたね。しかし、データが100万件になるとリストでは動作がとても遅くなります。
**NumPy** は、Pythonの中で大量の数字を扱うための「超高速な計算職人」です。

**【リストとNumPyの違い（イメージ図）】**
```text
▼ 普通のリスト（for文で1つずつ計算する）
 [ 400, 500, 450 ] ──▶ (1つずつロボットに渡す) ──▶ [ 440, 550, 495 ] (税込み計算)
 ※データが多いと時間がかかる…

▼ NumPy配列（一気にまとめて計算する魔法！）
 [ 400, 500, 450 ] ━━▶ (巨大プレス機で一気に1.1倍！) ━━▶ [ 440, 550, 495 ]
 ※for文を書かなくても、超高速で全員まとめて処理できる！
```

---
## 10.2 importの使い方（モジュールのインポート） (約5分)

拡張機能を使うには、Pythonに「この道具箱を持ってきて！」とお願いする必要があります。それが `import`（インポート）文です。

*   `import 〇〇` : 〇〇という道具箱を丸ごと持ってくる。
*   `import 〇〇 as △△` : 〇〇という道具箱に、**短いあだ名（△△）**をつけて持ってくる。
*   `from 〇〇 import ××` : 〇〇という道具箱の中から、××という特定の道具だけを取り出して持ってくる。

データ分析の世界では、NumPyをインポートする時は **`np` というあだ名をつけるのが世界共通のルール** になっています。

```

<!-- コードセル -->
# NumPyライブラリを「np」というあだ名で読み込みます！
import numpy as np

# これ以降は、np.〇〇 と書くだけでNumPyの便利機能が使えます。

# 特定の機能だけを取り出す from の例（※参考）
# from datetime import date

<!-- Markdownセル -->
---
## 10.3 NumPyライブラリの基礎（配列の構築と操作） (約15分)

さっそく、NumPyの職人（`np`）にデータを作ってもらいましょう！

### 配列の構築（np.array）
普通のリストをNumPy専用の箱（**ndarray** と呼ばれます）に変換します。
```

<!-- コードセル -->
# 通常のリスト
sales_list = 

# np.array() を使ってNumPy配列（ndarray）に変換！
sales_array = np.array(sales_list)

print("リスト:", sales_list)
print("NumPy配列:", sales_array) # カンマ(,)がない形で表示されるのが特徴です
print("型を確認:", type(sales_array))

<!-- Markdownセル -->
### 配列要素を生成する構築関数
「0だけが100個入ったデータ」などを一瞬で作ってくれる魔法の関数です。テストデータの準備（初期化）などでよく使います。
*   `np.zeros()`: すべて0の配列を作る。
*   `np.ones()`: すべて1の配列を作る。
*   `np.arange()`: range()関数のNumPy版。連番の配列を作る。

```

<!-- コードセル -->
# 要素がすべて0の配列を作る（要素数を指定します）
print("ゼロ配列:", np.zeros(5))

# 連番の配列を作る（0から4まで）
print("連番配列:", np.arange(5))

<!-- Markdownセル -->
### 配列要素の操作と多次元配列
NumPyの配列は、リストと同じようにインデックス（`` など）やスライス（`[0:2]`）でデータを取り出せます。
また、エクセルの表のように「行と列」を持つ2次元（多次元）のデータも作れます。その形は `.shape`（シェイプ）という属性で確認できます。

```

<!-- コードセル -->
# 2次元配列（月曜、火曜の、午前・午後の客数）
visitors = np.array([
   ,  # 月曜
      # 火曜
])

print("2次元配列:\n", visitors)
print("データの形(shape):", visitors.shape) # (2, 2) つまり「2行、2列」と教えてくれます

# データを取り出す（1行目・0列目：火曜の午前）
print("火曜の午前:", visitors)

<!-- Markdownセル -->
---
## 10.4 NumPyを使った高度な計算とデータ操作 (約15分)

ここからがNumPyの真骨頂です！for文を使わずにデータを一気に処理します。

### 要素毎の演算
配列そのものに対して足し算や掛け算をすると、**中身のデータすべてに対して一気に計算**してくれます。また、サイズの同じ配列同士を足すと、同じ場所にあるデータ同士を足してくれます。

```

<!-- コードセル -->
prices = np.array()
amounts = np.array()

# 一気に税込み価格にする！（for文は不要です）
tax_in_prices = prices * 1.1
print("税込み:", tax_in_prices)

# 単価 × 個数 を一気に計算する！（同じ要素番号同士が掛け算されます）
sales = prices * amounts
print("売上:", sales)

<!-- Markdownセル -->
### よく使われる配列操作（統計量）
データ分析で一番よく使う「合計」「平均」「最大値」などは、配列に用意されたメソッドを呼ぶだけで一発で計算できます。

```

<!-- コードセル -->
data = np.array()

print("合計 (sum):", data.sum())
print("平均 (mean):", data.mean())
print("最大値 (max):", data.max())

<!-- Markdownセル -->
### 真理値配列によるインデックスアクセス（超重要！）
データ分析では「売上が300以上のデータだけ欲しい」といった条件抽出が必須です。
NumPyでは、条件式（`data >= 300`）を入れると「True / False」が入った配列（真理値配列）が作られます。それを元の配列の `[ ]` の中に入れると、**Trueの場所のデータだけを抽出**してくれます！

```

<!-- コードセル -->
sales = np.array()

# 1. まず条件式を書くと、True/False の配列ができる
is_high_sales = sales >= 300
print("条件チェック:", is_high_sales)

# 2. それを [ ] の中に入れると、True のものだけ取り出せる！
good_sales = sales[is_high_sales]

# 慣れると1行でこう書きます（非常によく使う書き方です）
# good_sales = sales[sales >= 300]

print("300以上の売上:", good_sales)

<!-- Markdownセル -->
### 配列の保存と復元
分析した結果は、パソコン内にファイルとして保存（`np.save`）し、後で読み込む（`np.load`）ことができます。NumPy専用の `.npy` という形式で保存されます。

### ▲ 線形代数の演算（内積：dot）
「線形代数（行列の計算）」と聞くと難しそうですが、文系のデータ分析でも実は使います。例えば「商品の価格リスト」と「売れた個数リスト」を掛け合わせて、全部の合計金額を出す計算を **内積（ドット積）** と呼び、`np.dot()` で一発で計算できます。

```

<!-- コードセル -->
# 配列の保存と復元
np.save("my_data.npy", sales)            # フォルダ📁に保存されます
loaded_data = np.load("my_data.npy")     # ファイルから読み込みます
print("復元したデータ:", loaded_data)

# 線形代数の演算（内積）
# [400×2] + [500×1] + [450×3] を一気に計算します
total_revenue = np.dot(prices, amounts)
print("1日の総売上:", total_revenue, "円")

<!-- Markdownセル -->
---
## 💻 演習課題 (約50分)

さあ、今日学んだNumPyの力を試す時間です！
講義と演習の時間は約半分ずつです。これまでfor文を使って苦労して書いていた処理が、いかにシンプルに書けるかを体感してください！

エラー（例えば `ValueError: operands could not be broadcast together` など）が出たら、それは「配列同士のサイズ（長さ）が違って計算できないよ！」というNumPyからの警告です。焦らず、AIにヒントをもらいながら進めましょう。

以下の `# TODO: ` の部分を書き換えて、プログラムを完成させてください。
```

<!-- コードセル -->
# 演習1: NumPyのインポートと配列作成（難易度：易）
# TODO: numpy を np というあだ名で import してください。
# TODO: リスト my_list を np.array() を使って NumPy配列 に変換してください。

# TODO: import を書く
import 

my_list =

# TODO: NumPy配列に変換して np_array に入れる
np_array = 

print("NumPy配列:", np_array)

<!-- コードセル -->
# 演習2: 構築関数と要素の操作（難易度：易）
# TODO: np.arange() を使って、1 から 5 までの連番の配列を作ってください（※終了の数字に注意）。
# TODO: 作った配列に 10 を掛け算して、print で表示してください。

# TODO: 1, 2, 3, 4, 5 の配列を作る（終了は含まれないので6を指定します）
numbers = 

# TODO: numbers のすべての要素に 10 を掛ける
result = 

print("10倍にした配列:", result)

<!-- コードセル -->
# 演習3: 要素ごとの演算と統計量（難易度：中）
# カフェの1週間の来客数データです。
# TODO: .mean() を使って、1週間の「平均」来客数を計算してください。
# TODO: .max() を使って、「最大」来客数を計算してください。

visitors = np.array()

# TODO: 平均を計算する
average_visitors = 

# TODO: 最大値を計算する
max_visitors = 

print("平均来客数:", average_visitors, "人")
print("最大来客数:", max_visitors, "人")

<!-- コードセル -->
# 演習4: 真理値配列によるインデックスアクセス（難易度：中）
# データ抽出の超重要テクニックです！
# TODO: 1週間の来客数(visitors)の中から、「150人以上」の日だけを抽出して print してください。

# TODO: [ ] の中に条件式（visitors >= 150）を書く
busy_days = visitors[             ]

print("150人以上の日のデータ:", busy_days)

<!-- コードセル -->
# 演習5: 実践！配列の計算と保存（難易度：難）
# 3種類のケーキの「価格(prices)」と「売れた個数(amounts)」のデータです。
# TODO: 要素ごとの掛け算を使って、それぞれのケーキの「売上(sales)」配列を作ってください。
# TODO: np.save() を使って、その sales 配列を "cake_sales.npy" という名前で保存してください。

cake_prices = np.array()
cake_amounts = np.array()

# TODO: prices と amounts を掛け算して sales を作る
sales = 

print("それぞれの売上:", sales)
print("全体の合計売上(参考):", sales.sum(), "円")

# TODO: np.save("ファイル名", 保存するデータ) で保存する


print("データを保存しました！（左の📁アイコンから確認できます）")

<!-- Markdownセル -->
---
## 💡 演習問題の解答例

（どうしても分からない時や、答え合わせをしたい時に展開して見てくださいね。エラーが出たら、遠慮なく教員や生成AIに聞いてください！）

<details>
<summary>👉 解答例を見る（ここをクリックして展開）</summary>

**演習1の解答**
```python
import numpy as np
np_array = np.array(my_list)
```

**演習2の解答**
```python
numbers = np.arange(1, 6) # になります
result = numbers * 10
#  と出れば正解です！
```

**演習3の解答**
```python
average_visitors = visitors.mean()
max_visitors = visitors.max()
```

**演習4の解答**
```python
busy_days = visitors[visitors >= 150]
#  が抽出されていれば大正解です！
```

**演習5の解答**
```python
sales = cake_prices * cake_amounts
np.save("cake_sales.npy", sales)
# 4000, 2250, 4000 と計算されていれば完璧です！
```

</details>

---
## 📚 第10回のまとめ

お疲れ様でした！今日はデータ分析の強力な味方「NumPy」を学びました。これまでfor文を使って何行も書いていた処理が、魔法のように短く、しかも高速に書けるようになりましたね。

### ✨ 今日の重要ポイントまとめ
*   **import numpy as np**: ライブラリを読み込む世界共通の「おまじない」。
*   **ndarray**: NumPy専用の配列（箱）。`np.array()` でリストから作れる。
*   **要素ごとの演算と統計量**: for文を使わずに一気に計算(`* 1.1`など)でき、`.sum()` や `.mean()` で集計も一瞬！
*   **真理値配列のアクセス**: `データ[データ >= 300]` のように書くだけで、条件に合うデータだけを簡単に抽出できる。

### ❓ よくある質問と回答 (FAQ)
*   **Q. `ValueError: operands could not be broadcast together with shapes (3,) (4,)` というエラーが出ました。**
    *   **A.** これもNumPyあるあるのエラーです！「3個のデータが入った配列」と「4個のデータが入った配列」を足し算や掛け算しようとしていませんか？NumPyは要素ごとに計算をするため、**配列同士のサイズ（長さ）が同じでないと計算できません**。データの個数を確認してみてくださいね。
*   **Q. `.npy` ファイルってExcelで開けますか？**
    *   **A.** 残念ながらExcelでは直接開けません。`.npy` はPython（NumPy）が最も速く読み書きできるように作られた専用のファイル形式です。Excelで開きたい場合は、CSV形式にする必要があります（今後の授業で学びます！）。

### 🌟 Colabで試すおすすめコード
真理値配列の応用です。「複数の条件」を組み合わせてデータを抽出する、データ分析の実務で頻繁に使うテクニックです。
```

<!-- コードセル -->
# カフェの1週間の売上データ
daily_sales = np.array()

# 「10000円より多くて（>）、かつ（&）、20000円未満（<）」の日だけ抽出したい！
# NumPyでは、and の代わりに 「 & 」を使い、条件ごとに ( ) で囲むルールがあります。
normal_days = daily_sales[(daily_sales > 10000) & (daily_sales < 20000)]

print("条件に合う売上データ:", normal_days)
```
```markdown
<!-- Markdownセル -->
次回（第11回）は、今日学んだNumPyをベースにして作られた、データ分析の「主役」ツールである「pandas（パンダス）」ライブラリを学びます。エクセルのような表データをPythonで自由自在に操れるようになりますよ。今日はお疲れ様でした！
```

---

### 【教員（AI）からの工夫ポイント解説】
1. **ライブラリ概念の比喩化**: 初学者が戸惑う「モジュール・パッケージ・ライブラリ」の違いや `import` の意味を、「カフェの拡張キット」や「あだ名をつける」という比喩で説明し、心理的ハードルを下げました。
2. **NumPyの利点の可視化**: 「なぜリストではなくNumPyなのか？」という疑問に対し、for文のループ処理とNumPyのベクトル化演算（一括処理）の違いを、ロボットと巨大プレス機のアスキーアートで図解し、直感的に理解できるようにしました。
3. **データ分析への実践的接続**: カリキュラム（Weekly Topics Summary）にある「真理値配列によるインデックスアクセス」や「要素毎の演算」を、単なる数学パズルにせず、「売上が目標を超えた日の抽出」や「客単価×人数での売上計算」といった文系学生に身近なデータ分析の文脈に落とし込みました。
4. **初心者が陥りやすいエラーの先回り**: NumPy特有の「ブロードキャストエラー（形状不一致による `ValueError`）」について、FAQでわかりやすく解説し、エラーが出てもパニックにならず自己解決できる力を養う構成にしています。
5. **講義要件の網羅**: `import`の各種構文から、`zeros/arange`などの構築関数、線形代数（`dot`）まで、指定されたトピックを105分の枠（講義約50分・演習約50分）で網羅しつつ、文系向けに難易度を調整しています。

内容をご確認いただき、修正等のご要望がなければ、続いて**第11回「高階関数とpandasライブラリ」**の教材を出力いたします。いかがでしょうか？